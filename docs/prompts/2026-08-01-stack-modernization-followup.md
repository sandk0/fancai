# Промпт: завершение обновления стека fancai

> **Репозиторий:** `/Users/sandk/Documents/GitHub/fancai`, `main`, HEAD `5346abb0`
> **Предшественники:** промпты `…-audit.md` (Фазы 0–4) и `…-execution.md` (Волны 0–5)

## §1. Вход

Прочитать и **не переделывать сделанное**: `docs/handoff.md` (верхняя запись
самодостаточна; там же врезка о десяти незакоммиченных документах, таблица
«Что НЕ выполнено», раздел «Долг» и операционные детали — пароль тестового
пользователя, порты, известные грабли), затем `docs/progress.md` и
`docs/plans/2026-08-01-stack-modernization-plan.md` (Волны 6–7).

Волны 0–5 смержены. `pip-audit` чист, `npm audit` фронта 6, Modal удалён.

## §2. Запреты

Прод и всё из `scripts/`; компоненты `HOLD`/`REPLACE` (TypeScript 6/7, redis-py 8,
mypy 2, epub.js, PostgreSQL 18, `mher/flower`); Волна 7; рефакторинг сверх §3.

**Дерево грязное чужой работой: 27 изменённых файлов.** `reset --hard`,
`checkout -- .`, `clean -fd`, `stash`, `git add -A` запрещены; коммит любого из
27 — только с разрешения. Снять `md5sum` этих файлов на входе и сверять после
каждого шага, способного их тронуть.

## §3. Порядок работ

- **A. Документация после удаления Modal.** `docs/architecture/ai-pipeline.md`
  описывает несуществующий пайплайн; `CHANGELOG.md` нужна запись в `[Unreleased]`
  (раздел v1.6 не трогать). Оба файла — в чужих 27, блокируются решением 1.
  Полный список — `docs/_drafts/2026-08-01-docs-impact-list.md`.
- **B. Открытые критерии Волн 3–5.** B1 `npm run lint` (1 ошибка + 30 warning при
  `--max-warnings 0`); B2 `build:size` (2.18 MB / 669 KB против 800 / 500 KB);
  B3 `test:e2e` (тесты ждут 305 `data-testid`, в коде 40). Все были красными и в
  базовом коммите — это **не waiver**: относительно до-среза план формулирует
  только `pytest`. B2 и B3 — отдельные проекты, не хвосты.
- **C. Волна 6.** Решения уже приняты (см. `docs/progress.md`, запись о стоп-точке
  §12.1) — повторно не согласовывать. Состав 6.1–6.11 в плане; порядок 6.1→6.2,
  6.6→6.2 обязателен; 6.11 только на локальном Redis **DB 0** (DB 1 — брокер).
- **D. Пункты 4.7/4.8.** Только после включения GitHub Actions — сейчас
  `enabled: false`, проверить нечем.

## §4. Спросить первым сообщением

1. Чужие правки в `ai-pipeline.md` и `CHANGELOG.md` — закоммитить, отложить или
   править поверх? *Рек.: закоммитить, затем агент правит отдельно.*
2. Политика линта — снять `--max-warnings 0`, перевести демоутнутые react-hooks в
   `off`, или чинить 31 находку? *Рек.: `off` со списком — конфиг уже заявляет
   «постепенное внедрение», а `--max-warnings 0` это обесценивает.*
3. Волна 6 — исполнять со smoke ≈ $0,90, без smoke, или отложить?
   *Рек.: целиком, иначе критерий выхода не проверить.*
4. Десять локальных документов сессий — коммитить? *Рек.: да, иначе ссылки в
   `handoff.md` и `progress.md` битые в чистом клоне.*
5. B2 и B3 — отдельными задачами? *Рек.: да, это не обновление версий.*

## §5. Критерии готовности

- [ ] `ai-pipeline.md` без Modal; запись в `CHANGELOG.md [Unreleased]`.
- [ ] `pytest` — набор падающих ID не вырос против **459** (сравнивать множества:
      FAILED и ERROR переклассифицируются между прогонами).
- [ ] `npm test` не хуже **566 passed / 1 skipped**; `tsc` ≤ **22**, все в `__tests__`.
- [ ] `pre-commit run --all-files` на чистом worktree: зелёных хуков не меньше
      **9 из 10**, прогон не переписывает ни одного файла.
- [ ] `pip-audit` пуст; в `npm audit` нет новых high.
- [ ] Волна 6: в локальном `llm_usage_log` есть `model='gemini-3.6-flash'` с
      ненулевым `cost_dollars`, изображение сгенерировано.
- [ ] `md5sum -c` — чужие 27 файлов побайтово целы.
- [ ] `docs/progress.md` и `docs/handoff.md` обновлены, следующий шаг назван.

## §6. Стоп-точки

Прод; траты сверх оценённых; коммит любого из 27 чужих файлов; начало B2 или B3.
В остальном работать непрерывно, промежуточные проверки — молча.
