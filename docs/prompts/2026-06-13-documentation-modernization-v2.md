# Промпт: Глобальная модернизация документации fancai — проход v2

> **Дата создания:** 2026-06-13 · **Целевая модель:** Claude Fable 5, effort max
> **Репозиторий:** `/Users/sandk/Documents/GitHub/fancai`, ветка `main`
> **Предыдущий проход:** v1, 2026-04-30 (см. §2)

---

## 1. Миссия

Привести **всю** markdown-документацию проекта (корень + `docs/` + вложенные agent-доки) в состояние, точно отражающее текущий код и инфраструктуру:

1. **Актуализировать** живые документы (сверка каждого факта с кодом);
2. **Архивировать** устаревшее (перемещение в `docs/_archive/`, не удаление);
3. **Объединить** дубликаты (в `docs/research/` лежит 5–6 почти-копий одного аудита);
4. **Создать** недостающее — только подтверждённые лакуны, верифицируемые по коду;
5. **Обновить** навигацию (`docs/README.md`) до соответствия дереву 1:1.

Это **второй** проход модернизации. Первый (2026-04-30) задал методологию и исправил часть дрейфа, но после него проект пережил минимум три крупных события, не отражённых в доках: вероятная миграция AI-пайплайна на Gemini Direct (май), миграция сервера (2026-05-10), удаление GSD-тулчейна (2026-06-13). Твоя работа — дифф от v1, а не повторение v1.

**Масштаб:** ~870 `.md` в `docs/` (из них ~530 в `reports/` — исторические, контент не трогаем), 7 корневых, плюс `backend/CLAUDE.md`, `frontend/CLAUDE.md`, `backend/SECURITY.md`. Живых документов, требующих сверки, — порядка 60–100.

---

## 2. Отправная точка — артефакты прохода v1 (прочитай первыми)

| Артефакт                                                                            | Путь                                                     |
| ----------------------------------------------------------------------------------- | -------------------------------------------------------- |
| Отчёт v1: что сделано, что отложено                                                 | `docs/reports/2026-04-30-documentation-modernization.md` |
| Drift-матрица v1 (формат: Файл:строка \| Заявлено \| Фактически \| Источник истины) | `docs/_drafts/2026-04-30-doc-drift-matrix.md`            |
| Промпт v1                                                                           | `docs/prompts/2026-04-30-documentation-modernization.md` |
| Коммиты v1                                                                          | `git log --oneline 09e1b913^..e3767a16`                  |
| Действующий навигатор (заголовок «актуально на 2026-04-30»)                         | `docs/README.md`                                         |

Что v1 исправил — могло устареть снова (README переписывался под «v1.5 stack» 30 апреля, но май всё изменил). Что v1 отложил (Diataxis-placeholder'ы `guides/`/`reference/`/`explanations/`, рассинхрон `docs/ru/`) — твои кандидаты на финальное решение в Фазе 3.

---

## 3. Принцип №1: единственный источник истины — код

Иерархия источников (от высшего к низшему):

1. Код: `backend/app/**`, `frontend/src/**`
2. Манифесты: `backend/requirements.txt`, `frontend/package.json` (pyproject.toml НЕТ — установка через `uv pip install -r requirements.txt`)
3. Инфра: `docker-compose.prod.yml` / `.dev.yml` / `.monitoring.yml`, `Caddyfile`, `Caddyfile.dev`, `.github/workflows/{ci,security,modal-deploy}.yml`
4. Вложенные `backend/CLAUDE.md`, `frontend/CLAUDE.md` — **с оговоркой:** оба от 2026-03-04, сами могли протухнуть; сверь их с кодом, прежде чем использовать как истину (drift-матрица v1 опиралась на них — перепроверь её выводы)
5. `.planning/PROJECT.md`, `.planning/STATE.md` — только как историческая рамка (vision/scope), не как текущее состояние

**Не доверяй ни одному факту из этого промпта и из существующих доков без проверки по коду — включая seed-лист §4.** Промпт написан 2026-06-13 и сам может устареть к моменту запуска.

---

## 4. Известные точки дрейфа (seed-лист: проверь, разреши, расширь)

### 4.1. AI-провайдер — главное противоречие, разреши его ПЕРВЫМ

Четыре источника утверждают разное:

| Источник                                                               | Утверждение                                                                                                                               |
| ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `CLAUDE.md` (корень, строка стека)                                     | OpenRouter: LLM `google/gemini-2.5-flash` (+lite fallback), images `black-forest-labs/flux.2-klein-4b`                                    |
| `backend/requirements.txt:22`                                          | «Description extraction now via LLM API (Google Gemini 3.0 Flash)»                                                                        |
| `backend/requirements.txt:29-30`                                       | `google-genai>=1.69.0` — «Google Gemini direct API (context caching, batch, thinking control)»                                            |
| `backend/app/services/`                                                | Сосуществуют: `openrouter_client.py` (в core/), `gemini_extractor.py`, `image_generator.py`, **`imagen_generator.py`**, `modal_client.py` |
| Drift-матрица v1 (30.04)                                               | «imagen_generator.py НЕ существует (заменено OpenRouter)» — **а файл существует** ⇒ реинтродуцирован в мае                                |
| `docs/plans/2026-05-03-gemini-direct-migration-plan-v2-with-audits.md` | План миграции на Gemini Direct — выясни, исполнен ли и в каком объёме                                                                     |

Установи **фактическую** цепочку по: `backend/app/core/config.py`, `core/container.py`, `services/feature_flag_manager.py`, `services/settings_manager.py`, самим клиентам и их вызовам, именам env-ключей в `.env.production.example` (читай только имена ключей, не значения). Ответь на вопросы: какой клиент — primary для LLM-экстракции? для генерации изображений (FLUX через OpenRouter или Imagen через google-genai)? есть ли feature-flag-переключение и fallback-цепочка? какие env-ключи реально обязательны?

Зафиксируй вывод одним абзацем — **«как реально работает AI-пайплайн на дату аудита»** — это станет канонической формулировкой для всех доков (README, CLAUDE.md, deployment, architecture).

### 4.2. GSD удалён (2026-06-13, коммиты `3af7e43d`, `947f21c2`)

- `.planning/` теперь **read-only архив**; корневой `CONTRIBUTING.md` §7 уже переписан, `CLAUDE.md` уже де-брендирован — но **остальные** живые доки не проверялись;
- `docs/README.md:105-106` всё ещё советует `/gsd:plan-phase` — мёртвая команда; секция «Конвенции для авторов» там же требует правки;
- Прогрепай живые доки: `grep -rnE '/gsd:|get-shit-done|\bGSD\b' --include='*.md'` (исторические reports/prompts/research/plans НЕ исправлять — там это правда момента);
- `AGENTS.md` (корень, ~30K, мозг oh-my-codex) и `GEMINI.md` (корень) — высокая вероятность мёртвых GSD-ссылок.

### 4.3. Миграция сервера (2026-05-10)

- Свидетельства: `docs/prompts/2026-05-10-server-full-backup-migration.md`, untracked `docs/operations/migration/`, `scripts/migration-backup.sh`, `scripts/migration-restore.sh`;
- Если прод переехал — устарели: `docs/deployment/PRODUCTION_INFRASTRUCTURE.md`, `DEPLOYMENT_CHECKLIST.md`, `QUICK_REFERENCE.md`, `DISASTER_RECOVERY.md`, VLESS-доки в `operations/`;
- Сверь с `.claude/skills/deploy/SKILL.md` и `.claude/skills/incident/SKILL.md` (они описывают актуальные прод-операции). **На прод не ходи** (ни ssh, ни docker) — факты только из репо; неустранимые неясности — в вопросы Фазы 3.

### 4.4. Modal — заброшен (v1.4/v1.5, решение 2026-03-27), но следы живут

- `backend/app/services/modal_client.py` и `.github/workflows/modal-deploy.yml` существуют;
- Живые доки не должны представлять Modal как действующий путь. Код/CI не трогаешь — факт зафиксируй в drift-матрице как кандидата на отдельную задачу.

### 4.5. Прочее (быстрые проверки)

- `CLAUDE.md` (корень): строка стека — сверить с §4.1; «Known Claude Code Bugs» (упоминание v2.1.68) — проверить актуальность;
- `README.md` / `README-ru.md`: переписаны 30.04 «под v1.5» — пересверить после майских событий; `README-ru.md` имеет права 600 → `chmod 644` (единственное разрешённое не-md действие);
- `CHANGELOG.md`: история до v1.5 — добавить записи мая–июня (Gemini Direct если был, server migration, GSD removal) в Keep-a-Changelog формате;
- `docs/research/`: семейство `gemini-api-consolidated*.md` (≈6 вариантов) + `PROMPT-*.md` + `_drafts/` — выбрать канонический, остальное в `_archive/`;
- **Untracked-доки** из `git status` (`docs/operations/migration/`, `docs/plans/2026-05-03-…`, пачка `docs/research/*`, `docs/reports/2026-04-26-status-recap.md`, `docs/prompts/2026-05-*`) — триаж: правильное место по конвенциям §8 + коммит;
- Гигиена: `docs/.DS_Store`, `docs/.bg-shell/`, `docs/pencil-landing-prompt.md` (висит в корне docs/ вне категорий), пустая `docs/diagrams/`, placeholder `docs/security/` — навести порядок (решения — в план);
- `backend/SECURITY.md` от 2025-10-29 — сверить с `docs/SECURITY.md` (30.04), устранить дублирование политик;
- `backend/CLAUDE.md`, `frontend/CLAUDE.md` (оба 2026-03-04, по ~1K) — обновить факты (LOC-цифры, counts тестов, модели).

---

## 5. Scope

**Корень:** `README.md`, `README-ru.md`, `CHANGELOG.md`, `CONTRIBUTING.md`, `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`.
**Вложенные agent-доки:** `backend/CLAUDE.md`, `frontend/CLAUDE.md`, `backend/SECURITY.md`.
**docs/ живые:** `README.md` (навигатор), `deployment/`, `operations/`, `ci-cd/`, `development/`, `security/`, `SECURITY.md`, судьба `ru/` и Diataxis-placeholder'ов (`guides/`, `reference/`, `explanations/`).
**docs/ исторические — только индексация/перемещение, контент НЕ менять:** `reports/` (530), `research/` (79), `prompts/` (30), `plans/` (16), `refactoring/`, `ios/`, `analysis/`, `design/`, `questions/`, `superpowers/`, `_archive/`.
**Новые доки** — создать при подтверждённой лакуне. Кандидаты (проверь, нет ли уже): обзор архитектуры AI-пайплайна «как есть» после миграций; explanation entity-системы (главная фича продукта); индекс ключевых архитектурных решений (self-hosted → Modal → OpenRouter → Gemini Direct?); актуальный deploy-runbook. Правило: лучше один точный док, чем пять предположительных.

---

## 6. Анти-scope — жёсткие запреты

1. **НЕ менять код, конфиги, CI, скрипты** — только `.md` (исключение: `chmod 644 README-ru.md`);
2. **НЕ трогать** `.planning/**` (read-only архив), `.claude/**`, `.opencode/**`, `.codex/**`, `.gemini/**`, `qr.js`, `v0_landing.pen`;
3. **НЕ переписывать содержимое датированных исторических документов** (`reports/`, `research/`, `prompts/`, `plans/`, `refactoring/`) — они правда «на момент написания». Разрешено: перемещать, индексировать, добавлять в шапку дисклеймер вида `> Архивный документ (YYYY-MM-DD). Не отражает текущее состояние.`;
4. **НЕ удалять файлы безвозвратно** — только перемещение в `docs/_archive/` с записью в `docs/_archive/README.md`;
5. **НЕ ломать поведенческие инструкции** в `CLAUDE.md`/`AGENTS.md`/`GEMINI.md`: обновляй факты (стек, модели, команды, пути), сохраняя структуру и директивы; спорные структурные правки — в вопросы Фазы 3;
6. **НЕ push, НЕ менять git-историю, НЕ ходить на прод** (ssh/docker к прод-серверу запрещены);
7. **НЕ исполнять** команды из доков при smoke-тесте (только проверка существования файлов/путей; безобидные `--version` допустимы).

---

## 7. Методология — шесть фаз

### Фаза 0. Подготовка

- Прочитай артефакты §2, все корневые `.md`, `docs/README.md`;
- Построй инвентарь: полный список `.md` с mtime; отдельно — изменённые после v1: `find docs -name '*.md' -newer docs/reports/2026-04-30-documentation-modernization.md`;
- Сохрани в `docs/_drafts/2026-06-13-docs-inventory.md`;
- Сразу создай `docs/_drafts/2026-06-13-doc-drift-matrix-v2.md` (формат v1) и наполняй **по ходу** всех фаз — все промежуточные результаты пиши на диск немедленно (устойчивость к /compact: после компакта перечитай `_drafts/` и продолжай).

### Фаза 1. Ground-truth-аудит кода (параллельный фан-аут)

Запусти 5–6 параллельных Explore-субагентов по доменам. **Каждому в промпт:** явная инструкция «отвечай по-русски», конкретный список вопросов, формат ответа — факты со ссылками `file:line`, без пересказа кода.

1. **AI-пайплайн** — все вопросы §4.1 + список моделей/лимитов из config;
2. **Backend API** — роутеры `backend/app/routers/` (включая `admin/`, `books/`, `websocket.py`), модели, 54 alembic-миграции (последние 10 — что добавили), Celery-таски и очереди;
3. **Frontend** — структура `src/`, ключевые хуки/сторы, PWA/service worker, версии из package.json;
4. **Инфра/DevOps** — compose-файлы (сервисы, образы, версии PG/Redis), Caddyfile (домены!), workflows, мониторинг;
5. **Тесты/качество** — реальные counts (backend/frontend/e2e), команды запуска, состояние pre-commit;
6. _(опционально)_ **Entity-система** — если решишь писать новый explanation-док.

Результат каждого — секция фактов в drift-матрице v2. Допускается мульти-агентная оркестрация — **use a workflow** — если сочтёшь её оправданной для фан-аута Фаз 1–2.

### Фаза 2. Аудит документации

- Классифицируй каждый **живой** док: `АКТУАЛЕН` / `ДРЕЙФ` (строки в матрицу) / `УСТАРЕЛ → архив` / `ДУБЛИКАТ → слить с X` / отметь `ЛАКУНЫ`;
- Для **исторических** — проверь только размещение и datestamp-имена (`YYYY-MM-DD-<topic>.md`), контент не читай сплошняком: 530 reports обрабатывай по именам/датам, выборочно;
- Дополни drift-матрицу v2; зафиксируй накопившиеся вопросы к пользователю.

### Фаза 3. План → ЕДИНСТВЕННЫЙ ОБЯЗАТЕЛЬНЫЙ ЧЕКПОИНТ

Сформируй план: целевое дерево `docs/`, таблица операций (обновить / архивировать / слить / создать / переместить) с обоснованием каждой, порядок коммитов, открытые вопросы. Полный план — в `docs/_drafts/2026-06-13-docs-modernization-plan.md`; пользователю покажи **компактную сводку ≤60 строк** + вопросы, требующие решения:

- судьба `docs/ru/` (предложение по умолчанию: living-доки ведём на русском; `README.md` — английская витрина GitHub, `README-ru.md` — русская; рассинхронизированный `docs/ru/` слить или архивировать — двойную бухгалтерию не вести);
- судьба Diataxis-placeholder'ов (`guides/`, `reference/`, `explanations/`): наполнить минимально / архивировать README-заглушки / оставить;
- судьба `AGENTS.md`/`GEMINI.md` при найденных мёртвых ссылках: чинить факты или сократить;
- работать в `main` (по умолчанию, как v1) или в ветке;
- всё неразрешимое по коду (например, актуальный прод-хост).

**Дождись явного «go».** После него — полная автономия до конца. Вопросы задавай plain-text (НЕ AskUserQuestion внутри Skill-контекста — известный баг, см. CLAUDE.md).

### Фаза 4. Исполнение

- Порядок: листья → категории → `docs/README.md` (навигатор) → корневые README → `CHANGELOG.md`. Навигатор всегда последним в своей группе;
- Перемещения — через `git mv`; после каждого — грепни старый путь и почини входящие ссылки;
- Каждому обновлённому живому доку — футер: `_Последнее обновление: 2026-06-13. Сверено с: <конкретные файлы-источники>._`;
- Атомарные conventional-коммиты по логическим группам (англ., imperative): `docs(ai): align AI pipeline docs with actual provider chain`, `docs(archive): consolidate gemini research duplicates`, `docs(deployment): update infrastructure docs after server migration`, `docs(navigation): rebuild docs/README for v2 tree`;
- Историческим untracked-докам — место и коммит `docs(triage): commit untracked research and migration artifacts`.

### Фаза 5. Верификация (обязательна до финального отчёта)

1. **Линк-чек** всех относительных ссылок (корень + docs/ + backend/ + frontend/ `.md`):

```bash
python3 - <<'PY'
import re, pathlib
bad = []
for md in [*pathlib.Path('.').glob('*.md'), *pathlib.Path('docs').rglob('*.md'),
           *pathlib.Path('backend').glob('*.md'), *pathlib.Path('frontend').glob('*.md')]:
    text = md.read_text(encoding='utf-8', errors='ignore')
    for m in re.finditer(r'\]\(([^)\s]+)\)', text):
        t = m.group(1).split('#')[0].strip()
        if not t or '://' in t or t.startswith('mailto:'):
            continue
        if not (md.parent / t).exists():
            bad.append(f'{md}: {t}')
print('\n'.join(bad) or 'OK')
print(f'{len(bad)} broken links')
PY
```

2. **Грепы запрещённого в живых доках** (исторические исключи по путям): `/gsd:|get-shit-done`; `docker-compose ` (с дефисом как команда); устаревшие имена моделей (список — из вывода §4.1); неверные env-ключи; `bookreader-ai`; Modal-как-действующий;
3. **Smoke-тест setup-пути:** каждый файл и путь, упомянутый в Quick Start README и CONTRIBUTING §1, существует (`docker-compose.dev.yml`, `.env.production.example`, и т.д.);
4. **Навигатор = дерево 1:1:** каждая упомянутая в `docs/README.md` директория существует, каждая существующая поддиректория docs/ упомянута;
5. Перед заявлением о завершении — пройди verification-before-completion: команды + их вывод в отчёт.

### Фаза 6. Финальный отчёт

`docs/reports/2026-06-13-documentation-modernization-v2.md` по формату v1: сделано / отложено-почему / принятые решения / метрики до–после (число живых доков, исправленных фактов, перемещённых в архив, битых ссылок до/после) / рекомендации на v3. Закоммить вместе с финальной drift-матрицей.

---

## 8. Конвенции документации

- Имена снапшотов: `YYYY-MM-DD-<topic>.md`; живые доки — без дат в имени;
- Шапка каждого живого дока: 1–2 строки «что это и для кого»;
- Один факт живёт в одном месте, остальные ссылаются (стек — в README, модели — в каноническом AI-доке);
- Футер сверки обязателен (формат в Фазе 4);
- Обнови секцию «Конвенции для авторов» в `docs/README.md`: убери `/gsd:plan-phase`, замени «AI-модели — openrouter_client.py» на фактический источник из §4.1, добавь правило футера;
- Коммиты: `docs(<scope>): <subject>`, английский, imperative, ≤72 символа.

---

## 9. Definition of Done

- [ ] 0 битых относительных ссылок (скрипт Фазы 5 → `OK`);
- [ ] 0 упоминаний GSD / Modal / устаревших моделей / неверных env-ключей **как действующих** в живых доках;
- [ ] Противоречие §4.1 разрешено; каноническая формулировка AI-пайплайна применена везде единообразно;
- [ ] Все обновлённые живые доки имеют футер сверки от 2026-06-13;
- [ ] `docs/README.md` = дерево 1:1, дата обновлена;
- [ ] README.md / README-ru.md проходят smoke-тест Quick Start;
- [ ] CHANGELOG.md содержит события мая–июня 2026;
- [ ] Untracked-доки триажированы и закоммичены;
- [ ] Drift-матрица v2 + инвентарь + план + финальный отчёт существуют и закоммичены;
- [ ] Все коммиты атомарные, conventional, без изменений кода; ничего не запушено.

---

## 10. Деливераблы

1. `docs/_drafts/2026-06-13-docs-inventory.md` — инвентарь;
2. `docs/_drafts/2026-06-13-doc-drift-matrix-v2.md` — drift-матрица v2;
3. `docs/_drafts/2026-06-13-docs-modernization-plan.md` — утверждённый план;
4. Обновлённые / созданные / перемещённые доки + пополненный `docs/_archive/` с индексом;
5. `docs/reports/2026-06-13-documentation-modernization-v2.md` — финальный отчёт;
6. Серия атомарных коммитов в `main` (или ветке — по решению чекпоинта), **без push**.

---

## 11. Процессные требования

- **Skills:** `research-and-analysis` (Фазы 0–2), `writing-plans` (Фаза 3), `verification-before-completion` (Фаза 5), `dispatching-parallel-agents` (фан-аут Фазы 1);
- **Субагентам** — явная инструкция отвечать по-русски в каждом промпте (иначе вернут английский);
- **Бюджет внимания:** `docs/reports/` (530 файлов) и `docs/research/` сплошняком не читать — работать по инвентарю, именам и датам; читать выборочно;
- **Устойчивость:** все промежуточные артефакты — на диск сразу (`_drafts/`); после /compact перечитать их и продолжить с места остановки;
- **Один чекпоинт** (Фаза 3). До него — никаких правок файлов, кроме `_drafts/`-артефактов. После «go» — автономно до конца, включая коммиты;
- Накопившиеся неразрешимые вопросы — пачкой на чекпоинте, не по одному.

**Начни с Фазы 0.**
