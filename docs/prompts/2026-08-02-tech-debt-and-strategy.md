# Промпт: мелкий техдолг и стратегические решения по стеку

> **Репозиторий:** `/Users/sandk/Documents/GitHub/fancai`, `main`, HEAD `1686f3d2`
> **Предшественники:** промпты `2026-08-01-stack-modernization-{audit,execution,followup}.md`

## §1. Вход

Прочитать и **не переделывать сделанное**: верхнюю запись `docs/handoff.md`
(самодостаточна: срез проверок, семь исправленных багов, раздел «Не исправлено:
контракт кэша изображений сломан целиком», операционные детали окружения), затем
верхнюю запись `docs/progress.md` и `docs/_drafts/2026-08-01-tech-debt-backlog.md`.

Состояние стека: **реализация Волн 0–6 завершена и смержена**. Открытыми остаются
только заблокированные пункты 4.7/4.8 и вынесенные отдельными проектами B2/B3;
Волна 7 из объёма исключена. Прод не трогали ни разу.

Базовые срезы, относительно которых сравнивать (не ухудшать):
`pytest` — **452** падающих ID (сравнивать **множества**, не счётчики);
`npm test` — **566 passed / 1 skipped**; `tsc` — **22**, все в `__tests__`;
`npm run lint` при `--max-warnings 0` — **зелёный**;
`pre-commit run --all-files` на чистом worktree — **10 из 10, ноль переписанных файлов**;
`pip-audit` пуст, `npm audit` фронта — 6 (5 high).

## §2. Запреты

Прод и всё из `scripts/`; деплой Волны 6; Волна 7; B2 и B3; перегенерация
существующих изображений; включение GitHub Actions.

**Дерево грязное чужой работой: 25 изменённых файлов** плюс 4 untracked
(`docs/superpowers/plans/2026-07-18-…`, `scripts/migration-{backup,restore}.sh`,
`v0_landing.pen`). `reset --hard`, `checkout -- .`, `clean -fd`, `stash`,
`git add -A` запрещены; коммит любого из них — только с разрешения. Снять
`md5sum` на входе и сверять после каждого шага, способного их тронуть.

## §3. Порядок работ

### A. Четыре мелкие правки — приоритет, делать первыми

Все проверены вживую на HEAD `1686f3d2`. Это **не** «нулевой риск»: у A4 и C6
есть последствия, см. пометки.

| # | Что | Где | Риск |
| --- | --- | --- | --- |
| A4 | docstring `ImagenService` утверждает «через OpenRouter FLUX.2 Klein 4B», хотя путь — Gemini/Vertex; имя класса тоже вводит в заблуждение | `imagen_generator.py:347-351` | docstring — XS. **Переименование класса задевает 71 упоминание в 12 файлах**, включая `container.py`, `conftest.py` и четыре теста: либо делать через `lsp rename` целиком, либо ограничиться docstring и явно записать имя в долг |
| A7 | `test_image_generator_TEMPLATE.py` мокает `aiohttp.ClientSession`, которого в коде нет | `backend/tests/services/` | XS — удалить файл, сверив, что он не единственный покрывающий свой сценарий |
| C3 | `pyrightconfig.json` объявляет Python 3.11 при рантайме 3.12 (`mypy.ini` уже 3.12) | `pyrightconfig.json:5` | XS, но после правки прогнать pyright: могут всплыть подавленные находки |
| C6 | `--cov-fail-under=70` в `addopts` — падает на любом узком прогоне | `backend/pytest.ini:15` | **меняет политику гейта покрытия**: вынести порог в CI-команду `ci.yml`, а не просто удалить, иначе покрытие перестанет проверяться где-либо |

### B. Стратегические решения — с интерактивным обсуждением

Каждый пункт: сначала собрать факты по коду и озвучить варианты с ценой, дождаться
решения, только потом исполнять. Не начинать реализацию до явного ответа.

| # | Тема | Что нужно решить |
| --- | --- | --- |
| S1 | **Контракт кэша изображений** (`ai-pipeline.md`, раздел «Image storage») | В Redis кладётся data-URI целиком; ключ `md5(description + aspect)` без `description_type`, `genre`, `custom_style`, модели; на hit шесть потребителей ведут себя по-разному, regenerate отдаёт HTTP 500. Решить: что кэшируем, чем ключуем, создаёт ли hit новую строку `generated_images`. Ограничение: `delete_with_file()` удаляет по `local_path`, общий путь между строками недопустим |
| S2 | **B1 — нет lock-файла бэкенда** | `pip install` без хешей. Варианты: `constraints.txt` через `uv pip compile`, переход на `uv.lock`, оставить как есть. Смена инструментария сборки |
| S3 | **C1 — `mypy.ini` с `ignore_errors = True`** | Типизация фактически не проверяется, и это блокирует смысл mypy 2.x. Решить объём type-debt цикла: весь `app/`, по модулям, или заморозить |
| S4 | **A6 — GLiNER2** | Путь реализован, выключен флагом `USE_GLINER_NER`, `chapter_embeddings` пуста, ~1,2 ГБ зависимостей в celery-образе. Оживлять или удалять |
| S5 | **Волна 7** | epub.js, `@xmldom/xmldom`, `mher/flower` (образ не публикуется с 2023-08-13), PostgreSQL 18, TypeScript 6/7. Что из этого превращаем в задачу, что закрываем как принятый риск |

Порядок обсуждения: S1 первым — он единственный про действующий продовый дефект.
Остальные — в любом порядке, по интересу владельца.

## §4. Спросить первым сообщением

1. A4 — переименовывать `ImagenService` целиком через `lsp rename` (71 упоминание,
   12 файлов) или ограничиться docstring? *Рек.: только docstring, имя — в долг:
   переименование в одном коммите с мелочами затрудняет ревью.*
2. C6 — переносим порог покрытия в `ci.yml` или снимаем совсем?
   *Рек.: переносим, иначе гейт исчезает молча.*
3. Стратегические S1–S5 — обсуждаем все в этой сессии или выбираем подмножество?
   *Рек.: S1 обязательно, остальные — по времени.*
4. Разрешено ли коммитить правки в `pytest.ini` и `pyrightconfig.json`
   (оба сейчас чистые, не из 25 чужих)? *Рек.: да.*

## §5. Критерии готовности

- [ ] A4, A7, C3, C6 закрыты; для каждой — доказательство, что она действительно
      снята (docstring соответствует коду, файла нет, pyright на 3.12, узкий
      прогон `pytest` не падает по покрытию, а CI-гейт сохранён).
- [ ] `pytest` — набор падающих ID не вырос против **452** (сравнивать множества).
- [ ] `npm test` не хуже **566 passed / 1 skipped**; `tsc` ≤ **22**, все в `__tests__`.
- [ ] `npm run lint` при `--max-warnings 0` остаётся зелёным.
- [ ] `pre-commit run --all-files` на чистом worktree: **10 из 10**, прогон
      не переписывает ни одного файла.
- [ ] `pip-audit` пуст; в `npm audit` нет новых high.
- [ ] По каждому обсуждённому S-пункту в `docs/progress.md` записано решение
      с обоснованием — даже если решение «не делаем».
- [ ] `md5sum -c` — 25 чужих файлов побайтово целы.
- [ ] `docs/progress.md` и `docs/handoff.md` обновлены, следующий шаг назван.

## §6. Стоп-точки

Прод; любые траты на платные API; коммит любого из 25 чужих файлов; начало
реализации любого S-пункта до явного решения; переименование `ImagenService`;
включение GitHub Actions. В остальном работать непрерывно, промежуточные
проверки — молча.
