# Промпт: снять блокеры и закрыть хвосты

> Репозиторий `/Users/sandk/Documents/GitHub/fancai`, ветка `main`.
> **Якорь `8b706f49`** — обязан быть предком HEAD.
>
> **Ограничение «25 чужих файлов» отменено.** У проекта один разработчик
> и владелец, он же принимает все решения; незакоммиченные изменения
> в рабочем дереве — его собственные. Всё, что прошлые сессии откладывали
> «пока владелец не закоммитит своё», теперь исполнимо.
>
> Сессия начинается с **§1 — разбора блокеров**. Это разговор, а не работа:
> до решений исполнять нечего.

## §0. Вход

Прочитать целиком, не пересказывая здесь:

1. Верхняя запись `docs/handoff.md` — пять ловушек, долг, состояние e2e.
2. Верхняя запись `docs/progress.md` — замеры и доказательства, включая
   план по бандлу (§3.7) и вердикт по каждому этапу.
3. `docs/prompts/2026-08-04-accumulated-tails.md` — методология доказательств
   (§2 там) действует без изменений: живой прогон, обязательная мутация,
   сравнение с baseline, уборка за собой, цитата из вывода.

```bash
git merge-base --is-ancestor 8b706f49 HEAD && echo OK
git status --porcelain=v1     # 25 изменённых + 4 untracked — файлы владельца
```

Перед разбором WIP сделать полную страховку — именно коммитом или архивом,
`git diff` не сохраняет untracked и ненадёжен для бинарных файлов:

```bash
git stash push -u -m "wip-before-2026-08-05"   # и сразу git stash apply
```

## §1. Решения — одним сообщением, до любой правки

| # | Вопрос | Рекомендация |
| --- | --- | --- |
| 1 | **WIP владельца** (25 файлов, +485/−275): документация, `.planning/*`, `scripts/deploy*.sh`. Закоммитить как есть / с доработкой / откатить? | с доработкой — эти файлы блокируют четыре хвоста и расходятся с кодом |
| 2 | **Выкатка на прод**: ждут Волна 6, S1, S2, S4, TS 6, xmldom и 20 коммитов, среди них фикс теряющейся главы и починка `/images/generate/chapter`. Когда? | да, отдельным этапом после §2.1 |
| 3 | **Окно PostgreSQL 18** (≥ 45 мин, runbook готов). Назначаем? | после выкатки — не два необратимых изменения в одном окне |
| 4 | **E2E: вердикт по каждой из 8 спек** — актуальна / переписать / удалить. Часть описывает функциональность, которой нет (загрузка EPUB, галерея, offline-PWA). Список и состояние — в `handoff.md` | пройти вместе; зелёный прогон на честном подмножестве ценнее 103 тестов, из которых половина фантомная |
| 5 | **`.secrets.baseline`**: 146 файлов, все записи `is_verified: false`, включая `.env` и CI. Аудировать сейчас? | да — среди них могут быть настоящие ключи |
| 6 | **GitHub Actions** включать? От этого зависят 4.7/4.8 | по усмотрению |
| 7 | **Слить `Dockerfile.celery` и `Dockerfile.prod`?** Разблокируется вместе с #1 | да, различий три: `CMD`, healthcheck, `EXPOSE` |
| 8 | **Покрытие 47,61 % при гейте 70 %** — поднимать или оставить долгом? | оставить долгом, гейт не понижать |

**Ответы записать в `docs/handoff.md` таблицей «вопрос → решение» до начала
работы.** Дальше исполнять по ней.

## §2. Этапы

### 2.1. E2E до зелёного — главный этап

Приёмка прошлого промпта не выполнена: `npm run test:e2e` не проходит,
**25 passed / 78 failed** на chromium с пустой dev-БД. Это незакрытая работа,
а не внешний блокер.

1. **Обратимая фикстура среды** в `globalSetup`/`globalTeardown` Playwright:
   сохранять провенанс каждого аккаунта (существовал ли, с каким
   `password_hash`), создавать только отсутствующее, удалять **только
   созданное этим прогоном по ID**, восстанавливать в `finally`, проверять,
   что БД — dev. Прошлая попытка удалена именно за нарушение этих правил.
2. Довести спеки по вердиктам §1.4: актуальные — до зелёного, фантомные —
   удалить с обоснованием в коммите.
3. Прогнать все пять браузерных проектов, не только chromium.

**Приёмка:** `npm run test:e2e` зелёный; счётчики dev-БД после прогона равны
входным (1 книга / 23 главы / 0 сущностей / 0 описаний / 1 пользователь);
удалённые спеки и причины — в `progress.md`.

### 2.2. Хвосты, разблокированные решением #1

Flower из `README.md:81`, `README-ru.md:82`, `docs/deployment/README.md:42`,
`04-MIGRATION_RUNBOOK.md:18,398`; `MONITOR_USER` и сырой `MONITOR_PASSWORD`
из `.env.production.example` (их потреблял только Flower); упоминания
`requirements.txt` в README/CONTRIBUTING и в `AGENTS.md` — файла нет,
манифест `pyproject.toml` + `uv.lock`; слияние Dockerfile'ов по решению #7.

**Приёмка:** `grep -ri flower` по живым файлам даёт только исторические
отчёты; `docker compose -f docker-compose.prod.yml config -q` проходит.

### 2.3. Бандл

Разрыв: **2,12 МБ raw против 800 КБ**, 651 КБ gzip против 500 КБ.
План по чанкам — `progress.md` §3.7. Пороги в `check-bundle-size.js`
**не трогать**.

**Приёмка:** суммарный raw снижен названным числом; живая регрессия читалки
зелёная.

### 2.4. Выкатка на прод — стоп-точка, по решению #2

Бэкап → пересборка **backend, celery и frontend** → `alembic upgrade head`
(`e5f6a7b8c9d0`) → `/health` → smoke на боевых данных → живая проверка SPA
и читалки.

Frontend обязателен: накопленное включает React Router 8, замену xmldom
и резку бандла. Сервис `frontend` в `docker-compose.prod.yml:63` — build-only,
он пишет артефакт в том `frontend_build`, который читает Caddy; без его
пересборки прод останется на старой сборке при новом бэкенде.

Старые ключи `imagen:cache:` новый код не читает — истекут за 7 суток.

### 2.5. PostgreSQL 18 — стоп-точка, по решению #3

Строго по `docs/operations/migration/05-POSTGRES18_UPGRADE.md`, без
импровизаций: пять правок одним движением, заморозка очередей до дампа,
`docker inspect` как доказательство нового тома.

### 2.6. Аудит `.secrets.baseline` — по решению #5

`detect-secrets audit`, каждая запись помечается осознанно; настоящие ключи
ротировать, а не allowlist'ить. После — проверить, что подложенный тестовый
ключ хук по-прежнему отвергает.

## §3. Стоп-точки

Пауза и явное «да» перед: деплоем и любыми записями на проде; боевым
dump/restore PostgreSQL; выкаткой изменённого мониторинга; инвалидацией
продового Redis; запуском чего-либо из `scripts/`; `down -v` и `volume rm`
над dev-стеком (там единственный реальный EPUB, на котором стоят все smoke);
платными вызовами AI — smoke гоняется с погашенными ключами.

Полный `pytest` внутри `fancai_backend_dev` небезопасен без подготовки:
compose передаёт туда настоящий `OPENROUTER_API_KEY`, а `cache_manager.clear_all()`
(`app/core/cache.py:313`) делает `flushdb`.

Уточнение, которое стоит держать в голове: инвариант из `AGENTS.md`
(0 = app cache, 1 = брокер, 2 = результаты) соблюдён **только в проде** —
`docker-compose.prod.yml:102-103` задаёт `CELERY_BROKER_URL=…/1`
и `CELERY_RESULT_BACKEND=…/2`. В `docker-compose.dev.yml` этих переменных нет,
Celery падает на `settings.REDIS_URL` без суффикса БД, и кэш, брокер
и результаты в dev делят **одну DB 0**. Проверяется так:

```bash
docker exec fancai_celery_dev python -c \
  "from app.core.celery_app import celery_app; print(celery_app.conf.broker_url)"
```

То есть в dev `flushdb` сносит и брокер тоже. Расхождение dev с прод-инвариантом
— кандидат в отдельную задачу.

## §4. Готовность

- [ ] Решения §1 зафиксированы в `handoff.md` до начала исполнения.
- [ ] `npm run test:e2e` зелёный на всех проектах; удалённые спеки обоснованы.
- [ ] Срезы не хуже предыдущей сессии: `pytest` — множество падающих ID
      не выросло; `npm test` 566/1; `tsc` 22 в `__tests__`, 0 в build-конфиге;
      `pre-commit` 10 из 10; `mypy app/` зелёный; `pip-audit` пуст;
      `npm audit` без high; smoke — 6 режимов PASS.
- [ ] Счётчики dev-БД вернулись к 1 / 23 / 0 / 0 / 1.
- [ ] `progress.md`, `handoff.md`, `CHANGELOG.md` обновлены; следующий шаг
      назван явно.
