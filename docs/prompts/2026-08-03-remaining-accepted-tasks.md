# Промпт: оставшиеся хвосты после сессии техдолга

> Репозиторий `/Users/sandk/Documents/GitHub/fancai`, ветка `main`.
> **Якорь `795bbcf6`** — обязан быть предком HEAD; поверх него допустимы только
> docs-коммиты (сам этот промпт — один из них). Фактический HEAD брать из `git log -1`,
> в тексте он не назван: коммит не может содержать собственный хэш.

## Вход

Прочитать верхнюю запись **`docs/handoff.md`** — она самодостаточна и содержит
срезы проверок, четыре грабли окружения (`uv` вместо `pip`, гейт покрытия,
отсутствующая база `fancai_test`, боевой ключ AI внутри dev-контейнера),
список долга и границу по миграции GLiNER. Здесь это не дублируется.
Детали решений — верхняя запись `docs/progress.md`.

Проверить якорь; дерево грязное **25 чужими файлами + 4 untracked**, ни один
не трогать и не коммитить, снять `md5sum` на входе и сверять после каждого шага:

```bash
git merge-base --is-ancestor 795bbcf6 HEAD          # якорь на месте
git diff --name-only 795bbcf6..HEAD | grep -v '^docs/'   # пусто = код не коммитили
git status --porcelain=v1 -- backend frontend            # пусто = срезам можно верить
# Снимок делать ПЕРВЫМ действием, до любых своих правок: фильтр берёт все
# изменённые файлы, а не только чужие. Строк обязано быть ровно 25.
git status --porcelain=v1 | awk '/^ M /{print substr($0,4)}' | xargs md5sum > /tmp/foreign.md5
wc -l < /tmp/foreign.md5   # 25; другое число = сначала разобраться, что изменилось
```

## Хвосты по приоритету

Всё ниже — **уже принятые решения** (`docs/progress.md`, раздел «Пять
стратегических решений»), заново не обсуждать. Брать сверху; сколько успеется —
остальное перенести в handoff.

| # | Задача | Объём | Приёмка |
| --- | --- | --- | --- |
| 1 | **Удалить GLiNER2** (S4) | M | `grep -ri "gliner\|ner_service\|USE_GLINER" backend/app backend/tests` пуст; `torch` вне `uv.lock`; celery-образ пересобран и **замерен** против 1,52 ГБ; обработка книги по LLM-ветке живая; `setuptools 81.0.0` ушёл из `pip-audit` |
| 2 | **TypeScript 6 + `strict: true`** в `tsconfig-build.json` (S5, C2) | S | `npm run build` проходит; lint при `--max-warnings 0` зелёный; `tsc` ≤ 22 и все в `__tests__`. **TS 7 остаётся HOLD** |
| 3 | **`@xmldom/xmldom` → 0.9.x** через override (S5) | S+ | минус 5 high в `npm audit` **и живая регрессия читалки** на реальном EPUB: оглавление, переходы, CFI, подсветка. Зелёный `npm test` приёмкой не является |
| 4 | **Снять `ignore_errors`** в `mypy.ini` (S3) | L | 237 ошибок в 42 файлах; отдельная сессия, свой промпт |
| 5 | **B2** бюджет бандла (2,18 МБ / 669 КБ против 800 / 500 КБ) | M | отдельный проект |
| 6 | **B3** ~265 `data-testid` + форма логина | M | отдельный проект |
| 7 | **`mher/flower`** — замена или отказ; **PostgreSQL 17 → 18** | M | операции, нужно окно |
| 8 | Мелочи: переименование `ImagenService` (71 упоминание), удаление неиспользуемого `aiohttp`, строка про `backend/check-coverage.sh` в `CONTRIBUTING.md` | XS | последняя — только после того, как владелец закоммитит свои 25 файлов |

Пункты 1–3 реалистично закрыть за сессию, 4–8 — нет. 4.7/4.8 по-прежнему
заблокированы выключенными GitHub Actions.

## Спросить первым сообщением

1. Удалять ли `ChapterEmbedding` и пустую таблицу `chapter_embeddings` вместе
   с GLiNER? *Рек.: удалить — но **новой forward-миграцией**, не откатом
   `2026_03_24_0001` (почему — в handoff).*
2. Идти 1 → 2 → 3 непрерывно или показывать результат после каждого?
   *Рек.: непрерывно, приёмка в конце.*

## Стоп-точки

Прод; всё из `scripts/`; платные вызовы AI; коммит любого из 25 чужих файлов;
откат миграции `2026_03_24_0001` и `DROP EXTENSION vector`; включение Actions.

## Готовность

- [ ] Взятые пункты закрыты с приёмкой из таблицы; невзятые названы в handoff.
- [ ] Срезы не хуже baseline из handoff: `pytest` **458** (сравнивать множества),
      `npm test` **566 / 1**, `tsc` ≤ **22** в `__tests__`, lint зелёный,
      `pre-commit --all-files` на чистом worktree **10 из 10** без переписывания,
      `pip-audit` пуст, `npm audit` без новых high.
- [ ] `md5sum -c` — 25 чужих файлов целы.
- [ ] `docs/progress.md` и `docs/handoff.md` обновлены, следующий шаг назван.
