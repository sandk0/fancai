---
phase: 18-text-selection-notes
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - frontend/src/components/Reader/__tests__/HighlightTooltip.test.tsx
  - frontend/src/components/Reader/HighlightTooltip.tsx
  - frontend/src/components/Reader/EpubReader.tsx
  - frontend/src/components/Reader/Core/ReaderModals.tsx
  - frontend/src/hooks/epub/useAnnotationRendering.ts
  - frontend/src/components/Reader/SelectionMenu.tsx
autonomous: true
requirements:
  - SEL-02

must_haves:
  truths:
    - "Тап по существующему highlight показывает компактный tooltip с текстом заметки и кнопками Edit/Delete"
    - "Кнопка Delete удаляет заметку, tooltip закрывается"
    - "Кнопка Edit открывает SelectionMenu в режиме редактирования заметки"
    - "Тап вне tooltip закрывает его"
    - "highlightPopup из useAnnotationRendering подключён к UI"
  artifacts:
    - path: "frontend/src/components/Reader/__tests__/HighlightTooltip.test.tsx"
      provides: "Тест-стабы: рендер null при popup=null, рендер заметки, вызов onEdit/onDelete с bookmarkId"
      min_lines: 40
    - path: "frontend/src/components/Reader/HighlightTooltip.tsx"
      provides: "Компактный tooltip для тапа по highlight: текст заметки, Edit, Delete"
      min_lines: 50
    - path: "frontend/src/components/Reader/EpubReader.tsx"
      provides: "Wiring highlightPopup -> HighlightTooltip, обработчики onEdit/onDelete, editingBookmark state для edit mode"
      contains: "highlightPopup"
    - path: "frontend/src/components/Reader/Core/ReaderModals.tsx"
      provides: "HighlightTooltip в составе ReaderModals"
      contains: "HighlightTooltip"
  key_links:
    - from: "useAnnotationRendering.ts"
      to: "HighlightTooltip.tsx"
      via: "highlightPopup state передаётся через EpubReader -> ReaderModals"
      pattern: "highlightPopup"
    - from: "HighlightTooltip.tsx onDelete"
      to: "useBookmarkActions.deleteBookmark"
      via: "callback через EpubReader"
      pattern: "deleteBookmark"
    - from: "HighlightTooltip.tsx onEdit"
      to: "SelectionMenu (note edit mode)"
      via: "editingBookmark state -> SelectionMenu initialData prop"
      pattern: "editingBookmark"
---

<objective>
Создание HighlightTooltip компонента и подключение highlightPopup из useAnnotationRendering к UI.

Purpose: useAnnotationRendering уже генерирует highlightPopup state при клике по `.user-annotation`, но этот state нигде не отображается. EpubReader деструктурирует только `flashAnnotation`, игнорируя `highlightPopup` и `closePopup`. Нужно создать UI-компонент для отображения tooltip, подключить всё в EpubReader, и реализовать Edit через pre-populated SelectionMenu (per user decision).

Output: Рабочий tooltip при тапе по highlight с возможностью удаления и редактирования заметки.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-text-selection-notes/18-CONTEXT.md
@.planning/phases/18-text-selection-notes/18-RESEARCH.md
@.planning/phases/18-text-selection-notes/18-01-SUMMARY.md

@frontend/src/hooks/epub/useAnnotationRendering.ts
@frontend/src/components/Reader/EpubReader.tsx
@frontend/src/components/Reader/Core/ReaderModals.tsx
@frontend/src/components/Reader/SelectionMenu.tsx

<interfaces>
<!-- Ключевые типы и контракты для исполнителя -->

From frontend/src/hooks/epub/useAnnotationRendering.ts:
```typescript
interface HighlightPopup {
  bookmarkId: string;
  cfiRange: string;
  note: string | null;
  color: string | null;
  text_color: string | null;
  style: string;
  position: { x: number; y: number };
}

// Return type:
return {
  highlightPopup,   // HighlightPopup | null -- state, сейчас НЕ используется в EpubReader
  closePopup,       // () => void -- сейчас НЕ используется в EpubReader
  flashAnnotation,  // (bookmarkId: string) => void -- единственное что сейчас деструктурируется
};
```

From frontend/src/components/Reader/EpubReader.tsx (текущее подключение):
```typescript
// Строка 397: деструктурируется только flashAnnotation
const { flashAnnotation } = useAnnotationRendering({
  rendition,
  bookId: book.id,
  currentChapter,
  enabled: renditionReady,
});

// Строка 392: доступные bookmark actions
const { bookmarks, createBookmark, updateBookmark, deleteBookmark } = useBookmarkActions({
  bookId: book.id,
  currentChapter,
});

// Строка 445: существующий handleUpdateBookmarkNote
const handleUpdateBookmarkNote = useCallback(
  (bookmarkId: string, note: string) => {
    updateBookmark(bookmarkId, { note });
  },
  [updateBookmark]
);
```

From frontend/src/components/Reader/Core/ReaderModals.tsx:
```typescript
interface ReaderModalsProps {
  imageModal: { ... };
  entityDrawer: { ... };
  selection: {
    data: Selection | null;
    onCopy: () => void;
    onBookmark?: (opts: { color?: string | null; style?: string; note?: string; text_color?: string | null }) => void;
    onClose: () => void;
  };
  toc: { ... };
  positionConflict: { ... };
}
```

From frontend/src/hooks/epub/useBookmarks.ts:
```typescript
// BOOKMARK_COLORS используется для палитры цветов
export const BOOKMARK_COLORS = [
  { key: 'yellow', ... },
  { key: 'green', ... },
  { key: 'blue', ... },
  { key: 'pink', ... },
];
```

From frontend/src/components/Reader/SelectionMenu.tsx:
```typescript
interface SelectionMenuProps {
  selection: Selection | null;
  onCopy: () => void;
  onBookmark?: (opts: {
    color?: string | null;
    style?: string;
    note?: string;
    text_color?: string | null;
  }) => void;
  onClose: () => void;
}
// SelectionMenu имеет submenu states: 'main' | 'note'
// 'note' показывает палитру цветов, стили, textarea, Save
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Создание тест-стабов для HighlightTooltip (Wave 0 — Nyquist)</name>
  <files>frontend/src/components/Reader/__tests__/HighlightTooltip.test.tsx</files>
  <action>
Создать тестовый файл `frontend/src/components/Reader/__tests__/HighlightTooltip.test.tsx` по паттерну существующего `EntityPopup.test.tsx`.

Моки:
```typescript
vi.mock('react-i18next', () => ({
  useTranslation: () => ({
    t: (key: string, fallback?: string) => (typeof fallback === 'string' ? fallback : key),
  }),
}));
```

Не нужен мок lucide-react — иконки рендерятся как SVG без проблем в JSDOM.

**Тесты:**

1. `it('рендерит null когда popup === null')`: рендер с `popup={null}`, проверить что container пуст.

2. `it('рендерит текст заметки когда popup.note заполнен')`: рендер с popup, содержащим `note: 'Тестовая заметка'`, проверить что текст отображается через `screen.getByText`.

3. `it('не рендерит блок заметки когда note === null')`: рендер с `popup.note = null`, проверить что нет элемента с `line-clamp-3` или проверить отсутствие текста заметки.

4. `it('вызывает onEdit с bookmarkId при клике на Edit')`: рендер, найти кнопку Edit по aria-label, `fireEvent.click`, проверить `onEdit.toHaveBeenCalledWith('bm-1')`.

5. `it('вызывает onDelete с bookmarkId при клике на Delete')`: аналогично для Delete.

6. `it('вызывает onClose при клике вне tooltip')`: рендер, `fireEvent.pointerDown(document.body)`, проверить `onClose` вызван (с учётом setTimeout задержки из click outside — использовать `act` + `vi.advanceTimersByTime`).

Mock popup данные:
```typescript
const mockPopup = {
  bookmarkId: 'bm-1',
  cfiRange: 'epubcfi(/6/2!/4/2)',
  note: 'Тестовая заметка',
  color: '#fef08a',
  text_color: null,
  style: 'none',
  position: { x: 200, y: 300 },
};
```

Паттерн тестов: `describe('HighlightTooltip')` с `beforeEach` для fake timers.

ВАЖНО: Компонент ещё не создан — тесты должны импортировать из `'../HighlightTooltip'` и будут КРАСНЫМИ до создания компонента в Task 1. Это ожидаемое поведение TDD. Если import fails, тесты упадут с module not found — это корректное RED состояние.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npx vitest run src/components/Reader/__tests__/HighlightTooltip.test.tsx --reporter=verbose 2>&1 | tail -20; echo "Exit: $?"</automated>
  </verify>
  <done>
- Файл HighlightTooltip.test.tsx создан в __tests__/
- Тесты покрывают: null render, note text, onEdit callback, onDelete callback, click outside
- Тесты КРАСНЫЕ (failing) — ожидаемо, компонент ещё не создан
- Файл компилируется без TypeScript ошибок (кроме missing module)
  </done>
</task>

<task type="auto">
  <name>Task 1: Создание HighlightTooltip компонента</name>
  <files>frontend/src/components/Reader/HighlightTooltip.tsx, frontend/src/hooks/epub/useAnnotationRendering.ts</files>
  <action>
Создать новый компонент `HighlightTooltip.tsx` в `frontend/src/components/Reader/`.

**Экспорт HighlightPopup из useAnnotationRendering.ts:**
Добавить `export` к интерфейсу `HighlightPopup` в useAnnotationRendering.ts (строка 29):
```typescript
// БЫЛО: interface HighlightPopup {
// СТАНЕТ: export interface HighlightPopup {
```

**Props interface:**
```typescript
import type { HighlightPopup } from '@/hooks/epub/useAnnotationRendering';

interface HighlightTooltipProps {
  popup: HighlightPopup | null;
  onEdit: (bookmarkId: string) => void;
  onDelete: (bookmarkId: string) => void;
  onClose: () => void;
}
```

**Компонент:**
- Если `popup === null`, рендерить `null`
- Компактная карточка с fixed позиционированием, аналогично SelectionMenu.tsx (getMenuStyle pattern)
- Стилистика: `bg-popover text-popover-foreground rounded-lg shadow-lg border` (Tailwind, как SelectionMenu)
- Позиционирование: использовать `popup.position.x` и `popup.position.y`
  - Если position.y > 50% viewport — показать сверху точки тапа
  - Если position.y <= 50% viewport — показать снизу точки тапа
  - Горизонтально по центру (popup.position.x), с clamp чтобы не выходить за экран
- Padding: `p-3`
- Max-width: `max-w-[280px]`

**Содержимое tooltip:**
1. **Текст заметки** (если `popup.note` не null и не пустая строка):
   - `<p>` с `text-sm text-muted-foreground line-clamp-3` (максимум 3 строки)
   - Если note пустая/null — не показывать этот блок, только кнопки
2. **Цветовой индикатор**: маленький кружок (8px) с цветом `popup.color` (если есть)
3. **Кнопки** в горизонтальном flex:
   - **Edit** (Pencil icon из lucide-react): `onClick={() => onEdit(popup.bookmarkId)}`, aria-label="Edit"
   - **Delete** (Trash2 icon из lucide-react): `onClick={() => onDelete(popup.bookmarkId)}`, `text-destructive` цвет, aria-label="Delete"
   - Кнопки с `min-h-[44px] min-w-[44px]` touch target
   - Иконки размером 18px

**Click outside для закрытия:**
- useEffect с document.addEventListener('pointerdown') — если клик вне tooltip, вызвать onClose
- useRef на tooltip контейнер для проверки contains()
- setTimeout задержка (100ms) перед регистрацией listener (как в SelectionMenu), чтобы не закрывать при том же клике
- Cleanup в return useEffect

**Анимация:**
- Без анимации — простой conditional render (per user decision: без toast, без haptic)

Использовать `memo` из React для оптимизации.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npx vitest run src/components/Reader/__tests__/HighlightTooltip.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
- HighlightTooltip.tsx создан с props: popup, onEdit, onDelete, onClose
- Компактная карточка с позиционированием сверху/снизу точки тапа
- Текст заметки (line-clamp-3), цветовой индикатор, кнопки Edit/Delete
- Click outside закрывает tooltip
- Touch targets >= 44px
- HighlightPopup экспортирован из useAnnotationRendering.ts
- Тесты HighlightTooltip.test.tsx проходят (ЗЕЛЁНЫЕ)
  </done>
</task>

<task type="auto">
  <name>Task 2: Wiring HighlightTooltip в EpubReader и ReaderModals + Edit mode</name>
  <files>frontend/src/components/Reader/EpubReader.tsx, frontend/src/components/Reader/Core/ReaderModals.tsx, frontend/src/components/Reader/SelectionMenu.tsx</files>
  <action>
**SelectionMenu.tsx — добавить поддержку edit mode:**

1. **Расширить SelectionMenuProps:**
```typescript
interface SelectionMenuProps {
  selection: Selection | null;
  onCopy: () => void;
  onBookmark?: (opts: {
    color?: string | null;
    style?: string;
    note?: string;
    text_color?: string | null;
  }) => void;
  onClose: () => void;
  // NEW: для режима редактирования существующей заметки
  editMode?: {
    bookmarkId: string;
    note: string | null;
    color: string | null;
    text_color: string | null;
    style: string;
    position: { x: number; y: number };
    onSave: (bookmarkId: string, opts: { color?: string | null; style?: string; note?: string; text_color?: string | null }) => void;
  };
}
```

2. **Поведение в edit mode:**
- Если `editMode` задан, SelectionMenu рендерится ДАЖЕ при `selection === null` — позиция берётся из `editMode.position`
- `submenu` по умолчанию `'note'` (сразу открыт редактор)
- `noteText` pre-populated из `editMode.note`
- `selectedColor` pre-populated из `editMode.color`
- `selectedTextColor` pre-populated из `editMode.text_color`
- `selectedStyle` pre-populated из `editMode.style`
- Save вызывает `editMode.onSave(editMode.bookmarkId, opts)` вместо `onBookmark(opts)`
- В main submenu кнопка Copy скрыта (нет выделения для копирования) — показать только Note

3. **Изменения в useEffect сброса state (строка 63-69):**
```typescript
useEffect(() => {
  if (editMode) {
    // Edit mode: pre-populate from existing bookmark
    setSubmenu('note');
    setNoteText(editMode.note ?? '');
    setSelectedColor(editMode.color ?? BOOKMARK_COLORS[0].value);
    setSelectedTextColor(editMode.text_color ?? null);
    setSelectedStyle((editMode.style as BookmarkStyle) ?? 'none');
  } else {
    setSubmenu('main');
    setNoteText('');
    setSelectedColor(BOOKMARK_COLORS[0].value);
    setSelectedTextColor(null);
    setSelectedStyle('none');
  }
}, [selection?.cfiRange, editMode?.bookmarkId]);
```

4. **getMenuStyle: при editMode брать position из editMode.position:**
```typescript
const getMenuStyle = useCallback((): React.CSSProperties => {
  const pos = editMode?.position ?? selection?.position;
  if (!pos) return { display: 'none' };
  // ... rest of positioning logic using pos.x, pos.y
}, [selection, editMode, submenu]);
```

5. **handleSaveNote: при editMode вызвать onSave:**
```typescript
const handleSaveNote = useCallback(() => {
  const opts = {
    color: selectedColor,
    style: selectedStyle,
    note: noteText.trim() || undefined,
    text_color: selectedTextColor,
  };

  if (editMode) {
    editMode.onSave(editMode.bookmarkId, opts);
    onClose();
    return;
  }

  if (!onBookmark) return;
  const hasVisualStyle = selectedStyle !== 'none' || selectedColor || selectedTextColor;
  const hasNote = noteText.trim();
  if (!hasVisualStyle && !hasNote) return;
  onBookmark(opts);
  onClose();
}, [editMode, onBookmark, selectedColor, selectedTextColor, selectedStyle, noteText, onClose]);
```

6. **Render guard: при editMode или selection:**
```typescript
if (!selection && !editMode) return null;
```

**EpubReader.tsx:**

1. **Деструктурировать highlightPopup и closePopup** из useAnnotationRendering (строка 397):
```typescript
const { highlightPopup, closePopup, flashAnnotation } = useAnnotationRendering({ ... });
```

2. **Добавить editingBookmark state** для режима редактирования:
```typescript
const [editingBookmark, setEditingBookmark] = useState<{
  bookmarkId: string;
  note: string | null;
  color: string | null;
  text_color: string | null;
  style: string;
  position: { x: number; y: number };
} | null>(null);
```

3. **Добавить handleHighlightEdit callback:**
```typescript
const handleHighlightEdit = useCallback(
  (bookmarkId: string) => {
    closePopup();
    // Найти bookmark data для pre-population в SelectionMenu
    const bookmark = bookmarks.find((b) => b.id === bookmarkId);
    if (!bookmark) return;
    // Открыть SelectionMenu в edit mode с данными bookmark
    // Позиция берётся из highlightPopup (пока он ещё доступен) или bookmark position
    setEditingBookmark({
      bookmarkId,
      note: bookmark.note ?? null,
      color: bookmark.color ?? null,
      text_color: bookmark.text_color ?? null,
      style: bookmark.style ?? 'none',
      position: highlightPopup?.position ?? { x: window.innerWidth / 2, y: window.innerHeight / 2 },
    });
  },
  [closePopup, bookmarks, highlightPopup]
);
```

4. **Добавить handleEditSave callback:**
```typescript
const handleEditSave = useCallback(
  (bookmarkId: string, opts: { color?: string | null; style?: string; note?: string; text_color?: string | null }) => {
    updateBookmark(bookmarkId, opts);
    setEditingBookmark(null);
  },
  [updateBookmark]
);
```

5. **Добавить handleHighlightDelete callback:**
```typescript
const handleHighlightDelete = useCallback(
  (bookmarkId: string) => {
    deleteBookmark(bookmarkId);
    closePopup();
  },
  [deleteBookmark, closePopup]
);
```

6. **Закрывать editMode при clearSelection и closeEditMode:**
```typescript
const handleCloseEditMode = useCallback(() => {
  setEditingBookmark(null);
}, []);
```

7. **Передать highlight и editMode props в ReaderModals** (~строка 720):
```typescript
<ReaderModals
  // ... existing props ...
  highlight={{
    popup: highlightPopup,
    onEdit: handleHighlightEdit,
    onDelete: handleHighlightDelete,
    onClose: closePopup,
  }}
  selectionEditMode={editingBookmark ? {
    ...editingBookmark,
    onSave: handleEditSave,
  } : undefined}
  onCloseEditMode={handleCloseEditMode}
/>
```

**ReaderModals.tsx:**

1. **Импортировать HighlightTooltip:**
```typescript
import { HighlightTooltip } from '../HighlightTooltip';
import type { HighlightPopup } from '@/hooks/epub/useAnnotationRendering';
```

2. **Добавить highlight, selectionEditMode, onCloseEditMode props в ReaderModalsProps:**
```typescript
highlight?: {
  popup: HighlightPopup | null;
  onEdit: (bookmarkId: string) => void;
  onDelete: (bookmarkId: string) => void;
  onClose: () => void;
};
selectionEditMode?: {
  bookmarkId: string;
  note: string | null;
  color: string | null;
  text_color: string | null;
  style: string;
  position: { x: number; y: number };
  onSave: (bookmarkId: string, opts: { color?: string | null; style?: string; note?: string; text_color?: string | null }) => void;
};
onCloseEditMode?: () => void;
```

3. **Рендерить HighlightTooltip** в JSX, после SelectionMenu:
```typescript
{highlight && (
  <HighlightTooltip
    popup={highlight.popup}
    onEdit={highlight.onEdit}
    onDelete={highlight.onDelete}
    onClose={highlight.onClose}
  />
)}
```

4. **Передать editMode в SelectionMenu:**
```typescript
<SelectionMenu
  selection={selection.data}
  onCopy={selection.onCopy}
  onBookmark={selection.onBookmark}
  onClose={selectionEditMode ? onCloseEditMode! : selection.onClose}
  editMode={selectionEditMode}
/>
```
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
- highlightPopup и closePopup деструктурируются из useAnnotationRendering в EpubReader
- handleHighlightEdit открывает SelectionMenu в edit mode (per user decision) с pre-populated данными bookmark
- handleHighlightDelete удаляет закладку и закрывает popup
- SelectionMenu поддерживает editMode prop: pre-populated fields, save через updateBookmark
- ReaderModals принимает и рендерит HighlightTooltip + передаёт editMode в SelectionMenu
- Все тесты проходят, TypeScript build успешен
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vitest run src/components/Reader/__tests__/HighlightTooltip.test.tsx --reporter=verbose` -- тесты HighlightTooltip проходят
2. `cd frontend && npx vitest run --reporter=verbose` -- все тесты проходят
3. `cd frontend && npm run build` -- сборка проходит без ошибок
4. Grep: `grep -rn "HighlightTooltip" frontend/src/` -- компонент создан и подключён в ReaderModals
5. Grep: `grep -n "highlightPopup" frontend/src/components/Reader/EpubReader.tsx` -- деструктурирован из useAnnotationRendering
6. Grep: `grep -n "export interface HighlightPopup" frontend/src/hooks/epub/useAnnotationRendering.ts` -- тип экспортирован
7. Grep: `grep -n "editMode" frontend/src/components/Reader/SelectionMenu.tsx` -- edit mode поддерживается
8. Grep: `grep -n "editingBookmark" frontend/src/components/Reader/EpubReader.tsx` -- state для edit mode
</verification>

<success_criteria>
- HighlightTooltip.test.tsx создан и проходит (null render, note text, callbacks)
- HighlightTooltip.tsx создан с компактным UI (заметка, Edit, Delete)
- highlightPopup из useAnnotationRendering подключён через EpubReader -> ReaderModals -> HighlightTooltip
- Кнопка Delete удаляет закладку через deleteBookmark
- Кнопка Edit открывает SelectionMenu в режиме редактирования (per user decision) с pre-populated данными
- SelectionMenu поддерживает editMode: предзаполненные поля, save через updateBookmark
- Click outside закрывает tooltip
- Все тесты проходят, build успешен
</success_criteria>

<output>
After completion, create `.planning/phases/18-text-selection-notes/18-02-SUMMARY.md`
</output>
