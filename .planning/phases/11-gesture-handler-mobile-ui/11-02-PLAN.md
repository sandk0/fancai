---
phase: 11-gesture-handler-mobile-ui
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - frontend/src/components/UI/MobilePanel.tsx
  - frontend/src/components/Reader/TocSidebar.tsx
  - frontend/src/components/Reader/ReaderControls.tsx
  - frontend/src/components/Reader/Core/ReaderModals.tsx
  - frontend/src/components/Reader/ReaderHeader.tsx
autonomous: true
requirements: [MUI-01, MUI-03, MUI-04, MUI-06]

must_haves:
  truths:
    - "TocSidebar на мобильных отображается как vaul bottom-sheet с snap points 50%/90%, drag-to-dismiss и handle-полоской"
    - "ReaderControls (settings) на мобильных отображается как vaul bottom-sheet вместо dropdown"
    - "Все интерактивные кнопки ридера имеют минимум 44px touch target"
    - "Панели автоматически учитывают safe-area-inset-bottom на iOS"
    - "На desktop панели работают как раньше (боковая панель для TOC, dropdown для settings)"
  artifacts:
    - path: "frontend/src/components/UI/MobilePanel.tsx"
      provides: "Универсальная обертка: vaul bottom-sheet на мобильных, children на desktop"
      exports: ["MobilePanel"]
    - path: "frontend/src/components/Reader/TocSidebar.tsx"
      provides: "Оглавление — vaul bottom-sheet на мобильных, side panel на desktop"
    - path: "frontend/src/components/Reader/ReaderControls.tsx"
      provides: "Настройки — vaul bottom-sheet на мобильных, dropdown на desktop"
  key_links:
    - from: "frontend/src/components/UI/MobilePanel.tsx"
      to: "vaul Drawer"
      via: "Drawer.Root с snap points [0.5, 0.9], handle, backdrop blur"
      pattern: "Drawer\\.Root|snapPoints"
    - from: "frontend/src/components/Reader/TocSidebar.tsx"
      to: "frontend/src/components/UI/MobilePanel.tsx"
      via: "MobilePanel оборачивает контент TOC на мобильных"
      pattern: "MobilePanel|useIsMobile"
---

<objective>
Миграция панелей на vaul bottom-sheet + touch targets + safe areas

Создать MobilePanel обертку для vaul и мигрировать TocSidebar и ReaderControls на bottom-sheet для мобильных. Аудит и фикс touch targets во всех интерактивных элементах ридера. Проверка safe area insets.

Purpose: Удобное использование панелей одной рукой на мобильных, как Apple Books / iOS Maps
Output: MobilePanel.tsx, обновленные TocSidebar, ReaderControls, ReaderHeader
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-gesture-handler-mobile-ui/11-CONTEXT.md
@.planning/phases/11-gesture-handler-mobile-ui/11-RESEARCH.md
@.planning/phases/11-gesture-handler-mobile-ui/11-01-SUMMARY.md

@frontend/src/components/Reader/TocSidebar.tsx
@frontend/src/components/Reader/ReaderControls.tsx
@frontend/src/components/Reader/ReaderHeader.tsx
@frontend/src/components/Reader/Core/ReaderModals.tsx
@frontend/src/components/Entities/EntityDrawer.tsx
@frontend/src/components/Reader/ReaderSettingsPanel/ReaderSettingsPanel.tsx

<interfaces>
<!-- Существующие паттерны vaul из EntityDrawer.tsx -->
From frontend/src/components/Entities/EntityDrawer.tsx:
```typescript
import { Drawer } from 'vaul';

<Drawer.Root open={isOpen} onOpenChange={(open) => !open && onClose()}>
  <Drawer.Portal>
    <Drawer.Overlay className="fixed inset-0 bg-[var(--color-bg-overlay)] backdrop-blur-sm z-50" />
    <Drawer.Content className="bg-[var(--color-bg-base)] ...">
      <div className="mx-auto w-12 h-1.5 rounded-full bg-[var(--color-bg-hover)] mb-4" />
      {/* content */}
    </Drawer.Content>
  </Drawer.Portal>
</Drawer.Root>
```

From frontend/src/hooks/shared/useIsMobile.ts (создан в Plan 01):
```typescript
export const useIsMobile: () => boolean;
```

From frontend/src/lib/zIndex.ts:
```typescript
export const Z_INDEX = { sidebar: ..., modal: ..., modalOverlay: ... };
```

From frontend/src/styles/globals.css:
```css
@utility touch-target { min-width: 44px; min-height: 44px; }
@utility mt-safe { margin-top: env(safe-area-inset-top); }
@utility pb-safe { padding-bottom: env(safe-area-inset-bottom); }
@utility px-safe { padding-left: env(safe-area-inset-left); padding-right: env(safe-area-inset-right); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MobilePanel обертка + миграция TocSidebar и ReaderControls</name>
  <files>
    frontend/src/components/UI/MobilePanel.tsx
    frontend/src/components/Reader/TocSidebar.tsx
    frontend/src/components/Reader/ReaderControls.tsx
    frontend/src/components/Reader/Core/ReaderModals.tsx
  </files>
  <action>
1. **MobilePanel.tsx** — Универсальная обертка vaul:
   ```typescript
   interface MobilePanelProps {
     isOpen: boolean;
     onClose: () => void;
     title: string;
     children: React.ReactNode;
     snapPoints?: (number | string)[];
     defaultSnap?: number | string;
   }
   ```
   - На мобильных (useIsMobile): рендерить vaul `Drawer.Root` с:
     - `snapPoints={snapPoints || [0.5, 0.9]}` (snap 50% и 90%)
     - `activeSnapPoint` default = 0.9 (полностью открыта)
     - `Drawer.Overlay` с backdrop-blur-sm и dimmed background
     - `Drawer.Content` с rounded-t-2xl, bg-background, flex flex-col, max-h-[90vh]
     - Handle-полоска сверху: `<div className="mx-auto w-10 h-1 rounded-full bg-muted-foreground/30 my-3" />`
     - Header с title и кнопкой X (44px touch target)
     - Children обернуть в scrollable div с `overflow-y-auto flex-1 pb-safe`
     - `pb-safe` на внутреннем контенте для safe-area-inset-bottom
   - На desktop: рендерить children напрямую (без обертки) — каждый компонент сам управляет своим desktop-рендером
   - Экспортировать как named export

2. **TocSidebar.tsx** — Миграция на MobilePanel:
   - Импортировать useIsMobile из `@/hooks/shared/useIsMobile`
   - На мобильных: обернуть весь контент (tabs, search, chapter list, bookmarks) в `<MobilePanel>` вместо текущего m.div slide-in
   - На desktop: оставить текущий slide-in side panel (m.div с x: '100%' анимацией)
   - Удалить собственный backdrop на мобильных (MobilePanel предоставляет)
   - Snap points: [0.5, 0.9] (50% для быстрого просмотра, 90% для полного)
   - Убедиться что виртуализированный список (@tanstack/react-virtual) работает внутри vaul — контейнер scrollRef должен быть внутри Drawer.Content с overflow-y-auto
   - Сохранить `useFocusTrap` для desktop варианта

3. **ReaderControls.tsx** — Миграция settings dropdown на MobilePanel:
   - Импортировать useIsMobile
   - На мобильных: вместо `DropdownMenu` использовать `<MobilePanel>` с тем же контентом (theme, font size, wake lock, navigation mode, name highlighting)
   - На desktop: оставить DropdownMenu как есть
   - Props: `isOpen` + `onOpenChange` уже есть, использовать для MobilePanel `isOpen`/`onClose`
   - Snap points: [0.5, 0.9]

4. **ReaderModals.tsx** — Обновить проброс props:
   - Если TocSidebar теперь имеет MobilePanel внутри, убедиться что props проброшены правильно
   - Никаких новых props не нужно — TocSidebar сам определяет isMobile внутри
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - MobilePanel экспортирован и использует vaul с snap points [0.5, 0.9]
    - TocSidebar на мобильных показывается как bottom-sheet, на desktop как side panel
    - ReaderControls на мобильных показывается как bottom-sheet, на desktop как dropdown
    - Handle-полоска, backdrop blur, drag-to-dismiss работают
  </done>
</task>

<task type="auto">
  <name>Task 2: Аудит touch targets + safe area + финальная сборка</name>
  <files>
    frontend/src/components/Reader/ReaderHeader.tsx
    frontend/src/components/Reader/ReaderControls.tsx
    frontend/src/components/Reader/TocSidebar.tsx
  </files>
  <action>
1. **Touch target аудит (MUI-01):**
   Проверить и исправить ВСЕ интерактивные элементы ридера — минимум 44px:

   **ReaderHeader.tsx:**
   - Кнопка Back: уже `min-h-[44px] min-w-[44px]` — OK
   - Кнопки TOC, Info: `w-11 h-11` = 44px — OK
   - Кнопки Entities, Search, Settings: `w-11 h-11` = 44px — OK
   - Все OK, добавить `touch-target` class как fallback

   **ReaderControls.tsx (settings panel content):**
   - Кнопки темы: добавить `min-h-[44px]` (сейчас `px-3 py-2` может быть <44px)
   - Кнопки +/- размера шрифта: `h-10 w-10` = 40px — ИСПРАВИТЬ на `h-11 w-11` (44px)
   - Кнопки навигации (swipe/tap): добавить `min-h-[44px]`
   - Switch компоненты: проверить что touch area >= 44px

   **TocSidebar.tsx:**
   - ChapterItem кнопки: уже `min-h-[44px]` — OK
   - Кнопка закрытия X: уже `min-w-[44px] min-h-[44px]` — OK
   - Tab buttons: уже `min-h-[44px]` — OK
   - Search input: уже `min-h-[44px]` — OK

2. **Safe area insets (MUI-06):**
   - ReaderHeader: уже имеет `mt-safe` — OK
   - TocSidebar: уже имеет `pt-safe pb-safe` — OK
   - MobilePanel: контент уже имеет `pb-safe` (из Task 1) — OK
   - Проверить что все панели на iOS не перекрываются home indicator

3. **Финальная сборка и тест:**
   - `npm run build` — проверить что production build проходит
   - `npx vitest run` — проверить что существующие тесты не сломаны
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npm run build 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Все интерактивные кнопки ридера >= 44px touch target
    - Safe area insets корректно применяются на header, панелях и контенте
    - Production build проходит без ошибок
    - Существующие тесты проходят
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — TypeScript без ошибок
2. `cd frontend && npm run build` — Production build проходит
3. `cd frontend && npx vitest run --reporter=verbose` — Тесты проходят
4. Визуально: на мобильных TocSidebar и Settings — bottom-sheet с handle, на desktop — как раньше
</verification>

<success_criteria>
- MobilePanel обертка работает с vaul, snap points, handle, backdrop blur
- TocSidebar и ReaderControls мигрированы на bottom-sheet для мобильных
- Все кнопки ридера >= 44px touch target
- Safe areas корректны
- Desktop поведение не изменилось
</success_criteria>

<output>
После завершения создать `.planning/phases/11-gesture-handler-mobile-ui/11-02-SUMMARY.md`
</output>
