---
phase: 17-shapka-i-paneli
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/epub/useCFITracking.ts
  - frontend/src/components/Reader/ReaderFooter.tsx
  - frontend/src/components/Reader/ReaderHeader.tsx
  - frontend/src/components/Reader/Core/ReaderUI.tsx
  - frontend/src/components/Reader/EpubReader.tsx
autonomous: true
gap_closure: true
requirements: [HDR-01, HDR-02]

must_haves:
  truths:
    - "Прогресс-линия с процентом отображается внизу экрана, не в шапке"
    - "Прогресс-линия на ~95% ширины, процент ~5% справа"
    - "Под прогресс-линией: слева счетчик страниц (15 из 800), справа — страниц до конца главы"
    - "ReaderFooter показывается/скрывается вместе с шапкой (isHeaderVisible)"
    - "Шапка ридера больше не содержит прогресс-линию и процент"
  artifacts:
    - path: "frontend/src/components/Reader/ReaderFooter.tsx"
      provides: "Компонент нижней панели с прогресс-линией, процентом, страницами"
      min_lines: 40
    - path: "frontend/src/components/Reader/ReaderHeader.tsx"
      provides: "Шапка без прогресса — только кнопки навигации"
    - path: "frontend/src/hooks/epub/useCFITracking.ts"
      provides: "Данные chapterPage/chapterTotalPages для отображения страниц внутри главы"
  key_links:
    - from: "frontend/src/components/Reader/ReaderFooter.tsx"
      to: "frontend/src/components/Reader/Core/ReaderUI.tsx"
      via: "Рендер ReaderFooter в ReaderUI"
      pattern: "<ReaderFooter"
    - from: "frontend/src/components/Reader/EpubReader.tsx"
      to: "frontend/src/hooks/epub/useCFITracking.ts"
      via: "chapterPage и chapterTotalPages из useCFITracking"
      pattern: "chapterPage|chapterTotalPages"
---

<objective>
Создать ReaderFooter с прогресс-линией и удалить прогресс из ReaderHeader.

Purpose: Пользователь хочет видеть прогресс внизу читалки, а не в шапке — это освободит место в шапке для кнопок и уберёт визуальный шум. Прогресс-линия на всю ширину с процентом, счетчиком страниц и страницами до конца главы.

Output: Новый компонент ReaderFooter.tsx, обновлённый ReaderHeader без прогресса, расширенный useCFITracking с данными страниц внутри главы.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-shapka-i-paneli/17-01-SUMMARY.md
@.planning/phases/17-shapka-i-paneli/17-02-SUMMARY.md

<interfaces>
<!-- useCFITracking — нужно расширить возвращаемый объект -->
From frontend/src/hooks/epub/useCFITracking.ts:
```typescript
// Текущий return:
return {
  currentCFI,
  progress,
  scrollOffsetPercent,
  currentPage,    // Global page (1-based, location-based)
  totalPages,     // Global total pages
  goToCFI,
  skipNextRelocated,
  setInitialProgress,
};

// В handleRelocated уже вычисляются:
const displayedPage = location.start.displayed?.page || 1;    // Chapter page
const displayedTotal = location.start.displayed?.total || 1;   // Chapter total
// Но НЕ сохраняются в state и НЕ экспортируются
```

From frontend/src/components/Reader/Core/ReaderUI.tsx:
```typescript
interface ReaderUIProps {
  isVisible: boolean;
  isHeaderVisible: boolean;
  header: {
    metadata: ReaderUIHeaderMetadata;
    progress: number;
    currentPage?: number;
    totalPages?: number;
    onBack: () => void;
    onTocToggle: () => void;
    onSettingsOpen: () => void;
    onEntitiesOpen: () => void;
    onSearchToggle: () => void;
  };
  settings: { ... };
  imageStatus: { ... };
  saveStatus: { ... };
}
```

From frontend/src/components/Reader/ReaderHeader.tsx:
```typescript
interface ReaderHeaderProps {
  isVisible: boolean;
  title: string;
  author: string;
  progress: number;       // УДАЛИТЬ
  currentPage?: number;   // УДАЛИТЬ
  totalPages?: number;    // УДАЛИТЬ
  onBack: () => void;
  onTocToggle: () => void;
  onSettingsOpen: () => void;
  onEntitiesOpen: () => void;
  onSearchToggle: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить useCFITracking и создать ReaderFooter</name>
  <files>frontend/src/hooks/epub/useCFITracking.ts, frontend/src/components/Reader/ReaderFooter.tsx</files>
  <action>
1. В useCFITracking.ts:
   - Добавить state: `const [chapterPage, setChapterPage] = useState<number | null>(null);`
   - Добавить state: `const [chapterTotalPages, setChapterTotalPages] = useState<number | null>(null);`
   - В handleRelocated (после строки 271 где вычисляются displayedPage/displayedTotal): добавить `setChapterPage(displayedPage); setChapterTotalPages(displayedTotal);`
   - Добавить в return объект: `chapterPage, chapterTotalPages`
   - Обновить интерфейс UseCFITrackingReturn (если есть) добавив поля

2. Создать frontend/src/components/Reader/ReaderFooter.tsx:
   - Props: `isVisible: boolean`, `progress: number`, `currentPage?: number`, `totalPages?: number`, `chapterPage?: number`, `chapterTotalPages?: number`
   - Обернуть в AnimatePresence + m.div (аналогично ReaderHeader но снизу)
   - Анимация: initial={{ y: 60, opacity: 0 }}, animate={{ y: 0, opacity: 1 }}, exit={{ y: 60, opacity: 0 }}
   - Spring-конфиг как у HEADER_SPRING (stiffness: 400, damping: 35, mass: 1)
   - Layout:
     ```
     fixed left-0 right-0 bottom-0 mb-safe z-10 backdrop-blur-md bg-card/95 border-t border-border

     Первая строка (прогресс): flex items-center gap-2 px-4 pt-3 pb-1
       - Полоса прогресса (flex-1, h-1.5, rounded-full, bg-muted с внутренним bg-primary)
       - Процент (text-xs font-bold tabular-nums text-foreground, shrink-0)

     Вторая строка (страницы): flex items-center justify-between px-4 pb-3 pt-0
       - Слева: "{currentPage} из {totalPages}" (text-xs text-muted-foreground)
       - Справа: "{pagesLeft} стр. до конца главы" (text-xs text-muted-foreground)
         где pagesLeft = (chapterTotalPages - chapterPage) если оба доступны
     ```
   - Если currentPage/totalPages не определены — скрыть вторую строку
   - Использовать useTranslation для i18n ключей (reader.footer.page_of, reader.footer.pages_left)
   - memo() обёртка для оптимизации
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npm run build</automated>
  </verify>
  <done>ReaderFooter.tsx создан с полосой прогресса, процентом, страницами, анимацией. useCFITracking экспортирует chapterPage и chapterTotalPages. TypeScript build проходит.</done>
</task>

<task type="auto">
  <name>Task 2: Удалить прогресс из ReaderHeader, подключить ReaderFooter</name>
  <files>frontend/src/components/Reader/ReaderHeader.tsx, frontend/src/components/Reader/Core/ReaderUI.tsx, frontend/src/components/Reader/EpubReader.tsx</files>
  <action>
1. В ReaderHeader.tsx:
   - Удалить из ReaderHeaderProps: progress, currentPage, totalPages
   - Удалить из деструктуризации props: progress, currentPage, totalPages
   - Удалить clampedProgress, progressText вычисления
   - Удалить весь центральный блок с прогрессом (div className="flex-1 flex flex-col items-center min-w-0 gap-0.5"):
     - Строки с h1 (title), p (author), progress row, progress bar
   - На место центрального блока поставить div с flex-1 min-w-0 для title/author (только md+):
     ```tsx
     <div className="flex-1 flex flex-col items-center min-w-0">
       <h1 className="hidden md:block text-sm font-semibold truncate max-w-[240px] text-foreground">{title}</h1>
       <p className="hidden md:block text-xs truncate max-w-[200px] text-muted-foreground">{author}</p>
     </div>
     ```
   - Это сохранит flex-1 spacer для правильного расположения кнопок

2. В ReaderUI.tsx:
   - Импортировать ReaderFooter из '../ReaderFooter'
   - Добавить в ReaderUIProps: footer: { progress: number; currentPage?: number; totalPages?: number; chapterPage?: number; chapterTotalPages?: number; }
   - Удалить из header props: progress, currentPage, totalPages
   - Добавить рендер ReaderFooter после ReaderHeader (внутри <>...</>):
     ```tsx
     <ReaderFooter
       isVisible={isHeaderVisible}
       progress={footer.progress}
       currentPage={footer.currentPage}
       totalPages={footer.totalPages}
       chapterPage={footer.chapterPage}
       chapterTotalPages={footer.chapterTotalPages}
     />
     ```

3. В EpubReader.tsx:
   - Из useCFITracking деструктурировать дополнительно: chapterPage, chapterTotalPages
   - В ReaderUI props.header: удалить progress, currentPage, totalPages
   - Добавить в ReaderUI props: footer: { progress, currentPage: currentPage ?? undefined, totalPages: totalPages ?? undefined, chapterPage: chapterPage ?? undefined, chapterTotalPages: chapterTotalPages ?? undefined }
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npm run build</automated>
  </verify>
  <done>Прогресс удалён из шапки. ReaderFooter отображает прогресс-линию, процент, страницы, страницы до конца главы. Показывается/скрывается вместе с шапкой. TypeScript build проходит без ошибок.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` проходит без ошибок
- ReaderHeader.tsx не содержит progress, currentPage, totalPages
- ReaderFooter.tsx существует и экспортирует ReaderFooter
- useCFITracking.ts экспортирует chapterPage и chapterTotalPages
- ReaderUI.tsx рендерит ReaderFooter с isVisible={isHeaderVisible}
</verification>

<success_criteria>
- Прогресс-линия отображается внизу экрана (fixed bottom-0)
- Полоса прогресса занимает ~95% ширины, процент ~5% справа
- Под полосой: слева "{N} из {M}", справа "{K} стр. до конца главы"
- Footer показывается/скрывается вместе с шапкой
- Шапка содержит только кнопки навигации и title/author (md+)
</success_criteria>

<output>
After completion, create `.planning/phases/17-shapka-i-paneli/17-03-SUMMARY.md`
</output>
