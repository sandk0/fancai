---
phase: 17-shapka-i-paneli
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/epub/useGestureController.ts
  - frontend/src/components/UI/MobilePanel.tsx
  - frontend/src/components/Reader/SearchPanel.tsx
autonomous: true
gap_closure: true
requirements: [PNL-01, PNL-02]

must_haves:
  truths:
    - "Тап внутри epub iframe при открытой панели закрывает панель"
    - "MobilePanel (mobile TocSidebar, mobile Settings) корректно закрывается по свайпу вниз и outside-click"
    - "SearchPanel закрывается по клику вне неё (backdrop или iframe tap)"
    - "Desktop TocSidebar закрывается по клику внутри epub iframe"
  artifacts:
    - path: "frontend/src/hooks/epub/useGestureController.ts"
      provides: "Bridge iframe touch events к хост-странице для dismiss панелей"
    - path: "frontend/src/components/UI/MobilePanel.tsx"
      provides: "Реактивный activeSnapPoint вместо замороженной константы"
    - path: "frontend/src/components/Reader/SearchPanel.tsx"
      provides: "Backdrop или механизм outside-click для закрытия"
  key_links:
    - from: "frontend/src/hooks/epub/useGestureController.ts"
      to: "frontend/src/components/Reader/EpubReader.tsx"
      via: "onOutsideTap callback при isPanelOpen и тапе в iframe"
      pattern: "onOutsideTap|onPanelDismiss"
    - from: "frontend/src/components/UI/MobilePanel.tsx"
      to: "vaul"
      via: "setActiveSnapPoint вместо frozen activeSnapPoint"
      pattern: "setActiveSnapPoint|activeSnapPoint"
---

<objective>
Исправить закрытие панелей по клику вне них — bridge iframe events, фикс MobilePanel, backdrop для SearchPanel.

Purpose: Все панели (TocSidebar, Settings, EntityDrawer, SearchPanel) не закрываются при клике внутри epub iframe, потому что epub.js рендерит книгу в iframe со своим document. Dismiss-механизмы (Vaul, Radix) слушают host document и не видят iframe events. Дополнительно MobilePanel имеет замороженный activeSnapPoint, а SearchPanel вообще не имеет outside-click.

Output: Universal iframe bridge для dismiss панелей, исправленный MobilePanel, backdrop для SearchPanel.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-shapka-i-paneli/17-01-SUMMARY.md
@.planning/phases/17-shapka-i-paneli/17-02-SUMMARY.md
@.planning/debug/panels-close-outside-click.md

<interfaces>
<!-- useGestureController — текущие props и isPanelOpen handling -->
From frontend/src/hooks/epub/useGestureController.ts:
```typescript
interface UseGestureControllerOptions {
  rendition: Rendition | null;
  enabled: boolean;
  onNavigate: (dir: 'next' | 'prev') => Promise<void>;
  onChapterChange: (dir: 'next' | 'prev') => Promise<void>;
  onEdgeTap: (dir: 'next' | 'prev') => void;
  onCenterTap: (viewportX: number, viewportY: number) => void;
  onToggleUI: () => void;
  onSwipeStart: () => void;
  onTapNavigate: () => void;
  navLock: React.RefObject<boolean>;
  isPanelOpen: boolean;       // <-- уже есть
  pageAnimationEnabled: boolean;
}

// Когда isPanelOpen=true:
// - Строка 406: touchmove — блокирует свайп (cancelSwipe + return)
// - Строка 533: touchend — блокирует тап-навигацию (return early)
// - Строка 740: handleClick — блокирует клик-навигацию (return early)
// НО: при тапе в iframe с isPanelOpen=true — НИЧЕГО НЕ ДЕЛАЕТСЯ для закрытия панели
```

From frontend/src/components/Reader/EpubReader.tsx:
```typescript
// isPanelOpen вычисляется как:
const isPanelOpen = isTocOpen || isSettingsOpen || isEntityDrawerOpen || isSearchOpen;

// Передаётся в useGestureController:
const gestureController = useGestureController({
  ...
  isPanelOpen,
  ...
});
```

From frontend/src/components/UI/MobilePanel.tsx:
```typescript
// ПРОБЛЕМА: activeSnapPoint замороженная константа
const activeSnap = defaultSnap ?? snapPoints[snapPoints.length - 1];
// ...
<Drawer.Root
  open={isOpen}
  onOpenChange={(open) => !open && onClose()}
  snapPoints={snapPoints}
  activeSnapPoint={activeSnap}  // <-- FROZEN: Vaul не может dismiss
>
```

From frontend/src/components/Reader/SearchPanel.tsx:
```typescript
// ПРОБЛЕМА: нет backdrop, нет outside-click handler
// Закрытие только через: Escape key, X button
<m.div className="fixed left-0 right-0 z-20 ...">
  {/* Нет backdrop overlay */}
</m.div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bridge iframe touch events и фикс MobilePanel activeSnapPoint</name>
  <files>frontend/src/hooks/epub/useGestureController.ts, frontend/src/components/UI/MobilePanel.tsx</files>
  <action>
1. В useGestureController.ts — добавить callback `onPanelDismiss` в options interface:
   ```typescript
   interface UseGestureControllerOptions {
     // ... existing props ...
     onPanelDismiss?: () => void;  // Вызывается при тапе в iframe когда isPanelOpen=true
   }
   ```

2. В деструктуризации options добавить onPanelDismiss, создать ref:
   ```typescript
   const onPanelDismissRef = useRef(onPanelDismiss);
   // В useEffect для обновления refs:
   onPanelDismissRef.current = onPanelDismiss;
   ```

3. В handleTouchEnd (строка ~533, где isPanelOpen блокирует навигацию):
   Вместо простого `return;` при isPanelOpen — вызвать dismiss:
   ```typescript
   if (isPanelOpenRef.current) {
     // Bridge: тап в iframe при открытой панели → закрыть панель
     onPanelDismissRef.current?.();
     return;
   }
   ```

4. В handleClick (строка ~740, где isPanelOpen блокирует клик):
   Аналогично:
   ```typescript
   if (isPanelOpenRef.current) {
     onPanelDismissRef.current?.();
     return;
   }
   ```

5. В iOS overlay handleOverlayTouchEnd: добавить такую же проверку ПЕРЕД существующей логикой тапа:
   ```typescript
   if (isPanelOpenRef.current) {
     onPanelDismissRef.current?.();
     touchStart = null;
     return;
   }
   ```

6. В MobilePanel.tsx — исправить замороженный activeSnapPoint:
   ```typescript
   const [activeSnap, setActiveSnap] = useState<number | string | null>(
     defaultSnap ?? snapPoints[snapPoints.length - 1]
   );

   // Синхронизировать activeSnap при изменении defaultSnap/snapPoints
   useEffect(() => {
     if (isOpen) {
       setActiveSnap(defaultSnap ?? snapPoints[snapPoints.length - 1]);
     }
   }, [isOpen, defaultSnap, snapPoints]);
   ```

   Обновить Drawer.Root:
   ```tsx
   <Drawer.Root
     open={isOpen}
     onOpenChange={(open) => {
       if (!open) {
         setActiveSnap(null);
         onClose();
       }
     }}
     snapPoints={snapPoints}
     activeSnapPoint={activeSnap}
     setActiveSnapPoint={setActiveSnap}
   >
   ```

   Добавить импорт useState, useEffect в MobilePanel.tsx (если отсутствуют).
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npm run build</automated>
  </verify>
  <done>useGestureController вызывает onPanelDismiss при тапе в iframe с открытой панелью. MobilePanel имеет реактивный activeSnapPoint с setActiveSnapPoint. TypeScript build проходит.</done>
</task>

<task type="auto">
  <name>Task 2: Backdrop для SearchPanel и подключение onPanelDismiss в EpubReader</name>
  <files>frontend/src/components/Reader/SearchPanel.tsx, frontend/src/components/Reader/EpubReader.tsx</files>
  <action>
1. В SearchPanel.tsx — добавить невидимый backdrop при открытии:
   Внутри AnimatePresence, ПЕРЕД m.div с search bar, добавить backdrop:
   ```tsx
   {isOpen && (
     <>
       {/* Backdrop для outside-click dismiss */}
       <m.div
         initial={{ opacity: 0 }}
         animate={{ opacity: 1 }}
         exit={{ opacity: 0 }}
         className="fixed inset-0 z-19"
         onClick={onClose}
         aria-hidden="true"
       />
       <m.div
         className="fixed left-0 right-0 z-20 ..."
         // ... existing search bar content
       >
         ...
       </m.div>
     </>
   )}
   ```
   Backdrop имеет z-19 (ниже search bar z-20), прозрачный (без bg), но ловит клики.
   Использовать `z-[19]` вместо z-19 если Tailwind не имеет этого класса: `className="fixed inset-0 z-[19]"`

   ВАЖНО: backdrop должен быть внутри AnimatePresence и иметь key="search-backdrop" для корректной анимации.

2. В EpubReader.tsx — подключить onPanelDismiss:

   Создать callback для закрытия всех панелей:
   ```typescript
   const handlePanelDismiss = useCallback(() => {
     setIsTocOpen(false);
     setIsSettingsOpen(false);
     setIsEntityDrawerOpen(false);
     setIsSearchOpen(false);
   }, []);
   ```

   Передать в useGestureController:
   ```typescript
   const gestureController = useGestureController({
     // ... existing props ...
     onPanelDismiss: handlePanelDismiss,
   });
   ```

   Проверить что setIsEntityDrawerOpen существует. Найти через grep:
   - Если EntityDrawer управляется через другой state — использовать его setter
   - Проверить: `isEntityDrawerOpen` и его setter в EpubReader.tsx
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npm run build</automated>
  </verify>
  <done>SearchPanel имеет backdrop для outside-click dismiss. EpubReader передаёт onPanelDismiss в gestureController. Тап в iframe при открытой панели закрывает все панели. TypeScript build проходит.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` проходит без ошибок
- useGestureController.ts: onPanelDismiss в interface и вызывается при isPanelOpen + тапе
- MobilePanel.tsx: activeSnapPoint управляется через useState + setActiveSnapPoint
- SearchPanel.tsx: backdrop div с onClick={onClose} рендерится при isOpen
- EpubReader.tsx: handlePanelDismiss callback передаётся в gestureController
</verification>

<success_criteria>
- Тап внутри epub iframe при открытой панели (любой) закрывает панель
- MobilePanel (TocSidebar mobile, Settings mobile) закрывается свайпом вниз и по outside-click
- SearchPanel закрывается по клику на backdrop (вне search bar)
- Desktop TocSidebar/EntityDrawer закрываются при тапе в epub iframe
</success_criteria>

<output>
After completion, create `.planning/phases/17-shapka-i-paneli/17-05-SUMMARY.md`
</output>
