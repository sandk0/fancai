---
phase: 29-docker-db
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/entity.py
  - backend/app/models/description.py
  - backend/app/models/chapter_embedding.py
  - backend/app/models/__init__.py
  - backend/app/models/feature_flag.py
  - backend/alembic/env.py
  - backend/requirements.txt
  - backend/tests/services/test_feature_flag_model.py
  - backend/tests/services/test_chapter_embedding_model.py
autonomous: true
requirements:
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "Колонка pipeline_version существует в моделях Entity и Description (String(50), nullable, indexed)"
    - "Модель ChapterEmbedding определена с vector(384), FK на chapters.id, HNSW индекс"
    - "4 feature flags (USE_GLINER_NER, USE_DESCRIPTION_CLASSIFIER, USE_HYBRID_PIPELINE, USE_PGVECTOR_EMBEDDINGS) в DEFAULT_FEATURE_FLAGS"
    - "pgvector==0.4.2 добавлен в requirements.txt"
    - "Все тесты проходят: test_feature_flag_model.py и test_chapter_embedding_model.py"
  artifacts:
    - path: "backend/app/models/chapter_embedding.py"
      provides: "SQLAlchemy модель ChapterEmbedding с vector(384) и HNSW индексом"
      min_lines: 40
      contains: "class ChapterEmbedding"
    - path: "backend/app/models/entity.py"
      provides: "Entity модель с добавленной колонкой pipeline_version"
      contains: "pipeline_version"
    - path: "backend/app/models/description.py"
      provides: "Description модель с добавленной колонкой pipeline_version"
      contains: "pipeline_version"
    - path: "backend/app/models/feature_flag.py"
      provides: "DEFAULT_FEATURE_FLAGS с 10 флагами (6 старых + 4 новых NLP)"
      contains: "USE_GLINER_NER"
    - path: "backend/tests/services/test_chapter_embedding_model.py"
      provides: "Тесты для модели ChapterEmbedding"
      contains: "class TestChapterEmbeddingModel"
  key_links:
    - from: "backend/app/models/chapter_embedding.py"
      to: "backend/app/models/__init__.py"
      via: "import и export в __all__"
      pattern: "from .chapter_embedding import ChapterEmbedding"
    - from: "backend/app/models/chapter_embedding.py"
      to: "backend/alembic/env.py"
      via: "import для Alembic autogenerate"
      pattern: "from app.models.chapter_embedding import ChapterEmbedding"
    - from: "backend/app/models/feature_flag.py"
      to: "backend/tests/services/test_feature_flag_model.py"
      via: "тесты проверяют DEFAULT_FEATURE_FLAGS"
      pattern: "DEFAULT_FEATURE_FLAGS"
---

<objective>
Добавить schema для hybrid pipeline: pipeline_version в Entity/Description, модель ChapterEmbedding с pgvector, 4 NLP feature flags, и тесты.

Purpose: Schema и feature flags — фундамент для Phase 30-34. pipeline_version для A/B трекинга, chapter_embeddings для vector search, feature flags для поэтапного rollout.
Output: Обновлённые модели, новая модель ChapterEmbedding, 4 feature flags, pgvector в requirements.txt, тесты.
</objective>

<execution_context>
@/Users/sandk/Documents/GitHub/fancai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/Documents/GitHub/fancai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-docker-db/29-CONTEXT.md
@.planning/phases/29-docker-db/29-RESEARCH.md
</context>

<interfaces>
<!-- Текущие модели и паттерны -->

Из backend/app/models/entity.py (последняя колонка перед created_at):
```python
base_role: Mapped[str | None] = mapped_column(
    String(50), nullable=True, index=True
)

created_at: Mapped[datetime] = mapped_column(
    DateTime(timezone=True), nullable=False, server_default=func.now()
)
```

Из backend/app/models/description.py (последняя колонка перед created_at):
```python
generation_requested: Mapped[bool] = mapped_column(default=False, nullable=False)

created_at: Mapped[datetime] = mapped_column(
    DateTime(timezone=True), nullable=False, server_default=func.now()
)
```

Из backend/app/models/__init__.py:
```python
from .bookmark import Bookmark

__all__ = [
    # ...existing...
    "Bookmark",
]
```

Из backend/app/models/feature_flag.py:
```python
DEFAULT_FEATURE_FLAGS = [
    # 6 существующих флагов
    # ...
]
```

Из backend/app/core/database.py:
```python
from sqlalchemy.orm import DeclarativeBase

class Base(DeclarativeBase):
    pass
```

Из backend/alembic/env.py:
```python
from app.models.user import User, Subscription  # noqa: F401
# ... все модели импортируются для autogenerate ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Добавить pipeline_version в модели, создать ChapterEmbedding, обновить imports</name>
  <files>
    backend/app/models/entity.py,
    backend/app/models/description.py,
    backend/app/models/chapter_embedding.py,
    backend/app/models/__init__.py,
    backend/app/models/feature_flag.py,
    backend/alembic/env.py,
    backend/requirements.txt
  </files>
  <read_first>
    - backend/app/models/entity.py (текущее состояние модели — вставить pipeline_version после base_role)
    - backend/app/models/description.py (текущее состояние — вставить pipeline_version после generation_requested)
    - backend/app/models/__init__.py (текущие imports и __all__)
    - backend/app/models/feature_flag.py (текущие DEFAULT_FEATURE_FLAGS — 6 штук)
    - backend/alembic/env.py (текущие imports моделей)
    - backend/requirements.txt (текущие зависимости)
    - backend/app/core/database.py (Base class для наследования)
    - backend/app/models/chapter.py (проверить __tablename__ = "chapters" для FK)
  </read_first>
  <action>
**1. backend/app/models/entity.py (per D-10/INFRA-03):**
Добавить после `base_role` колонки и перед `created_at`:
```python
pipeline_version: Mapped[str | None] = mapped_column(
    String(50), nullable=True, index=True,
    comment="Pipeline version: null=legacy LLM, 'hybrid_v1'=GLiNER2+classifier"
)
```
Добавить `String` в import из sqlalchemy (уже есть).

**2. backend/app/models/description.py (per D-10/INFRA-03):**
Добавить после `generation_requested` и перед `created_at`:
```python
pipeline_version: Mapped[str | None] = mapped_column(
    String(50), nullable=True, index=True,
    comment="Pipeline version: null=legacy LLM, 'hybrid_v1'=GLiNER2+classifier"
)
```
Добавить `String` в import из sqlalchemy если нет (уже есть).

**3. backend/app/models/chapter_embedding.py (НОВЫЙ, per D-11/INFRA-03):**
Создать модель ChapterEmbedding:
```python
from datetime import datetime
from typing import TYPE_CHECKING
from uuid import UUID
import uuid as uuid_module

from sqlalchemy import Integer, Text, ForeignKey, DateTime, func, UniqueConstraint, Index
from sqlalchemy.dialects.postgresql import UUID as PG_UUID
from sqlalchemy.orm import relationship, Mapped, mapped_column
from pgvector.sqlalchemy import Vector

from ..core.database import Base

if TYPE_CHECKING:
    from .chapter import Chapter


class ChapterEmbedding(Base):
    __tablename__ = "chapter_embeddings"
    __table_args__ = (
        UniqueConstraint("chapter_id", "chunk_index",
                        name="uq_chapter_embeddings_chapter_chunk"),
        Index(
            "ix_chapter_embeddings_embedding_hnsw",
            "embedding",
            postgresql_using="hnsw",
            postgresql_with={"m": 16, "ef_construction": 64},
            postgresql_ops={"embedding": "vector_cosine_ops"},
        ),
    )

    id: Mapped[UUID] = mapped_column(
        PG_UUID(as_uuid=True), primary_key=True,
        default=uuid_module.uuid4, index=True
    )
    chapter_id: Mapped[UUID] = mapped_column(
        PG_UUID(as_uuid=True),
        ForeignKey("chapters.id", ondelete="CASCADE"),
        nullable=False, index=True,
    )
    chunk_index: Mapped[int] = mapped_column(
        Integer, nullable=False, server_default="0"
    )
    chunk_text: Mapped[str] = mapped_column(Text, nullable=False)
    embedding: Mapped[list] = mapped_column(Vector(384), nullable=False)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), nullable=False, server_default=func.now()
    )

    chapter: Mapped["Chapter"] = relationship("Chapter", lazy="raise")

    def __repr__(self) -> str:
        return (f"<ChapterEmbedding(id={self.id}, chapter_id={self.chapter_id}, "
                f"chunk_index={self.chunk_index})>")
```

**4. backend/app/models/__init__.py:**
Добавить import и __all__ entry:
```python
from .chapter_embedding import ChapterEmbedding
```
Добавить `"ChapterEmbedding"` в __all__ (после "Bookmark").

**5. backend/app/models/feature_flag.py (per D-13/INFRA-04):**
Добавить 4 новых словаря в DEFAULT_FEATURE_FLAGS (после последнего existing — ENABLE_IMAGE_CACHING):
```python
{
    "name": "USE_GLINER_NER",
    "enabled": False,
    "category": FeatureFlagCategory.NLP.value,
    "description": "Использовать GLiNER2 для локальной entity extraction вместо LLM",
    "default_value": False,
},
{
    "name": "USE_DESCRIPTION_CLASSIFIER",
    "enabled": False,
    "category": FeatureFlagCategory.NLP.value,
    "description": "Использовать TF-IDF/sentence-transformer для классификации описаний",
    "default_value": False,
},
{
    "name": "USE_HYBRID_PIPELINE",
    "enabled": False,
    "category": FeatureFlagCategory.NLP.value,
    "description": "Активировать полный гибридный pipeline (NER + classifier + synthesis)",
    "default_value": False,
},
{
    "name": "USE_PGVECTOR_EMBEDDINGS",
    "enabled": False,
    "category": FeatureFlagCategory.NLP.value,
    "description": "Использовать pgvector embeddings для контекстного обогащения при synthesis",
    "default_value": False,
},
```

**6. backend/alembic/env.py:**
Добавить import после существующих model imports (после bookmark):
```python
from app.models.chapter_embedding import ChapterEmbedding  # noqa: F401
```

**7. backend/requirements.txt:**
Добавить в секцию "# База данных" после `asyncpg==0.31.0`:
```
pgvector==0.4.2
```

Важно (ловушка 5 из RESEARCH.md): pgvector нужен в requirements.txt (shared), не только в Dockerfile.celery, потому что Alembic и backend API используют Vector type.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/backend && python3 -c "from app.models.chapter_embedding import ChapterEmbedding; from app.models.entity import Entity; from app.models.description import Description; from app.models.feature_flag import DEFAULT_FEATURE_FLAGS; print(f'ChapterEmbedding: {ChapterEmbedding.__tablename__}'); print(f'Entity pipeline_version: {hasattr(Entity, \"pipeline_version\")}'); print(f'Description pipeline_version: {hasattr(Description, \"pipeline_version\")}'); print(f'Flags count: {len(DEFAULT_FEATURE_FLAGS)}'); nlp_flags = [f[\"name\"] for f in DEFAULT_FEATURE_FLAGS if \"GLINER\" in f[\"name\"] or \"CLASSIFIER\" in f[\"name\"] or \"HYBRID\" in f[\"name\"] or \"PGVECTOR\" in f[\"name\"]]; print(f'NLP flags: {nlp_flags}')" 2>&1 || echo "IMPORT FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/models/entity.py содержит `pipeline_version: Mapped[str | None] = mapped_column(String(50), nullable=True, index=True`
    - backend/app/models/description.py содержит `pipeline_version: Mapped[str | None] = mapped_column(String(50), nullable=True, index=True`
    - backend/app/models/chapter_embedding.py содержит `class ChapterEmbedding(Base)`
    - backend/app/models/chapter_embedding.py содержит `__tablename__ = "chapter_embeddings"`
    - backend/app/models/chapter_embedding.py содержит `Vector(384)`
    - backend/app/models/chapter_embedding.py содержит `ForeignKey("chapters.id", ondelete="CASCADE")`
    - backend/app/models/chapter_embedding.py содержит `postgresql_using="hnsw"`
    - backend/app/models/chapter_embedding.py содержит `vector_cosine_ops`
    - backend/app/models/chapter_embedding.py содержит `lazy="raise"` на relationship
    - backend/app/models/__init__.py содержит `from .chapter_embedding import ChapterEmbedding`
    - backend/app/models/__init__.py содержит `"ChapterEmbedding"` в __all__
    - backend/app/models/feature_flag.py содержит `"USE_GLINER_NER"`
    - backend/app/models/feature_flag.py содержит `"USE_DESCRIPTION_CLASSIFIER"`
    - backend/app/models/feature_flag.py содержит `"USE_HYBRID_PIPELINE"`
    - backend/app/models/feature_flag.py содержит `"USE_PGVECTOR_EMBEDDINGS"`
    - len(DEFAULT_FEATURE_FLAGS) == 10 (6 старых + 4 новых)
    - Все 4 новых флага имеют enabled=False, category="nlp", default_value=False
    - backend/alembic/env.py содержит `from app.models.chapter_embedding import ChapterEmbedding`
    - backend/requirements.txt содержит `pgvector==0.4.2`
  </acceptance_criteria>
  <done>pipeline_version добавлен в Entity и Description, ChapterEmbedding создан с vector(384) и HNSW индексом, 4 NLP feature flags добавлены в DEFAULT_FEATURE_FLAGS, pgvector в requirements.txt, imports обновлены</done>
</task>

<task type="auto">
  <name>Task 2: Создать Alembic-миграцию для hybrid pipeline schema</name>
  <files>backend/alembic/versions/</files>
  <read_first>
    - backend/app/models/entity.py (модель с pipeline_version — из Task 1)
    - backend/app/models/description.py (модель с pipeline_version — из Task 1)
    - backend/app/models/chapter_embedding.py (новая модель — из Task 1)
    - backend/alembic/env.py (обновлённый с import ChapterEmbedding)
  </read_first>
  <action>
Создать одну Alembic-миграцию для всех schema-изменений (per D-11, D-12):

```bash
cd /Users/sandk/Documents/GitHub/fancai/backend && alembic revision --autogenerate -m "add hybrid pipeline schema: pgvector, pipeline_version, chapter_embeddings"
```

Если autogenerate не работает (требует production DB connection), создать миграцию вручную:
```bash
cd /Users/sandk/Documents/GitHub/fancai/backend && alembic revision -m "add hybrid pipeline schema: pgvector, pipeline_version, chapter_embeddings"
```

Затем вручную заполнить upgrade() и downgrade():

**upgrade():**
```python
from alembic import op
import sqlalchemy as sa
from pgvector.sqlalchemy import Vector

def upgrade() -> None:
    # 1. pgvector extension (per D-12)
    op.execute("CREATE EXTENSION IF NOT EXISTS vector")

    # 2. pipeline_version в entities (per D-10)
    op.add_column("entities", sa.Column(
        "pipeline_version", sa.String(50), nullable=True,
        comment="Pipeline version: null=legacy LLM, 'hybrid_v1'=GLiNER2+classifier"
    ))
    op.create_index("ix_entities_pipeline_version", "entities", ["pipeline_version"])

    # 3. pipeline_version в descriptions (per D-10)
    op.add_column("descriptions", sa.Column(
        "pipeline_version", sa.String(50), nullable=True,
        comment="Pipeline version: null=legacy LLM, 'hybrid_v1'=GLiNER2+classifier"
    ))
    op.create_index("ix_descriptions_pipeline_version", "descriptions", ["pipeline_version"])

    # 4. chapter_embeddings таблица (per D-11)
    op.create_table(
        "chapter_embeddings",
        sa.Column("id", sa.UUID(), primary_key=True,
                  server_default=sa.text("gen_random_uuid()")),
        sa.Column("chapter_id", sa.UUID(),
                  sa.ForeignKey("chapters.id", ondelete="CASCADE"),
                  nullable=False, index=True),
        sa.Column("chunk_index", sa.Integer(), nullable=False, server_default="0"),
        sa.Column("chunk_text", sa.Text(), nullable=False),
        sa.Column("embedding", Vector(384), nullable=False),
        sa.Column("created_at", sa.DateTime(timezone=True),
                  server_default=sa.text("NOW()"), nullable=False),
        sa.UniqueConstraint("chapter_id", "chunk_index",
                           name="uq_chapter_embeddings_chapter_chunk"),
    )

    # 5. HNSW индекс для cosine similarity (per D-11)
    op.create_index(
        "ix_chapter_embeddings_embedding_hnsw",
        "chapter_embeddings",
        ["embedding"],
        postgresql_using="hnsw",
        postgresql_with={"m": 16, "ef_construction": 64},
        postgresql_ops={"embedding": "vector_cosine_ops"},
    )
```

**downgrade():**
```python
def downgrade() -> None:
    op.drop_index("ix_chapter_embeddings_embedding_hnsw",
                  table_name="chapter_embeddings")
    op.drop_table("chapter_embeddings")
    op.drop_index("ix_descriptions_pipeline_version", table_name="descriptions")
    op.drop_column("descriptions", "pipeline_version")
    op.drop_index("ix_entities_pipeline_version", table_name="entities")
    op.drop_column("entities", "pipeline_version")
    op.execute("DROP EXTENSION IF EXISTS vector")
```

Важно: Миграция создаётся локально и будет выполнена на production при deploy (alembic upgrade head). Автогенерация может не работать без production DB — тогда писать вручную.
  </action>
  <verify>
    <automated>ls /Users/sandk/Documents/GitHub/fancai/backend/alembic/versions/*hybrid_pipeline* 2>/dev/null | head -1 | xargs -I{} grep -l "CREATE EXTENSION" {} && echo "PASS" || echo "FAIL - migration file not found or missing CREATE EXTENSION"</automated>
  </verify>
  <acceptance_criteria>
    - Файл миграции существует в backend/alembic/versions/ с именем содержащим "hybrid_pipeline"
    - Миграция содержит `CREATE EXTENSION IF NOT EXISTS vector`
    - Миграция содержит `add_column("entities"` с `pipeline_version`
    - Миграция содержит `add_column("descriptions"` с `pipeline_version`
    - Миграция содержит `create_table("chapter_embeddings"`
    - Миграция содержит `Vector(384)`
    - Миграция содержит `create_index` с `hnsw` и `vector_cosine_ops`
    - Миграция содержит `ForeignKey("chapters.id", ondelete="CASCADE")`
    - downgrade() содержит `drop_table("chapter_embeddings")`
    - downgrade() содержит `drop_column` для обеих таблиц
    - downgrade() содержит `DROP EXTENSION IF EXISTS vector`
  </acceptance_criteria>
  <done>Alembic-миграция создана: pgvector extension, pipeline_version в entities/descriptions, таблица chapter_embeddings с vector(384) и HNSW индексом, downgrade для rollback</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Тесты для ChapterEmbedding модели и обновление тестов feature flags</name>
  <files>
    backend/tests/services/test_chapter_embedding_model.py,
    backend/tests/services/test_feature_flag_model.py
  </files>
  <read_first>
    - backend/tests/services/test_feature_flag_model.py (текущие тесты — обновить count 6->10 и добавить тесты для новых флагов)
    - backend/app/models/chapter_embedding.py (модель для тестирования)
    - backend/app/models/feature_flag.py (обновлённый DEFAULT_FEATURE_FLAGS с 10 флагами)
    - backend/app/models/entity.py (проверить наличие pipeline_version)
    - backend/app/models/description.py (проверить наличие pipeline_version)
  </read_first>
  <behavior>
    - Test: DEFAULT_FEATURE_FLAGS содержит 10 записей (6 старых + 4 новых)
    - Test: USE_GLINER_NER, USE_DESCRIPTION_CLASSIFIER, USE_HYBRID_PIPELINE, USE_PGVECTOR_EMBEDDINGS присутствуют в DEFAULT_FEATURE_FLAGS
    - Test: Все 4 новых NLP флага имеют enabled=False, default_value=False, category="nlp"
    - Test: ChapterEmbedding.__tablename__ == "chapter_embeddings"
    - Test: ChapterEmbedding создаётся с chapter_id, chunk_index, chunk_text, embedding
    - Test: ChapterEmbedding repr содержит id и chapter_id
    - Test: Entity модель имеет атрибут pipeline_version
    - Test: Description модель имеет атрибут pipeline_version
  </behavior>
  <action>
**1. backend/tests/services/test_chapter_embedding_model.py (НОВЫЙ):**
```python
"""Тесты для модели ChapterEmbedding."""
import uuid
from datetime import datetime

from app.models.chapter_embedding import ChapterEmbedding


class TestChapterEmbeddingModel:
    """Тесты для модели ChapterEmbedding."""

    def test_chapter_embedding_table_name(self):
        """Тест что правильное имя таблицы."""
        assert ChapterEmbedding.__tablename__ == "chapter_embeddings"

    def test_chapter_embedding_creation(self):
        """Тест создания экземпляра ChapterEmbedding."""
        chapter_id = uuid.uuid4()
        embedding = ChapterEmbedding(
            chapter_id=chapter_id,
            chunk_index=0,
            chunk_text="Тестовый текст главы для embedding",
            embedding=[0.1] * 384,
        )
        assert embedding.chapter_id == chapter_id
        assert embedding.chunk_index == 0
        assert embedding.chunk_text == "Тестовый текст главы для embedding"
        assert len(embedding.embedding) == 384

    def test_chapter_embedding_repr(self):
        """Тест строкового представления."""
        test_id = uuid.uuid4()
        chapter_id = uuid.uuid4()
        embedding = ChapterEmbedding(
            id=test_id,
            chapter_id=chapter_id,
            chunk_index=3,
        )
        repr_str = repr(embedding)
        assert "ChapterEmbedding" in repr_str
        assert str(test_id) in repr_str
        assert str(chapter_id) in repr_str

    def test_chapter_embedding_vector_dimension(self):
        """Тест что embedding колонка поддерживает 384 измерения."""
        embedding = ChapterEmbedding(
            chapter_id=uuid.uuid4(),
            chunk_index=0,
            chunk_text="Test",
            embedding=[0.0] * 384,
        )
        assert len(embedding.embedding) == 384

    def test_chapter_embedding_hnsw_index_defined(self):
        """Тест что HNSW индекс определён в table_args."""
        table_args = ChapterEmbedding.__table_args__
        index_names = [
            arg.name for arg in table_args
            if hasattr(arg, 'name') and arg.name and 'hnsw' in arg.name
        ]
        assert "ix_chapter_embeddings_embedding_hnsw" in index_names

    def test_chapter_embedding_unique_constraint(self):
        """Тест что unique constraint на (chapter_id, chunk_index) определён."""
        table_args = ChapterEmbedding.__table_args__
        constraint_names = [
            arg.name for arg in table_args
            if hasattr(arg, 'name') and arg.name and 'uq_' in arg.name
        ]
        assert "uq_chapter_embeddings_chapter_chunk" in constraint_names


class TestPipelineVersionColumn:
    """Тесты для pipeline_version колонки в Entity и Description."""

    def test_entity_has_pipeline_version(self):
        """Тест что Entity имеет атрибут pipeline_version."""
        from app.models.entity import Entity
        assert hasattr(Entity, "pipeline_version")

    def test_description_has_pipeline_version(self):
        """Тест что Description имеет атрибут pipeline_version."""
        from app.models.description import Description
        assert hasattr(Description, "pipeline_version")

    def test_entity_pipeline_version_nullable(self):
        """Тест что pipeline_version в Entity nullable (default None)."""
        from app.models.entity import Entity
        entity = Entity(name="Test", type="character", name_lower="test")
        assert entity.pipeline_version is None

    def test_description_pipeline_version_nullable(self):
        """Тест что pipeline_version в Description nullable (default None)."""
        from app.models.description import Description
        desc = Description(
            content="test", type="character",
            confidence_score=0.5, position_in_chapter=0,
        )
        assert desc.pipeline_version is None
```

**2. backend/tests/services/test_feature_flag_model.py:**
Обновить существующие тесты:

- `test_default_feature_flags_count`: изменить `assert len(DEFAULT_FEATURE_FLAGS) == 6` на `assert len(DEFAULT_FEATURE_FLAGS) == 10`
- `test_default_feature_flags_names`: добавить 4 новых имени в expected_names set:
  ```python
  expected_names = {
      "USE_NEW_NLP_ARCHITECTURE",
      "USE_ADVANCED_PARSER",
      "USE_LLM_ENRICHMENT",
      "ENABLE_ENSEMBLE_VOTING",
      "ENABLE_PARALLEL_PROCESSING",
      "ENABLE_IMAGE_CACHING",
      "USE_GLINER_NER",
      "USE_DESCRIPTION_CLASSIFIER",
      "USE_HYBRID_PIPELINE",
      "USE_PGVECTOR_EMBEDDINGS",
  }
  ```
- `test_default_feature_flags_categories`: добавить 4 новых маппинга:
  ```python
  "USE_GLINER_NER": FeatureFlagCategory.NLP.value,
  "USE_DESCRIPTION_CLASSIFIER": FeatureFlagCategory.NLP.value,
  "USE_HYBRID_PIPELINE": FeatureFlagCategory.NLP.value,
  "USE_PGVECTOR_EMBEDDINGS": FeatureFlagCategory.NLP.value,
  ```

Добавить 4 новых теста для каждого NLP флага:
```python
def test_default_feature_flags_use_gliner_ner_disabled(self):
    """Тест что USE_GLINER_NER отключен по умолчанию."""
    flag = next(f for f in DEFAULT_FEATURE_FLAGS if f["name"] == "USE_GLINER_NER")
    assert flag["enabled"] is False
    assert flag["default_value"] is False
    assert flag["category"] == FeatureFlagCategory.NLP.value

def test_default_feature_flags_use_description_classifier_disabled(self):
    """Тест что USE_DESCRIPTION_CLASSIFIER отключен по умолчанию."""
    flag = next(f for f in DEFAULT_FEATURE_FLAGS if f["name"] == "USE_DESCRIPTION_CLASSIFIER")
    assert flag["enabled"] is False
    assert flag["default_value"] is False

def test_default_feature_flags_use_hybrid_pipeline_disabled(self):
    """Тест что USE_HYBRID_PIPELINE отключен по умолчанию."""
    flag = next(f for f in DEFAULT_FEATURE_FLAGS if f["name"] == "USE_HYBRID_PIPELINE")
    assert flag["enabled"] is False
    assert flag["default_value"] is False

def test_default_feature_flags_use_pgvector_embeddings_disabled(self):
    """Тест что USE_PGVECTOR_EMBEDDINGS отключен по умолчанию."""
    flag = next(f for f in DEFAULT_FEATURE_FLAGS if f["name"] == "USE_PGVECTOR_EMBEDDINGS")
    assert flag["enabled"] is False
    assert flag["default_value"] is False
```
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/backend && uv run python -m pytest tests/services/test_feature_flag_model.py tests/services/test_chapter_embedding_model.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/services/test_chapter_embedding_model.py существует и содержит `class TestChapterEmbeddingModel`
    - backend/tests/services/test_chapter_embedding_model.py содержит тесты: test_chapter_embedding_table_name, test_chapter_embedding_creation, test_chapter_embedding_repr, test_chapter_embedding_vector_dimension, test_chapter_embedding_hnsw_index_defined, test_chapter_embedding_unique_constraint
    - backend/tests/services/test_chapter_embedding_model.py содержит `class TestPipelineVersionColumn` с тестами для Entity и Description pipeline_version
    - backend/tests/services/test_feature_flag_model.py: `assert len(DEFAULT_FEATURE_FLAGS) == 10`
    - backend/tests/services/test_feature_flag_model.py содержит `"USE_GLINER_NER"` в expected_names
    - backend/tests/services/test_feature_flag_model.py содержит тест `test_default_feature_flags_use_gliner_ner_disabled`
    - `uv run python -m pytest tests/services/test_feature_flag_model.py tests/services/test_chapter_embedding_model.py -v` exits 0
  </acceptance_criteria>
  <done>Тесты для ChapterEmbedding модели (table_name, creation, repr, vector dimension, HNSW index, unique constraint) и pipeline_version (Entity, Description) созданы и проходят. Тесты feature flags обновлены для 10 флагов и проходят.</done>
</task>

</tasks>

<verification>
- `cd backend && uv run python -m pytest tests/services/test_feature_flag_model.py tests/services/test_chapter_embedding_model.py -v --tb=short` — все тесты зелёные
- `grep "pipeline_version" backend/app/models/entity.py` — колонка добавлена
- `grep "pipeline_version" backend/app/models/description.py` — колонка добавлена
- `grep "USE_GLINER_NER" backend/app/models/feature_flag.py` — флаг присутствует
- `grep "pgvector" backend/requirements.txt` — пакет добавлен
- `grep "ChapterEmbedding" backend/app/models/__init__.py` — import присутствует
- `grep "chapter_embedding" backend/alembic/env.py` — import для autogenerate
- `ls backend/alembic/versions/*hybrid*` — миграция существует
</verification>

<success_criteria>
- pipeline_version (String(50), nullable, indexed) добавлен в Entity и Description
- ChapterEmbedding модель создана с vector(384), FK на chapters, HNSW индекс, unique constraint
- 4 NLP feature flags добавлены в DEFAULT_FEATURE_FLAGS (все disabled, category nlp)
- pgvector==0.4.2 в requirements.txt
- Alembic-миграция создана с pgvector extension, columns, table, indexes
- Все тесты проходят (feature_flag_model + chapter_embedding_model)
</success_criteria>

<output>
После завершения создать `.planning/phases/29-docker-db/29-02-SUMMARY.md`
</output>
