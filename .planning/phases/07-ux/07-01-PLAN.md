---
phase: 07-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/utils/errorMessages.ts
  - frontend/src/locales/ru/translation.json
  - frontend/src/locales/en/translation.json
  - frontend/src/components/ErrorBoundary.tsx
  - frontend/src/components/Reader/EpubReader.tsx
  - frontend/src/components/Reader/Core/ReaderOverlays.tsx
autonomous: true
requirements: [UX-02, UX-04]

must_haves:
  truths:
    - "Все API-ошибки маппятся на понятные русские сообщения через централизованную утилиту"
    - "ErrorBoundary показывает русские тексты вместо английских"
    - "ReaderOverlays использует mapApiError вместо ad-hoc getReadableError"
    - "Ошибки CircuitBreaker отображаются как 'AI-сервис временно перегружен'"
    - "UX-04 — при смене глав loading НЕ показывается (решение пользователя)"
  artifacts:
    - path: "frontend/src/utils/errorMessages.ts"
      provides: "Централизованный маппинг API-ошибок на русские сообщения"
      exports: ["mapApiError", "ErrorMapping"]
    - path: "frontend/src/locales/ru/translation.json"
      provides: "Новые ключи ошибок: errors.ai_overloaded, errors.server_unavailable, errors.network и т.д."
    - path: "frontend/src/components/ErrorBoundary.tsx"
      provides: "Переведённые на русский тексты ошибок"
  key_links:
    - from: "frontend/src/utils/errorMessages.ts"
      to: "frontend/src/locales/ru/translation.json"
      via: "i18n.t() вызовы для русских сообщений"
      pattern: "i18n\\.t\\('errors\\."
    - from: "frontend/src/components/Reader/EpubReader.tsx"
      to: "frontend/src/utils/errorMessages.ts"
      via: "импорт mapApiError вместо getReadableError"
      pattern: "import.*mapApiError.*from.*errorMessages"
---

<objective>
Создать централизованную утилиту маппинга ошибок и стандартизировать отображение ошибок во всём приложении.

Purpose: UX-02 требует единообразных, информативных сообщений на русском для всех состояний ошибок. UX-04 — задокументировать решение не показывать loading при смене глав. Сейчас маппинг ошибок дублируется ad-hoc (getReadableError в EpubReader), ErrorBoundary содержит английские строки. Нужна единая утилита + перевод всех пользовательских сообщений.

Output: Утилита `errorMessages.ts`, расширенные i18n ключи, переведённый ErrorBoundary, обновлённый EpubReader/ReaderOverlays
</objective>

<execution_context>
@/Users/sandk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ux/07-CONTEXT.md
@.planning/phases/07-ux/07-RESEARCH.md

<interfaces>
<!-- Ключевые типы и контракты для исполнителя. Извлечены из кодовой базы. -->

From frontend/src/components/UI/ErrorMessage.tsx:
```typescript
interface ErrorMessageProps {
  title?: string;
  message: string;
  onRetry?: () => void;
  action?: { label: string; onClick: () => void; };
  className?: string;
  variant?: 'default' | 'compact';
}
```

From frontend/src/stores/ui.ts:
```typescript
export const notify = {
  success: (title: string, message?: string) => void;
  error: (title: string, message?: string, action?: NotificationAction) => void;
  warning: (title: string, message?: string, action?: NotificationAction) => void;
  info: (title: string, message?: string) => void;
};
```

From frontend/src/components/Reader/Core/ReaderOverlays.tsx:
```typescript
interface ReaderOverlaysProps {
  error: {
    message: string | null;
    readableMessage: string;
    onRetry: () => void;
    onHome: () => void;
  };
  // ...другие пропсы
}
```

From frontend/src/components/Reader/EpubReader.tsx (строки 54-64):
```typescript
// Ad-hoc маппинг, который нужно заменить на mapApiError:
const getReadableError = useCallback((errorStr: string) => {
  const lowerError = errorStr.toLowerCase();
  if (lowerError.includes('network') || lowerError.includes('failed to fetch')) return t('reader.error.network');
  if (lowerError.includes('404')) return t('reader.error.not_found');
  if (lowerError.includes('401')) return t('reader.error.session_expired');
  if (lowerError.includes('403')) return t('reader.error.forbidden');
  if (lowerError.includes('500')) return t('reader.error.server_error');
  if (lowerError.includes('timeout')) return t('reader.error.timeout');
  if (lowerError.includes('epub') || lowerError.includes('parse')) return t('reader.error.format_error');
  return t('reader.error.unknown');
}, [t]);
```

From frontend/src/components/ErrorBoundary.tsx (строки 176-186):
```typescript
// Английские хардкоды, которые нужно заменить на i18n:
{isAppLevel && 'Ups! Something went wrong'}
{isPageLevel && 'Page load error'}
{!isAppLevel && !isPageLevel && 'Component error'}
// ...
{isAppLevel && 'We apologize for the inconvenience...'}
{isAppLevel ? 'Reload page' : 'Try again'}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Утилита маппинга ошибок + i18n ключи + перевод ErrorBoundary</name>
  <files>
    frontend/src/utils/errorMessages.ts,
    frontend/src/locales/ru/translation.json,
    frontend/src/locales/en/translation.json,
    frontend/src/components/ErrorBoundary.tsx
  </files>
  <behavior>
    - mapApiError({response: {status: 503, data: {detail: 'circuit breaker open'}}}) возвращает {message: русское сообщение о перегрузке AI, isRetryable: false}
    - mapApiError({response: {status: 429}}) возвращает {message: русское сообщение о лимите, isRetryable: true}
    - mapApiError({response: {status: 500}}) возвращает {message: русское сообщение о недоступности сервера, isRetryable: true}
    - mapApiError({response: {status: 404}}) возвращает {message: русское сообщение "не найдено", isRetryable: false}
    - mapApiError(new Error('network error')) возвращает {message: русское сообщение о сети, isRetryable: true}
    - mapApiError(new Error('timeout')) возвращает {message: русское сообщение о таймауте, isRetryable: true}
    - mapApiError('unknown') возвращает {message: русское сообщение об общей ошибке, isRetryable: true}
    - mapApiError для строк (совместимость с getReadableError): mapApiError('404 not found') корректно маппится
    - ErrorBoundary рендерится с русскими текстами (проверить через i18n ключи)
  </behavior>
  <action>
1. Создать `frontend/src/utils/errorMessages.ts`:
   - Экспортировать интерфейс `ErrorMapping { message: string; action?: string; isRetryable: boolean }`
   - Экспортировать функцию `mapApiError(error: unknown): ErrorMapping`
   - Обрабатывать: axios-ошибки (error.response.status/data.detail), Error объекты, строки (для обратной совместимости с getReadableError)
   - Использовать `i18n.t()` для получения русских сообщений
   - CircuitBreaker (503 + detail содержит 'circuit'): isRetryable=false, сообщение "AI-сервис временно перегружен"
   - 503 без circuit: isRetryable=true, сообщение "Сервис временно недоступен"
   - 429: isRetryable=true
   - 500+: isRetryable=true
   - 404: isRetryable=false
   - 401/403: isRetryable=false
   - Network/timeout: isRetryable=true
   - Fallback: isRetryable=true

2. Добавить новые ключи в `frontend/src/locales/ru/translation.json` в секцию `errors`:
   - `errors.ai_overloaded`: "AI-сервис временно перегружен. Попробуйте через несколько минут."
   - `errors.server_unavailable`: "Сервер временно недоступен. Попробуйте позже."
   - `errors.rate_limit`: "Слишком много запросов. Подождите немного."
   - `errors.network`: "Проверьте подключение к интернету и попробуйте снова."
   - `errors.timeout`: "Время ожидания истекло. Попробуйте повторить."
   - `errors.not_found`: "Запрашиваемый ресурс не найден."
   - `errors.unauthorized`: "Сессия истекла. Войдите снова."
   - `errors.forbidden`: "Доступ запрещён."
   - `errors.format_error`: "Ошибка формата книги. Попробуйте загрузить другой файл."
   - `errors.unknown`: "Произошла непредвиденная ошибка. Попробуйте обновить страницу."
   - `errors.retry`: "Повторить"
   - Также добавить ключи для ErrorBoundary: `errorBoundary.app_title`, `errorBoundary.page_title`, `errorBoundary.component_title`, `errorBoundary.app_message`, `errorBoundary.page_message`, `errorBoundary.component_message`, `errorBoundary.reload`, `errorBoundary.try_again`, `errorBoundary.home`, `errorBoundary.help`

3. Добавить те же ключи в `frontend/src/locales/en/translation.json` (английские варианты для полноты).

4. Обновить `frontend/src/components/ErrorBoundary.tsx`:
   - Импортировать `useTranslation` нельзя (class component). Импортировать `i18n` напрямую из `@/lib/i18n`
   - Заменить хардкоденные английские строки на `i18n.t('errorBoundary.app_title')` и т.д.
   - Заменить 'Reload page' на `i18n.t('errorBoundary.reload')`, 'Try again' на `i18n.t('errorBoundary.try_again')`, 'Home' на `i18n.t('errorBoundary.home')`
   - Заменить 'If the problem persists, please contact support' на `i18n.t('errorBoundary.help')`

UX-04: loading при смене глав НЕ реализуется — решение пользователя. Никаких skeleton/shimmer. Документировано в SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/frontend && npx vitest run src/utils/__tests__/errorMessages.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Файл errorMessages.ts экспортирует mapApiError, которая маппит все типы ошибок на русские сообщения
    - Все ошибки CircuitBreaker возвращают isRetryable=false
    - i18n ключи добавлены в оба файла переводов (ru + en)
    - ErrorBoundary показывает русские тексты через i18n
    - Тесты маппинга ошибок зелёные
  </done>
</task>

<task type="auto">
  <name>Task 2: Интеграция mapApiError в EpubReader и ReaderOverlays</name>
  <files>
    frontend/src/components/Reader/EpubReader.tsx,
    frontend/src/components/Reader/Core/ReaderOverlays.tsx
  </files>
  <action>
1. В `frontend/src/components/Reader/EpubReader.tsx`:
   - Импортировать `mapApiError` из `@/utils/errorMessages`
   - Удалить функцию `getReadableError` (строки 54-64)
   - Заменить использование `getReadableError(error)` на `mapApiError(error).message` в пропсах ReaderOverlays (строка 242):
     ```typescript
     error={{
       message: error,
       readableMessage: error ? mapApiError(error).message : '',
       isRetryable: error ? mapApiError(error).isRetryable : true,
       onRetry: reload,
       onHome: () => navigate('/library')
     }}
     ```

2. В `frontend/src/components/Reader/Core/ReaderOverlays.tsx`:
   - Расширить интерфейс `error` пропсом `isRetryable?: boolean`
   - Если `isRetryable === false`, НЕ показывать кнопку "Повторить" — только "Вернуться в библиотеку"
   - Использовать компонент `ErrorMessage` из `@/components/UI/ErrorMessage` вместо inline разметки для единообразия (variant='default')
   - Импортировать ErrorMessage: `import ErrorMessage from '@/components/UI/ErrorMessage'`
   - Заменить inline error-блок (строки 54-65) на:
     ```tsx
     <div className={`absolute inset-0 flex items-center justify-center ${backgroundColor} z-10`}>
       <ErrorMessage
         message={error.readableMessage}
         onRetry={error.isRetryable !== false ? error.onRetry : undefined}
         action={{ label: t('reader.error.back_to_library'), onClick: error.onHome }}
       />
     </div>
     ```
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - getReadableError удалён из EpubReader.tsx
    - EpubReader использует mapApiError для маппинга ошибок
    - ReaderOverlays использует ErrorMessage компонент для единообразного отображения
    - Кнопка retry скрыта для не-retryable ошибок (CircuitBreaker)
    - Билд проходит без ошибок TypeScript
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vitest run src/utils/__tests__/errorMessages.test.ts` — тесты маппинга ошибок зелёные
2. `cd frontend && npm run build` — TypeScript билд проходит
3. Файл `errorMessages.ts` экспортирует `mapApiError` и `ErrorMapping`
4. `getReadableError` больше не существует в EpubReader.tsx
5. ErrorBoundary.tsx не содержит английских хардкодов (кроме dev-only error details)
</verification>

<success_criteria>
- Централизованная утилита маппинга ошибок используется вместо ad-hoc решений
- Все пользовательские сообщения об ошибках на русском языке
- CircuitBreakerError отображается как "AI-сервис временно перегружен" без кнопки retry
- ReaderOverlays использует единообразный ErrorMessage компонент
- UX-04 задокументирован как "не реализуется" (решение пользователя)
</success_criteria>

<output>
После завершения создать `.planning/phases/07-ux/07-01-SUMMARY.md`
</output>
