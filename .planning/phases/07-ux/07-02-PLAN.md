---
phase: 07-ux
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/components/UI/ParsingOverlay.tsx
  - frontend/src/components/Reader/ExtractionIndicator.tsx
  - frontend/src/components/Reader/EpubReader.tsx
  - frontend/src/hooks/api/useDescriptions.ts
  - frontend/src/locales/ru/translation.json
  - frontend/src/locales/en/translation.json
autonomous: true
requirements: [UX-03, UX-05]

must_haves:
  truths:
    - "При ошибке парсинга книги пользователь видит информативное сообщение с объяснением и кнопку повтора"
    - "При сбое AI-извлечения описаний пользователь видит понятное объяснение и может повторить без перезагрузки страницы"
    - "Лимит retry ограничен (3 попытки), после исчерпания показывается сообщение о необходимости подождать"
    - "CircuitBreakerError при извлечении не показывает кнопку retry"
  artifacts:
    - path: "frontend/src/components/UI/ParsingOverlay.tsx"
      provides: "Обработка статуса error от бэкенда + показ parsing_error + кнопка повторной обработки"
    - path: "frontend/src/components/Reader/ExtractionIndicator.tsx"
      provides: "Error state + retry кнопка + лимит попыток"
      exports: ["ExtractionIndicator"]
  key_links:
    - from: "frontend/src/components/UI/ParsingOverlay.tsx"
      to: "бэкенд API getParsingStatus"
      via: "polling/WebSocket — обработка status=error + Chapter.parsing_error"
      pattern: "status.*error|parsing_error"
    - from: "frontend/src/components/Reader/ExtractionIndicator.tsx"
      to: "frontend/src/hooks/api/useDescriptions.ts"
      via: "extractionError и onRetry пропсы из хука описаний"
      pattern: "extractionError|onRetry"
    - from: "frontend/src/components/Reader/EpubReader.tsx"
      to: "frontend/src/components/Reader/ExtractionIndicator.tsx"
      via: "рендер ExtractionIndicator с error/retry пропсами"
      pattern: "ExtractionIndicator"
---

<objective>
Расширить ParsingOverlay и ExtractionIndicator для обработки ошибок с retry-функциональностью.

Purpose: UX-03 — при сбое парсинга книги пользователь видит информативное сообщение + кнопка повтора. UX-05 — при сбое AI-извлечения описаний пользователь видит объяснение + может повторить генерацию без перезагрузки. Сейчас ParsingOverlay не обрабатывает ошибки (прогресс-бар застревает), ExtractionIndicator не имеет error state (компонент экспортирован, но не подключён к ошибкам).

Output: Расширенные ParsingOverlay и ExtractionIndicator, подключённый ExtractionIndicator в EpubReader
</objective>

<execution_context>
@/Users/sandk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ux/07-CONTEXT.md
@.planning/phases/07-ux/07-RESEARCH.md
@.planning/phases/07-ux/07-01-SUMMARY.md

<interfaces>
<!-- Ключевые типы и контракты из Plan 01 и существующего кода. -->

From frontend/src/utils/errorMessages.ts (создаётся в Plan 01):
```typescript
export interface ErrorMapping {
  message: string;
  action?: string;
  isRetryable: boolean;
}
export function mapApiError(error: unknown): ErrorMapping;
```

From frontend/src/components/UI/ParsingOverlay.tsx (текущие пропсы):
```typescript
interface ParsingOverlayProps {
  bookId: string;
  onParsingComplete?: () => void;
  onCancel?: () => void;
  forceBlock?: boolean;
  useWebSocket?: boolean;
}
// Внутри: useBookProgressWS для WebSocket + polling fallback
// Polling вызывает booksAPI.getParsingStatus(bookId) и проверяет statusData.status
// Сейчас обрабатывает только 'completed', НЕ обрабатывает 'error'/'failed'
```

From frontend/src/components/Reader/ExtractionIndicator.tsx (текущие пропсы):
```typescript
interface ExtractionIndicatorProps {
  isExtracting: boolean;
  onCancel?: () => void;
}
// Компонент экспортирован в index.ts, но НЕ используется в EpubReader.tsx
```

From frontend/src/hooks/api/useDescriptions.ts:
```typescript
// useChapterDescriptions — при ошибке LLM-извлечения TanStack Query возвращает error state
// Используется ...TANSTACK_RETRY_OPTIONS.descriptionExtraction (3 попытки)
export function useChapterDescriptions(bookId: string, chapterNumber: number, options?): UseQueryResult;
export function useReextractDescriptions(bookId: string, chapterNumber: number, options?): UseQueryResult;
```

From frontend/src/hooks/epub/useChapterManagement (вызывается в EpubReader):
```typescript
// Возвращает: { currentChapter, descriptions, images }
// descriptions загружаются через useChapterDescriptions внутри
```

From frontend/src/components/Library/BookCard/BookCard.tsx:
```typescript
// ParsingOverlay используется здесь для карточки книги в библиотеке
<ParsingOverlay bookId={book.id} onParsingComplete={...} onCancel={...} />
```

From бэкенд (Chapter модель):
```python
# backend/app/models/chapter.py:49
parsing_error: str | None  # Бэкенд уже хранит ошибку парсинга
```

From бэкенд (Book модель):
```python
# Book.parsing_error также существует
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ParsingOverlay — обработка ошибок парсинга + retry (UX-03)</name>
  <files>
    frontend/src/components/UI/ParsingOverlay.tsx,
    frontend/src/locales/ru/translation.json,
    frontend/src/locales/en/translation.json
  </files>
  <action>
1. Расширить ParsingOverlay для обработки статуса ошибки:

   В **polling fallback** (функция `checkProgress`, строки 136-160):
   - После получения `statusData`, добавить проверку на ошибку:
     ```typescript
     if (statusData.status === 'error' || statusData.status === 'failed') {
       setParsingError(statusData.parsing_error || statusData.error || t('errors.parsing_failed'));
       return; // Прекратить polling
     }
     ```
   - Добавить состояние: `const [parsingError, setParsingError] = useState<string | null>(null);`

   В **WebSocket** обработчике `useBookProgressWS`:
   - Добавить `onParsingError` callback в опции:
     ```typescript
     onError: (error) => {
       // Разделить: WebSocket connection error -> fallback к polling
       // vs parsing error (status='error' в WS-сообщении) -> показать ошибку
       if (error.type === 'parsing') {
         setParsingError(error.message || t('errors.parsing_failed'));
       } else {
         // connection error — fallback к polling (существующая логика)
         setUsePollingFallback(true);
       }
     },
     ```
   - ПРИМЕЧАНИЕ: Если useBookProgressWS не поддерживает type='parsing', просто ловить parsing errors через polling (достаточно).

2. Добавить UI для состояния ошибки в render:
   - Если `parsingError` установлен, заменить прогресс-кольцо на блок ошибки:
     ```tsx
     {parsingError ? (
       <div className="flex flex-col items-center gap-4 px-6 text-center">
         <div className="w-16 h-16 rounded-full bg-red-500/20 flex items-center justify-center">
           <AlertCircle className="w-8 h-8 text-red-400" />
         </div>
         <div>
           <p className="text-white font-medium text-base mb-1">{t('errors.parsing_failed')}</p>
           <p className="text-white/70 text-sm max-w-xs">{parsingError}</p>
         </div>
         <div className="flex gap-3">
           <button onClick={handleRetryParsing} className="flex items-center gap-2 px-4 py-2 rounded-lg bg-white/20 hover:bg-white/30 text-white font-medium transition-all">
             <RefreshCw className="w-4 h-4" />
             <span>{t('errors.retry')}</span>
           </button>
           {onCancel && (
             <button onClick={onCancel} className="px-4 py-2 rounded-lg bg-white/10 hover:bg-white/20 text-white/70">
               {t('ui.parsing.cancel')}
             </button>
           )}
         </div>
       </div>
     ) : (
       // ... существующее прогресс-кольцо ...
     )}
     ```
   - Импортировать `AlertCircle`, `RefreshCw` из lucide-react

3. Реализовать `handleRetryParsing`:
   - Вызвать `booksAPI.reprocessBook(bookId)` (POST /api/books/{id}/reprocess)
   - Сбросить `parsingError` и `progress` в начальное состояние
   - Если такого API нет — использовать `booksAPI.processBook(bookId)` или аналог
   - При ошибке повторного запуска — показать toast через notify.error
   - Проверить наличие API перед реализацией: `grep -r "reprocess\|process_book\|start_processing" backend/app/routers/`

4. Добавить i18n ключи (если не добавлены в Plan 01):
   - `errors.parsing_failed`: "Ошибка при обработке книги" (ru), "Book processing error" (en)
   - `errors.parsing_retry_started`: "Повторная обработка запущена" (ru), "Reprocessing started" (en)
   - `errors.parsing_chapter_error`: "Ошибка при обработке главы: {{error}}" (ru)
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - ParsingOverlay обрабатывает status=error/failed от polling/WebSocket
    - При ошибке парсинга показывается информативное сообщение с текстом ошибки
    - Кнопка "Повторить" запускает переобработку книги
    - Прогресс-бар не застревает при ошибках — пользователь видит что произошло
    - TypeScript билд проходит
  </done>
</task>

<task type="auto">
  <name>Task 2: ExtractionIndicator — error state + retry + подключение в EpubReader (UX-05)</name>
  <files>
    frontend/src/components/Reader/ExtractionIndicator.tsx,
    frontend/src/components/Reader/EpubReader.tsx,
    frontend/src/hooks/api/useDescriptions.ts,
    frontend/src/locales/ru/translation.json,
    frontend/src/locales/en/translation.json
  </files>
  <action>
1. Расширить интерфейс ExtractionIndicator:
   ```typescript
   interface ExtractionIndicatorProps {
     isExtracting: boolean;
     extractionError?: string | null;    // Текст ошибки (уже mapApiError'ннутый)
     retryCount?: number;                // Текущая попытка
     maxRetries?: number;                // Максимум попыток (default: 3)
     onRetry?: () => void;               // Callback повтора
     onCancel?: () => void;
   }
   ```

2. Обновить рендер ExtractionIndicator:
   - Если `extractionError` установлен — показать error state вместо спиннера:
     ```tsx
     // Вместо спиннера — иконка ошибки
     <div className="relative flex-shrink-0">
       <div className="w-10 h-10 rounded-full bg-destructive/10 flex items-center justify-center">
         <AlertCircle className="w-5 h-5 text-destructive" />
       </div>
     </div>
     // Текст ошибки
     <div className="flex-1 min-w-0">
       <p className="font-medium text-sm sm:text-base text-popover-foreground">
         {extractionError}
       </p>
       {retryCount !== undefined && maxRetries !== undefined && retryCount < maxRetries && (
         <p className="text-xs sm:text-sm text-muted-foreground">
           {t('reader.extraction.retry_hint')}
         </p>
       )}
       {retryCount !== undefined && maxRetries !== undefined && retryCount >= maxRetries && (
         <p className="text-xs sm:text-sm text-muted-foreground">
           {t('reader.extraction.retries_exhausted')}
         </p>
       )}
     </div>
     // Кнопка retry (только если isRetryable и retryCount < maxRetries)
     {onRetry && retryCount !== undefined && maxRetries !== undefined && retryCount < maxRetries && (
       <button onClick={onRetry} className="flex-shrink-0 p-2 rounded-lg transition-colors bg-primary/10 hover:bg-primary/20 text-primary">
         <RefreshCw className="w-5 h-5" />
       </button>
     )}
     ```
   - Компонент показывается если `isExtracting || extractionError` (не только isExtracting)
   - Импортировать `AlertCircle`, `RefreshCw` из lucide-react
   - При CircuitBreakerError (isRetryable=false в errorMessages): НЕ показывать retry кнопку вообще (error mapping делает Plan 01)

3. Подключить ExtractionIndicator в EpubReader.tsx:
   - ExtractionIndicator сейчас НЕ используется в EpubReader (только экспортируется).
   - Нужно определить: откуда берётся состояние извлечения описаний.
   - `useChapterManagement` внутренне использует `useChapterDescriptions` — нужно пробросить error state.
   - Проверить хук: `grep -n 'error\|isError\|isFetching' frontend/src/hooks/epub/useChapterManagement.ts`
   - Два варианта подключения:
     a) Если useChapterManagement уже возвращает error/isLoading от описаний — использовать их
     b) Если нет — добавить возврат `{ descriptionError, isExtractingDescriptions, refetchDescriptions }` из useChapterManagement

   - В EpubReader.tsx добавить:
     ```typescript
     import { ExtractionIndicator } from './ExtractionIndicator';
     import { mapApiError } from '@/utils/errorMessages';

     // Из useChapterManagement:
     const { currentChapter, descriptions, images, descriptionError, isExtractingDescriptions, refetchDescriptions } = useChapterManagement({...});

     // Состояние retry
     const [extractionRetryCount, setExtractionRetryCount] = useState(0);
     const MAX_EXTRACTION_RETRIES = 3;

     const handleExtractionRetry = useCallback(() => {
       if (extractionRetryCount < MAX_EXTRACTION_RETRIES) {
         setExtractionRetryCount(prev => prev + 1);
         refetchDescriptions();
       }
     }, [extractionRetryCount, refetchDescriptions]);

     // Сброс retry count при смене главы
     useEffect(() => {
       setExtractionRetryCount(0);
     }, [currentChapter]);
     ```

   - Добавить ExtractionIndicator в JSX после ReaderOverlays:
     ```tsx
     <ExtractionIndicator
       isExtracting={isExtractingDescriptions}
       extractionError={descriptionError ? mapApiError(descriptionError).message : null}
       retryCount={extractionRetryCount}
       maxRetries={MAX_EXTRACTION_RETRIES}
       onRetry={descriptionError && mapApiError(descriptionError).isRetryable ? handleExtractionRetry : undefined}
       onCancel={() => {}} // Отмена извлечения — просто скрыть индикатор
     />
     ```

4. Расширить useChapterManagement (или useChapterDescriptions) для проброса error state:
   - Добавить возврат `descriptionError`, `isExtractingDescriptions`, `refetchDescriptions`
   - `isExtractingDescriptions` = isFetching от useChapterDescriptions
   - `descriptionError` = error от useChapterDescriptions
   - `refetchDescriptions` = refetch от useChapterDescriptions

5. Добавить i18n ключи:
   - `reader.extraction.error`: "Не удалось проанализировать главу" (ru)
   - `reader.extraction.retry_hint`: "Нажмите для повторной попытки" (ru)
   - `reader.extraction.retries_exhausted`: "Попробуйте повторить позже" (ru)
   - Английские варианты в en/translation.json
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - ExtractionIndicator показывает error state с информативным русским сообщением
    - Кнопка retry позволяет повторить извлечение (до 3 попыток)
    - После исчерпания попыток retry скрывается, показывается сообщение "попробуйте позже"
    - CircuitBreakerError не показывает кнопку retry
    - ExtractionIndicator подключён в EpubReader и реагирует на ошибки описаний
    - Retry count сбрасывается при смене главы
    - TypeScript билд проходит
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` — TypeScript билд проходит
2. ParsingOverlay: при status=error показывает сообщение + кнопку повтора (вместо застывшего прогресс-бара)
3. ExtractionIndicator: при ошибке показывает сообщение + retry (до 3 попыток)
4. ExtractionIndicator подключён в EpubReader.tsx — ранее только экспортировался
5. CircuitBreakerError — нет кнопки retry ни в одном компоненте
</verification>

<success_criteria>
- Сбои парсинга книг показывают информативное сообщение с объяснением причины и кнопку повтора
- Сбои AI-извлечения показывают понятное объяснение причины и предлагают действие повтора
- Пользователь может повторить генерацию без перезагрузки страницы (до 3 попыток)
- Все сообщения на русском, информативные, без технических деталей
</success_criteria>

<output>
После завершения создать `.planning/phases/07-ux/07-02-SUMMARY.md`
</output>
