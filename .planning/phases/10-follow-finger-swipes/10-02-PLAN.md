---
phase: 10-follow-finger-swipes
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - frontend/src/components/Reader/EpubReader.tsx
  - frontend/src/components/Reader/Core/ReaderOverlays.tsx
  - frontend/src/components/Reader/IOSTapZones.tsx
  - frontend/src/hooks/epub/useSwipeNavigation.ts
  - frontend/src/components/Reader/SwipeOverlay.tsx
  - frontend/src/components/Reader/SwipeIndicator.tsx
autonomous: true
requirements: [NAV-01, NAV-02]

must_haves:
  truths:
    - "EpubReader использует useFollowFingerSwipe вместо useSwipeNavigation"
    - "epub-viewer обёрнут в FollowFingerContainer — палец физически двигает страницу"
    - "SwipeOverlay и SwipeIndicator удалены из проекта"
    - "ReaderOverlays не рендерит SwipeOverlay, не принимает swipe props"
    - "При тап-навигации через IOSTapZones запускается slide-in анимация (~150ms) через FollowFingerContainer"
    - "TypeScript build проходит без ошибок"
    - "Существующие тесты не сломаны"
  artifacts:
    - path: "frontend/src/components/Reader/EpubReader.tsx"
      provides: "Интеграция useFollowFingerSwipe + FollowFingerContainer вместо useSwipeNavigation + SwipeOverlay"
    - path: "frontend/src/components/Reader/Core/ReaderOverlays.tsx"
      provides: "Убран SwipeOverlay, убраны swipe props"
    - path: "frontend/src/components/Reader/IOSTapZones.tsx"
      provides: "Callback для trigger slide-in анимации при тап-навигации"
  key_links:
    - from: "frontend/src/components/Reader/EpubReader.tsx"
      to: "frontend/src/hooks/epub/useFollowFingerSwipe.ts"
      via: "import useFollowFingerSwipe, передача translateX в FollowFingerContainer"
      pattern: "useFollowFingerSwipe"
    - from: "frontend/src/components/Reader/EpubReader.tsx"
      to: "frontend/src/components/Reader/FollowFingerContainer.tsx"
      via: "обёртка epub-viewer div в FollowFingerContainer"
      pattern: "FollowFingerContainer"
    - from: "frontend/src/components/Reader/IOSTapZones.tsx"
      to: "frontend/src/hooks/epub/useFollowFingerSwipe.ts"
      via: "triggerSlideAnimation callback для тап-навигации"
      pattern: "triggerSlideAnimation|onTapNavigate"
---

<objective>
Интегрировать follow-finger систему в EpubReader, удалить старую SwipeOverlay/SwipeIndicator, добавить slide-in анимацию при тап-навигации.

Purpose: Завершить Phase 10 — подключить новую follow-finger систему из Plan 01 к ридеру, обеспечив полный пользовательский опыт: follow-finger свайпы + slide-in при тапах + удаление устаревших overlay.
Output: Рабочий ридер с follow-finger свайпами, spring-анимацией и slide-in тап-навигацией. Старый код SwipeOverlay удалён.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-follow-finger-swipes/10-CONTEXT.md
@.planning/phases/10-follow-finger-swipes/10-RESEARCH.md
@.planning/phases/10-follow-finger-swipes/10-01-SUMMARY.md

<interfaces>
<!-- Из Plan 01 — интерфейсы, созданные в первом плане -->

From frontend/src/hooks/epub/useFollowFingerSwipe.ts (создан в Plan 01):
```typescript
export interface UseFollowFingerSwipeOptions {
  rendition: Rendition | null;
  enabled: boolean;
  onNavigate: (direction: 'next' | 'prev') => Promise<void>;
  onChapterChange?: (direction: 'next' | 'prev') => Promise<void>;
  navLock: NavigationLock;
}

export interface UseFollowFingerSwipeReturn {
  translateX: MotionValue<number>;
  phase: 'idle' | 'tracking' | 'animating';
  isAtBoundary: 'start' | 'end' | null;
  showChapterHint: boolean;
  chapterHintDirection: 'next' | 'prev' | null;
  // Может также экспортировать triggerSlideAnimation для тап-навигации
}
```

From frontend/src/components/Reader/FollowFingerContainer.tsx (создан в Plan 01):
```typescript
interface FollowFingerContainerProps {
  translateX: MotionValue<number>;
  phase: 'idle' | 'tracking' | 'animating';
  isAtBoundary: 'start' | 'end' | null;
  showChapterHint: boolean;
  chapterHintDirection: 'next' | 'prev' | null;
  children: React.ReactNode;
}
```

From frontend/src/hooks/shared/useNavigationLock.ts:
```typescript
export interface NavigationLock {
  acquire: () => boolean;
  release: () => void;
  forceRelease: () => void;
  isLocked: () => boolean;
}
```

Текущий EpubReader.tsx (строки 222-229 — useSwipeNavigation):
```typescript
const { swipeState, touchHandlers: swipeTouchHandlers } = useSwipeNavigation({
  rendition,
  enabled: renditionReady && effectiveNavigationMode === 'swipe' && !isModalOpen,
  onNavigate: async (dir) => {
    if (dir === 'next') await nextPage();
    else await prevPage();
  },
});
```

Текущий ReaderOverlays.tsx (строки 78-84 — SwipeOverlay рендер):
```typescript
{effectiveNavigationMode === 'swipe' && isMobileDevice && (
  <SwipeOverlay
    swipeState={swipe.state}
    viewportWidth={swipe.viewportWidth}
    headerHeight={swipe.headerHeight}
  />
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Интеграция useFollowFingerSwipe в EpubReader + FollowFingerContainer wrapper</name>
  <files>frontend/src/components/Reader/EpubReader.tsx, frontend/src/components/Reader/Core/ReaderOverlays.tsx</files>
  <action>
**EpubReader.tsx — замена useSwipeNavigation на useFollowFingerSwipe:**

1. Заменить импорт:
   - Убрать: `import { useSwipeNavigation } from '@/hooks/epub/useSwipeNavigation';`
   - Добавить: `import { useFollowFingerSwipe } from '@/hooks/epub/useFollowFingerSwipe';`
   - Добавить: `import { FollowFingerContainer } from './FollowFingerContainer';`

2. Заменить вызов хука (строки ~222-229):
   - Убрать: `const { swipeState, touchHandlers: swipeTouchHandlers } = useSwipeNavigation({...})`
   - Добавить:
   ```typescript
   const followFinger = useFollowFingerSwipe({
     rendition,
     enabled: renditionReady && effectiveNavigationMode === 'swipe' && !isModalOpen,
     onNavigate: async (dir) => {
       if (dir === 'next') await nextPage();
       else await prevPage();
     },
     onChapterChange: async (dir) => {
       if (dir === 'next') await rendition?.next();
       else await rendition?.prev();
     },
     navLock,
   });
   ```

3. Обернуть epub-viewer div в FollowFingerContainer (строки ~498-512):
   ```tsx
   <FollowFingerContainer
     translateX={followFinger.translateX}
     phase={followFinger.phase}
     isAtBoundary={followFinger.isAtBoundary}
     showChapterHint={followFinger.showChapterHint}
     chapterHintDirection={followFinger.chapterHintDirection}
   >
     <div
       ref={viewerRef}
       id="epub-viewer"
       tabIndex={-1}
       className={`h-full w-full ${backgroundColor} outline-hidden`}
       style={{
         paddingTop: 'calc(70px + env(safe-area-inset-top))',
         paddingLeft: 'env(safe-area-inset-left)',
         paddingRight: 'env(safe-area-inset-right)',
         paddingBottom: 'env(safe-area-inset-bottom)',
         touchAction: 'pan-x pan-y',
       }}
     />
   </FollowFingerContainer>
   ```
   - УБРАТЬ `{...(effectiveNavigationMode === 'swipe' ? swipeTouchHandlers : {})}` с div — follow-finger привязывается к iframe напрямую через hooks.content.register, не нужны handlers на parent div

4. Убрать swipe props из ReaderOverlays (строки ~514-541):
   - Убрать проп `swipe={{ state: swipeState, viewportWidth: window.innerWidth, headerHeight: 70 }}`
   - ReaderOverlays больше не получает swipe state

**ReaderOverlays.tsx — убрать SwipeOverlay:**

1. Убрать импорты:
   - `import { SwipeOverlay } from '../SwipeOverlay';`
   - `import type { SwipeState } from '@/hooks/epub/useSwipeNavigation';`

2. Убрать из ReaderOverlaysProps:
   - Убрать `swipe` prop целиком (state, viewportWidth, headerHeight)

3. Убрать из JSX (строки ~78-84):
   - Убрать весь блок `{effectiveNavigationMode === 'swipe' && isMobileDevice && (<SwipeOverlay ... />)}`

4. Убрать `const isMobileDevice = ...` если больше не используется (проверить)

**НЕ удалять файлы** SwipeOverlay.tsx, SwipeIndicator.tsx, useSwipeNavigation.ts в этой задаче — удаление в Task 2 после проверки TypeScript.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>EpubReader использует useFollowFingerSwipe вместо useSwipeNavigation. epub-viewer обёрнут в FollowFingerContainer. ReaderOverlays не рендерит SwipeOverlay. TypeScript компилируется без ошибок.</done>
</task>

<task type="auto">
  <name>Task 2: Slide-in тап-анимация + удаление SwipeOverlay/SwipeIndicator + cleanup</name>
  <files>frontend/src/components/Reader/IOSTapZones.tsx, frontend/src/hooks/epub/useFollowFingerSwipe.ts, frontend/src/hooks/epub/useSwipeNavigation.ts, frontend/src/components/Reader/SwipeOverlay.tsx, frontend/src/components/Reader/SwipeIndicator.tsx</files>
  <action>
**Slide-in анимация при тап-навигации (решение из CONTEXT.md: "При тап-навигации: быстрая slide-in анимация (~150ms)"):**

Два варианта реализации (выбрать при имплементации на основе того, что создал Plan 01):

**Вариант A (предпочтительный) — через хук useFollowFingerSwipe:**
Если Plan 01 экспортирует функцию `triggerSlideAnimation(direction: 'next' | 'prev')` из UseFollowFingerSwipeReturn:
1. В EpubReader.tsx: передать `followFinger.triggerSlideAnimation` в ReaderOverlays -> IOSTapZones через props
2. В IOSTapZones: при тап-навигации вызвать triggerSlideAnimation(direction) перед onNavigate
3. Функция programmatically: animate(translateX, -viewportWidth * direction, { type: 'spring', stiffness: 400, damping: 40, mass: 1 }), onComplete: translateX.set(0)

**Вариант B — callback из EpubReader:**
Если Plan 01 НЕ экспортирует triggerSlideAnimation:
1. Добавить triggerSlideAnimation в useFollowFingerSwipe: принимает direction, выполняет animate(translateX, +-viewportWidth, spring_fast) -> onComplete: set(0)
2. Передать через props chain: EpubReader -> ReaderOverlays -> IOSTapZones

**IOSTapZones.tsx модификация:**
1. Добавить опциональный проп: `onTapNavigateAnimation?: (direction: 'next' | 'prev') => void`
2. В handleNavigation (строка ~98): перед вызовом onNextPage/onPrevPage — вызвать onTapNavigateAnimation?.(action) для запуска slide-in
3. Slide-in запускается ОДНОВРЕМЕННО с навигацией, не блокирует — это чисто визуальный эффект

**Обновить props chain:**
- ReaderOverlaysProps: добавить `onTapNavigateAnimation?: (direction: 'next' | 'prev') => void` в tapZones
- EpubReader.tsx: передать `followFinger.triggerSlideAnimation` через tapZones prop

**Удаление устаревших файлов:**
1. Удалить `frontend/src/components/Reader/SwipeOverlay.tsx`
2. Удалить `frontend/src/components/Reader/SwipeIndicator.tsx`
3. НЕ удалять `frontend/src/hooks/epub/useSwipeNavigation.ts` пока — он может импортироваться где-то ещё. Проверить: `grep -r "useSwipeNavigation\|SwipeOverlay\|SwipeIndicator\|SwipeState\|SWIPE_CONFIG" frontend/src/ --include="*.ts" --include="*.tsx"`. Если единственные оставшиеся ссылки — сам файл и уже обновлённые файлы, то удалить.
4. Если useSwipeNavigation.ts больше не импортируется — удалить
5. Если SWIPE_CONFIG используется где-то кроме useSwipeNavigation — перенести нужные константы в useFollowFingerSwipe перед удалением

**Финальная верификация:**
- `npx tsc --noEmit` — нет ошибок
- `npm run build` — production build проходит
- `npm test -- --watchAll=false` — существующие тесты не сломаны
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npm run build 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Тап-навигация через IOSTapZones запускает slide-in анимацию (~150ms) через FollowFingerContainer. SwipeOverlay.tsx и SwipeIndicator.tsx удалены. useSwipeNavigation.ts удалён (если не используется). Production build проходит. Все существующие тесты проходят.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — нет ошибок TypeScript
2. `cd frontend && npm run build` — production build проходит
3. `cd frontend && npx vitest run` — все тесты проходят (включая новые из Plan 01 и существующие)
4. Файлы SwipeOverlay.tsx и SwipeIndicator.tsx не существуют
5. Grep по `SwipeOverlay|SwipeIndicator` в frontend/src не возвращает результатов (кроме, возможно, git history)
</verification>

<success_criteria>
- Follow-finger свайпы полностью интегрированы в ридер: палец двигает страницу в реальном времени
- Spring-анимация завершает навигацию при отпускании пальца
- При тапе на IOSTapZones зоны — slide-in анимация (~150ms) вместо мгновенного переключения
- Rubber-band на границе главы + плашка "Следующая глава"
- SwipeOverlay и SwipeIndicator удалены из проекта
- Production build проходит, все тесты зелёные
</success_criteria>

<output>
After completion, create `.planning/phases/10-follow-finger-swipes/10-02-SUMMARY.md`
</output>
