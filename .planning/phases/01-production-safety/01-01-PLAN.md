---
phase: 01-production-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/config.py
  - backend/app/core/secrets.py
  - backend/app/core/celery_app.py
  - backend/app/main.py
  - backend/gunicorn.conf.py
  - backend/tests/test_config_security.py
  - docker-compose.lite.yml
  - docker-compose.lite.prod.yml
  - docker-compose.staging.yml
  - .env.example
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, DEPLOY-01, DEPLOY-05, DEPLOY-06, DEPLOY-07, DEPLOY-08, UX-01]

must_haves:
  truths:
    - "Приложение запускается с DEBUG=False по умолчанию и отказывается стартовать при дефолтном SECRET_KEY в не-debug режиме"
    - "Health check /health возвращает реальный статус подключения к PostgreSQL, Redis и Celery (не захардкоженный 'checking...')"
    - "Бэкенд запускается через Gunicorn с UvicornWorker в продакшене (не uvicorn --reload)"
    - "Celery visibility_timeout (14400) превышает time_limit (10800) — нет дублирования задач"
    - "LANGEXTRACT_MODEL синхронизирован как gemini-3-flash-preview во всех docker-compose файлах"
    - "PostgreSQL образ обновлён до 17.9-alpine в docker-compose.lite.prod.yml"
    - "Memory limits для Celery workers — 512MB во всех конфигурациях"
  artifacts:
    - path: "backend/app/core/config.py"
      provides: "DEBUG=False по умолчанию, NLP-валидатор удалён"
      contains: "DEBUG: bool = False"
    - path: "backend/app/core/celery_app.py"
      provides: "visibility_timeout=14400, worker_max_memory_per_child=512000"
      contains: "visibility_timeout.*14400"
    - path: "backend/gunicorn.conf.py"
      provides: "Конфигурация Gunicorn для продакшена"
      exports: ["bind", "workers", "worker_class"]
    - path: "backend/app/main.py"
      provides: "Реальный health check вместо заглушки"
      contains: "async_session"
    - path: "backend/tests/test_config_security.py"
      provides: "Тесты SEC-01, SEC-02, UX-01"
      min_lines: 50
  key_links:
    - from: "docker-compose.lite.yml"
      to: "backend/gunicorn.conf.py"
      via: "Команда запуска gunicorn --config gunicorn.conf.py"
      pattern: "gunicorn.*gunicorn\\.conf\\.py"
    - from: "backend/app/main.py:/health"
      to: "backend/app/core/database.py"
      via: "Реальная проверка подключения к БД через AsyncSession"
      pattern: "select.*1|text.*SELECT"
---

<objective>
Безопасность, конфигурация продакшена и исправление инфраструктурных багов

Назначение: Устранить эксплуатируемые уязвимости безопасности (DEBUG=True, дефолтный SECRET_KEY), исправить заглушку health check, перевести бэкенд на Gunicorn, исправить критические баги Celery (visibility_timeout, memory limits), синхронизировать конфигурацию docker-compose файлов и обновить PostgreSQL для устранения CVE.

Результат: Безопасная конфигурация бэкенда, реальный health check, Gunicorn вместо uvicorn --reload, исправленные конфиги Celery и docker-compose, тесты безопасности.
</objective>

<execution_context>
@/Users/sandk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-production-safety/01-RESEARCH.md

<interfaces>
<!-- Ключевые интерфейсы из кодовой базы, которые исполнитель будет модифицировать. -->

Из backend/app/core/config.py:
```python
class Settings(BaseSettings):
    DEBUG: bool = True  # SEC-01: Изменить на False
    SECRET_KEY: str = "dev-secret-key-change-in-production"  # SEC-02: Fail-fast при дефолте
    WORKERS_COUNT: int = Field(default=4, ge=1, le=8)
    WORKER_TIMEOUT: int = Field(default=300, ge=60, le=600)
    WORKER_MAX_REQUESTS: int = Field(default=1000, ge=100, le=5000)
    WORKER_MAX_REQUESTS_JITTER: int = Field(default=100, ge=0, le=500)

    # NLP настройки — УДАЛИТЬ (мёртвый код, Phase 2 предмет, но валидатор validate_nlp_weights ломает запуск):
    SPACY_MODEL: str = "ru_core_news_lg"
    NLTK_DATA_PATH: str = "./nltk_data"
    MULTI_NLP_MODE: str = Field(default="ensemble")
    # ... ещё 4 NLP-поля + validate_nlp_weights валидатор

    @model_validator(mode="after")
    def validate_production_settings(self): ...  # Уже проверяет SECRET_KEY при DEBUG=False
```

Из backend/app/core/celery_app.py:
```python
celery_app.conf.update(
    task_soft_time_limit=int(os.getenv("CELERY_SOFT_TIME_LIMIT", "1500")),  # 25 min
    task_time_limit=int(os.getenv("CELERY_TIME_LIMIT", "1800")),  # 30 min
    broker_transport_options={"visibility_timeout": 3600},  # DEPLOY-05: 3600 < 10800 — БАГИ
    worker_max_memory_per_child=int(os.getenv("CELERY_MAX_MEMORY_PER_CHILD", "150000")),  # 150MB
)
```

Из backend/app/main.py (строки 313-334):
```python
@app.get("/health")
async def health_check(request: Request) -> Dict[str, Any]:
    return {
        "status": "healthy",
        "checks": {
            "api": "ok",
            "database": "checking...",  # TODO: заглушка
            "redis": redis_status,
        },
    }
```

Health-роутер (backend/app/routers/health.py) уже имеет реальные проверки через /api/v1/health/deep.
Lifespan в main.py уже вызывает startup_secrets_check().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Исправление безопасности конфигурации и тесты</name>
  <files>backend/app/core/config.py, backend/app/core/secrets.py, backend/tests/test_config_security.py, .env.example</files>
  <behavior>
    - Тест SEC-01: Settings() с пустыми env создаётся с DEBUG=False (не True)
    - Тест SEC-02: При DEBUG=False и SECRET_KEY="dev-secret-key-change-in-production" Settings() выбрасывает ValueError
    - Тест SEC-02: При DEBUG=True и дефолтном SECRET_KEY Settings() создаётся без ошибки (dev-режим)
    - Тест SEC-02: При DEBUG=False и надёжном SECRET_KEY Settings() создаётся успешно
    - Тест: validate_nlp_weights не мешает запуску (валидатор удалён или NLP-поля удалены)
  </behavior>
  <action>
    1. Создать backend/tests/test_config_security.py с тестами для SEC-01 и SEC-02 (RED-фаза)
    2. Изменить backend/app/core/config.py:
       - Строка 19-21: Изменить `DEBUG: bool = True` → `DEBUG: bool = False`
       - Удалить NLP-поля (строки 79-90): SPACY_MODEL, NLTK_DATA_PATH, MULTI_NLP_MODE, CONSENSUS_THRESHOLD, SPACY_WEIGHT, NATASHA_WEIGHT, STANZA_WEIGHT
       - Удалить валидатор `validate_nlp_weights` (строки 193-214) — он ломает запуск при отсутствии NLP env vars
       - ПРИМЕЧАНИЕ: Полная очистка NLP — Phase 2 (CLEAN-02). Здесь удаляем только то, что мешает запуску при DEBUG=False
    3. Обновить backend/app/core/secrets.py:
       - Изменить SENTRY_DSN → HAWK_TOKEN в SECRETS_CONFIG[RECOMMENDED] (строки 64-69)
       - Описание: "Hawk Tracker error tracking token"
    4. Обновить .env.example — добавить HAWK_TOKEN, убедиться что SECRET_KEY с пояснением
    5. Запустить тесты (GREEN-фаза)
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/backend && python -m pytest tests/test_config_security.py -x -v</automated>
  </verify>
  <done>DEBUG по умолчанию False, дефолтный SECRET_KEY отклоняется при DEBUG=False, NLP-валидатор не блокирует запуск, тесты зелёные</done>
</task>

<task type="auto">
  <name>Задача 2: Gunicorn, health check и исправление docker-compose</name>
  <files>backend/gunicorn.conf.py, backend/app/main.py, backend/app/core/celery_app.py, docker-compose.lite.yml, docker-compose.lite.prod.yml, docker-compose.staging.yml</files>
  <action>
    1. Создать backend/gunicorn.conf.py (конфигурация Gunicorn для продакшена):
       ```python
       import os
       bind = "0.0.0.0:8000"
       workers = int(os.getenv("WORKERS_COUNT", "2"))
       worker_class = "uvicorn.workers.UvicornWorker"
       timeout = int(os.getenv("WORKER_TIMEOUT", "300"))
       keepalive = 5
       max_requests = int(os.getenv("WORKER_MAX_REQUESTS", "1000"))
       max_requests_jitter = int(os.getenv("WORKER_MAX_REQUESTS_JITTER", "100"))
       accesslog = "-"
       errorlog = "-"
       loglevel = os.getenv("LOG_LEVEL", "info").lower()
       ```

    2. Исправить health check в backend/app/main.py (строки 313-334):
       - Заменить заглушку `"database": "checking..."` реальной проверкой
       - Импортировать AsyncSession и get_database_session из core.database
       - Выполнить `SELECT 1` для проверки PostgreSQL
       - Проверить Celery через celery_app.control.inspect().ping() в try/catch с таймаутом 2 сек
       - Формат ответа: статус "healthy"/"degraded"/"unhealthy", каждый check: "ok"/"error"
       - HTTP-код: 200 если хотя бы API работает, 503 если все критические сервисы недоступны
       - ВАЖНО: Health check не должен зависеть от rate_limiter (убрать декоратор rate_limit или увеличить лимит до 60/мин, так как Docker healthcheck вызывает каждые 30 сек)

    3. Исправить docker-compose.lite.yml:
       - Строка 155: Заменить `command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload` на `command: gunicorn app.main:app --config gunicorn.conf.py` (DEPLOY-01)
       - Строки 110, 174: Изменить `LANGEXTRACT_MODEL:-gemini-2.0-flash` → `LANGEXTRACT_MODEL:-gemini-3-flash-preview` (DEPLOY-06)
       - Строка 29: postgres:15-alpine остаётся (ПРИМЕЧАНИЕ: v15 используется потому что данные инициализированы на v15 — см. комментарий в файле. DEPLOY-07 применяется к docker-compose.lite.prod.yml)
       - Celery worker memory: DEPLOY-08 — в docker-compose.lite.yml memory limit уже 2.5G для контейнера. Фактический memory per child контролируется через celery_app.py (шаг 4)
       - Строка 210: Заменить `--concurrency=4` на `--concurrency=${CELERY_CONCURRENCY:-2}` (решение пользователя: 2 воркера)

    4. Исправить backend/app/core/celery_app.py:
       - Строка 42: `broker_transport_options={"visibility_timeout": 14400}` (DEPLOY-05: было 3600, нужно > time_limit)
       - Обновить task_time_limit с учётом book processing: тяжёлые задачи имеют soft_time_limit=10800 (3 часа) через декоратор задачи, но глобальный default=1800 — visibility_timeout=14400 покрывает оба случая
       - Строка 30-31: `worker_max_memory_per_child=512000` (DEPLOY-08: 512MB = 512000 KB, было 150000)

    5. Исправить docker-compose.lite.prod.yml:
       - Строки 125, 184: `LANGEXTRACT_MODEL:-gemini-3-flash-preview` (DEPLOY-06)
       - Строка 252: `postgres:17.9-alpine` (DEPLOY-07: CVE fix, prod использует PG17)
       - Строка 172: `--max-memory-per-child=512000` (DEPLOY-08: было 400000)

    6. Исправить docker-compose.staging.yml:
       - Строки 118, 182: `LANGEXTRACT_MODEL:-gemini-3-flash-preview` (DEPLOY-06)
       - Строка 169: `--max-memory-per-child=512000` (DEPLOY-08: было 300000)
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon && FAIL=0; grep -q "gunicorn" docker-compose.lite.yml || { echo "FAIL: gunicorn отсутствует в docker-compose.lite.yml"; FAIL=1; }; grep -q "visibility_timeout.*14400" backend/app/core/celery_app.py || { echo "FAIL: visibility_timeout не 14400"; FAIL=1; }; grep -q "gemini-3-flash-preview" docker-compose.lite.yml || { echo "FAIL: LANGEXTRACT_MODEL не обновлён"; FAIL=1; }; grep -q "17.9-alpine" docker-compose.lite.prod.yml || { echo "FAIL: PostgreSQL не обновлён до 17.9-alpine"; FAIL=1; }; grep -q "512000" backend/app/core/celery_app.py || { echo "FAIL: memory не 512000"; FAIL=1; }; grep -q "checking" backend/app/main.py && { echo "FAIL: заглушка checking всё ещё присутствует"; FAIL=1; }; test $FAIL -eq 0 && echo "PASS: все проверки пройдены"</automated>
  </verify>
  <done>
    - Gunicorn conf создан, docker-compose.lite.yml использует gunicorn (DEPLOY-01)
    - Health check /health выполняет реальные проверки PostgreSQL, Redis, Celery (UX-01)
    - visibility_timeout=14400 > time_limit (DEPLOY-05)
    - LANGEXTRACT_MODEL=gemini-3-flash-preview во всех 3 файлах (DEPLOY-06)
    - PostgreSQL 17.9-alpine в prod (DEPLOY-07)
    - Celery memory 512MB везде (DEPLOY-08)
  </done>
</task>

<task type="auto">
  <name>Задача 3: Интеграционная проверка — все бэкенд-тесты проходят</name>
  <files>backend/tests/test_config_security.py</files>
  <action>
    1. Запустить полный набор бэкенд-тестов: `cd backend && python -m pytest -v --tb=short`
    2. Если тесты падают из-за удаления NLP-полей из config.py — найти и исправить зависимости:
       - Проверить, что никакие тесты не импортируют SPACY_MODEL, MULTI_NLP_MODE и т.п. из settings
       - При обнаружении — удалить ссылки на NLP-настройки из тестов (это мёртвый код)
    3. Если тесты падают из-за изменения DEBUG=False — добавить `DEBUG=true` в тестовые fixtures через monkeypatch или conftest
    4. Убедиться, что test_health.py всё ещё проходит после изменений в /health endpoint
    5. Финальный прогон всех тестов
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/backend && python -m pytest -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>Все бэкенд-тесты проходят без ошибок, включая новые тесты безопасности и существующие тесты health check</done>
</task>

</tasks>

<verification>
Проверка всего плана после выполнения:

1. Безопасность (SEC-01, SEC-02):
   - `cd backend && python -c "from app.core.config import Settings; s = Settings(); print(f'DEBUG={s.DEBUG}')"` → DEBUG=False
   - Тесты: `cd backend && python -m pytest tests/test_config_security.py -v`

2. Gunicorn (DEPLOY-01):
   - `grep "gunicorn" docker-compose.lite.yml` → содержит gunicorn command

3. Health check (UX-01):
   - `grep -v "checking" backend/app/main.py | grep -c "health"` → нет заглушки

4. Celery (DEPLOY-05, DEPLOY-08):
   - `grep "visibility_timeout" backend/app/core/celery_app.py` → 14400
   - `grep "max_memory_per_child" backend/app/core/celery_app.py` → 512000

5. LANGEXTRACT_MODEL (DEPLOY-06):
   - `grep "LANGEXTRACT_MODEL" docker-compose.lite.yml docker-compose.lite.prod.yml docker-compose.staging.yml` → все gemini-3-flash-preview

6. PostgreSQL (DEPLOY-07):
   - `grep "postgres:" docker-compose.lite.prod.yml` → 17.9-alpine

7. Все тесты:
   - `cd backend && python -m pytest -v`
</verification>

<success_criteria>
- DEBUG=False по умолчанию в config.py
- Приложение отказывается стартовать при дефолтном SECRET_KEY вне debug-режима
- SEC-03 подтверждён как выполненный (PyJWT уже установлен)
- /health возвращает реальный статус БД, Redis, Celery
- docker-compose.lite.yml использует gunicorn (без --reload)
- gunicorn.conf.py создан с UvicornWorker, 2 воркера, timeout 300
- Celery visibility_timeout=14400, memory=512MB
- LANGEXTRACT_MODEL=gemini-3-flash-preview во всех docker-compose
- PostgreSQL 17.9-alpine в prod конфигурации
- Все бэкенд-тесты проходят
</success_criteria>

<output>
После завершения создать `.planning/phases/01-production-safety/01-01-SUMMARY.md`
</output>
