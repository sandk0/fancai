---
phase: 01-production-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.lite.txt
  - backend/requirements.txt
  - backend/app/core/hawk.py
  - backend/app/main.py
  - backend/app/core/celery_app.py
  - backend/tests/test_hawk_init.py
  - frontend/package.json
  - frontend/src/config/hawk.ts
  - frontend/src/main.tsx
autonomous: false
requirements: [DEPLOY-02, DEPLOY-03]

user_setup:
  - service: hawk-tracker
    why: "Мониторинг ошибок бэкенда и фронтенда"
    env_vars:
      - name: HAWK_TOKEN
        source: "Hawk Tracker (hawk-tracker.ru) -> Создать проект Python -> Скопировать Integration Token"
      - name: VITE_HAWK_TOKEN
        source: "Hawk Tracker (hawk-tracker.ru) -> Создать проект JavaScript -> Скопировать Integration Token"
    dashboard_config:
      - task: "Создать два проекта: Python (бэкенд) и JavaScript (фронтенд)"
        location: "hawk-tracker.ru -> Workspace -> Add Project"
must_haves:
  truths:
    - "Ошибки бэкенда отображаются в дашборде Hawk Tracker с полными стек-трейсами"
    - "JavaScript-ошибки фронтенда отображаются в отдельном проекте Hawk Tracker"
    - "Celery-задачи при сбое отправляют события в Hawk через task_failure сигнал"
  artifacts:
    - path: "backend/app/core/hawk.py"
      provides: "Инициализация Hawk Tracker для FastAPI и Celery с task_failure signal"
      exports: ["init_hawk", "init_hawk_celery"]
    - path: "frontend/src/config/hawk.ts"
      provides: "Инициализация Hawk SDK для React-фронтенда"
      exports: ["initHawk"]
    - path: "backend/tests/test_hawk_init.py"
      provides: "Тесты инициализации Hawk: с токеном, без токена, Celery task_failure signal"
      min_lines: 20
  key_links:
    - from: "backend/app/main.py:lifespan"
      to: "backend/app/core/hawk.py:init_hawk"
      via: "Вызов init_hawk(app) в lifespan startup"
      pattern: "init_hawk.*app"
    - from: "backend/app/core/celery_app.py"
      to: "backend/app/core/hawk.py:init_hawk_celery"
      via: "Вызов init_hawk_celery(celery_app) с подключением task_failure сигнала"
      pattern: "init_hawk_celery"
    - from: "frontend/src/main.tsx"
      to: "frontend/src/config/hawk.ts:initHawk"
      via: "Вызов initHawk() при запуске приложения"
      pattern: "initHawk"
---

<objective>
Мониторинг ошибок (Hawk Tracker) и резервное копирование базы данных

Назначение: Настроить отслеживание ошибок на бэкенде (hawk-python-sdk), фронтенде (@hawk.so/javascript) и в Celery-задачах.

Результат: Hawk Tracker интегрирован на бэкенде и фронтенде, Celery-задачи при сбое отправляют события через task_failure сигнал.
</objective>

<execution_context>
@/Users/sandk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-production-safety/01-RESEARCH.md

<interfaces>
<!-- Ключевые интерфейсы, с которыми будет работать исполнитель. -->

Из backend/app/main.py (lifespan, строки 51-145):
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    # STARTUP
    logger.info("Starting fancai", version=VERSION)
    startup_secrets_check(is_production=not settings.DEBUG)
    await rate_limiter.connect()
    await cache_manager.initialize()
    await settings_manager.initialize_default_settings()
    yield
    # SHUTDOWN
    await rate_limiter.close()
    await cache_manager.close()

app = FastAPI(..., lifespan=lifespan)
```

Из backend/app/core/celery_app.py (строки 1-16):
```python
from celery import Celery
import os
from app.core.config import settings

celery_app = Celery(
    "bookreader",
    broker=settings.REDIS_URL,
    backend=settings.REDIS_URL,
    include=["app.core.tasks", "app.tasks.reading_sessions_tasks"],
)
```

Из frontend/src/main.tsx:
```typescript
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import ErrorBoundary from './components/ErrorBoundary'
import './lib/i18n'
import './styles/globals.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ErrorBoundary level="app">
      <App />
    </ErrorBoundary>
  </React.StrictMode>,
)
```

Из frontend/src/components/ErrorBoundary.tsx:
```typescript
class ErrorBoundary extends Component<Props, State> {
  // Компонент ловит ошибки React — здесь нужно отправить событие в Hawk
}
```

Из backend/requirements.lite.txt (строка 55):
```
sentry-sdk[fastapi]==2.51.0  # ЗАМЕНИТЬ на hawk-python-sdk[fastapi]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Интеграция Hawk Tracker на бэкенде + Celery</name>
  <files>backend/requirements.lite.txt, backend/requirements.txt, backend/app/core/hawk.py, backend/app/main.py, backend/app/core/celery_app.py, backend/app/core/config.py, backend/tests/test_hawk_init.py</files>
  <behavior>
    - Тест: init_hawk(app) при наличии HAWK_TOKEN создаёт экземпляр HawkFastapi
    - Тест: init_hawk(app) при отсутствии HAWK_TOKEN возвращает None (graceful skip)
    - Тест: init_hawk_celery(celery_app) при наличии HAWK_TOKEN создаёт экземпляр Hawk и подключает task_failure signal
    - Тест: init_hawk_celery(celery_app) при отсутствии HAWK_TOKEN возвращает None
    - Тест: при подключённом task_failure signal, исключение в Celery-задаче вызывает hawk.send()
  </behavior>
  <action>
    1. Обновить backend/requirements.lite.txt:
       - Заменить `sentry-sdk[fastapi]==2.51.0` на `hawk-python-sdk[fastapi]>=1.0.0` (latest stable)
       - Убедиться что sentry-sdk удалён (также из requirements.txt если есть)

    2. Добавить HAWK_TOKEN в backend/app/core/config.py:
       - `HAWK_TOKEN: Optional[str] = None`

    3. Создать backend/app/core/hawk.py:
       ```python
       """Hawk Tracker — мониторинг ошибок для FastAPI и Celery."""
       from celery.signals import task_failure
       from hawk_python_sdk.modules.fastapi import HawkFastapi
       from hawk_python_sdk import Hawk
       from .config import settings
       from .logging import logger

       _hawk_fastapi: HawkFastapi | None = None
       _hawk_celery: Hawk | None = None

       def init_hawk(app) -> HawkFastapi | None:
           """Инициализация Hawk Tracker для FastAPI приложения."""
           global _hawk_fastapi
           token = settings.HAWK_TOKEN
           if not token:
               logger.info("Hawk Tracker отключен (HAWK_TOKEN не установлен)")
               return None
           _hawk_fastapi = HawkFastapi({
               'app_instance': app,
               'token': token,
               'release': f"fancai@{settings.APP_VERSION}",
           })
           logger.info("Hawk Tracker инициализирован для FastAPI")
           return _hawk_fastapi

       def init_hawk_celery(celery_app_instance=None) -> Hawk | None:
           """Инициализация Hawk Tracker для Celery workers.

           Подключает сигнал task_failure для автоматической отправки
           ошибок Celery-задач в Hawk Tracker.
           """
           global _hawk_celery
           token = settings.HAWK_TOKEN
           if not token:
               return None
           _hawk_celery = Hawk({'token': token})

           # Подключить Celery task_failure signal для перехвата ошибок задач
           @task_failure.connect
           def on_task_failure(sender=None, exception=None, traceback=None,
                               task_id=None, args=None, kwargs=None, **kw):
               """Отправить ошибку Celery-задачи в Hawk Tracker."""
               if _hawk_celery and exception:
                   _hawk_celery.send(exception, {
                       'task_id': task_id,
                       'task_name': sender.name if sender else 'unknown',
                       'args': str(args)[:200],
                   })

           logger.info("Hawk Tracker инициализирован для Celery (task_failure signal подключён)")
           return _hawk_celery
       ```

    4. Интегрировать в backend/app/main.py lifespan:
       - Добавить import: `from .core.hawk import init_hawk`
       - После startup_secrets_check: `init_hawk(app)`

    5. Интегрировать в backend/app/core/celery_app.py:
       - После конфигурации celery_app.conf.update: вызвать `init_hawk_celery(celery_app)`
       - Обернуть в try/except чтобы не сломать запуск Celery при отсутствии SDK

    6. Создать backend/tests/test_hawk_init.py с тестами:
       - Тест init_hawk с токеном (mock HawkFastapi)
       - Тест init_hawk без токена (возвращает None)
       - Тест init_hawk_celery с токеном — проверить что task_failure.connect вызван
       - Тест init_hawk_celery без токена (возвращает None)
       - Тест on_task_failure handler — проверить что hawk.send() вызывается при exception

    7. Запустить тесты: `cd backend && python -m pytest tests/test_hawk_init.py -x -v`
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/backend && python -m pytest tests/test_hawk_init.py -x -v</automated>
  </verify>
  <done>hawk-python-sdk установлен вместо sentry-sdk, Hawk инициализируется в lifespan FastAPI и при запуске Celery, task_failure signal подключён для автоматической отправки ошибок задач, graceful skip при отсутствии токена, тесты зелёные</done>
</task>

<task type="auto">
  <name>Задача 2: Интеграция Hawk Tracker на фронтенде</name>
  <files>frontend/package.json, frontend/src/config/hawk.ts, frontend/src/main.tsx, frontend/src/components/ErrorBoundary.tsx</files>
  <action>
    1. Установить SDK: `cd frontend && npm install @hawk.so/javascript --save`

    2. Создать frontend/src/config/hawk.ts:
       ```typescript
       import HawkCatcher from "@hawk.so/javascript";

       let hawk: HawkCatcher | null = null;

       export function initHawk(): void {
         const token = import.meta.env.VITE_HAWK_TOKEN;
         if (!token) {
           console.info("Hawk Tracker отключен (VITE_HAWK_TOKEN не установлен)");
           return;
         }
         hawk = new HawkCatcher({
           token,
           release: import.meta.env.VITE_APP_VERSION || "dev",
         });
       }

       export function getHawk(): HawkCatcher | null {
         return hawk;
       }
       ```

    3. Обновить frontend/src/main.tsx:
       - Добавить import: `import { initHawk } from "./config/hawk";`
       - Перед ReactDOM.createRoot: `initHawk();`

    4. Обновить frontend/src/components/ErrorBoundary.tsx:
       - Добавить import: `import { getHawk } from "@/config/hawk";`
       - В методе componentDidCatch добавить отправку ошибки в Hawk:
         ```typescript
         componentDidCatch(error: Error, errorInfo: ErrorInfo) {
           const hawk = getHawk();
           if (hawk) {
             hawk.send(error, { componentStack: errorInfo.componentStack });
           }
           // ... существующая логика
         }
         ```

    5. Проверить что фронтенд собирается: `cd frontend && npm run build`
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>@hawk.so/javascript установлен, Hawk инициализируется в main.tsx, ErrorBoundary отправляет ошибки в Hawk, фронтенд успешно собирается</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Задача 3: Верификация Hawk Tracker пользователем</name>
  <files></files>
  <action>
    Что построено:
    - Бэкенд: hawk-python-sdk[fastapi] заменил sentry-sdk, инициализация в lifespan
    - Фронтенд: @hawk.so/javascript, инициализация в main.tsx, ErrorBoundary отправляет ошибки
    - Celery: Hawk инициализируется при запуске воркера, task_failure signal подключён для автоматической отправки ошибок задач

    Шаги верификации:
    1. Создайте два проекта на hawk-tracker.ru (Python + JavaScript)
    2. Скопируйте токены в .env: HAWK_TOKEN=..., VITE_HAWK_TOKEN=...
    3. Перезапустите: docker compose -f docker-compose.lite.yml up -d --build
    4. Вызовите ошибку на бэкенде (запрос к несуществующему endpoint)
    5. Проверьте дашборд Hawk — событие должно появиться

    Напишите "approved" если ошибки отображаются в Hawk, или опишите проблемы.
  </action>
  <verify>Пользователь подтверждает работу Hawk Tracker</verify>
  <done>Ошибки отображаются в дашборде Hawk Tracker (бэкенд + фронтенд)</done>
</task>

</tasks>

<verification>
Проверка всего плана после выполнения:

1. Hawk бэкенд (DEPLOY-02):
   - `grep -r "hawk" backend/app/core/hawk.py` -> файл существует с init_hawk
   - `grep "hawk" backend/requirements.lite.txt` -> hawk-python-sdk[fastapi]
   - `grep -v "sentry" backend/requirements.lite.txt | wc -l` -> sentry-sdk удалён
   - `grep "task_failure" backend/app/core/hawk.py` -> task_failure signal подключён
   - `cd backend && python -m pytest tests/test_hawk_init.py -v`

2. Hawk фронтенд (DEPLOY-03):
   - `grep "hawk" frontend/package.json` -> @hawk.so/javascript
   - `test -f frontend/src/config/hawk.ts` -> файл существует
   - `cd frontend && npm run build` -> сборка успешна

</verification>

<success_criteria>
- hawk-python-sdk[fastapi] установлен, sentry-sdk удалён из requirements
- Hawk инициализируется в lifespan FastAPI (бэкенд) и при запуске Celery
- Celery task_failure signal подключён — ошибки задач отправляются в Hawk автоматически
- Graceful skip при отсутствии HAWK_TOKEN (без краша)
- @hawk.so/javascript установлен на фронтенде
- Hawk инициализируется в main.tsx, ErrorBoundary отправляет ошибки
- Фронтенд успешно собирается
- Тесты инициализации Hawk проходят (включая тест task_failure signal)
</success_criteria>

<output>
После завершения создать `.planning/phases/01-production-safety/01-02-SUMMARY.md`
</output>
