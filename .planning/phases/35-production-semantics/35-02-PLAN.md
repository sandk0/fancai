---
phase: 35-production-semantics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/tasks/book_tasks.py
  - backend/app/core/pubsub.py
  - backend/tests/tasks/test_book_status_semantics.py
  - backend/tests/tasks/test_modal_timeout.py
  - backend/tests/tasks/test_time_budget.py
autonomous: true
requirements: [STAB-01, STAB-05, STAB-06]

must_haves:
  truths:
    - "Книга со сбойными главами получает descriptions_extracted=False и status=completed_with_errors — пользователь видит честный результат"
    - "Push notification НЕ отправляется при partial failures — пользователь увидит статус при следующем визите"
    - "VPS-side asyncio.wait_for() не даёт Celery thread блокироваться при зависании Modal"
    - "Per-book time budget check пропускает оставшиеся главы при нехватке времени до Celery hard limit"
  artifacts:
    - path: "backend/app/tasks/book_tasks.py"
      provides: "Реорганизованный порядок операций: failed chapters check -> статусы -> WebSocket -> push notification + VPS timeout + budget check"
      contains: "completed_with_errors"
    - path: "backend/app/core/pubsub.py"
      provides: "publish_book_progress с optional kwargs: chapters_failed, failed_chapter_numbers"
      contains: "chapters_failed"
    - path: "backend/tests/tasks/test_book_status_semantics.py"
      provides: "Тесты status semantics: 0 failures -> True, partial -> False+completed_with_errors, push notification guard"
    - path: "backend/tests/tasks/test_modal_timeout.py"
      provides: "Тесты VPS-side timeout: asyncio.wait_for отменяет при timeout"
    - path: "backend/tests/tasks/test_time_budget.py"
      provides: "Тесты time budget: skip chapter при нехватке времени"
  key_links:
    - from: "backend/app/tasks/book_tasks.py"
      to: "backend/app/core/pubsub.py"
      via: "publish_book_progress с chapters_failed, failed_chapter_numbers kwargs"
      pattern: "chapters_failed="
    - from: "backend/app/tasks/book_tasks.py"
      to: "backend/app/services/push_notification_service.py"
      via: "условный вызов send_book_ready_notification только при not has_failures"
      pattern: "if not has_failures"
---

<objective>
Семантика статусов книги: корректные статусы, VPS-side timeout, per-book time budget check.

Purpose: Книга с failed chapters получает `completed_with_errors` вместо ложного `descriptions_extracted=True` (STAB-01). Modal вызовы защищены VPS-side timeout (STAB-05). Time budget check предотвращает превышение Celery hard limit (STAB-06).
Output: Обновлённые book_tasks.py, pubsub.py + 3 тестовых файла.
</objective>

<execution_context>
@/Users/sandk/Documents/GitHub/fancai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/Documents/GitHub/fancai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-production-semantics/35-CONTEXT.md
@.planning/phases/35-production-semantics/35-RESEARCH.md

<interfaces>
<!-- Текущий порядок операций в book_tasks.py (BROKEN) — executor видит что именно менять -->

From backend/app/tasks/book_tasks.py (строки 914-1001):
```python
# Строка 918: БЕЗУСЛОВНО ставит True
book.descriptions_extracted = True  # НОВОЕ: флаг успешного извлечения
book.descriptions_processing_error = None  # Сбрасываем ошибку
await db.commit()

# Строка 924: БЕЗУСЛОВНО публикует "completed"
await publish_book_progress(
    book_id=str(book_id), progress=100, chapter=total_chapters,
    total_chapters=total_chapters, status="completed",
    message="Обработка завершена успешно!",
)

# Строка 952: ПОСЛЕ commit'а проверяет failed chapters
failed_chapters_result = await db.execute(
    select(Chapter.chapter_number, Chapter.parsing_error)
    .where(Chapter.book_id == book_id)
    .where(Chapter.parsing_error.isnot(None))
)
failed_chapters = [(r[0], r[1][:100]) for r in failed_chapters_result.fetchall()]

# Строка 990: БЕЗУСЛОВНО push notification
await push_notification_service.send_book_ready_notification(
    db=db, user_id=book.user_id, book_id=book.id, book_title=book.title,
)
```

From backend/app/core/pubsub.py:
```python
async def publish_book_progress(
    book_id: str, progress: int, chapter: int = 0,
    total_chapters: int = 0, status: str = "processing", message: str = "",
):
    data = {
        "type": "progress" if status == "processing" else status,
        "book_id": book_id, "progress": progress,
        "chapter": chapter, "total_chapters": total_chapters,
        "status": status, "message": message,
    }
```

From backend/app/tasks/book_tasks.py (строки 360, 441-448) — process_chapter_safe + Modal call:
```python
async def process_chapter_safe(idx: int, chapter_id: UUID):
    # ... внутри chapter_semaphore ...
    if use_modal:
        extractor = get_llm_extractor()
        modal_json = await asyncio.to_thread(
            extractor.extract_chapter.remote,
            chapter_text=local_chapter.content,
            system_prompt=EXTRACTION_SYSTEM_PROMPT,
            schema_json=EXTRACTION_SCHEMA_JSON,
        )
```

From backend/app/core/celery_app.py:
```python
time_limit=10800,  # 3 hours hard limit (task-level override в book_tasks.py)
soft_time_limit=10500,  # 2 hours 55 minutes
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Реорганизация порядка операций в book_tasks.py + pubsub.py + тесты</name>
  <files>backend/app/tasks/book_tasks.py, backend/app/core/pubsub.py, backend/tests/tasks/test_book_status_semantics.py</files>
  <read_first>
    - backend/app/tasks/book_tasks.py (строки 900-1001: текущий broken порядок операций)
    - backend/app/core/pubsub.py (publish_book_progress без chapters_failed kwargs)
    - backend/app/services/push_notification_service.py (send_book_ready_notification сигнатура)
    - backend/tests/tasks/test_modal_integration.py (паттерн тестирования modal tasks)
  </read_first>
  <behavior>
    - При 0 failed chapters: descriptions_extracted=True, WebSocket status="completed", push notification отправляется
    - При >0 failed chapters: descriptions_extracted=False, descriptions_processing_error содержит список номеров глав, WebSocket status="completed_with_errors" с chapters_failed count и failed_chapter_numbers array, push notification НЕ отправляется
    - publish_book_progress принимает optional kwargs chapters_failed и failed_chapter_numbers, включает их в data dict если переданы
  </behavior>
  <action>
    1. Создать `backend/tests/tasks/test_book_status_semantics.py`:
       - `test_all_chapters_success_sets_extracted_true`: mock DB с 0 failed chapters -> `book.descriptions_extracted == True`, `book.descriptions_processing_error is None`
       - `test_partial_failure_sets_extracted_false`: mock DB с 2 failed chapters -> `book.descriptions_extracted == False`, `book.descriptions_processing_error` содержит "Ошибки в 2 главах"
       - `test_partial_failure_publishes_completed_with_errors`: при failed chapters WebSocket вызывается с `status="completed_with_errors"`, `chapters_failed=2`, `failed_chapter_numbers=[3, 7]`
       - `test_push_notification_not_sent_on_partial_failure`: при failed chapters `send_book_ready_notification` НЕ вызывается
       - `test_push_notification_sent_on_full_success`: при 0 failed chapters `send_book_ready_notification` вызывается
       - Использовать `unittest.mock.AsyncMock` для mock DB session, `patch` для pubsub и push_notification
       - Тестировать логику через helper-функцию или извлечённый блок из _process_book_async

    2. Обновить `backend/app/core/pubsub.py` — расширить `publish_book_progress()`:
       - Добавить `**kwargs` в сигнатуру: `async def publish_book_progress(book_id, progress, chapter=0, total_chapters=0, status="processing", message="", **kwargs):`
       - После формирования `data` dict добавить: `data.update(kwargs)` — это передаст `chapters_failed` и `failed_chapter_numbers` в WebSocket message без хардкода новых полей
       - Не менять остальную логику pubsub

    3. Обновить `backend/app/tasks/book_tasks.py` — реорганизовать строки 914-1001:
       - УДАЛИТЬ строки 918-920 (безусловный `descriptions_extracted = True` + commit)
       - УДАЛИТЬ строки 924-933 (безусловный WebSocket "completed")
       - ПЕРЕМЕСТИТЬ failed chapters query (строки 952-959) ПЕРЕД установкой статусов
       - ВСТАВИТЬ новый блок на место строк 914-933:

       ```python
       # 1. Подсчитать failed chapters ПЕРЕД установкой статусов
       failed_chapters_result = await db.execute(
           select(Chapter.chapter_number, Chapter.parsing_error)
           .where(Chapter.book_id == book_id)
           .where(Chapter.parsing_error.isnot(None))
       )
       failed_chapters = [
           (r[0], r[1][:100]) for r in failed_chapters_result.fetchall()
       ]
       has_failures = len(failed_chapters) > 0

       # 2. Установить статусы на основе результата (D-01, D-03)
       book.is_processing = False
       book.is_parsed = True
       book.parsing_progress = 100
       book.descriptions_extracted = not has_failures  # D-01: True только при 0 failures
       book.descriptions_processing_error = (
           f"Ошибки в {len(failed_chapters)} главах: {[c[0] for c in failed_chapters]}"
           if has_failures else None
       )  # D-03: сохраняем сообщение при partial failure
       await db.commit()

       # 3. WebSocket с корректным статусом (D-02)
       try:
           ws_status = "completed_with_errors" if has_failures else "completed"
           ws_message = (
               f"Обработка завершена с ошибками ({len(failed_chapters)} глав)"
               if has_failures else "Обработка завершена успешно!"
           )
           await publish_book_progress(
               book_id=str(book_id),
               progress=100,
               chapter=total_chapters,
               total_chapters=total_chapters,
               status=ws_status,
               message=ws_message,
               **({"chapters_failed": len(failed_chapters),
                    "failed_chapter_numbers": [c[0] for c in failed_chapters]}
                  if has_failures else {}),
           )
       except Exception as ws_err:
           logger.warning("Failed to publish WebSocket completion", error=str(ws_err))
       ```

       - ОСТАВИТЬ cache invalidation блок (строки 935-950) без изменений

       - УДАЛИТЬ старый failed_chapters query (строки 952-965, уже перемещён выше)

       - ОБЕРНУТЬ push notification в условие (per D-04):
       ```python
       # Push notification ТОЛЬКО при полном успехе (D-04)
       if not has_failures:
           try:
               await push_notification_service.send_book_ready_notification(
                   db=db, user_id=book.user_id, book_id=book.id, book_title=book.title,
               )
               logger.debug("Push notification sent for book ready", book_id=str(book_id))
           except Exception as e:
               logger.warning("Failed to send book ready push notification", error=str(e))
       else:
           logger.info(
               f"Push notification skipped: {len(failed_chapters)} failed chapters",
               book_id=str(book_id),
           )
       ```

       - Обновить `result` dict (строки 967-978) — `has_failures` уже доступен, использовать его:
       ```python
       result = {
           "book_id": str(book_id),
           "status": "completed_with_errors" if has_failures else "completed",
           ...
       }
       ```

    4. Запустить тесты — GREEN
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/backend && uv run python -m pytest tests/tasks/test_book_status_semantics.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - book_tasks.py содержит `book.descriptions_extracted = not has_failures`
    - book_tasks.py содержит `"completed_with_errors" if has_failures`
    - book_tasks.py содержит `if not has_failures:` перед `send_book_ready_notification`
    - book_tasks.py содержит failed chapters query ПЕРЕД `book.descriptions_extracted`
    - pubsub.py содержит `**kwargs` в сигнатуре publish_book_progress
    - pubsub.py содержит `data.update(kwargs)`
    - test_book_status_semantics.py содержит минимум 5 тестов
    - Все тесты проходят
  </acceptance_criteria>
  <done>Failed chapters check происходит ДО установки статусов. descriptions_extracted=False при partial failures. WebSocket публикует completed_with_errors. Push notification не отправляется при ошибках.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VPS-side timeout (asyncio.wait_for) + per-book time budget check + тесты</name>
  <files>backend/app/tasks/book_tasks.py, backend/tests/tasks/test_modal_timeout.py, backend/tests/tasks/test_time_budget.py</files>
  <read_first>
    - backend/app/tasks/book_tasks.py (строки 273-310: _process_book_async начало, строки 354-470: process_chapter_safe + Modal call)
    - backend/app/core/celery_app.py (time_limit=10800 hard limit)
  </read_first>
  <behavior>
    - asyncio.wait_for wraps Modal remote call с timeout = VPS_TIMEOUT (960s = LLM_TIMEOUT 900 + 60s buffer)
    - При asyncio.TimeoutError глава помечается failed с parsing_error содержащим "VPS-side timeout"
    - Time budget check: если remaining time до Celery hard limit < VPS_TIMEOUT, глава пропускается
    - Budget exhaustion: глава помечается failed с parsing_error содержащим "Time budget exhausted"
  </behavior>
  <action>
    1. Создать `backend/tests/tasks/test_modal_timeout.py`:
       - `test_vps_timeout_wraps_modal_call`: mock `asyncio.to_thread` чтобы вернул корoutine, wrap в `asyncio.wait_for`, при timeout получаем `asyncio.TimeoutError`
       - `test_vps_timeout_marks_chapter_as_failed`: при timeout глава должна получить `parsing_error` содержащий "VPS-side timeout"
       - Использовать `unittest.mock.AsyncMock`, `pytest.mark.asyncio`

    2. Создать `backend/tests/tasks/test_time_budget.py`:
       - `test_budget_check_skips_chapter_when_exhausted`: mock `time.monotonic()` так что elapsed=10000s (из 10800 hard limit), remaining < VPS_TIMEOUT -> TimeoutError с "Time budget exhausted"
       - `test_budget_check_allows_chapter_when_sufficient`: mock monotonic так что elapsed=100s -> проходит без ошибки
       - `test_budget_safety_margin_300s`: проверить что SAFETY_MARGIN=300 используется в вычислении

    3. Обновить `backend/app/tasks/book_tasks.py`:

       a) В начале `_process_book_async()` (после строки 282 `async with AsyncSessionLocal() as db:`) добавить:
       ```python
       import time
       task_start_time = time.monotonic()
       CELERY_HARD_LIMIT = 10800  # 3 часа, из celery_app.py
       SAFETY_MARGIN = 300  # 5 минут запас на cleanup/commit
       VPS_TIMEOUT = 960  # LLM_TIMEOUT(900) + 60s buffer (D-10). Локальная константа — не импортируется из modal/config.py (разные сервисы)
       ```

       b) В `process_chapter_safe()`, внутри `async with chapter_semaphore:`, ПЕРЕД Modal call (строка ~441 `if use_modal:`), добавить time budget check:
       ```python
       # Per-book time budget check (D-12, STAB-06)
       elapsed = time.monotonic() - task_start_time
       remaining = CELERY_HARD_LIMIT - elapsed - SAFETY_MARGIN
       if remaining < VPS_TIMEOUT:
           raise TimeoutError(
               f"Time budget exhausted: remaining={remaining:.0f}s, "
               f"needed={VPS_TIMEOUT}s. Skipping chapter {idx}."
           )
       ```
       Переменная `task_start_time` доступна через closure из `_process_book_async`.

       c) Обернуть Modal call в `asyncio.wait_for()` (STAB-05, D-10):
       ```python
       if use_modal:
           extractor = get_llm_extractor()
           try:
               modal_json = await asyncio.wait_for(
                   asyncio.to_thread(
                       extractor.extract_chapter.remote,
                       chapter_text=local_chapter.content,
                       system_prompt=EXTRACTION_SYSTEM_PROMPT,
                       schema_json=EXTRACTION_SCHEMA_JSON,
                   ),
                   timeout=VPS_TIMEOUT,
               )
           except asyncio.TimeoutError:
               raise TimeoutError(
                   f"VPS-side timeout: Modal не ответил за {VPS_TIMEOUT}s "
                   f"(LLM_TIMEOUT=900s + buffer=60s)"
               )
       ```

       d) Убедиться что except блок в `process_chapter_safe` (который ловит ALL exceptions) записывает parsing_error в chapter. Проверить что текущий catch-all записывает `local_chapter.parsing_error = str(e)` — если нет, добавить.

    4. Запустить все новые тесты — GREEN
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/backend && uv run python -m pytest tests/tasks/test_modal_timeout.py tests/tasks/test_time_budget.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - book_tasks.py содержит `task_start_time = time.monotonic()`
    - book_tasks.py содержит `CELERY_HARD_LIMIT = 10800`
    - book_tasks.py содержит `SAFETY_MARGIN = 300`
    - book_tasks.py содержит `VPS_TIMEOUT = 960`
    - book_tasks.py содержит `asyncio.wait_for(` с `timeout=VPS_TIMEOUT`
    - book_tasks.py содержит `Time budget exhausted` в raise TimeoutError
    - book_tasks.py содержит `VPS-side timeout` в raise TimeoutError
    - test_modal_timeout.py содержит минимум 2 теста
    - test_time_budget.py содержит минимум 3 теста
    - Все тесты проходят
  </acceptance_criteria>
  <done>Modal вызовы защищены VPS-side timeout (960s). Per-book budget check пропускает главы при нехватке времени. TimeoutError записывается в chapter.parsing_error.</done>
</task>

</tasks>

<verification>
- `cd backend && uv run python -m pytest tests/tasks/test_book_status_semantics.py tests/tasks/test_modal_timeout.py tests/tasks/test_time_budget.py -x -v` — все тесты проходят
- `grep "descriptions_extracted = not has_failures" backend/app/tasks/book_tasks.py` — корректная установка статуса
- `grep "asyncio.wait_for" backend/app/tasks/book_tasks.py` — VPS timeout wrapper
- `grep "Time budget exhausted" backend/app/tasks/book_tasks.py` — budget check
- `grep "if not has_failures" backend/app/tasks/book_tasks.py` — push notification guard
- Существующие тесты не сломаны: `cd backend && uv run python -m pytest tests/tasks/ -x --tb=short`
</verification>

<success_criteria>
- Failed chapters query выполняется ПЕРЕД установкой descriptions_extracted
- descriptions_extracted=False при partial failures, True только при 0 failures
- WebSocket публикует completed_with_errors с chapters_failed и failed_chapter_numbers
- Push notification отправляется только при полном успехе
- Modal вызовы обёрнуты в asyncio.wait_for с timeout=960s
- Per-book time budget check предотвращает превышение Celery hard limit
- Все 10+ новых тестов проходят
</success_criteria>

<output>
После завершения создать `.planning/phases/35-production-semantics/35-02-SUMMARY.md`
</output>
