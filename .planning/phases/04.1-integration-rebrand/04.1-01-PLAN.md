---
phase: 04.1-integration-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.prod.yml
  - docker-compose.dev.yml
  - docker-compose.monitoring.yml
  - backend/Dockerfile.lite
  - backend/Dockerfile.lite.prod
  - backend/app/services/llm_description_enricher.py
  - backend/app/services/gemini_extractor.py
  - backend/app/core/config.py
  - backend/app/services/image_generator.py
  - backend/app/tasks/image_tasks.py
  - backend/app/routers/descriptions.py
  - backend/app/services/description_extraction_service.py
  - .env.example
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03
  - INT-04
  - INT-05

must_haves:
  truths:
    - "Docker-сеть в prod.yml и monitoring.yml использует одинаковое имя fancai_network — мониторинг-стек видит Redis через общую сеть"
    - "HAWK_TOKEN передаётся в celery-worker и celery-beat через docker-compose.prod.yml — Hawk фиксирует ошибки Celery задач"
    - "Мёртвые переменные LANGEXTRACT_MODEL, LANGEXTRACT_API_KEY, USE_LANGEXTRACT_PRIMARY, GOOGLE_API_KEY удалены из docker-compose, Dockerfiles и config.py"
    - "Мёртвый файл llm_description_enricher.py удалён — кодовая база не содержит неиспользуемых LangExtract артефактов"
    - "Сообщения об ошибках в 4 файлах ссылаются на OPENROUTER_API_KEY (не на GOOGLE_API_KEY)"
    - ".env.example содержит все актуальные переменные: OPENROUTER_API_KEY, OPENROUTER_IMAGE_MODEL, HAWK_TOKEN, MONITOR_PASSWORD_HASH, METRICS_USER, METRICS_PASSWORD"
  artifacts:
    - path: "docker-compose.prod.yml"
      provides: "Docker Compose prod конфигурация без мёртвых LANGEXTRACT/GOOGLE переменных, с HAWK_TOKEN в Celery и fancai_network"
    - path: "docker-compose.monitoring.yml"
      provides: "Мониторинг-стек с external network name: fancai_network"
    - path: ".env.example"
      provides: "Документация всех актуальных env-переменных для развёртывания на чистом сервере"
  key_links:
    - from: "docker-compose.prod.yml"
      to: "docker-compose.monitoring.yml"
      via: "Имя Docker-сети fancai_network (prod создаёт, monitoring ссылается external)"
      pattern: "name: fancai_network"
    - from: "docker-compose.prod.yml celery-worker/beat"
      to: "backend/app/core/celery_app.py init_hawk_celery()"
      via: "HAWK_TOKEN env var передаётся в контейнер"
      pattern: "HAWK_TOKEN"
    - from: "backend/app/services/gemini_extractor.py"
      to: "OPENROUTER_API_KEY"
      via: "Единственный API ключ для AI (без fallback на LANGEXTRACT_API_KEY)"
      pattern: "os\\.getenv\\(\"OPENROUTER_API_KEY\"\\)"
---

<objective>
Интеграционные фиксы и очистка Gemini/LangExtract легаси

Закрыть 5 интеграционных пробелов аудита v1.0: исправить несоответствие Docker-сетей между prod.yml и monitoring.yml (INT-01), добавить HAWK_TOKEN в Celery-сервисы (INT-03), удалить все мёртвые Gemini/LangExtract переменные вместо прокидывания (INT-04 — переосмысленное), создать .env.example с актуальными переменными (INT-02, INT-05).

Purpose: Без фикса Docker-сетей мониторинг-стек (Flower) не видит Redis. Без HAWK_TOKEN Celery-ошибки не попадают в Hawk Tracker. Мёртвые LANGEXTRACT/GOOGLE переменные создают ложное впечатление что эти сервисы используются. Без .env.example невозможно развернуть проект на чистом сервере.
Output: Рабочие docker-compose файлы без мёртвого кода, чистый backend без LangExtract артефактов, .env.example для развёртывания.
</objective>

<execution_context>
@/Users/sandk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-integration-rebrand/04.1-CONTEXT.md
@.planning/phases/04.1-integration-rebrand/04.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Очистка Docker-compose и Dockerfiles от мёртвых переменных + фикс сети + HAWK_TOKEN</name>
  <files>docker-compose.prod.yml, docker-compose.dev.yml, docker-compose.monitoring.yml, backend/Dockerfile.lite, backend/Dockerfile.lite.prod</files>
  <action>
**docker-compose.prod.yml:**
1. Секция `networks:` внизу файла — переименовать alias `bookreader_network:` → `fancai_network:`, добавить `name: fancai_network` (явное имя для external ref из monitoring.yml), bridge name `br-bookreader-lite` → `br-fancai`
2. Все сервисы (frontend, backend, postgres, redis, caddy, celery-worker, celery-beat): `networks: - bookreader_network` → `networks: - fancai_network`
3. **УДАЛИТЬ** из сервиса backend (строки ~102-104):
   - `USE_LANGEXTRACT_PRIMARY=true`
   - `LANGEXTRACT_API_KEY=${LANGEXTRACT_API_KEY:?LANGEXTRACT_API_KEY is required}` (убрать :? валидацию тоже — ключ мёртвый)
   - `LANGEXTRACT_MODEL=${LANGEXTRACT_MODEL:-gemini-3-flash-preview}`
4. **УДАЛИТЬ** из сервиса celery-worker (строки ~163-165):
   - `USE_LANGEXTRACT_PRIMARY=true`
   - `LANGEXTRACT_API_KEY=${LANGEXTRACT_API_KEY}`
   - `LANGEXTRACT_MODEL=${LANGEXTRACT_MODEL:-gemini-3-flash-preview}`
5. **ДОБАВИТЬ** в celery-worker environment: `- HAWK_TOKEN=${HAWK_TOKEN:-}`
6. **ДОБАВИТЬ** в celery-beat environment: `- HAWK_TOKEN=${HAWK_TOKEN:-}`
7. Удалить комментарий в заголовке файла (строка ~8): `# - LANGEXTRACT_API_KEY in .env file`

**docker-compose.dev.yml:**
1. **УДАЛИТЬ** из сервиса backend (строки ~91-95):
   - `GOOGLE_API_KEY=${GOOGLE_API_KEY:-${LANGEXTRACT_API_KEY:?...}}`
   - `LANGEXTRACT_API_KEY=${LANGEXTRACT_API_KEY:-${GOOGLE_API_KEY}}`
   - `LANGEXTRACT_MODEL=${LANGEXTRACT_MODEL:-gemini-3-flash-preview}`
   - `USE_LANGEXTRACT_PRIMARY=true`
2. **УДАЛИТЬ** из сервиса celery-worker (строки ~145-149):
   - `GOOGLE_API_KEY=${GOOGLE_API_KEY:-${LANGEXTRACT_API_KEY}}`
   - `LANGEXTRACT_API_KEY=${LANGEXTRACT_API_KEY:-${GOOGLE_API_KEY}}`
   - `LANGEXTRACT_MODEL=${LANGEXTRACT_MODEL:-gemini-3-flash-preview}`
   - `USE_LANGEXTRACT_PRIMARY=true`

**docker-compose.monitoring.yml:**
1. External network: `name: bookreader_lite_network` → `name: fancai_network`
2. Flower networks: `- bookreader_network` → `- fancai_network`
3. Network alias в секции networks: `bookreader_network:` → `fancai_network:`

**backend/Dockerfile.lite (строка 23):**
- Удалить `USE_LANGEXTRACT_PRIMARY=true \` из ENV блока

**backend/Dockerfile.lite.prod:**
- Удалить комментарий про LANGEXTRACT_API_KEY (строка ~8)
- Удалить `USE_LANGEXTRACT_PRIMARY=true \` из ENV блока (строка ~47)

**НЕ ТРОГАТЬ:** docker-compose.dev.yml сети (уже `fancai_network`), HAWK_TOKEN в dev.yml (не требуется), имена контейнеров/образов (это REBRAND план 04.1-02).
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon && grep -c "LANGEXTRACT_MODEL\|LANGEXTRACT_API_KEY\|USE_LANGEXTRACT_PRIMARY" docker-compose.prod.yml docker-compose.dev.yml backend/Dockerfile.lite backend/Dockerfile.lite.prod && echo "FAIL: dead vars remain" || echo "PASS: no dead vars" && grep -c "GOOGLE_API_KEY" docker-compose.dev.yml && echo "FAIL: GOOGLE_API_KEY remains" || echo "PASS" && grep "HAWK_TOKEN" docker-compose.prod.yml | wc -l | xargs -I{} test {} -ge 2 && echo "PASS: HAWK_TOKEN in 2+ services" || echo "FAIL: HAWK_TOKEN missing" && grep "fancai_network" docker-compose.prod.yml docker-compose.monitoring.yml | wc -l | xargs -I{} test {} -ge 2 && echo "PASS: fancai_network in both files" || echo "FAIL"</automated>
  </verify>
  <done>
- Нет строк LANGEXTRACT_MODEL, LANGEXTRACT_API_KEY, USE_LANGEXTRACT_PRIMARY в docker-compose.prod.yml, docker-compose.dev.yml, Dockerfile.lite, Dockerfile.lite.prod
- Нет GOOGLE_API_KEY в docker-compose.dev.yml
- HAWK_TOKEN присутствует в celery-worker и celery-beat environment в prod.yml
- Docker-сеть называется fancai_network в prod.yml и monitoring.yml (external ref совпадает)
  </done>
</task>

<task type="auto">
  <name>Task 2: Удаление мёртвого LangExtract кода и обновление error messages</name>
  <files>backend/app/services/llm_description_enricher.py, backend/app/services/gemini_extractor.py, backend/app/core/config.py, backend/app/services/image_generator.py, backend/app/tasks/image_tasks.py, backend/app/routers/descriptions.py, backend/app/services/description_extraction_service.py</files>
  <action>
1. **УДАЛИТЬ ФАЙЛ** `backend/app/services/llm_description_enricher.py` целиком (rm). Ноль внешних импортов — мёртвый код, 15K строк.

2. **backend/app/services/gemini_extractor.py строка 501-504:** Удалить fallback на LANGEXTRACT_API_KEY. Сейчас:
```python
self.config.api_key = (
    self.config.api_key
    or os.getenv("OPENROUTER_API_KEY")
    or os.getenv("LANGEXTRACT_API_KEY")  # ← удалить эту строку
)
```
Станет:
```python
self.config.api_key = (
    self.config.api_key
    or os.getenv("OPENROUTER_API_KEY")
)
```

3. **backend/app/core/config.py строка 66:** Удалить `GOOGLE_API_KEY: Optional[str] = None` и комментарий. Эта переменная больше нигде не используется для логики — только в error messages, которые обновляются ниже.

4. **Обновить error messages** (GOOGLE_API_KEY → OPENROUTER_API_KEY) в 4 файлах:
   - `backend/app/services/image_generator.py` строка 122: `"Check GOOGLE_API_KEY."` → `"Check OPENROUTER_API_KEY."`
   - `backend/app/tasks/image_tasks.py` строка 140: `"Check GOOGLE_API_KEY."` → `"Check OPENROUTER_API_KEY."`
   - `backend/app/routers/descriptions.py` строка 93: `"Check GOOGLE_API_KEY."` → `"Check OPENROUTER_API_KEY."`
   - `backend/app/services/description_extraction_service.py` строка 74: `"Check GOOGLE_API_KEY."` → `"Check OPENROUTER_API_KEY."`
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon && test ! -f backend/app/services/llm_description_enricher.py && echo "PASS: enricher deleted" || echo "FAIL: enricher exists" && grep -r "LANGEXTRACT_API_KEY" backend/app/services/gemini_extractor.py && echo "FAIL" || echo "PASS: no LANGEXTRACT fallback" && grep -r "GOOGLE_API_KEY" backend/app/core/config.py && echo "FAIL" || echo "PASS: no GOOGLE_API_KEY in config" && grep -r "GOOGLE_API_KEY" backend/app/services/ backend/app/tasks/ backend/app/routers/ && echo "FAIL: old error messages remain" || echo "PASS: all error messages updated"</automated>
  </verify>
  <done>
- llm_description_enricher.py удалён (файл не существует)
- gemini_extractor.py не содержит LANGEXTRACT_API_KEY
- config.py не содержит GOOGLE_API_KEY
- Все 4 файла с error messages ссылаются на OPENROUTER_API_KEY
  </done>
</task>

<task type="auto">
  <name>Task 3: Создание .env.example с актуальными переменными</name>
  <files>.env.example</files>
  <action>
Создать (или полностью переписать) корневой `.env.example` с документацией всех актуальных env-переменных. Файл должен быть достаточно подробным для развёртывания на чистом сервере.

**НЕ ДОКУМЕНТИРОВАТЬ** мёртвые переменные: LANGEXTRACT_MODEL, LANGEXTRACT_API_KEY, GOOGLE_API_KEY, USE_LANGEXTRACT_PRIMARY.

**Обязательные секции и переменные:**

```env
# =============================================================================
# fancai — Конфигурация окружения
# Скопировать в .env и заполнить значения перед запуском
# =============================================================================

# --- Приложение ---
APP_NAME=fancai
DEBUG=false
SECRET_KEY=  # ОБЯЗАТЕЛЬНО: сгенерировать openssl rand -hex 32

# --- База данных (PostgreSQL) ---
POSTGRES_USER=fancai_user
POSTGRES_PASSWORD=  # ОБЯЗАТЕЛЬНО: надёжный пароль
POSTGRES_DB=fancai
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# --- Redis ---
REDIS_PASSWORD=  # ОБЯЗАТЕЛЬНО: надёжный пароль
REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0

# --- AI / OpenRouter (все LLM и генерация изображений) ---
# Получить API ключ: https://openrouter.ai/settings/keys
OPENROUTER_API_KEY=  # ОБЯЗАТЕЛЬНО: sk-or-...
# Модель для генерации изображений
# Доступные: black-forest-labs/flux.2-klein-4b, black-forest-labs/flux-pro
OPENROUTER_IMAGE_MODEL=black-forest-labs/flux.2-klein-4b

# --- Мониторинг ошибок (Hawk Tracker) ---
# Токен проекта из hawk-tracker.ru
HAWK_TOKEN=  # Опционально: без него Hawk не инициализируется

# --- Мониторинг инфраструктуры ---
# Пароль для monitor.fancai.ru (basicauth через Caddy)
# Генерировать: docker run caddy:2-alpine caddy hash-password --plaintext YOUR_PASSWORD
MONITOR_PASSWORD_HASH=  # ОБЯЗАТЕЛЬНО для мониторинга: $2a$14$...
# Метрики endpoint (/health/metrics) — Prometheus/VictoriaMetrics scraping
METRICS_USER=admin
METRICS_PASSWORD=  # ОБЯЗАТЕЛЬНО: надёжный пароль

# --- Email (SMTP) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=noreply@fancai.ru

# --- Push-уведомления (VAPID) ---
# Генерировать: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@fancai.ru
```

**Принципы:**
- Все критичные переменные помечены `# ОБЯЗАТЕЛЬНО:`
- Порядок: App → DB → Redis → AI → Monitoring → Email → Push
- Каждая секция с комментарием-разделителем
- Дефолтные значения для некритичных параметров (OPENROUTER_IMAGE_MODEL, METRICS_USER)
- Инструкции по генерации паролей/ключей рядом с переменными

**Важно:** Просмотреть существующий .env.example перед перезаписью — сохранить любые актуальные переменные, которые могут быть там (например, CORS, ALLOWED_HOSTS). Но удалить все упоминания LANGEXTRACT, GOOGLE_API_KEY и любые другие мёртвые переменные.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon && grep -c "OPENROUTER_API_KEY" .env.example | xargs -I{} test {} -ge 1 && echo "PASS: OPENROUTER_API_KEY documented" || echo "FAIL" && grep -c "OPENROUTER_IMAGE_MODEL" .env.example | xargs -I{} test {} -ge 1 && echo "PASS" || echo "FAIL" && grep -c "HAWK_TOKEN" .env.example | xargs -I{} test {} -ge 1 && echo "PASS" || echo "FAIL" && grep -c "MONITOR_PASSWORD_HASH" .env.example | xargs -I{} test {} -ge 1 && echo "PASS" || echo "FAIL" && grep -c "METRICS_USER" .env.example | xargs -I{} test {} -ge 1 && echo "PASS" || echo "FAIL" && grep -c "METRICS_PASSWORD" .env.example | xargs -I{} test {} -ge 1 && echo "PASS" || echo "FAIL" && grep "LANGEXTRACT\|GOOGLE_API_KEY" .env.example && echo "FAIL: dead vars in .env.example" || echo "PASS: no dead vars"</automated>
  </verify>
  <done>
- .env.example содержит OPENROUTER_API_KEY, OPENROUTER_IMAGE_MODEL, HAWK_TOKEN, MONITOR_PASSWORD_HASH, METRICS_USER, METRICS_PASSWORD
- .env.example НЕ содержит LANGEXTRACT_MODEL, LANGEXTRACT_API_KEY, GOOGLE_API_KEY
- Переменные сгруппированы по секциям с инструкциями
  </done>
</task>

</tasks>

<verification>
Финальная верификация после выполнения всех задач:

```bash
# 1. Мёртвые переменные полностью удалены из Docker/Dockerfiles:
grep -r "LANGEXTRACT_MODEL\|LANGEXTRACT_API_KEY\|USE_LANGEXTRACT_PRIMARY" docker-compose.prod.yml docker-compose.dev.yml backend/Dockerfile.lite backend/Dockerfile.lite.prod
# Ожидается: пустой вывод

# 2. GOOGLE_API_KEY удалён отовсюду:
grep -rn "GOOGLE_API_KEY" backend/app/ docker-compose.dev.yml docker-compose.prod.yml .env.example
# Ожидается: пустой вывод

# 3. Мёртвый файл удалён:
test ! -f backend/app/services/llm_description_enricher.py && echo "OK"

# 4. Docker-сеть единообразна:
grep "fancai_network" docker-compose.prod.yml docker-compose.monitoring.yml

# 5. HAWK_TOKEN в Celery:
grep "HAWK_TOKEN" docker-compose.prod.yml

# 6. Error messages обновлены:
grep -r "OPENROUTER_API_KEY" backend/app/services/image_generator.py backend/app/tasks/image_tasks.py backend/app/routers/descriptions.py backend/app/services/description_extraction_service.py

# 7. .env.example полный:
grep -E "OPENROUTER_API_KEY|OPENROUTER_IMAGE_MODEL|HAWK_TOKEN|MONITOR_PASSWORD_HASH|METRICS_USER|METRICS_PASSWORD" .env.example

# 8. Frontend build + backend imports не сломаны:
cd frontend && npm run build
cd backend && python -c "from app.core.config import settings; print('config OK')"
```
</verification>

<success_criteria>
1. Ноль упоминаний LANGEXTRACT_MODEL, LANGEXTRACT_API_KEY, USE_LANGEXTRACT_PRIMARY, GOOGLE_API_KEY в docker-compose файлах, Dockerfiles и backend/app/core/config.py
2. llm_description_enricher.py не существует на диске
3. gemini_extractor.py не содержит fallback на LANGEXTRACT_API_KEY
4. 4 файла с error messages ссылаются на OPENROUTER_API_KEY
5. Docker-сеть fancai_network используется одинаково в prod.yml и monitoring.yml
6. HAWK_TOKEN передаётся в celery-worker и celery-beat в prod.yml
7. .env.example содержит все 6 обязательных переменных и не содержит мёртвых
</success_criteria>

<output>
После завершения создать `.planning/phases/04.1-integration-rebrand/04.1-01-SUMMARY.md`
</output>
