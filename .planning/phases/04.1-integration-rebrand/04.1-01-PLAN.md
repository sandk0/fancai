---
phase: 04.1-integration-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/llm_description_enricher.py
  - backend/app/services/gemini_extractor.py
  - backend/app/core/config.py
  - backend/app/services/image_generator.py
  - backend/app/tasks/image_tasks.py
  - backend/app/routers/descriptions.py
  - backend/app/services/description_extraction_service.py
  - backend/Dockerfile.lite
  - backend/Dockerfile.lite.prod
  - docker-compose.prod.yml
  - docker-compose.dev.yml
  - docker-compose.monitoring.yml
  - .env.example
  - server_tasks.py
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03
  - INT-04
  - INT-05

must_haves:
  truths:
    - "Мёртвые переменные LANGEXTRACT_MODEL, LANGEXTRACT_API_KEY, USE_LANGEXTRACT_PRIMARY, GOOGLE_API_KEY полностью удалены из docker-compose файлов, Dockerfiles и config.py"
    - "Мёртвый файл llm_description_enricher.py удалён"
    - "Docker-сеть в prod.yml и monitoring.yml использует одинаковое имя fancai_network"
    - "HAWK_TOKEN передаётся в celery-worker и celery-beat через docker-compose.prod.yml"
    - ".env.example содержит все живые переменные: OPENROUTER_API_KEY, OPENROUTER_IMAGE_MODEL, HAWK_TOKEN, MONITOR_PASSWORD_HASH, METRICS_USER, METRICS_PASSWORD"
    - "Сообщения об ошибках ссылаются на OPENROUTER_API_KEY вместо GOOGLE_API_KEY"
  artifacts:
    - path: ".env.example"
      provides: "Шаблон переменных окружения для нового сервера"
      contains: "OPENROUTER_API_KEY"
  key_links:
    - from: "docker-compose.prod.yml"
      to: "docker-compose.monitoring.yml"
      via: "Общее имя Docker-сети fancai_network"
      pattern: "fancai_network"
    - from: "docker-compose.prod.yml"
      to: "backend/app/core/config.py"
      via: "HAWK_TOKEN передаётся в celery-worker/beat"
      pattern: "HAWK_TOKEN"
---

<objective>
Очистка мёртвого Gemini/LangExtract легаси и закрытие всех 5 интеграционных пробелов аудита v1.0.

Purpose: После полного перехода на OpenRouter (фаза 3) в docker-compose файлах, Dockerfiles и config.py остались мёртвые переменные Gemini/LangExtract. Также есть несоответствие имени Docker-сети между prod.yml и monitoring.yml, HAWK_TOKEN не передаётся в Celery-контейнеры, и отсутствует .env.example для развёртывания на новом сервере.
Output: Чистые конфигурации без мёртвых переменных, рабочая связка prod+monitoring через единую сеть, корневой .env.example
</objective>

<execution_context>
@/Users/sandk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-integration-rebrand/04.1-RESEARCH.md

Файлы для изменения:
@docker-compose.prod.yml
@docker-compose.dev.yml
@docker-compose.monitoring.yml
@backend/app/core/config.py
@backend/app/services/gemini_extractor.py
@backend/app/services/llm_description_enricher.py
@backend/app/services/image_generator.py
@backend/app/tasks/image_tasks.py
@backend/app/routers/descriptions.py
@backend/app/services/description_extraction_service.py
@backend/Dockerfile.lite
@backend/Dockerfile.lite.prod
</context>

<tasks>

<task type="auto">
  <name>Task 1: Очистка мёртвого Gemini/LangExtract кода</name>
  <files>
    backend/app/services/llm_description_enricher.py,
    backend/app/services/gemini_extractor.py,
    backend/app/core/config.py,
    backend/app/services/image_generator.py,
    backend/app/tasks/image_tasks.py,
    backend/app/routers/descriptions.py,
    backend/app/services/description_extraction_service.py,
    docker-compose.prod.yml,
    docker-compose.dev.yml,
    backend/Dockerfile.lite,
    backend/Dockerfile.lite.prod,
    server_tasks.py
  </files>
  <action>
1. **Удалить мёртвый файл:** `rm backend/app/services/llm_description_enricher.py` — ноль внешних импортов, класс LLMDescriptionEnricher нигде не используется. Верифицировать отсутствие импортов через grep перед удалением.

2. **Удалить мёртвый файл:** `rm server_tasks.py` (корень проекта) — 0 импортов из других файлов, содержит устаревшую ссылку на GOOGLE_API_KEY.

3. **gemini_extractor.py строка ~504:** Удалить fallback `or os.getenv("LANGEXTRACT_API_KEY")`. Оставить только:
   ```python
   self.config.api_key = (
       self.config.api_key
       or os.getenv("OPENROUTER_API_KEY")
   )
   ```

4. **config.py строка 66:** Удалить `GOOGLE_API_KEY: Optional[str] = None` и комментарий. Если есть import Optional — проверить что он используется ещё где-то, иначе не удалять import (Optional используется в других полях).

5. **Обновить сообщения об ошибках в 4 файлах** — заменить "Check GOOGLE_API_KEY." на "Check OPENROUTER_API_KEY.":
   - `backend/app/services/image_generator.py` строка 122
   - `backend/app/tasks/image_tasks.py` строка 140
   - `backend/app/routers/descriptions.py` строка 93
   - `backend/app/services/description_extraction_service.py` строка 74

6. **docker-compose.prod.yml** — удалить из ВСЕХ сервисов (backend, celery-worker, celery-beat):
   - `USE_LANGEXTRACT_PRIMARY=true`
   - `LANGEXTRACT_API_KEY=${LANGEXTRACT_API_KEY:?...}` (и вариант без :?)
   - `LANGEXTRACT_MODEL=${LANGEXTRACT_MODEL:-gemini-3-flash-preview}`
   - Удалить комментарий строка 8: `# - LANGEXTRACT_API_KEY in .env file`

7. **docker-compose.dev.yml** — удалить из ВСЕХ сервисов (backend, celery-worker):
   - `GOOGLE_API_KEY=...`
   - `LANGEXTRACT_API_KEY=...`
   - `LANGEXTRACT_MODEL=...`
   - `USE_LANGEXTRACT_PRIMARY=true`

8. **backend/Dockerfile.lite строка 23:** Удалить `USE_LANGEXTRACT_PRIMARY=true \` из ENV блока.

9. **backend/Dockerfile.lite.prod:**
   - Строка 8: Удалить комментарий `# - LANGEXTRACT_API_KEY environment variable (Google Gemini API key)`
   - Строка 47: Удалить `USE_LANGEXTRACT_PRIMARY=true \` из ENV блока.

**ВАЖНО:** При удалении строк из ENV блоков с backslash continuation — проверить что следующая строка корректно продолжает (или завершает) блок. Последняя строка ENV не должна заканчиваться на `\`.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon && grep -rn "LANGEXTRACT\|GOOGLE_API_KEY\|USE_LANGEXTRACT" docker-compose.prod.yml docker-compose.dev.yml docker-compose.monitoring.yml backend/app/ backend/Dockerfile.lite backend/Dockerfile.lite.prod 2>/dev/null | grep -v __pycache__ | grep -v test_langextract; echo "---"; test ! -f backend/app/services/llm_description_enricher.py && echo "enricher deleted OK" || echo "FAIL: enricher still exists"; test ! -f server_tasks.py && echo "server_tasks deleted OK" || echo "FAIL: server_tasks still exists"</automated>
  </verify>
  <done>Все 4 мёртвые переменные (LANGEXTRACT_MODEL, LANGEXTRACT_API_KEY, USE_LANGEXTRACT_PRIMARY, GOOGLE_API_KEY) удалены из docker-compose файлов, Dockerfiles и config.py. Мёртвый файл llm_description_enricher.py удалён. Сообщения об ошибках обновлены на OPENROUTER_API_KEY.</done>
</task>

<task type="auto">
  <name>Task 2: Интеграционные фиксы — Docker-сеть, HAWK_TOKEN, .env.example</name>
  <files>
    docker-compose.prod.yml,
    docker-compose.monitoring.yml,
    .env.example
  </files>
  <action>
1. **docker-compose.prod.yml — исправить имя сети (INT-01):**
   В секции `networks:` (конец файла, строки ~320-327):
   - Переименовать ключ `bookreader_network:` → `fancai_network:`
   - Добавить `name: fancai_network` (явное имя для external reference из monitoring.yml)
   - Изменить bridge name: `br-bookreader-lite` → `br-fancai`
   - Во ВСЕХ сервисах (frontend, backend, postgres, redis, caddy, celery-worker, celery-beat): `networks: - bookreader_network` → `networks: - fancai_network`

   **ВАЖНО:** Добавить `name: fancai_network` в секции networks — без него Docker Compose создаёт имя `{project_name}_fancai_network`, и monitoring.yml не найдёт сеть.

2. **docker-compose.monitoring.yml — синхронизировать сеть (INT-01):**
   - Секция networks (конец файла): `bookreader_network: external: true, name: bookreader_lite_network` → `fancai_network: external: true, name: fancai_network`
   - Flower networks: `- bookreader_network` → `- fancai_network`

3. **docker-compose.prod.yml — добавить HAWK_TOKEN (INT-03):**
   В environment секцию `celery-worker` добавить:
   ```yaml
   - HAWK_TOKEN=${HAWK_TOKEN:-}
   ```
   В environment секцию `celery-beat` добавить:
   ```yaml
   - HAWK_TOKEN=${HAWK_TOKEN:-}
   ```

4. **Создать .env.example (INT-02, INT-05):**
   Создать НОВЫЙ файл `.env.example` в корне проекта (старый, если существует, перезаписать). Содержать ТОЛЬКО живые переменные, сгруппированные по секциям:

   ```
   # =============================================================================
   # fancai — Environment Variables
   # =============================================================================
   # Скопируйте в .env и заполните значения для вашего окружения
   # Все переменные без значений по умолчанию ОБЯЗАТЕЛЬНЫ

   # --- Приложение ---
   SECRET_KEY=           # ОБЯЗАТЕЛЬНО. Генерировать: python -c "import secrets; print(secrets.token_urlsafe(64))"
   DEBUG=false

   # --- База данных (PostgreSQL) ---
   POSTGRES_USER=fancai_user
   POSTGRES_PASSWORD=    # ОБЯЗАТЕЛЬНО. Генерировать: openssl rand -base64 32
   POSTGRES_DB=fancai
   DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

   # --- Redis ---
   REDIS_PASSWORD=       # ОБЯЗАТЕЛЬНО. Генерировать: openssl rand -base64 32
   REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0

   # --- AI / OpenRouter (все LLM и генерация изображений) ---
   OPENROUTER_API_KEY=   # ОБЯЗАТЕЛЬНО. Получить: https://openrouter.ai/settings/keys
   OPENROUTER_IMAGE_MODEL=black-forest-labs/flux.2-klein-4b  # Доступные: flux.2-klein-4b, flux-pro

   # --- Мониторинг ошибок (Hawk Tracker) ---
   HAWK_TOKEN=           # Получить: https://hawk-tracker.ru — создать проект, скопировать интеграционный токен

   # --- Мониторинг инфраструктуры (monitor.fancai.ru) ---
   MONITOR_PASSWORD_HASH=  # Генерировать: docker run caddy:alpine caddy hash-password --plaintext YOUR_PASSWORD
   METRICS_USER=admin
   METRICS_PASSWORD=       # ОБЯЗАТЕЛЬНО. Пароль для /health/metrics endpoint (Prometheus scraping)

   # --- Email (опционально) ---
   SMTP_HOST=
   SMTP_PORT=587
   SMTP_USER=
   SMTP_PASSWORD=
   EMAIL_FROM=noreply@fancai.ru

   # --- Push-уведомления (VAPID, опционально) ---
   VAPID_PRIVATE_KEY=
   VAPID_PUBLIC_KEY=
   VAPID_SUBJECT=mailto:admin@fancai.ru
   ```

   **НЕ документировать:** LANGEXTRACT_MODEL, LANGEXTRACT_API_KEY, GOOGLE_API_KEY (мёртвые — решение пользователя).
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon && echo "=== Network check ===" && grep -c "fancai_network" docker-compose.prod.yml && grep -c "fancai_network" docker-compose.monitoring.yml && echo "=== HAWK_TOKEN check ===" && grep -A50 "celery-worker:" docker-compose.prod.yml | grep -c "HAWK_TOKEN" && grep -A30 "celery-beat:" docker-compose.prod.yml | grep -c "HAWK_TOKEN" && echo "=== .env.example check ===" && grep -cE "OPENROUTER_API_KEY|OPENROUTER_IMAGE_MODEL|HAWK_TOKEN|MONITOR_PASSWORD_HASH|METRICS_USER|METRICS_PASSWORD" .env.example && echo "=== No dead vars in .env.example ===" && grep -c "LANGEXTRACT\|GOOGLE_API_KEY" .env.example || echo "OK: no dead vars"</automated>
  </verify>
  <done>Docker-сеть fancai_network согласована между prod.yml и monitoring.yml. HAWK_TOKEN передаётся в celery-worker и celery-beat. .env.example содержит все 6 обязательных переменных (OPENROUTER_API_KEY, OPENROUTER_IMAGE_MODEL, HAWK_TOKEN, MONITOR_PASSWORD_HASH, METRICS_USER, METRICS_PASSWORD) без мёртвых переменных.</done>
</task>

</tasks>

<verification>
```bash
# Полная верификация INT-01..05:

# INT-01: Сети согласованы
docker compose -f docker-compose.prod.yml config 2>/dev/null | grep -A5 "fancai_network"
docker compose -f docker-compose.monitoring.yml config 2>/dev/null | grep -A5 "fancai_network"

# INT-02 + INT-05: .env.example полный
grep -E "OPENROUTER_API_KEY|OPENROUTER_IMAGE_MODEL|HAWK_TOKEN|MONITOR_PASSWORD_HASH|METRICS_USER|METRICS_PASSWORD" .env.example

# INT-03: HAWK_TOKEN в Celery
grep -A50 "celery-worker:" docker-compose.prod.yml | grep HAWK_TOKEN
grep -A30 "celery-beat:" docker-compose.prod.yml | grep HAWK_TOKEN

# INT-04 (переосмыслен): Мёртвые переменные УДАЛЕНЫ
grep -rn "LANGEXTRACT\|GOOGLE_API_KEY\|USE_LANGEXTRACT" docker-compose.prod.yml docker-compose.dev.yml backend/app/core/config.py backend/Dockerfile.lite backend/Dockerfile.lite.prod
# Ожидается: пустой вывод

# Мёртвые файлы удалены
test ! -f backend/app/services/llm_description_enricher.py && echo "OK" || echo "FAIL"
test ! -f server_tasks.py && echo "OK" || echo "FAIL"

# Backend тесты
cd backend && python -m pytest tests/ -x -q --tb=short 2>&1 | tail -5
```
</verification>

<success_criteria>
- Ни одного вхождения LANGEXTRACT_MODEL, LANGEXTRACT_API_KEY, USE_LANGEXTRACT_PRIMARY, GOOGLE_API_KEY в docker-compose файлах, Dockerfiles, config.py
- Файл llm_description_enricher.py не существует
- Docker-сеть fancai_network в prod.yml имеет явный `name: fancai_network`
- monitoring.yml external network ссылается на `fancai_network`
- HAWK_TOKEN присутствует в environment celery-worker и celery-beat
- .env.example содержит ровно 6 ключевых переменных (OPENROUTER_API_KEY, OPENROUTER_IMAGE_MODEL, HAWK_TOKEN, MONITOR_PASSWORD_HASH, METRICS_USER, METRICS_PASSWORD)
- Все 4 сообщения об ошибках ссылаются на OPENROUTER_API_KEY
</success_criteria>

<output>
После завершения создать `.planning/phases/04.1-integration-rebrand/04.1-01-SUMMARY.md`
</output>
