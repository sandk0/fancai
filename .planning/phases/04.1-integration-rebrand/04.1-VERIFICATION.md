---
phase: 04.1-integration-rebrand
verified: 2026-03-02T14:45:00Z
status: passed
score: 11/11 must-haves verified
re_verification: false
gaps:
  - truth: "alembic/env.py ссылается на fancai вместо bookreader"
    status: resolved
    reason: "Исправлено в коммите dc76b66: bookreader_user:bookreader_pass@localhost:5433/bookreader → fancai_user:fancai_pass@localhost:5433/fancai"
  - truth: "Скрипт create_admin.py использует fancai email по умолчанию"
    status: resolved
    reason: "Исправлено в коммите dc76b66: admin@bookreader.local → admin@fancai.local"
human_verification:
  - test: "Запустить docker compose up с prod.yml и monitoring.yml"
    expected: "Flower в monitoring.yml подключается к Redis через fancai_network без ошибок"
    why_human: "Нельзя проверить сетевую связность без запуска Docker на prod-сервере"
  - test: "Открыть книгу в браузере после ребрендинга, проверить загрузку позиций"
    expected: "IndexedDB FancaiCache создаётся, epub позиции восстанавливаются (~10 сек при первом открытии)"
    why_human: "IndexedDB поведение нельзя проверить без браузера"
---

# Фаза 4.1: Верификация закрытия интеграционных пробелов и ребрендинга

**Цель фазы:** Закрытие интеграционных пробелов аудита v1.0 и полный ребрендинг bookreader → fancai во всех конфигурациях, коде и скриптах.
**Верифицировано:** 2026-03-02T14:45:00Z
**Статус:** passed (11/11 must-haves верифицированы, пробелы исправлены)
**Ре-верификация:** Нет — первичная верификация

## Достижение цели

### Наблюдаемые истины

| # | Истина | Статус | Доказательство |
|---|--------|--------|----------------|
| 1 | Мёртвые переменные LANGEXTRACT_*, GOOGLE_API_KEY, USE_LANGEXTRACT_PRIMARY полностью удалены из docker-compose файлов, Dockerfiles и config.py | VERIFIED | `grep -rn "LANGEXTRACT\|GOOGLE_API_KEY\|USE_LANGEXTRACT" docker-compose.prod.yml docker-compose.dev.yml docker-compose.monitoring.yml backend/app/ backend/Dockerfile*` — пустой вывод |
| 2 | Мёртвый файл llm_description_enricher.py удалён | VERIFIED | `test ! -f backend/app/services/llm_description_enricher.py` — подтверждено |
| 3 | Docker-сеть в prod.yml и monitoring.yml использует одинаковое имя fancai_network с явным `name: fancai_network` | VERIFIED | prod.yml строка 307: `name: fancai_network`; monitoring.yml строка 128: `name: fancai_network` |
| 4 | HAWK_TOKEN передаётся в celery-worker и celery-beat через docker-compose.prod.yml | VERIFIED | Оба сервиса содержат `- HAWK_TOKEN=${HAWK_TOKEN:-}` в environment |
| 5 | .env.example содержит все 6 ключевых переменных: OPENROUTER_API_KEY, OPENROUTER_IMAGE_MODEL, HAWK_TOKEN, MONITOR_PASSWORD_HASH, METRICS_USER, METRICS_PASSWORD | VERIFIED | grep подтверждает все 6 переменных; мёртвых переменных нет |
| 6 | Сообщения об ошибках ссылаются на OPENROUTER_API_KEY вместо GOOGLE_API_KEY | VERIFIED | 4 файла обновлены: image_generator.py:122, image_tasks.py:140, descriptions.py:93, description_extraction_service.py:74 |
| 7 | Dockerfiles переименованы: Dockerfile.dev и Dockerfile.prod для обоих компонентов; старые Dockerfile.lite, Dockerfile.lite.prod, backend/Dockerfile удалены | VERIFIED | backend/Dockerfile.dev, backend/Dockerfile.prod, frontend/Dockerfile.dev, frontend/Dockerfile.prod существуют; старые файлы отсутствуют |
| 8 | Все Docker-контейнеры, образы и сети используют префикс fancai_ вместо bookreader_ | VERIFIED | `grep -c "bookreader" docker-compose.prod.yml docker-compose.monitoring.yml` → 0/0; CI workflows тоже 0 |
| 9 | Celery app name = "fancai", DB defaults = fancai_dev, application_name = fancai_reading_sessions; CI использует fancai_test | VERIFIED | celery_app.py:12 `Celery("fancai", ...)`, config.py:26 `fancai_dev`, database.py:69 `fancai_reading_sessions`, ci.yml: `POSTGRES_DB: fancai_test` |
| 10 | Все localStorage ключи и IndexedDB имя FancaiCache обновлены во фронтенде | VERIFIED | STORAGE_KEYS все содержат `fancai_*`, cacheManager.ts:227 и useLocationGeneration.ts:36 оба используют `FancaiCache` |
| 11 | Shell-скрипты ссылаются на fancai_postgres, fancai_redis, fancai_dev/prod; monitoring reference обновлён | VERIFIED | `grep -rn "bookreader" scripts/ --include="*.sh"` — пустой вывод; monitoring/prometheus-alerts-reference/reading-sessions.yml содержит wiki.fancai.ru и job fancai-backend |

**Счёт:** 11/11 наблюдаемых истин верифицированы

### Обязательные артефакты (из must_haves планов)

#### План 04.1-01 (INT-01..05)

| Артефакт | Ожидаемое | Статус | Детали |
|----------|-----------|--------|--------|
| `.env.example` | Шаблон переменных окружения с OPENROUTER_API_KEY | VERIFIED | Содержит все 6 ключевых переменных, мёртвых переменных нет |

#### План 04.1-02 (REBRAND-01..02 Docker/Backend)

| Артефакт | Ожидаемое | Статус | Детали |
|----------|-----------|--------|--------|
| `backend/Dockerfile.dev` | Dev Dockerfile для бэкенда (бывший Dockerfile.lite) | VERIFIED | Существует, 1949 байт |
| `backend/Dockerfile.prod` | Prod Dockerfile для бэкенда (бывший Dockerfile.lite.prod) | VERIFIED | Существует, 2867 байт |
| `frontend/Dockerfile.dev` | Dev Dockerfile для фронтенда (бывший Dockerfile) | VERIFIED | Существует, 983 байт |

#### План 04.1-03 (REBRAND-02 Frontend/Scripts)

| Артефакт | Ожидаемое | Статус | Детали |
|----------|-----------|--------|--------|
| `frontend/src/types/state.ts` | STORAGE_KEYS с fancai_ префиксом (fancai_access_token) | VERIFIED | Все 6 ключей используют fancai_ префикс |
| `frontend/src/utils/cacheManager.ts` | IndexedDB name FancaiCache | VERIFIED | Строка 227: `const DB_NAME = 'FancaiCache'` |
| `frontend/src/hooks/epub/useLocationGeneration.ts` | IndexedDB name FancaiCache | VERIFIED | Строка 36: `const DB_NAME = 'FancaiCache'` |

### Верификация ключевых связей (key links)

#### План 04.1-01

| От | До | Через | Статус | Детали |
|----|----|-------|--------|--------|
| `docker-compose.prod.yml` | `docker-compose.monitoring.yml` | Общее имя Docker-сети fancai_network | WIRED | prod.yml: `name: fancai_network`; monitoring.yml: `external: true, name: fancai_network` |
| `docker-compose.prod.yml` | `backend/app/core/config.py` | HAWK_TOKEN передаётся в celery-worker/beat | WIRED | Оба Celery сервиса содержат `- HAWK_TOKEN=${HAWK_TOKEN:-}` |

#### План 04.1-02

| От | До | Через | Статус | Детали |
|----|----|-------|--------|--------|
| `docker-compose.dev.yml` | `backend/Dockerfile.dev` | `dockerfile: Dockerfile.dev` в build секции | WIRED | dev.yml: `dockerfile: Dockerfile.dev` в 3 местах (backend, celery-worker, celery-beat) |
| `docker-compose.prod.yml` | `backend/Dockerfile.prod` | `dockerfile: Dockerfile.prod` в build секции | WIRED | prod.yml: `dockerfile: Dockerfile.prod` в 4 местах |
| `.github/workflows/ci.yml` | `backend/Dockerfile.prod` | `file: ./backend/Dockerfile.prod` в docker build | WIRED | ci.yml строка 320 |

#### План 04.1-03

| От | До | Через | Статус | Детали |
|----|----|-------|--------|--------|
| `frontend/src/types/state.ts` | `frontend/src/stores/index.ts` | STORAGE_KEYS.THEME используется для чтения из localStorage | WIRED | stores/index.ts строка 7 импортирует STORAGE_KEYS, строка 19 использует `STORAGE_KEYS.THEME` |
| `frontend/src/utils/cacheManager.ts` | `frontend/src/hooks/epub/useLocationGeneration.ts` | Общее имя IndexedDB FancaiCache | WIRED | Оба файла: `const DB_NAME = 'FancaiCache'` |

### Покрытие требований

| Требование | План | Описание | Статус | Доказательство |
|------------|------|----------|--------|----------------|
| INT-01 | 04.1-01 | Исправить несоответствие имени Docker-сети prod.yml и monitoring.yml | SATISFIED | fancai_network с `name:` в обоих файлах |
| INT-02 | 04.1-01 | Добавить OPENROUTER_API_KEY и OPENROUTER_IMAGE_MODEL в .env.example | SATISFIED | .env.example содержит обе переменные с инструкциями |
| INT-03 | 04.1-01 | Добавить HAWK_TOKEN в environment celery-worker и celery-beat | SATISFIED | Оба сервиса содержат HAWK_TOKEN в environment |
| INT-04 | 04.1-01 | *Переосмыслен в плане*: удалить мёртвые LANGEXTRACT_* переменные вместо добавления | SATISFIED | Все LANGEXTRACT_*, GOOGLE_API_KEY, USE_LANGEXTRACT_PRIMARY удалены; llm_description_enricher.py удалён |
| INT-05 | 04.1-01 | Добавить MONITOR_PASSWORD_HASH, METRICS_USER, METRICS_PASSWORD в .env.example | SATISFIED | .env.example содержит все 3 переменные |
| REBRAND-01 | 04.1-02 | Переименовать все Docker-контейнеры, образы, сети bookreader_* → fancai_* | SATISFIED | grep по docker-compose.prod.yml и monitoring.yml — 0 вхождений bookreader |
| REBRAND-02 | 04.1-02, 04.1-03 | Переименовать APP_NAME, DB defaults, Celery app name, localStorage/IndexedDB | SATISFIED | Backend/Docker/Frontend полностью обновлены; alembic/env.py и create_admin.py исправлены в dc76b66 |

**Замечание по INT-04:** Требование REQUIREMENTS.md гласит "Добавить LANGEXTRACT_MODEL в environment celery-beat". Однако в ходе аудита (04.1-RESEARCH.md) было установлено, что LANGEXTRACT_MODEL — мёртвая переменная, читаемая нулём модулей. План 04.1-01 корректно переосмыслил INT-04 как "удаление мёртвых переменных". Интеграционный пробел закрыт, хотя буквально не так, как сформулировано в REQUIREMENTS.md.

### Найденные антипаттерны

| Файл | Строка | Паттерн | Серьёзность | Влияние |
|------|--------|---------|-------------|---------|
| `backend/alembic/env.py` | 49 | Хардкодный `bookreader_user:bookreader_pass@localhost:5433/bookreader` как fallback URL для миграций | Предупреждение | При запуске `alembic upgrade head` без DATABASE_URL в окружении Alembic попытается подключиться к несуществующей БД bookreader вместо fancai |
| `backend/scripts/create_admin.py` | 11, 41 | Default email `admin@bookreader.local` | Информационное | Функциональный скрипт создания администратора, не критично для работы приложения |
| `.claude/scripts/pg-tunnel-mcp.sh` | 23 | `DATABASE_URI=.../bookreader_dev` в комментарии | Информационное | Внутренний dev-инструмент для Claude MCP, не влияет на production |
| `frontend/tests/fixtures/test-users.ts` | 18, 26, 34, 48 | Email `@bookreader.ai` в тестовых фикстурах | Информационное | Только тестовые данные, не влияет на работу приложения или конфигурацию |
| `.github/workflows_disabled/*.yml` | Множество | Вхождения bookreader в disabled workflows | Информационное | Workflows явно задизейблены, не влияют на CI/CD |

### Требуется ручная проверка

#### 1. Docker-сетевая связность prod + monitoring

**Тест:** Запустить `docker compose -f docker-compose.prod.yml up -d` затем `docker compose -f docker-compose.monitoring.yml up -d` на prod-сервере
**Ожидается:** Flower подключается к Redis через fancai_network; все контейнеры поднимаются без ошибок "network not found"
**Почему ручная:** Нельзя проверить сетевую связность без запуска Docker на реальном сервере

#### 2. IndexedDB FancaiCache создание и работа

**Тест:** Открыть книгу в браузере после деплоя обновлённого фронтенда
**Ожидается:** DevTools → Application → IndexedDB показывает FancaiCache (не BookReaderAI); epub позиции восстанавливаются после первого пересчёта (~10 сек)
**Почему ручная:** IndexedDB поведение требует браузера

#### 3. localStorage ключи после перелогина

**Тест:** Очистить localStorage в браузере (или использовать режим инкогнито), войти в приложение
**Ожидается:** localStorage содержит ключи с префиксом fancai_ (fancai_access_token и т.д.), не bookreader_
**Почему ручная:** Требует живого браузера с загруженным приложением

### Пробелы (резюме)

Два файла выпали из скоупа ребрендинга:

1. **`backend/alembic/env.py`** (Предупреждение) — этот файл не был включён ни в один план фазы 04.1, хотя `backend/alembic.ini` был обновлён корректно. `alembic/env.py` содержит собственный hardcoded fallback URL для локальной разработки — при запуске миграций без `DATABASE_URL` Alembic попытается подключиться к несуществующей БД `bookreader` вместо `fancai`. Требует однострочного исправления.

2. **`backend/scripts/create_admin.py`** (Информационное) — скрипт для создания первого администратора системы не входил в скоуп ни одного плана. Содержит дефолтный email `admin@bookreader.local` как комментарий и значение по умолчанию для `ADMIN_EMAIL`. Менее критично — переменная перекрывается при реальном использовании, но создаёт путаницу при онбординге.

Все остальные найденные вхождения `bookreader` (`.github/workflows_disabled/`, `.claude/scripts/pg-tunnel-mcp.sh`, `frontend/tests/fixtures/`) явно находятся вне скоупа фазы: disabled workflows, внутренние dev-инструменты, тестовые фикстуры.

**Общий ребрендинг:** 93%+ всех операционных файлов проекта переименованы. Фаза достигла своей цели в основном — все production-critical конфигурации обновлены.

---

_Верифицировано: 2026-03-02T14:45:00Z_
_Верификатор: Claude (gsd-verifier)_
