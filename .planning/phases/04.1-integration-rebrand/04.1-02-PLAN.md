---
phase: 04.1-integration-rebrand
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - docker-compose.prod.yml
  - docker-compose.monitoring.yml
  - backend/app/core/celery_app.py
  - backend/app/core/config.py
  - backend/app/core/database.py
  - backend/app/core/secrets.py
  - backend/tests/conftest.py
  - backend/alembic.ini
  - frontend/src/types/state.ts
  - frontend/src/stores/index.ts
  - frontend/src/api/readingSessions.ts
  - frontend/src/utils/cacheManager.ts
  - frontend/src/hooks/epub/useLocationGeneration.ts
  - frontend/src/components/Images/ImageModal.tsx
  - frontend/src/components/Images/ImageGallery.tsx
  - frontend/src/utils/bookDataReset.ts
  - .github/workflows/ci.yml
  - scripts/backup.sh
  - scripts/backup-database.sh
  - scripts/deploy.sh
  - scripts/prepare-server.sh
  - scripts/generate-secrets.sh
  - scripts/setup-monitoring.sh
  - scripts/deploy-production.sh
  - scripts/restore.sh
  - scripts/verify-database-config.sh
  - monitoring/prometheus-alerts-reference/reading-sessions.yml
  - .env.example
autonomous: true
requirements: [INT-01, INT-02, INT-03, INT-04, INT-05, REBRAND-01, REBRAND-02]

must_haves:
  truths:
    - "Все Docker-контейнеры, образы и сети используют префикс fancai_ вместо bookreader_"
    - "Celery app name — 'fancai', DB default — fancai_dev, application_name — fancai_reading_sessions"
    - "localStorage ключи используют префикс fancai_ вместо bookreader_"
    - "IndexedDB BookReaderAI переименован в FancaiCache в обоих файлах"
    - "APP_NAME во всех конфигурациях — 'fancai'"
    - "CI workflow использует fancai_test/fancai-backend:test/fancai-frontend:test"
    - "Shell-скрипты ссылаются на fancai_postgres/fancai_redis"
  artifacts:
    - path: "docker-compose.prod.yml"
      provides: "Все контейнеры и образы fancai_* / fancai-*"
      contains: "fancai-backend"
    - path: "docker-compose.monitoring.yml"
      provides: "Все контейнеры fancai_*"
      contains: "fancai_netdata"
    - path: "backend/app/core/celery_app.py"
      provides: "Celery app name 'fancai'"
      contains: "Celery(\"fancai\""
    - path: "frontend/src/types/state.ts"
      provides: "STORAGE_KEYS с fancai_ префиксом"
      contains: "fancai_access_token"
    - path: "frontend/src/utils/cacheManager.ts"
      provides: "IndexedDB FancaiCache"
      contains: "FancaiCache"
    - path: "frontend/src/hooks/epub/useLocationGeneration.ts"
      provides: "IndexedDB FancaiCache (согласованно с cacheManager)"
      contains: "FancaiCache"
    - path: ".github/workflows/ci.yml"
      provides: "CI с fancai_test DB и fancai-backend:test образами"
      contains: "fancai_test"
  key_links:
    - from: "frontend/src/utils/cacheManager.ts"
      to: "frontend/src/hooks/epub/useLocationGeneration.ts"
      via: "Оба используют одинаковое имя IndexedDB FancaiCache"
      pattern: "FancaiCache"
    - from: "backend/app/core/config.py"
      to: "backend/app/core/secrets.py"
      via: "Default DB name fancai_dev должен совпадать с forbidden_values"
      pattern: "fancai_dev"
    - from: "scripts/backup.sh"
      to: "docker-compose.prod.yml"
      via: "Container name fancai_postgres должен совпадать"
      pattern: "fancai_postgres"
---

<objective>
Полный ребрендинг bookreader -> fancai во всех конфигурациях проекта: Docker-контейнеры и образы, backend-конфиги (Celery, DB, secrets), frontend localStorage/IndexedDB ключи, CI workflow, shell-скрипты.

Purpose: Проект переименован в fancai, но ~55 мест в коде и конфигурациях всё ещё содержат "bookreader". Это путает при деплое на новый сервер и создаёт несоответствия между именами контейнеров, образов и ссылок в скриптах.
Output: Нулевое вхождение "bookreader" в рабочих конфигурациях (Docker, backend, frontend, CI, скрипты)
</objective>

<execution_context>
@/Users/sandk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-integration-rebrand/04.1-CONTEXT.md
@.planning/phases/04.1-integration-rebrand/04.1-RESEARCH.md
@.planning/phases/04.1-integration-rebrand/04.1-01-SUMMARY.md

<interfaces>
<!-- Полная карта переименований из RESEARCH.md -->

docker-compose.prod.yml контейнеры/образы:
| Сейчас | Станет |
|--------|--------|
| bookreader-frontend:lite | fancai-frontend:lite |
| bookreader_frontend | fancai_frontend |
| bookreader-backend:lite | fancai-backend:lite |
| bookreader_backend | fancai_backend |
| bookreader_celery | fancai_celery |
| bookreader_beat | fancai_beat |
| bookreader_postgres | fancai_postgres |
| bookreader_redis | fancai_redis |
| VITE_APP_NAME "BookReader AI" | "fancai" |

docker-compose.monitoring.yml контейнеры:
| bookreader_netdata | fancai_netdata |
| bookreader_victoriametrics | fancai_victoriametrics |
| bookreader_uptime_kuma | fancai_uptime_kuma |
| bookreader_dozzle | fancai_dozzle |
| bookreader_flower | fancai_flower |

backend:
- celery_app.py: Celery("bookreader") -> Celery("fancai")
- config.py: bookreader_dev -> fancai_dev (default DB URL)
- database.py: application_name bookreader_reading_sessions -> fancai_reading_sessions
- secrets.py: forbidden_values bookreader_dev -> fancai_dev
- conftest.py: комментарий bookreader_dev -> fancai_dev, bookreader_test -> fancai_test
- alembic.ini: bookreader_user/bookreader -> fancai_user/fancai

frontend:
- state.ts: STORAGE_KEYS bookreader_* -> fancai_* (6 ключей)
- stores/index.ts: 'bookreader_theme' -> STORAGE_KEYS.THEME или 'fancai_theme'
- readingSessions.ts: bookreader_pending_sessions -> fancai_pending_sessions
- cacheManager.ts: 'BookReaderAI' -> 'FancaiCache', bookreader_pending_sessions -> fancai_pending_sessions
- useLocationGeneration.ts: 'BookReaderAI' -> 'FancaiCache'
- ImageModal.tsx: bookreader-image- -> fancai-image-
- ImageGallery.tsx: bookreader- -> fancai-
- bookDataReset.ts: комментарии BookReaderAI -> FancaiCache

CI:
- ci.yml: bookreader_test -> fancai_test (3 места), bookreader-backend:test -> fancai-backend:test, bookreader-frontend:test -> fancai-frontend:test

НЕ переименовывать:
- BookReader.tsx, BookReaderPage.tsx — имена React-компонентов, не видны пользователю
- IndexedDB FancaiDatabase — уже переименована
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ребрендинг backend + frontend + .env.example</name>
  <files>backend/app/core/celery_app.py, backend/app/core/config.py, backend/app/core/database.py, backend/app/core/secrets.py, backend/tests/conftest.py, backend/alembic.ini, frontend/src/types/state.ts, frontend/src/stores/index.ts, frontend/src/api/readingSessions.ts, frontend/src/utils/cacheManager.ts, frontend/src/hooks/epub/useLocationGeneration.ts, frontend/src/components/Images/ImageModal.tsx, frontend/src/components/Images/ImageGallery.tsx, frontend/src/utils/bookDataReset.ts, .env.example</files>
  <action>
Переименовать все вхождения bookreader -> fancai в коде и конфигурациях. Порядок: backend, frontend, .env.example.

**Backend (6 файлов):**

1. `celery_app.py` строка 12: `Celery("bookreader"` -> `Celery("fancai"`
2. `config.py` строка ~26: default DATABASE_URL `bookreader_dev` -> `fancai_dev`
3. `config.py`: если есть validation проверяющий `"bookreader_dev"` в forbidden — обновить на `"fancai_dev"`
4. `database.py` строка ~69: `application_name: "bookreader_reading_sessions"` -> `"fancai_reading_sessions"`
5. `secrets.py` строка ~53: `"bookreader_dev"` в forbidden_values -> `"fancai_dev"`
6. `conftest.py` строка ~29: обновить комментарий `bookreader_dev -> bookreader_test` на `fancai_dev -> fancai_test`
7. `alembic.ini` строка ~52: `bookreader_user:bookreader_pass@localhost:5433/bookreader` -> `fancai_user:fancai_pass@localhost:5433/fancai`

**Frontend (7 файлов):**

8. `state.ts` строки 134-141: все 6 ключей STORAGE_KEYS: `bookreader_*` -> `fancai_*`
9. `stores/index.ts` строка 18: `'bookreader_theme'` -> использовать `STORAGE_KEYS.THEME` (добавить импорт из `@/types/state`) ИЛИ просто `'fancai_theme'`
10. `readingSessions.ts` строка ~270: `'bookreader_pending_sessions'` -> `'fancai_pending_sessions'`
11. `cacheManager.ts` строка ~227: `DB_NAME = 'BookReaderAI'` -> `'FancaiCache'`; строка ~169: `'bookreader_pending_sessions'` -> `'fancai_pending_sessions'`
12. `useLocationGeneration.ts` строка ~36: `DB_NAME = 'BookReaderAI'` -> `'FancaiCache'` (ДОЛЖНО совпадать с cacheManager.ts!)
13. `ImageModal.tsx` строка ~94: `bookreader-image-` -> `fancai-image-`
14. `ImageGallery.tsx` строка ~87: `bookreader-` -> `fancai-`
15. `bookDataReset.ts` строки ~9, ~107: обновить комментарии `BookReaderAI` -> `FancaiCache`

**Корневой .env.example:**

16. Проверить на оставшиеся `bookreader` refs и заменить на `fancai`. Если Plan 04.1-01 создал .env.example с bookreader refs в DB defaults — обновить.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon && echo "=== Backend ===" && grep -rn "bookreader" backend/app/core/ backend/tests/conftest.py backend/alembic.ini --include="*.py" --include="*.ini" | grep -v __pycache__ | grep -v ".pyc" && echo "=== Frontend ===" && grep -rn "bookreader\|BookReader" frontend/src/types/state.ts frontend/src/stores/index.ts frontend/src/api/readingSessions.ts frontend/src/utils/cacheManager.ts frontend/src/hooks/epub/useLocationGeneration.ts frontend/src/components/Images/ImageModal.tsx frontend/src/components/Images/ImageGallery.tsx frontend/src/utils/bookDataReset.ts 2>/dev/null && echo "=== .env.example ===" && grep -in "bookreader" .env.example 2>/dev/null && echo "--- Ожидается: пустой вывод выше ---" && cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Ноль вхождений "bookreader" (case-insensitive) в backend/app/core/, conftest.py, alembic.ini, state.ts, stores/index.ts, readingSessions.ts, cacheManager.ts, useLocationGeneration.ts, ImageModal.tsx, ImageGallery.tsx, bookDataReset.ts, .env.example. Frontend build проходит успешно.</done>
</task>

<task type="auto">
  <name>Task 2: Ребрендинг Docker-конфигов, CI workflow, shell-скриптов</name>
  <files>docker-compose.prod.yml, docker-compose.monitoring.yml, .github/workflows/ci.yml, scripts/backup.sh, scripts/backup-database.sh, scripts/deploy.sh, scripts/prepare-server.sh, scripts/generate-secrets.sh, scripts/setup-monitoring.sh, scripts/deploy-production.sh, scripts/restore.sh, scripts/verify-database-config.sh, monitoring/prometheus-alerts-reference/reading-sessions.yml</files>
  <action>
Переименовать все контейнеры, образы и ссылки bookreader -> fancai в Docker, CI и скриптах.

**docker-compose.prod.yml (после плана 04.1-01 сети уже fancai_network):**

1. Все `image:` — `bookreader-frontend:lite` -> `fancai-frontend:lite`, `bookreader-backend:lite` -> `fancai-backend:lite`
2. Все `container_name:` — `bookreader_frontend` -> `fancai_frontend`, `bookreader_backend` -> `fancai_backend`, `bookreader_celery` -> `fancai_celery`, `bookreader_beat` -> `fancai_beat`, `bookreader_postgres` -> `fancai_postgres`, `bookreader_redis` -> `fancai_redis`
3. `VITE_APP_NAME` build arg: `"BookReader AI"` -> `"fancai"`
4. Заголовок/комментарий файла: "BookReader AI - Lite Version" -> "fancai"
5. Любые оставшиеся `bookreader` в комментариях

**docker-compose.monitoring.yml:**

6. Все `container_name:` — `bookreader_netdata` -> `fancai_netdata`, `bookreader_victoriametrics` -> `fancai_victoriametrics`, `bookreader_uptime_kuma` -> `fancai_uptime_kuma`, `bookreader_dozzle` -> `fancai_dozzle`, `bookreader_flower` -> `fancai_flower`
7. Заголовок файла: обновить если содержит bookreader

**CI (.github/workflows/ci.yml):**

8. `bookreader_test` -> `fancai_test` (DB name, ~3 места: строки 53, 127, 139)
9. DATABASE_URL asyncpg: `bookreader_test` -> `fancai_test` (строка 138)
10. Docker tags: `bookreader-backend:test` -> `fancai-backend:test` (строка 326), `bookreader-frontend:test` -> `fancai-frontend:test` (строка 335)
11. (Опционально) Удалить NLP spaCy download step (строка ~97) если есть — NLP удалён в Phase 2

**Shell-скрипты (scripts/):**

12. `backup.sh`: POSTGRES_CONTAINER, REDIS_CONTAINER, grep filter, DB_NAME lookup, fallback — все bookreader -> fancai
13. `backup-database.sh`: DB_NAME default, DOCKER_CONTAINER default — bookreader -> fancai
14. `deploy.sh`: заголовок "BookReader AI" -> "fancai"
15. `prepare-server.sh`: `/opt/bookreader` -> `/opt/fancai`
16. `generate-secrets.sh`: DB_NAME, DB_USER, email — bookreader -> fancai
17. `setup-monitoring.sh`: dashboard tag bookreader -> fancai
18. `deploy-production.sh`: postgres user/db bookreader -> fancai
19. `restore.sh`: container refs bookreader -> fancai
20. `verify-database-config.sh`: DB refs bookreader -> fancai

**Мониторинг:**

21. `monitoring/prometheus-alerts-reference/reading-sessions.yml`: runbook_url `wiki.bookreader.ai` -> `wiki.fancai.ru`, job label `bookreader-backend` -> `fancai-backend`

**Финальная верификация:** После всех изменений выполнить полный grep по проекту:
```bash
grep -rn "bookreader" docker-compose.prod.yml docker-compose.monitoring.yml docker-compose.dev.yml .github/workflows/ scripts/ monitoring/ .env.example
```
Ожидается: пустой вывод (или только в git-ignored файлах).
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon && echo "=== Docker ===" && grep -in "bookreader" docker-compose.prod.yml docker-compose.monitoring.yml 2>/dev/null && echo "=== CI ===" && grep -in "bookreader" .github/workflows/ci.yml 2>/dev/null && echo "=== Scripts ===" && grep -rln "bookreader" scripts/ 2>/dev/null && echo "=== Monitoring ===" && grep -in "bookreader" monitoring/prometheus-alerts-reference/reading-sessions.yml 2>/dev/null && echo "--- Ожидается: пустой вывод выше ---" && docker compose -f docker-compose.prod.yml config --quiet 2>&1 | tail -3 && echo "PASS: docker-compose config валиден"</automated>
  </verify>
  <done>Ноль вхождений "bookreader" (case-insensitive) в docker-compose.prod.yml, docker-compose.monitoring.yml, .github/workflows/ci.yml, scripts/*, monitoring/*. docker-compose config проходит валидацию без ошибок. Все контейнеры — fancai_*, все образы — fancai-*.</done>
</task>

</tasks>

<verification>
```bash
# Полная верификация ребрендинга — ВЕСЬ проект

# 1. Docker конфиги
echo "=== Docker configs (должно быть пусто) ==="
grep -rn "bookreader" docker-compose.prod.yml docker-compose.monitoring.yml docker-compose.dev.yml

# 2. Backend код
echo "=== Backend (должно быть пусто) ==="
grep -rn "bookreader" backend/app/ backend/tests/ backend/alembic.ini --include="*.py" --include="*.ini" | grep -v __pycache__

# 3. Frontend код
echo "=== Frontend (должно быть пусто, кроме BookReader компонентов) ==="
grep -rn "bookreader" frontend/src/ --include="*.ts" --include="*.tsx" | grep -v "BookReader" | grep -v "BookReaderPage"

# 4. CI/CD
echo "=== CI (должно быть пусто) ==="
grep -rn "bookreader" .github/workflows/ --include="*.yml"

# 5. Скрипты
echo "=== Scripts (должно быть пусто) ==="
grep -rn "bookreader" scripts/ --include="*.sh"

# 6. .env.example
echo "=== .env.example (должно быть пусто) ==="
grep -in "bookreader" .env.example

# 7. Frontend build
echo "=== Frontend build ==="
cd frontend && npm run build 2>&1 | tail -3

# 8. Backend тесты (если есть)
echo "=== Backend tests ==="
cd ../backend && python -c "from app.core.celery_app import celery_app; assert celery_app.main == 'fancai', f'Expected fancai, got {celery_app.main}'; print('Celery app name: OK')" 2>&1 || echo "SKIP: нужен запущенный backend"
```
</verification>

<success_criteria>
1. `grep -rn "bookreader" docker-compose.prod.yml docker-compose.monitoring.yml` — пустой вывод
2. `grep -rn "bookreader" backend/app/ --include="*.py" | grep -v __pycache__` — пустой вывод
3. `grep -rn "bookreader" frontend/src/ --include="*.ts" --include="*.tsx" | grep -v BookReader` — пустой вывод (BookReader.tsx, BookReaderPage.tsx — допустимые исключения, это имена React-компонентов)
4. `grep -rn "bookreader" .github/workflows/ scripts/ monitoring/ .env.example` — пустой вывод
5. Frontend build (`npm run build`) проходит без ошибок
6. `docker compose -f docker-compose.prod.yml config` валиден
</success_criteria>

<output>
После завершения создать `.planning/phases/04.1-integration-rebrand/04.1-02-SUMMARY.md`
</output>
