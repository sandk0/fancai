---
phase: 04.1-integration-rebrand
plan: 02
type: execute
wave: 2
depends_on:
  - 04.1-01
files_modified:
  - docker-compose.prod.yml
  - docker-compose.monitoring.yml
  - backend/app/core/celery_app.py
  - backend/app/core/config.py
  - backend/app/core/database.py
  - backend/app/core/secrets.py
  - backend/tests/conftest.py
  - backend/alembic.ini
  - frontend/src/types/state.ts
  - frontend/src/stores/index.ts
  - frontend/src/api/readingSessions.ts
  - frontend/src/utils/cacheManager.ts
  - frontend/src/hooks/epub/useLocationGeneration.ts
  - frontend/src/components/Images/ImageModal.tsx
  - frontend/src/components/Images/ImageGallery.tsx
  - frontend/src/utils/bookDataReset.ts
  - .github/workflows/ci.yml
  - scripts/backup.sh
  - scripts/backup-database.sh
  - scripts/deploy.sh
  - scripts/prepare-server.sh
  - scripts/generate-secrets.sh
  - scripts/setup-monitoring.sh
  - scripts/deploy-production.sh
  - scripts/restore.sh
  - scripts/verify-database-config.sh
  - monitoring/prometheus-alerts-reference/reading-sessions.yml
autonomous: true
requirements:
  - REBRAND-01
  - REBRAND-02

must_haves:
  truths:
    - "Ни один docker-compose файл (prod, monitoring) не содержит строку bookreader — все контейнеры, образы, сети переименованы в fancai"
    - "Celery app name = fancai — задачи отображаются с префиксом fancai в Flower"
    - "localStorage-ключи фронтенда используют префикс fancai_ — авторизация, тема, прогресс чтения"
    - "IndexedDB кеш epub locations называется FancaiCache (не BookReaderAI) — согласованно в обоих файлах"
    - "CI workflow использует fancai в именах Docker-образов и БД тестов"
    - "Backend config.py и alembic.ini используют fancai в дефолтах БД"
    - "Frontend собирается без ошибок (npm run build)"
  artifacts:
    - path: "docker-compose.prod.yml"
      provides: "Docker Compose prod с fancai_ контейнерами и образами"
    - path: "frontend/src/types/state.ts"
      provides: "STORAGE_KEYS с fancai_ префиксом"
    - path: "backend/app/core/celery_app.py"
      provides: "Celery app с именем fancai"
    - path: ".github/workflows/ci.yml"
      provides: "CI с fancai Docker-тегами и БД"
  key_links:
    - from: "frontend/src/types/state.ts STORAGE_KEYS"
      to: "frontend/src/stores/index.ts"
      via: "localStorage.getItem использует STORAGE_KEYS.THEME (или обновлённый хардкод fancai_theme)"
      pattern: "fancai_theme"
    - from: "frontend/src/utils/cacheManager.ts DB_NAME"
      to: "frontend/src/hooks/epub/useLocationGeneration.ts DB_NAME"
      via: "Оба файла используют одинаковое имя IndexedDB FancaiCache"
      pattern: "FancaiCache"
    - from: "backend/app/core/config.py DATABASE_URL default"
      to: "backend/app/core/secrets.py forbidden_values"
      via: "Дефолтное имя БД fancai_dev совпадает в обоих файлах"
      pattern: "fancai_dev"
---

<objective>
Ребрендинг bookreader -> fancai во всей инфраструктуре

Переименовать все вхождения bookreader на fancai в Docker-конфигурациях (контейнеры, образы), backend (Celery, config, database, alembic), frontend (localStorage, IndexedDB, download filenames), CI workflow и shell-скриптах. Компоненты React (BookReader.tsx, BookReaderPage.tsx) НЕ переименовываются — это внутренние имена, не видимые пользователю.

Purpose: Проект называется fancai, но Docker-образы, контейнеры, ключи localStorage и множество конфигов содержат устаревшее имя bookreader. Это затрудняет навигацию и создаёт путаницу при развёртывании. Сервер новый и пустой — идеальный момент для чистого ребрендинга без рисков.
Output: Нулевые упоминания bookreader в рабочих конфигурациях (Docker, backend, frontend, CI, скрипты).
</objective>

<execution_context>
@/Users/sandk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-integration-rebrand/04.1-CONTEXT.md
@.planning/phases/04.1-integration-rebrand/04.1-RESEARCH.md
@.planning/phases/04.1-integration-rebrand/04.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ребрендинг Docker-compose и backend конфигов</name>
  <files>docker-compose.prod.yml, docker-compose.monitoring.yml, backend/app/core/celery_app.py, backend/app/core/config.py, backend/app/core/database.py, backend/app/core/secrets.py, backend/tests/conftest.py, backend/alembic.ini</files>
  <action>
**ВАЖНО:** Plan 04.1-01 уже переименовал сети в fancai_network. Этот таск переименовывает контейнеры, образы и backend-конфиги.

**docker-compose.prod.yml — контейнеры и образы:**

| Сервис | Поле | Сейчас | Станет |
|--------|------|--------|--------|
| frontend | image | `bookreader-frontend:lite` | `fancai-frontend:lite` |
| frontend | container_name | `bookreader_frontend` | `fancai_frontend` |
| frontend | build args VITE_APP_NAME | `BookReader AI` | `fancai` |
| backend | image | `bookreader-backend:lite` | `fancai-backend:lite` |
| backend | container_name | `bookreader_backend` | `fancai_backend` |
| celery-worker | image | `bookreader-backend:lite` | `fancai-backend:lite` |
| celery-worker | container_name | `bookreader_celery` | `fancai_celery` |
| celery-beat | image | `bookreader-backend:lite` | `fancai-backend:lite` |
| celery-beat | container_name | `bookreader_beat` | `fancai_beat` |
| postgres | container_name | `bookreader_postgres` | `fancai_postgres` |
| redis | container_name | `bookreader_redis` | `fancai_redis` |
| Заголовок файла | комментарий | "BookReader AI - Lite Version" | "fancai" |

**docker-compose.monitoring.yml — контейнеры:**

| Сервис | container_name сейчас | container_name станет |
|--------|----------------------|---------------------|
| netdata | `bookreader_netdata` | `fancai_netdata` |
| victoriametrics | `bookreader_victoriametrics` | `fancai_victoriametrics` |
| uptime-kuma | `bookreader_uptime_kuma` | `fancai_uptime_kuma` |
| dozzle | `bookreader_dozzle` | `fancai_dozzle` |
| flower | `bookreader_flower` | `fancai_flower` |

**НЕ ТРОГАТЬ:** docker-compose.dev.yml — уже ребрендирован (контейнеры fancai_*, сеть fancai_network).

**backend/app/core/celery_app.py строка 12:**
`Celery("bookreader", ...)` → `Celery("fancai", ...)`

**backend/app/core/config.py строка 26 (дефолт DATABASE_URL):**
`bookreader_dev` → `fancai_dev` в дефолтном значении DATABASE_URL
Также обновить комментарий если есть.

**backend/app/core/database.py строка 69:**
`"application_name": "bookreader_reading_sessions"` → `"application_name": "fancai_reading_sessions"`

**backend/app/core/secrets.py строка 53:**
`"forbidden_values": ["postgres123", "bookreader_dev"]` → `"forbidden_values": ["postgres123", "fancai_dev"]`

**backend/tests/conftest.py строка 29:**
Обновить комментарий: `"bookreader_dev -> bookreader_test"` → `"fancai_dev -> fancai_test"`

**backend/alembic.ini строка 52:**
`sqlalchemy.url = postgresql://bookreader_user:bookreader_pass@localhost:5433/bookreader` → `sqlalchemy.url = postgresql://fancai_user:fancai_pass@localhost:5433/fancai`
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon && grep -r "bookreader" docker-compose.prod.yml docker-compose.monitoring.yml && echo "FAIL: bookreader remains in docker-compose" || echo "PASS: docker-compose clean" && grep "bookreader" backend/app/core/celery_app.py backend/app/core/config.py backend/app/core/database.py backend/app/core/secrets.py backend/alembic.ini && echo "FAIL: bookreader remains in backend" || echo "PASS: backend clean" && python3 -c "import sys; sys.path.insert(0, 'backend'); exec(open('backend/app/core/celery_app.py').read().split('Celery(')[0])" 2>/dev/null; grep 'Celery("fancai"' backend/app/core/celery_app.py && echo "PASS: celery name=fancai" || echo "FAIL"</automated>
  </verify>
  <done>
- docker-compose.prod.yml и monitoring.yml не содержат строку bookreader
- Celery app name = "fancai"
- config.py дефолт БД = fancai_dev
- database.py application_name = fancai_reading_sessions
- secrets.py forbidden_values содержит fancai_dev
- alembic.ini использует fancai в URL
  </done>
</task>

<task type="auto">
  <name>Task 2: Ребрендинг frontend (localStorage, IndexedDB, download filenames) + CI + скрипты</name>
  <files>frontend/src/types/state.ts, frontend/src/stores/index.ts, frontend/src/api/readingSessions.ts, frontend/src/utils/cacheManager.ts, frontend/src/hooks/epub/useLocationGeneration.ts, frontend/src/components/Images/ImageModal.tsx, frontend/src/components/Images/ImageGallery.tsx, frontend/src/utils/bookDataReset.ts, .github/workflows/ci.yml, scripts/backup.sh, scripts/backup-database.sh, scripts/deploy.sh, scripts/prepare-server.sh, scripts/generate-secrets.sh, scripts/setup-monitoring.sh, scripts/deploy-production.sh, scripts/restore.sh, scripts/verify-database-config.sh, monitoring/prometheus-alerts-reference/reading-sessions.yml</files>
  <action>
**Frontend — localStorage ключи:**

`frontend/src/types/state.ts` строки 134-141 — STORAGE_KEYS:
```typescript
export const STORAGE_KEYS = {
  AUTH_TOKEN: 'fancai_access_token',
  REFRESH_TOKEN: 'fancai_refresh_token',
  USER_DATA: 'fancai_user_data',
  READER_SETTINGS: 'fancai_reader_settings',
  THEME: 'fancai_theme',
  READING_PROGRESS_BACKUP: 'fancai_reading_progress_backup',
} as const;
```

`frontend/src/stores/index.ts` строка 18:
`localStorage.getItem('bookreader_theme')` → `localStorage.getItem('fancai_theme')` (или использовать STORAGE_KEYS.THEME с импортом — на усмотрение, но проще обновить строку)

`frontend/src/api/readingSessions.ts` строка 270:
`'bookreader_pending_sessions'` → `'fancai_pending_sessions'`

**Frontend — IndexedDB:**

`frontend/src/utils/cacheManager.ts` строка 227:
`const DB_NAME = 'BookReaderAI'` → `const DB_NAME = 'FancaiCache'`

`frontend/src/hooks/epub/useLocationGeneration.ts` строка 36:
`const DB_NAME = 'BookReaderAI'` → `const DB_NAME = 'FancaiCache'`

`frontend/src/utils/bookDataReset.ts` строки 9, 107:
Обновить комментарии `BookReaderAI` → `FancaiCache`

**ВАЖНО:** Оба файла (cacheManager.ts и useLocationGeneration.ts) ДОЛЖНЫ использовать одинаковое имя `FancaiCache`. Расхождение приведёт к потере epub location cache.

**Frontend — download filenames:**

`frontend/src/components/Images/ImageModal.tsx` строка 94:
`bookreader-image-${Date.now()}.jpg` → `fancai-image-${Date.now()}.jpg`

`frontend/src/components/Images/ImageGallery.tsx` строка 87:
`bookreader-${image.id}-${Date.now()}.jpg` → `fancai-${image.id}-${Date.now()}.jpg`

**НЕ ПЕРЕИМЕНОВЫВАТЬ:** React-компоненты BookReader.tsx, BookReaderPage.tsx — это внутренние имена компонентов, не видимые пользователю (решение из CONTEXT.md).

**GitHub CI (.github/workflows/ci.yml):**
- Строка 53: `bookreader_test` → `fancai_test` (postgres DB name)
- Строки 127, 139: `POSTGRES_DB` env `bookreader_test` → `fancai_test`
- Строка 138: asyncpg URL `bookreader_test` → `fancai_test`
- Строка 326: Docker tag `bookreader-backend:test` → `fancai-backend:test`
- Строка 335: Docker tag `bookreader-frontend:test` → `fancai-frontend:test`
- Строка 97 (NLP step): Удалить step скачивания SpaCy ru_core_news_sm — NLP удалён в Phase 2, step бесполезен. Если step является частью большого block — просто удалить строки скачивания spacy модели.

**Shell-скрипты (полный список — искать и заменять bookreader → fancai):**

| Файл | Что менять |
|------|-----------|
| `scripts/backup.sh` | POSTGRES_CONTAINER, REDIS_CONTAINER, grep filter, DB_NAME lookup, fallback |
| `scripts/backup-database.sh` | DB_NAME default, DOCKER_CONTAINER default, комментарии |
| `scripts/deploy.sh` | Заголовок "BookReader AI" → "fancai" |
| `scripts/prepare-server.sh` | Путь `/opt/bookreader` → `/opt/fancai`, инструкции |
| `scripts/generate-secrets.sh` | DB_NAME, DB_USER, email → admin@fancai.local |
| `scripts/setup-monitoring.sh` | dashboard tag bookreader → fancai |
| `scripts/deploy-production.sh` | postgres user/db refs |
| `scripts/restore.sh` | container refs |
| `scripts/verify-database-config.sh` | DB refs |

Для каждого скрипта: открыть файл, найти все `bookreader` (case-insensitive), заменить на `fancai` (сохраняя регистр: BookReader → Fancai, bookreader → fancai, BOOKREADER → FANCAI).

**monitoring/prometheus-alerts-reference/reading-sessions.yml:**
- `wiki.bookreader.ai` → `wiki.fancai.ru`
- `bookreader-backend` → `fancai-backend`
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon && grep -r "bookreader" frontend/src/types/state.ts frontend/src/stores/index.ts frontend/src/api/readingSessions.ts frontend/src/utils/cacheManager.ts frontend/src/hooks/epub/useLocationGeneration.ts frontend/src/components/Images/ImageModal.tsx frontend/src/components/Images/ImageGallery.tsx && echo "FAIL: bookreader in frontend" || echo "PASS: frontend clean" && grep -r "BookReaderAI" frontend/src/ && echo "FAIL: BookReaderAI remains" || echo "FAIL" || echo "PASS" && grep "bookreader" .github/workflows/ci.yml && echo "FAIL: bookreader in CI" || echo "PASS: CI clean" && grep -rl "bookreader" scripts/ && echo "FAIL: bookreader in scripts" || echo "PASS: scripts clean" && cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>
- Ноль вхождений bookreader в frontend/src/ (кроме компонентов BookReader*.tsx — внутренние имена)
- STORAGE_KEYS использует fancai_ префикс
- IndexedDB DB_NAME = FancaiCache в обоих файлах (cacheManager.ts, useLocationGeneration.ts)
- CI workflow использует fancai в Docker-тегах и имени БД
- Shell-скрипты используют fancai в container refs и DB names
- Frontend собирается без ошибок (npm run build)
  </done>
</task>

</tasks>

<verification>
Финальная верификация после выполнения всех задач:

```bash
# 1. Docker-compose файлы чисты от bookreader:
grep -ri "bookreader" docker-compose.prod.yml docker-compose.monitoring.yml
# Ожидается: пустой вывод

# 2. Backend чист от bookreader:
grep -rn "bookreader" backend/app/core/ backend/alembic.ini --include="*.py" --include="*.ini"
# Ожидается: пустой вывод

# 3. Frontend чист от bookreader (кроме компонентов BookReader*.tsx):
grep -rn "bookreader" frontend/src/ --include="*.ts" --include="*.tsx" | grep -v "BookReader" | grep -v "bookReader"
# Ожидается: пустой вывод

# 4. CI чист:
grep -n "bookreader" .github/workflows/ci.yml
# Ожидается: пустой вывод

# 5. Scripts чисты:
grep -rn "bookreader" scripts/
# Ожидается: пустой вывод

# 6. Frontend build:
cd frontend && npm run build

# 7. IndexedDB согласован:
grep "FancaiCache" frontend/src/utils/cacheManager.ts frontend/src/hooks/epub/useLocationGeneration.ts
# Ожидается: 2 строки
```
</verification>

<success_criteria>
1. Ноль вхождений bookreader в docker-compose.prod.yml и docker-compose.monitoring.yml
2. Celery app name = "fancai", config.py default DB = fancai_dev, alembic.ini URL с fancai
3. STORAGE_KEYS в state.ts использует fancai_ префикс (6 ключей)
4. IndexedDB DB_NAME = FancaiCache в cacheManager.ts и useLocationGeneration.ts
5. CI workflow ci.yml не содержит bookreader
6. Все shell-скрипты в scripts/ не содержат bookreader
7. Frontend собирается без ошибок
</success_criteria>

<output>
После завершения создать `.planning/phases/04.1-integration-rebrand/04.1-02-SUMMARY.md`
</output>
