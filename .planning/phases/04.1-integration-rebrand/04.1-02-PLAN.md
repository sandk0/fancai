---
phase: 04.1-integration-rebrand
plan: 02
type: execute
wave: 2
depends_on:
  - 04.1-01
files_modified:
  - backend/Dockerfile.lite
  - backend/Dockerfile.lite.prod
  - backend/Dockerfile
  - frontend/Dockerfile
  - docker-compose.dev.yml
  - docker-compose.prod.yml
  - docker-compose.monitoring.yml
  - .github/workflows/ci.yml
  - .github/workflows/security.yml
  - backend/app/core/celery_app.py
  - backend/app/core/config.py
  - backend/app/core/database.py
  - backend/app/core/secrets.py
  - backend/tests/conftest.py
  - backend/alembic.ini
autonomous: true
requirements:
  - REBRAND-01
  - REBRAND-02

must_haves:
  truths:
    - "Dockerfiles имеют единообразные имена: Dockerfile.dev и Dockerfile.prod для обоих компонентов"
    - "backend/Dockerfile (старый CI) удалён — CI использует Dockerfile.prod"
    - "Все Docker-контейнеры, образы и сети используют префикс fancai_ вместо bookreader_"
    - "Celery app name — fancai, DB defaults — fancai_dev, application_name — fancai_reading_sessions"
    - "CI workflow использует fancai-backend:test и fancai_test DB name"
    - "docker-compose.dev.yml и docker-compose.prod.yml ссылаются на Dockerfile.dev и Dockerfile.prod соответственно"
  artifacts:
    - path: "backend/Dockerfile.dev"
      provides: "Dev Dockerfile для бэкенда (бывший Dockerfile.lite)"
    - path: "backend/Dockerfile.prod"
      provides: "Prod Dockerfile для бэкенда (бывший Dockerfile.lite.prod)"
    - path: "frontend/Dockerfile.dev"
      provides: "Dev Dockerfile для фронтенда (бывший Dockerfile)"
  key_links:
    - from: "docker-compose.dev.yml"
      to: "backend/Dockerfile.dev"
      via: "dockerfile: Dockerfile.dev в build секции"
      pattern: "Dockerfile\\.dev"
    - from: "docker-compose.prod.yml"
      to: "backend/Dockerfile.prod"
      via: "dockerfile: Dockerfile.prod в build секции"
      pattern: "Dockerfile\\.prod"
    - from: ".github/workflows/ci.yml"
      to: "backend/Dockerfile.prod"
      via: "file: ./backend/Dockerfile.prod в docker build"
      pattern: "Dockerfile\\.prod"
---

<objective>
Переименование Dockerfiles в единообразную схему (dev/prod) и полный ребрендинг bookreader → fancai в Docker-конфигурациях, бэкенд-коде и CI.

Purpose: Dockerfiles имеют запутанные имена (.lite, .lite.prod) оставшиеся от эволюции проекта. Docker-контейнеры/образы/сети и бэкенд-код всё ещё используют старое имя bookreader. Новый сервер пустой — ребрендинг безопасен.
Output: Единообразные Dockerfile.dev/Dockerfile.prod, все Docker/backend конфигурации с префиксом fancai
</objective>

<execution_context>
@/Users/sandk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-integration-rebrand/04.1-RESEARCH.md
@.planning/phases/04.1-integration-rebrand/04.1-01-SUMMARY.md

Файлы для изменения:
@docker-compose.prod.yml
@docker-compose.dev.yml
@docker-compose.monitoring.yml
@backend/Dockerfile.lite
@backend/Dockerfile.lite.prod
@backend/Dockerfile
@frontend/Dockerfile
@.github/workflows/ci.yml
@.github/workflows/security.yml
@backend/app/core/celery_app.py
@backend/app/core/config.py
@backend/app/core/database.py
@backend/app/core/secrets.py
@backend/tests/conftest.py
@backend/alembic.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Переименование Dockerfiles + обновление ссылок</name>
  <files>
    backend/Dockerfile.lite,
    backend/Dockerfile.lite.prod,
    backend/Dockerfile,
    frontend/Dockerfile,
    docker-compose.dev.yml,
    docker-compose.prod.yml,
    .github/workflows/ci.yml,
    .github/workflows/security.yml
  </files>
  <action>
1. **Переименовать Dockerfiles через git mv:**
   ```bash
   git mv backend/Dockerfile.lite backend/Dockerfile.dev
   git mv backend/Dockerfile.lite.prod backend/Dockerfile.prod
   git mv frontend/Dockerfile frontend/Dockerfile.dev
   ```

2. **Удалить старый backend/Dockerfile:**
   ```bash
   git rm backend/Dockerfile
   ```
   Этот файл использовался только в CI — CI переходит на Dockerfile.prod.

3. **docker-compose.dev.yml** — обновить ссылки на Dockerfile (3 места):
   - Строки ~74, ~135, ~179 (build секции backend, celery-worker, celery-beat):
     `dockerfile: Dockerfile.lite` → `dockerfile: Dockerfile.dev`

4. **docker-compose.prod.yml** — обновить ссылки на Dockerfile (3 места):
   - Строки ~80, ~150, ~204 (build секции backend, celery-worker, celery-beat):
     `dockerfile: Dockerfile.lite.prod` → `dockerfile: Dockerfile.prod`

5. **.github/workflows/ci.yml** — обновить docker build references:
   - Строка ~324: `file: ./backend/Dockerfile` → `file: ./backend/Dockerfile.prod`
   - Строка ~334: `file: ./frontend/Dockerfile` → `file: ./frontend/Dockerfile.prod`

6. **.github/workflows/security.yml** — обновить docker build references:
   - Строка ~212 (backend context): `-f Dockerfile .` → `-f Dockerfile.prod .`
   - Строка ~274 (frontend context): `-f Dockerfile .` → `-f Dockerfile.prod .`

7. **Удалить NLP step из ci.yml (если есть):** Строка ~97 со скачиванием SpaCy ru_core_news_sm — NLP удалён в Phase 2, step бесполезен. Удалить весь step.

**Примечание:** frontend/Dockerfile.prod остаётся как есть — имя уже правильное.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon && echo "=== Files exist ===" && test -f backend/Dockerfile.dev && echo "backend/Dockerfile.dev OK" || echo "FAIL"; test -f backend/Dockerfile.prod && echo "backend/Dockerfile.prod OK" || echo "FAIL"; test -f frontend/Dockerfile.dev && echo "frontend/Dockerfile.dev OK" || echo "FAIL"; test ! -f backend/Dockerfile && echo "backend/Dockerfile deleted OK" || echo "FAIL: old Dockerfile exists"; test ! -f backend/Dockerfile.lite && echo "Dockerfile.lite gone OK" || echo "FAIL"; test ! -f backend/Dockerfile.lite.prod && echo "Dockerfile.lite.prod gone OK" || echo "FAIL"; echo "=== References ===" && grep -c "Dockerfile.dev" docker-compose.dev.yml && grep -c "Dockerfile.prod" docker-compose.prod.yml && grep -c "Dockerfile.prod" .github/workflows/ci.yml</automated>
  </verify>
  <done>Dockerfiles переименованы: backend/Dockerfile.dev, backend/Dockerfile.prod, frontend/Dockerfile.dev. Старый backend/Dockerfile удалён. Все ссылки в docker-compose и CI обновлены.</done>
</task>

<task type="auto">
  <name>Task 2: Ребрендинг Docker + Backend — bookreader → fancai</name>
  <files>
    docker-compose.prod.yml,
    docker-compose.monitoring.yml,
    .github/workflows/ci.yml,
    backend/app/core/celery_app.py,
    backend/app/core/config.py,
    backend/app/core/database.py,
    backend/app/core/secrets.py,
    backend/tests/conftest.py,
    backend/alembic.ini
  </files>
  <action>
1. **docker-compose.prod.yml — ребрендинг контейнеров и образов:**
   Заменить ВСЕ вхождения:
   - `bookreader-frontend:lite` → `fancai-frontend:lite` (image)
   - `bookreader_frontend` → `fancai_frontend` (container_name)
   - `bookreader-backend:lite` → `fancai-backend:lite` (image, 3 раза: backend, celery-worker, celery-beat)
   - `bookreader_backend` → `fancai_backend` (container_name)
   - `bookreader_celery` → `fancai_celery` (container_name)
   - `bookreader_beat` → `fancai_beat` (container_name)
   - `bookreader_postgres` → `fancai_postgres` (container_name)
   - `bookreader_redis` → `fancai_redis` (container_name)
   - `VITE_APP_NAME=BookReader AI` → `VITE_APP_NAME=fancai` (build arg)
   - Заголовок файла: "BookReader AI - Lite Version" → "fancai"

   **Примечание:** Сеть fancai_network уже переименована в плане 04.1-01 — НЕ трогать.

2. **docker-compose.monitoring.yml — ребрендинг контейнеров:**
   - `bookreader_netdata` → `fancai_netdata`
   - `bookreader_victoriametrics` → `fancai_victoriametrics`
   - `bookreader_uptime_kuma` → `fancai_uptime_kuma`
   - `bookreader_dozzle` → `fancai_dozzle`
   - `bookreader_flower` → `fancai_flower`

   **Примечание:** Сеть fancai_network уже переименована в плане 04.1-01.

3. **.github/workflows/ci.yml — ребрендинг:**
   - `bookreader_test` → `fancai_test` (postgres DB, 3-4 вхождения)
   - `bookreader-backend:test` → `fancai-backend:test` (docker tag)
   - `bookreader-frontend:test` → `fancai-frontend:test` (docker tag)

4. **backend/app/core/celery_app.py строка ~12:**
   `Celery("bookreader", ...)` → `Celery("fancai", ...)`

5. **backend/app/core/config.py строка ~26:**
   - DEFAULT DATABASE_URL: `bookreader_dev` → `fancai_dev`
   - Полная строка: `postgresql+asyncpg://postgres:postgres123@postgres:5432/bookreader_dev` → `postgresql+asyncpg://postgres:postgres123@postgres:5432/fancai_dev`

6. **backend/app/core/database.py строка ~69:**
   `"application_name": "bookreader_reading_sessions"` → `"application_name": "fancai_reading_sessions"`

7. **backend/app/core/secrets.py строка ~53:**
   `"forbidden_values": ["postgres123", "bookreader_dev"]` → `"forbidden_values": ["postgres123", "fancai_dev"]`

8. **backend/tests/conftest.py строка ~29:**
   Обновить комментарий: `bookreader_dev -> bookreader_test` → `fancai_dev -> fancai_test`
   Если есть код подстановки DB имени — обновить `bookreader` → `fancai` в replace/regex.

9. **backend/alembic.ini строка ~52:**
   `sqlalchemy.url = postgresql://bookreader_user:bookreader_pass@localhost:5433/bookreader` → `sqlalchemy.url = postgresql://fancai_user:fancai_pass@localhost:5433/fancai`

**ВАЖНО:** docker-compose.dev.yml уже ребрендирован (контейнеры fancai_*, сеть fancai_network) — НЕ трогать.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon && echo "=== Docker no bookreader ===" && grep -c "bookreader" docker-compose.prod.yml docker-compose.monitoring.yml 2>/dev/null; echo "=== Backend no bookreader ===" && grep -rn "bookreader" backend/app/core/ backend/alembic.ini backend/tests/conftest.py 2>/dev/null | grep -v __pycache__ | grep -v ".pyc"; echo "=== CI no bookreader ===" && grep -c "bookreader" .github/workflows/ci.yml 2>/dev/null; echo "=== Celery app name ===" && grep "Celery(" backend/app/core/celery_app.py</automated>
  </verify>
  <done>Все Docker-контейнеры, образы используют префикс fancai. Celery app = "fancai". DB defaults = fancai_dev. CI workflow использует fancai_test и fancai-backend:test. Ни одного вхождения bookreader в docker-compose.prod.yml, monitoring.yml, backend/app/core/, alembic.ini, ci.yml.</done>
</task>

</tasks>

<verification>
```bash
# Полная верификация REBRAND-01 + REBRAND-02 (Docker/Backend):

# Dockerfiles переименованы
ls -la backend/Dockerfile.dev backend/Dockerfile.prod frontend/Dockerfile.dev frontend/Dockerfile.prod
test ! -f backend/Dockerfile && echo "old Dockerfile deleted" || echo "FAIL"
test ! -f backend/Dockerfile.lite && echo "Dockerfile.lite gone" || echo "FAIL"

# Нет bookreader в Docker configs
grep -rn "bookreader" docker-compose.prod.yml docker-compose.monitoring.yml
# Ожидается: пустой вывод

# Нет bookreader в backend
grep -rn "bookreader" backend/app/core/ backend/alembic.ini backend/tests/conftest.py | grep -v __pycache__
# Ожидается: пустой вывод

# CI обновлён
grep -n "bookreader" .github/workflows/ci.yml .github/workflows/security.yml
# Ожидается: пустой вывод

# Backend тесты
cd backend && python -m pytest tests/ -x -q --tb=short 2>&1 | tail -5
```
</verification>

<success_criteria>
- backend/Dockerfile.dev, backend/Dockerfile.prod, frontend/Dockerfile.dev существуют
- backend/Dockerfile, backend/Dockerfile.lite, backend/Dockerfile.lite.prod НЕ существуют
- grep -r "bookreader" по docker-compose.prod.yml, monitoring.yml, ci.yml, security.yml, backend/app/core/, alembic.ini — пустой вывод
- docker-compose.dev.yml ссылается на Dockerfile.dev, prod.yml на Dockerfile.prod
- CI ссылается на Dockerfile.prod для обоих компонентов
- Celery("fancai", ...) в celery_app.py
</success_criteria>

<output>
После завершения создать `.planning/phases/04.1-integration-rebrand/04.1-02-SUMMARY.md`
</output>
