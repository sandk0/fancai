---
phase: 04.1-integration-rebrand
plan: 02
subsystem: infra
tags: [docker, dockerfile, rebrand, celery, ci, alembic]

# Dependency graph
requires:
  - phase: 04.1-integration-rebrand
    plan: 01
    provides: "Очистка мертвых переменных, унификация Docker-сети fancai_network"
provides:
  - "Единообразная схема Dockerfile.dev/Dockerfile.prod для обоих компонентов"
  - "Все Docker-контейнеры/образы/сети с префиксом fancai_"
  - "Celery app name = fancai, DB defaults = fancai_dev"
  - "CI/security workflows с fancai image tags и DB names"
affects: [04.1-03-frontend-rebrand, deploy]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Dockerfile naming: Dockerfile.dev (development), Dockerfile.prod (production) для каждого компонента"
    - "Docker naming: fancai_ prefix для container_name, fancai- prefix для image tags"

key-files:
  created:
    - backend/Dockerfile.dev
    - backend/Dockerfile.prod
    - frontend/Dockerfile.dev
  modified:
    - docker-compose.dev.yml
    - docker-compose.prod.yml
    - docker-compose.monitoring.yml
    - .github/workflows/ci.yml
    - .github/workflows/security.yml
    - backend/app/core/celery_app.py
    - backend/app/core/config.py
    - backend/app/core/database.py
    - backend/app/core/secrets.py
    - backend/tests/conftest.py
    - backend/alembic.ini

key-decisions:
  - "Dockerfile.dev/Dockerfile.prod вместо .lite/.lite.prod -- единообразная и понятная схема"
  - "backend/Dockerfile (старый CI) удален -- CI переходит на Dockerfile.prod"
  - "NLP step (SpaCy ru_core_news_sm) удален из ci.yml -- NLP удален в Phase 2"
  - "security.yml image tags тоже переименованы fancai-backend:test/fancai-frontend:test"

patterns-established:
  - "Dockerfile naming: Dockerfile.dev для dev compose, Dockerfile.prod для prod compose и CI"
  - "Container naming: fancai_{service} для container_name, fancai-{component}:{tag} для image"

requirements-completed: [REBRAND-01, REBRAND-02]

# Metrics
duration: 5min
completed: 2026-03-02
---

# Phase 4.1 Plan 02: Dockerfile ренейминг и ребрендинг bookreader -> fancai Summary

**Переименование Dockerfiles в dev/prod схему, полный ребрендинг bookreader -> fancai в Docker-конфигурациях, backend-коде и CI workflows**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-02T14:06:35Z
- **Completed:** 2026-03-02T14:11:12Z
- **Tasks:** 2
- **Files modified:** 20 (3 renamed, 1 deleted, 16 modified)

## Accomplishments
- Dockerfiles переименованы: Dockerfile.lite -> Dockerfile.dev, Dockerfile.lite.prod -> Dockerfile.prod, frontend/Dockerfile -> Dockerfile.dev
- Старый backend/Dockerfile (CI-only) удален -- CI теперь использует Dockerfile.prod
- Все container_name в prod.yml и monitoring.yml: bookreader_* -> fancai_*
- Все image tags: bookreader-backend:lite -> fancai-backend:lite, bookreader-frontend:lite -> fancai-frontend:lite
- Celery("bookreader") -> Celery("fancai"), DB default bookreader_dev -> fancai_dev
- CI: bookreader_test -> fancai_test, image tags fancai-backend:test/fancai-frontend:test
- Удален мертвый NLP step (SpaCy model download) из ci.yml

## Task Commits

Each task was committed atomically:

1. **Task 1: Переименование Dockerfiles + обновление ссылок** - `2d9e782` (refactor)
2. **Task 2: Ребрендинг Docker + Backend bookreader -> fancai** - `d5087e0` (refactor)

## Files Created/Modified
- `backend/Dockerfile.dev` - Renamed from Dockerfile.lite (dev Dockerfile для бэкенда)
- `backend/Dockerfile.prod` - Renamed from Dockerfile.lite.prod (prod Dockerfile для бэкенда)
- `frontend/Dockerfile.dev` - Renamed from Dockerfile (dev Dockerfile для фронтенда)
- `backend/Dockerfile` - Deleted (старый CI-only Dockerfile)
- `docker-compose.dev.yml` - Dockerfile.lite -> Dockerfile.dev (3 места)
- `docker-compose.prod.yml` - Dockerfile.lite.prod -> Dockerfile.prod + все bookreader -> fancai
- `docker-compose.monitoring.yml` - Все container_name bookreader_* -> fancai_*
- `.github/workflows/ci.yml` - Dockerfile refs, image tags, DB name + удален NLP step
- `.github/workflows/security.yml` - Dockerfile refs, image tags bookreader -> fancai
- `backend/app/core/celery_app.py` - Celery("bookreader") -> Celery("fancai")
- `backend/app/core/config.py` - DATABASE_URL default bookreader_dev -> fancai_dev
- `backend/app/core/database.py` - application_name bookreader_reading_sessions -> fancai_reading_sessions
- `backend/app/core/secrets.py` - forbidden_values bookreader_dev -> fancai_dev
- `backend/tests/conftest.py` - Обновлен комментарий для fancai_dev -> fancai_test
- `backend/alembic.ini` - Connection string bookreader_user/pass -> fancai_user/pass

## Decisions Made
- Dockerfile.dev/Dockerfile.prod -- единообразная схема вместо запутанных .lite/.lite.prod имен
- backend/Dockerfile удален полностью -- CI переходит на Dockerfile.prod (multi-stage build)
- NLP step (SpaCy) удален из CI -- NLP был удален в Phase 2, step бесполезен
- security.yml image tags тоже переименованы -- консистентность с ci.yml

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] security.yml image tags не указаны в плане**
- **Found during:** Task 1
- **Issue:** План упоминал только Dockerfile refs в security.yml, но Trivy image-ref и docker build tags тоже использовали bookreader
- **Fix:** Заменены все bookreader-backend:test и bookreader-frontend:test на fancai-* в security.yml
- **Files modified:** .github/workflows/security.yml
- **Committed in:** 2d9e782 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** Необходимо для корректности -- без замены Trivy сканировал бы образы с несуществующими тегами.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Docker-конфигурации полностью ребрендированы (fancai_*)
- CI/CD workflows обновлены и готовы к использованию
- Plan 04.1-03 (фронтенд ребрендинг) может продолжить ребрендинг на стороне фронтенда
- При деплое: docker compose down (старые контейнеры bookreader_*) -> docker compose up (новые fancai_*)

## Self-Check: PASSED

All files verified present. Both commits (2d9e782, d5087e0) confirmed in git log. Zero bookreader references in target files.

---
*Phase: 04.1-integration-rebrand*
*Completed: 2026-03-02*
