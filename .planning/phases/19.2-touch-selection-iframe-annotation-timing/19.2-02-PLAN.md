---
phase: 19.2-touch-selection-iframe-annotation-timing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/epub/useAnnotationRendering.ts
  - frontend/src/hooks/epub/__tests__/useAnnotationRendering.test.ts
autonomous: true
requirements: [BUG-4]

must_haves:
  truths:
    - "Аннотации (выделения, заметки) появляются сразу при загрузке страницы, без задержки или мерцания"
    - "Аннотации наносятся через hooks.content callback, а не через rendered event listener"
    - "При создании новой заметки аннотация появляется немедленно (debounce 50ms от bookmark change)"
    - "Lifecycle: hooks.content callback выполняется ДО rendered event (гарантия epub.js)"
  artifacts:
    - path: "frontend/src/hooks/epub/useAnnotationRendering.ts"
      provides: "hooks.content registration для annotation rendering вместо rendered event"
      contains: "rendition.hooks.content.register"
    - path: "frontend/src/hooks/epub/__tests__/useAnnotationRendering.test.ts"
      provides: "Тесты hooks.content registration и bookmark-change debounce"
      contains: "hooks.content.register"
  key_links:
    - from: "frontend/src/hooks/epub/useAnnotationRendering.ts"
      to: "epub.js rendition.hooks.content"
      via: "register callback для применения аннотаций при загрузке контента"
      pattern: "rendition\\.hooks\\.content\\.register"
    - from: "frontend/src/hooks/epub/useAnnotationRendering.ts"
      to: "bookmarksRef.current"
      via: "ref для актуальных данных в hooks.content callback"
      pattern: "bookmarksRef\\.current"
---

<objective>
Исправление BUG-4: миграция annotation rendering с rendered event на hooks.content для правильного epub.js lifecycle. Аннотации должны появляться сразу при загрузке страницы, без задержки и мерцания.

Purpose: Phase 19.1 добавила bookmarksRef и раздельные debounce-таймеры, но основная проблема осталась — rendered event эмитится ПОСЛЕ hooks.content (epub.js rendition.js:418-440). Регистрация через hooks.content гарантирует применение аннотаций до финального рендеринга страницы.

Output: Аннотации отображаются синхронно с загрузкой контента через epub.js hooks.content, а bookmark changes запускают немедленное обновление через 50ms debounce.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-touch-selection-iframe-annotation-timing/19.2-CONTEXT.md
@.planning/phases/19.2-touch-selection-iframe-annotation-timing/19.2-RESEARCH.md

@frontend/src/hooks/epub/useAnnotationRendering.ts
@frontend/src/hooks/epub/__tests__/useAnnotationRendering.test.ts

<interfaces>
<!-- epub.js lifecycle (из rendition.js:418-440): -->
<!-- afterDisplayed() → hooks.render.trigger() → hooks.content.trigger() → emit('rendered') -->
<!-- hooks.content callback получает Contents object: { document, window, ... } -->

From frontend/src/hooks/epub/useAnnotationRendering.ts:
```typescript
// Текущая архитектура (ПРОБЛЕМНАЯ):
// 1. injectStyles — через hooks.content.register (ПРАВИЛЬНО)
// 2. registerClick — через hooks.content.register (ПРАВИЛЬНО)
// 3. applyAnnotations — через rendered event + debounce (НЕПРАВИЛЬНО — rendered fires AFTER hooks.content)
//
// Целевая архитектура:
// 1. injectStyles — через hooks.content.register (без изменений)
// 2. registerClick — через hooks.content.register (без изменений)
// 3. applyAnnotations — через hooks.content.register (НОВОЕ)
// 4. bookmark changes — через useEffect + 50ms debounce (остаётся для немедленного обновления)

// Ключевые функции (без изменений):
function cleanupAnnotations(doc: Document): void;
function wrapRangeWithSpan(doc: Document, range: Range, bookmark: BookmarkResponse): void;
function resolveRangeFallback(doc: Document, cfi: string): Range | null;
```

From frontend/src/hooks/api/useSync.ts:
```typescript
export interface BookmarkResponse {
  id: string;
  book_id: string;
  chapter_number: number;
  cfi_position: string;
  cfi_range: string;
  note: string | null;
  color: string | null;
  text_color: string | null;
  style: string; // 'none' | 'bold' | 'italic' | 'underline'
  selected_text: string | null;
  created_at: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BUG-4 — hooks.content registration для annotation rendering вместо rendered event</name>
  <files>
    frontend/src/hooks/epub/useAnnotationRendering.ts,
    frontend/src/hooks/epub/__tests__/useAnnotationRendering.test.ts
  </files>
  <behavior>
    - Test: hooks.content.register вызывается с annotation callback (а не rendition.on('rendered'))
    - Test: annotation callback вызывает cleanupAnnotations + wrapRangeWithSpan на загруженном document
    - Test: annotation callback читает bookmarks из bookmarksRef.current (не из closure)
    - Test: bookmark change (useEffect deps) запускает applyAnnotations с 50ms debounce
    - Test: hooks.content.deregister вызывается при cleanup
    - Test: renderedDebounceRef больше не используется (rendered event подписка удалена)
  </behavior>
  <action>
    **Рефакторинг useAnnotationRendering.ts:**

    Текущий useEffect "Restore annotations when chapter renders or bookmarks change" (строки 436-466) подписан на `rendered` event с 200ms debounce. Заменить на hooks.content registration.

    **Шаг 1: Создать hooks.content callback для аннотаций**

    Новый useEffect заменяет текущий "Restore annotations" effect:

    ```typescript
    // Apply annotations via hooks.content — runs during epub.js content loading
    // Lifecycle: hooks.render -> hooks.content -> emit('rendered')
    // This ensures annotations are visible BEFORE the page is rendered
    useEffect(() => {
      if (!rendition || !enabled) return;

      const annotationHook = (contents: unknown) => {
        const c = contents as { document: Document };
        const doc = c.document;
        if (!doc?.body) return;

        // Read bookmarks from ref — always up-to-date
        const currentBookmarks = bookmarksRef.current;
        const chapterBookmarks = currentBookmarks.filter(
          (b) => b.chapter_number === currentChapter
            && (b.style !== 'none' || b.color || b.text_color)
        );

        // Reuse applyingRef guard to prevent concurrent application
        if (applyingRef.current) return;
        applyingRef.current = true;

        try {
          cleanupAnnotations(doc);
          if (!chapterBookmarks.length) return;

          for (const bookmark of chapterBookmarks) {
            try {
              let range = rendition.getRange(bookmark.cfi_range);
              if (!range) {
                range = resolveRangeFallback(doc, bookmark.cfi_range);
              }
              if (!range) continue;
              wrapRangeWithSpan(doc, range, bookmark);
            } catch (err) {
              logger.error('[useAnnotationRendering] Failed to apply annotation:', bookmark.id, err);
            }
          }
        } finally {
          applyingRef.current = false;
        }
      };

      rendition.hooks.content.register(annotationHook);

      // Also apply to already-rendered content immediately
      try {
        const contents = rendition.getContents();
        if (contents?.length) {
          contents.forEach(annotationHook);
        }
      } catch {
        // Content may not be available yet
      }

      return () => {
        rendition.hooks.content.deregister(annotationHook);
      };
    }, [rendition, enabled, currentChapter]);
    ```

    **Шаг 2: Bookmark-change trigger (отдельный useEffect)**

    Сохранить отдельный useEffect для немедленного обновления при изменении bookmarks (debounce 50ms). Этот эффект нужен потому что hooks.content callback вызывается только при ЗАГРУЗКЕ нового контента (переход между главами/страницами), а при создании/редактировании заметки контент уже загружен.

    ```typescript
    // Re-apply annotations when bookmarks change (user creates/edits a note)
    // Short debounce (50ms) for near-instant visual feedback
    useEffect(() => {
      if (!rendition || !enabled) return;

      if (bookmarkDebounceRef.current) {
        clearTimeout(bookmarkDebounceRef.current);
      }
      bookmarkDebounceRef.current = setTimeout(applyAnnotations, 50);

      return () => {
        if (bookmarkDebounceRef.current) {
          clearTimeout(bookmarkDebounceRef.current);
        }
      };
    }, [rendition, enabled, bookmarks, currentChapter, applyAnnotations]);
    ```

    **Шаг 3: Удалить rendered event подписку**

    - Удалить `renderedDebounceRef` (больше не нужен — rendered event не используется)
    - Удалить `rendition.on('rendered', handleRendered)` и `rendition.off('rendered', handleRendered)`
    - Удалить `const handleRendered = () => { ... }` callback
    - Сохранить `bookmarkDebounceRef` для bookmark-change trigger

    **Шаг 4: Упростить refs**

    - Удалить `renderedDebounceRef` из объявления refs (строка 289)
    - `applyAnnotations` useCallback оставить как есть — используется bookmark-change effect

    **Тесты (useAnnotationRendering.test.ts):**

    Добавить/обновить тесты:
    1. `it('registers annotation hook via hooks.content.register')` — проверить что rendition.hooks.content.register вызывается с функцией
    2. `it('deregisters annotation hook on cleanup')` — проверить hooks.content.deregister
    3. `it('does NOT subscribe to rendered event')` — проверить что rendition.on НЕ вызывается с 'rendered'
    4. `it('applies annotations immediately on bookmark change with 50ms debounce')` — проверить setTimeout(applyAnnotations, 50)
    5. Обновить существующие тесты если они проверяли rendered event подписку
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npm test -- --watchAll=false --testPathPattern="hooks/epub/__tests__/useAnnotationRendering" 2>&1 | tail -30</automated>
  </verify>
  <done>
    1. Аннотации применяются через hooks.content.register callback — гарантированно ДО rendered event
    2. rendered event подписка полностью удалена (нет rendition.on('rendered'))
    3. renderedDebounceRef удалён — остался только bookmarkDebounceRef (50ms)
    4. Bookmark changes по-прежнему запускают немедленное обновление (50ms debounce)
    5. bookmarksRef.current используется в hooks.content callback (актуальные данные)
    6. Все тесты проходят
  </done>
</task>

</tasks>

<verification>
```bash
# Тесты annotation rendering проходят
cd /Users/sandk/Documents/GitHub/fancai/frontend && npm test -- --watchAll=false --testPathPattern="useAnnotationRendering" 2>&1 | tail -20

# Полная проверка epub hooks
cd /Users/sandk/Documents/GitHub/fancai/frontend && npm test -- --watchAll=false --testPathPattern="hooks/epub" 2>&1 | tail -20

# Build проходит
cd /Users/sandk/Documents/GitHub/fancai/frontend && npm run build 2>&1 | tail -10
```
</verification>

<success_criteria>
1. useAnnotationRendering.ts регистрирует annotation callback через rendition.hooks.content.register
2. Нет подписки на rendered event (rendition.on('rendered') отсутствует в annotation effect)
3. renderedDebounceRef удалён, bookmarkDebounceRef (50ms) сохранён
4. hooks.content callback читает bookmarks из bookmarksRef.current
5. Тесты подтверждают hooks.content registration и отсутствие rendered event
6. Build собирается без ошибок TypeScript
</success_criteria>

<output>
После завершения создать `.planning/phases/19.2-touch-selection-iframe-annotation-timing/19.2-02-SUMMARY.md`
</output>
