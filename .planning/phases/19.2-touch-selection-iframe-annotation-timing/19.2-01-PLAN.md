---
phase: 19.2-touch-selection-iframe-annotation-timing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/epub/useContentHooks.ts
  - frontend/src/hooks/epub/useGestureController.ts
  - frontend/src/styles/globals.css
  - frontend/src/hooks/epub/__tests__/useContentHooks.test.ts
  - frontend/src/hooks/epub/__tests__/useGestureController.test.ts
autonomous: true
requirements: [BUG-1, BUG-5]

must_haves:
  truths:
    - "Короткий тап (<300ms) по тексту в epub iframe НЕ вызывает выделение и Chrome Touch to Search"
    - "Long-press (>300ms) по тексту корректно запускает выделение текста для создания заметки"
    - "elementFromPoint в handleTouchEnd использует clientX/clientY напрямую (без вычитания iframeRect)"
    - "elementFromPoint в handleClick использует clientX/clientY напрямую (без вычитания iframeRect)"
    - "Zone detection (getTapAction) использует screenX = clientX + iframeOffset для корректных screen координат"
    - "onCenterTap получает clientX/clientY напрямую (iframe-viewport coords для description/entity обнаружения)"
    - "iOS overlay по-прежнему конвертирует screen->iframe через iframeRect вычитание (parent document events)"
  artifacts:
    - path: "frontend/src/hooks/epub/useContentHooks.ts"
      provides: "selectstart event listener с timing-based подавлением"
      contains: "selectstart"
    - path: "frontend/src/hooks/epub/useGestureController.ts"
      provides: "Корректные координаты без двойного вычитания iframeRect"
      contains: "doc.elementFromPoint(touch.clientX, touch.clientY)"
    - path: "frontend/src/styles/globals.css"
      provides: "Очистка неэффективных touch-action правил для iframe"
  key_links:
    - from: "frontend/src/hooks/epub/useContentHooks.ts"
      to: "epub.js iframe document"
      via: "rendition.hooks.content.register — selectstart listener injection"
      pattern: "selectstart.*preventDefault"
    - from: "frontend/src/hooks/epub/useGestureController.ts"
      to: "epub.js iframe document"
      via: "elementFromPoint с прямыми clientX/clientY для iframe events"
      pattern: "doc\\.elementFromPoint\\(touch\\.clientX"
---

<objective>
Исправление двух UAT-багов мобильного ридера: (1) BUG-1 — подавление Chrome Touch to Search при коротком тапе через selectstart listener в iframe; (2) BUG-5 — устранение двойного вычитания iframe offset при определении интерактивных элементов.

Purpose: Phase 19.1 применила поверхностные патчи (CSS touch-action, getIframeRect вычитание), которые не решили корневые проблемы. Этот план исправляет архитектурные причины: selectstart listener работает на уровне Chrome Touch to Search, а координаты в iframe events уже в iframe-viewport system и не требуют конвертации.

Output: Корректная обработка touch-событий в epub.js iframe — короткий тап не вызывает выделение, elementFromPoint работает с правильными координатами.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-touch-selection-iframe-annotation-timing/19.2-CONTEXT.md
@.planning/phases/19.2-touch-selection-iframe-annotation-timing/19.2-RESEARCH.md

@frontend/src/hooks/epub/useContentHooks.ts
@frontend/src/hooks/epub/useGestureController.ts
@frontend/src/styles/globals.css
@frontend/src/hooks/epub/__tests__/useContentHooks.test.ts
@frontend/src/hooks/epub/__tests__/useGestureController.test.ts

<interfaces>
<!-- Ключевые типы и контракты, используемые в этом плане -->

From frontend/src/types/epub.ts:
```typescript
export type Contents = {
  document: Document;
  window: Window;
  // ... other properties
};
export type Rendition = {
  hooks: { content: Hook; render: Hook };
  getContents(): Contents[];
  getRange(cfi: string): Range | null;
  on(event: string, cb: (...args: unknown[]) => void): void;
  off(event: string, cb: (...args: unknown[]) => void): void;
};
```

From frontend/src/hooks/epub/useGestureController.ts:
```typescript
// Координатная система:
// - Events из hooks.content.register() callback: clientX/clientY = iframe-viewport coords
// - Events из parent document (iOS overlay): clientX/clientY = screen coords
// - elementFromPoint на iframe doc: принимает iframe-viewport coords
// - getTapAction: принимает screen coords (screenX = clientX + iframeOffset)

const getIframeOffset = (contents: Contents): number => { /* returns iframeRect.left */ };
const getIframeRect = (contents: Contents): DOMRect | null => { /* full rect */ };
const getTapAction = (screenX: number, isIOSOverlay: boolean): 'prev' | 'next' | 'center' => { /* zone detection */ };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BUG-1 — selectstart listener для подавления Chrome Touch to Search + BUG-5 — убрать двойное вычитание iframe offset</name>
  <files>
    frontend/src/hooks/epub/useContentHooks.ts,
    frontend/src/hooks/epub/useGestureController.ts,
    frontend/src/styles/globals.css,
    frontend/src/hooks/epub/__tests__/useContentHooks.test.ts,
    frontend/src/hooks/epub/__tests__/useGestureController.test.ts
  </files>
  <behavior>
    BUG-1 тесты (useContentHooks.test.ts):
    - Test: selectstart listener регистрируется на iframe document через hooks.content
    - Test: selectstart event preventDefault вызывается при elapsed < 300ms (короткий тап)
    - Test: selectstart event НЕ блокируется при elapsed >= 300ms (long-press)
    - Test: touchstart listener устанавливает timing baseline (passive: true)

    BUG-5 тесты (useGestureController.test.ts):
    - Test: handleTouchEnd вызывает elementFromPoint с clientX/clientY напрямую (без вычитания iframeRect)
    - Test: handleClick вызывает elementFromPoint с clientX/clientY напрямую (без вычитания iframeRect)
    - Test: onCenterTap получает clientX/clientY напрямую для iframe events
    - Test: zone detection использует clientX + iframeOffset для screen coords
  </behavior>
  <action>
    **BUG-1: useContentHooks.ts**

    Внутри существующего `contentHook` callback (после блока CSS injection и contextmenu handler), добавить selectstart timing logic:

    ```typescript
    // BUG-1 fix: Suppress Chrome Touch to Search on short taps
    // Touch to Search ignores CSS touch-action; selectstart prevention is the correct approach
    // See: https://developer.chrome.com/blog/tap-to-search
    let touchStartTime = 0;
    const LONG_PRESS_THRESHOLD = 300; // ms

    doc.addEventListener('touchstart', () => {
      touchStartTime = Date.now();
    }, { passive: true });

    doc.addEventListener('selectstart', (e: Event) => {
      const elapsed = Date.now() - touchStartTime;
      if (touchStartTime > 0 && elapsed < LONG_PRESS_THRESHOLD) {
        e.preventDefault();
      }
      // long-press (>=300ms) — selection allowed for note creation
    });
    ```

    **BUG-5: useGestureController.ts**

    В `handleTouchEnd` (секция TAP DETECTION, ~строка 543-570):
    - УБРАТЬ: `const iframeRect = getIframeRect(contents);`
    - УБРАТЬ: `const iframeX = iframeRect ? touch.clientX - iframeRect.left : touch.clientX;`
    - УБРАТЬ: `const iframeY = iframeRect ? touch.clientY - iframeRect.top : touch.clientY;`
    - ЗАМЕНИТЬ на: `const el = tapDoc?.elementFromPoint(touch.clientX, touch.clientY);`
    - onCenterTap вызовы: передавать `touch.clientX, touch.clientY` напрямую (уже iframe coords)
    - Zone detection оставить: `const screenX = touch.clientX + iframeOffset;` (это СЛОЖЕНИЕ, правильно)

    В `handleClick` (секция click desktop fallback, ~строка 765-770):
    - Аналогично: УБРАТЬ вычитание `clickIframeRect.left/top`
    - ЗАМЕНИТЬ на: `const clickEl = clickDoc?.elementFromPoint(e.clientX, e.clientY);`
    - onCenterTap: передавать `e.clientX, e.clientY` напрямую
    - Zone detection оставить: `const screenX = e.clientX + iframeOffset;`

    В center-tap секции (после zone detection): убрать повторное вычитание `iframeRect ? touch.clientX - iframeRect.left : touch.clientX` для viewportX/viewportY. Передавать clientX/clientY напрямую.

    iOS overlay `handleOverlayTouchEnd` (строки 984-995): НЕ ТРОГАТЬ — это events из parent document, конвертация `touch.clientX - iframeRect.left` ПРАВИЛЬНА здесь.

    **Можно удалить `getIframeRect` helper** если он используется только в handleTouchEnd/handleClick (проверить — iOS overlay использует свой `iframe.getBoundingClientRect()`, не через helper). Если не используется нигде больше — удалить для чистоты.

    **globals.css очистка:**
    Секция `/* EPUB Reader - Touch gesture handling */` (строки 1063-1082): CSS правила `.epub-container iframe body` и `#viewer iframe body` задают `touch-action: pan-x pan-y !important`. Эти правила для **iframe body** избыточны — `useContentHooks.ts` уже устанавливает `touch-action: pan-x pan-y` на body внутри iframe через style injection. Оставить правила для iframe element (`.epub-container iframe` и `#viewer iframe`), но убрать дубликаты для `iframe body`.

    Упростить до:
    ```css
    .epub-container iframe,
    #viewer iframe {
      touch-action: pan-x pan-y !important;
      overscroll-behavior: contain !important;
    }
    ```

    **Тесты:**
    - useContentHooks.test.ts: добавить describe блок для selectstart suppression. Mock Date.now(), создать mock document с addEventListener. Проверить что selectstart.preventDefault вызывается при elapsed < 300ms и НЕ вызывается при elapsed >= 300ms.
    - useGestureController.test.ts: добавить/обновить тест для coordinate handling. Проверить что elementFromPoint вызывается с clientX/clientY напрямую (без вычитания).
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npm test -- --watchAll=false --testPathPattern="hooks/epub/__tests__/(useContentHooks|useGestureController)" 2>&1 | tail -30</automated>
  </verify>
  <done>
    1. selectstart listener зарегистрирован через hooks.content — короткий тап (<300ms) блокирует выделение, long-press (>=300ms) разрешает
    2. elementFromPoint в handleTouchEnd и handleClick использует clientX/clientY напрямую без вычитания iframeRect
    3. onCenterTap получает clientX/clientY напрямую (iframe-viewport coords)
    4. Zone detection по-прежнему использует screenX = clientX + iframeOffset
    5. iOS overlay конвертация (screen->iframe) не затронута
    6. globals.css очищен от избыточных iframe body правил
    7. Все новые и существующие тесты проходят
  </done>
</task>

</tasks>

<verification>
```bash
# Все тесты epub hooks проходят
cd /Users/sandk/Documents/GitHub/fancai/frontend && npm test -- --watchAll=false --testPathPattern="hooks/epub" 2>&1 | tail -20

# Build проходит без ошибок TypeScript
cd /Users/sandk/Documents/GitHub/fancai/frontend && npm run build 2>&1 | tail -10
```
</verification>

<success_criteria>
1. useContentHooks.ts содержит selectstart listener с timing-based подавлением (порог 300ms)
2. useGestureController.ts использует clientX/clientY напрямую для elementFromPoint и onCenterTap в iframe events
3. getIframeRect удалён или не используется в iframe event handlers (только в iOS overlay если нужно)
4. globals.css не содержит дублирующих touch-action правил для iframe body
5. Тесты проходят, build собирается
</success_criteria>

<output>
После завершения создать `.planning/phases/19.2-touch-selection-iframe-annotation-timing/19.2-01-SUMMARY.md`
</output>
