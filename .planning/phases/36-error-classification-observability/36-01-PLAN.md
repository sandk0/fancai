---
phase: 36-error-classification-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modal/llm_extractor.py
  - backend/tests/tasks/test_modal_metrics.py
autonomous: true
requirements: [STAB-04, OBS-02]

must_haves:
  truths:
    - "extract_chapter() возвращает dict с ключами 'result' и 'metrics'"
    - "При finish_reason='length' result=None и truncated_text содержит первые 500 символов"
    - "metrics содержит cold_start_ms, inference_ms, finish_reason, is_cold_start"
    - "reduce_entities() возвращает dict с 'result' и 'metrics' (консистентность)"
    - "cold_start_ms замеряет реальное время загрузки модели в @enter"
  artifacts:
    - path: "modal/llm_extractor.py"
      provides: "extract_chapter и reduce_entities с metrics transport и finish_reason check"
      contains: "finish_reason"
    - path: "backend/tests/tasks/test_modal_metrics.py"
      provides: "Тесты нового return формата Modal"
      contains: "test_extract_chapter_returns_metrics"
  key_links:
    - from: "modal/llm_extractor.py"
      to: "backend/app/tasks/book_tasks.py"
      via: "return dict с 'result' и 'metrics' ключами"
      pattern: 'metrics.*cold_start_ms.*inference_ms.*finish_reason'
---

<objective>
Modal metrics transport и finish_reason проверка

Расширить `extract_chapter()` и `reduce_entities()` в `modal/llm_extractor.py`: возврат `{"result": ..., "metrics": {"cold_start_ms", "inference_ms", "finish_reason", "is_cold_start"}}`. Проверка `finish_reason` ДО `json.loads()` — при `finish_reason="length"` возврат `result=None` + `truncated_text[:500]`.

Purpose: Основа для OBS-02 (structured logging на VPS) и STAB-04 (finish_reason проверка). Без этого плана VPS-код не получает метрики от Modal.
Output: Обновлённый `modal/llm_extractor.py` + тесты нового формата.
</objective>

<execution_context>
@/Users/sandk/Documents/GitHub/fancai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/Documents/GitHub/fancai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-error-classification-observability/36-CONTEXT.md
@.planning/phases/36-error-classification-observability/36-RESEARCH.md

<interfaces>
<!-- Текущий интерфейс Modal LLMExtractor — нужно расширить return формат -->

From modal/llm_extractor.py:
```python
@app.cls(image=llm_image, gpu=LLM_GPU, timeout=LLM_TIMEOUT, **COMMON_CLS_KWARGS)
class LLMExtractor:
    @modal.enter()
    def load_model(self):
        from vllm import LLM
        self.llm = LLM(model=LLM_MODEL_ID, ...)

    @modal.method()
    def extract_chapter(self, chapter_text: str, system_prompt: str, schema_json: str) -> dict:
        # Текущий: return json.loads(result[0].outputs[0].text)

    @modal.method()
    def reduce_entities(self, entities_json: str, system_prompt: str, schema_json: str) -> dict:
        # Текущий: return json.loads(result[0].outputs[0].text)
```

From backend/app/tasks/book_tasks.py (consumer):
```python
modal_json = await asyncio.wait_for(
    asyncio.to_thread(extractor.extract_chapter.remote, ...),
    timeout=VPS_TIMEOUT,
)
result = modal_response_to_chapter_result(modal_json)
# Будет: modal_json["result"], modal_json["metrics"]
```

From backend/app/services/modal_client.py:
```python
def modal_response_to_chapter_result(modal_json: Dict[str, Any]) -> ChapterAnalysisResult:
    # Читает entities, descriptions, relationships из modal_json
    # Будет: из modal_json["result"] вместо modal_json напрямую
```

vLLM CompletionOutput interface:
```python
class CompletionOutput:
    finish_reason: Optional[str]  # "stop", "length", "abort"
    text: str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Тесты нового return формата Modal</name>
  <files>backend/tests/tasks/test_modal_metrics.py</files>
  <read_first>
    - modal/llm_extractor.py
    - backend/tests/tasks/test_modal_integration.py
    - backend/tests/services/test_modal_client.py
  </read_first>
  <behavior>
    - Test 1: extract_chapter с finish_reason="stop" возвращает dict с ключами "result" (parsed JSON) и "metrics" (dict с 4 полями)
    - Test 2: extract_chapter с finish_reason="length" возвращает result=None, truncated_text (str[:500]), metrics с finish_reason="length"
    - Test 3: metrics содержит cold_start_ms (int >= 0), inference_ms (int >= 0), finish_reason (str), is_cold_start (bool)
    - Test 4: Первый вызов после cold start имеет is_cold_start=True, второй — False
    - Test 5: reduce_entities с finish_reason="stop" возвращает аналогичный формат {"result": ..., "metrics": ...}
    - Test 6: reduce_entities с finish_reason="length" возвращает result=None + truncated_text
  </behavior>
  <action>
Создать `backend/tests/tasks/test_modal_metrics.py` с unit-тестами нового return формата.

Тесты мокают vLLM `LLM.chat()` return через `MagicMock`:
```python
mock_output = MagicMock()
mock_output.text = '{"entities": [], "descriptions": [], "relationships": []}'
mock_output.finish_reason = "stop"
mock_result = MagicMock()
mock_result.__getitem__ = lambda self, idx: MagicMock(outputs=[mock_output])
```

Для cold start теста: создать экземпляр LLMExtractor, вызвать load_model (мок), затем extract_chapter дважды — проверить is_cold_start=True/False.

Важно: тесты НЕ импортируют modal напрямую — мокаем через unittest.mock.patch. Паттерн из test_modal_integration.py.

6 тестов в TestModalMetricsFormat class. Тесты ДОЛЖНЫ падать (RED) до реализации в Task 2.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/backend && uv run python -m pytest tests/tasks/test_modal_metrics.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/tasks/test_modal_metrics.py существует и содержит >= 6 тестов
    - Файл содержит `class TestModalMetricsFormat`
    - Файл содержит `test_extract_chapter_returns_metrics`
    - Файл содержит `test_extract_chapter_truncated_on_length`
    - Файл содержит `test_cold_start_flag`
    - Файл содержит `test_reduce_entities_returns_metrics`
    - Тесты падают (RED phase) — ожидаемо, т.к. modal/llm_extractor.py ещё не обновлён
  </acceptance_criteria>
  <done>6+ тестов написаны, документируют ожидаемый контракт нового формата Modal response</done>
</task>

<task type="auto">
  <name>Task 2: Реализация metrics transport и finish_reason в llm_extractor.py</name>
  <files>modal/llm_extractor.py</files>
  <read_first>
    - modal/llm_extractor.py
    - modal/config.py
    - backend/tests/tasks/test_modal_metrics.py
  </read_first>
  <action>
Обновить `modal/llm_extractor.py`:

**1. Добавить timing в `load_model()` (D-09):**
```python
import time

@modal.enter()
def load_model(self):
    enter_start = time.monotonic()
    from vllm import LLM
    self.llm = LLM(model=LLM_MODEL_ID, ...)
    self._cold_start_ms = int((time.monotonic() - enter_start) * 1000)
    self._is_first_call = True
    logging.info(f"vLLM initialized: cold_start_ms={self._cold_start_ms}, ...")
```

**2. Обновить `extract_chapter()` (STAB-04, D-03, D-08):**
```python
@modal.method()
def extract_chapter(self, chapter_text: str, system_prompt: str, schema_json: str) -> dict:
    from vllm import SamplingParams
    from vllm.sampling_params import StructuredOutputsParams

    inference_start = time.monotonic()
    params = SamplingParams(
        max_tokens=32768,
        temperature=0.1,
        structured_outputs=StructuredOutputsParams(json=schema_json),
    )
    messages = [
        {"role": "system", "content": system_prompt},
        {"role": "user", "content": f"<book_text>{chapter_text}</book_text>"},
    ]
    result = self.llm.chat(messages, params)
    output = result[0].outputs[0]

    inference_ms = int((time.monotonic() - inference_start) * 1000)
    metrics = {
        "cold_start_ms": self._cold_start_ms if self._is_first_call else 0,
        "inference_ms": inference_ms,
        "finish_reason": output.finish_reason,
        "is_cold_start": self._is_first_call,
    }
    self._is_first_call = False

    # STAB-04: check finish_reason BEFORE json.loads() (D-03)
    if output.finish_reason == "length":
        return {
            "result": None,
            "truncated_text": output.text[:500],  # D-05
            "metrics": metrics,
        }

    # Normal path
    parsed = json.loads(output.text)
    return {
        "result": parsed,
        "metrics": metrics,
    }
```

**3. Обновить `reduce_entities()` аналогично (Open Question 1 из RESEARCH — finish_reason + metrics):**
```python
@modal.method()
def reduce_entities(self, entities_json: str, system_prompt: str, schema_json: str) -> dict:
    from vllm import SamplingParams
    from vllm.sampling_params import StructuredOutputsParams

    inference_start = time.monotonic()
    params = SamplingParams(
        max_tokens=16384,
        temperature=0.0,
        structured_outputs=StructuredOutputsParams(json=schema_json),
    )
    messages = [
        {"role": "system", "content": system_prompt},
        {"role": "user", "content": entities_json},
    ]
    result = self.llm.chat(messages, params)
    output = result[0].outputs[0]

    inference_ms = int((time.monotonic() - inference_start) * 1000)
    metrics = {
        "cold_start_ms": self._cold_start_ms if self._is_first_call else 0,
        "inference_ms": inference_ms,
        "finish_reason": output.finish_reason,
        "is_cold_start": self._is_first_call,
    }
    self._is_first_call = False

    if output.finish_reason == "length":
        return {
            "result": None,
            "truncated_text": output.text[:500],
            "metrics": metrics,
        }

    parsed = json.loads(output.text)
    return {
        "result": parsed,
        "metrics": metrics,
    }
```

**Ключевые моменты:**
- `import time` добавить в начало файла (после `import json`)
- `self._cold_start_ms` и `self._is_first_call` инициализируются в `load_model()`
- `self._is_first_call = False` ставится ПОСЛЕ построения metrics dict, но ДО return
- `output.finish_reason` — свойство vLLM CompletionOutput, значения: "stop", "length", "abort"
- json.loads() вызывается ТОЛЬКО при finish_reason != "length"
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/backend && uv run python -m pytest tests/tasks/test_modal_metrics.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - modal/llm_extractor.py содержит `import time`
    - modal/llm_extractor.py содержит `self._cold_start_ms`
    - modal/llm_extractor.py содержит `self._is_first_call`
    - modal/llm_extractor.py extract_chapter содержит `if output.finish_reason == "length":`
    - modal/llm_extractor.py extract_chapter содержит `"truncated_text": output.text[:500]`
    - modal/llm_extractor.py extract_chapter содержит `"metrics": metrics`
    - modal/llm_extractor.py reduce_entities содержит `if output.finish_reason == "length":`
    - modal/llm_extractor.py reduce_entities содержит `"metrics": metrics`
    - Все тесты из test_modal_metrics.py проходят (GREEN)
  </acceptance_criteria>
  <done>extract_chapter() и reduce_entities() возвращают {"result": ..., "metrics": {...}} формат. finish_reason проверяется ДО json.loads(). cold_start_ms замеряется в @enter. Тесты GREEN.</done>
</task>

</tasks>

<verification>
- `cd backend && uv run python -m pytest tests/tasks/test_modal_metrics.py -x -v` — все тесты проходят
- `grep -n "finish_reason" modal/llm_extractor.py` — проверка присутствует в обоих методах
- `grep -n "_cold_start_ms" modal/llm_extractor.py` — timing инициализирован в load_model
- `grep -n '"metrics"' modal/llm_extractor.py` — metrics возвращается в обоих методах
</verification>

<success_criteria>
1. extract_chapter() возвращает `{"result": parsed_json, "metrics": {4 поля}}` при finish_reason="stop"
2. extract_chapter() возвращает `{"result": None, "truncated_text": str[:500], "metrics": {...}}` при finish_reason="length"
3. reduce_entities() возвращает аналогичный формат
4. cold_start_ms замеряет реальное время load_model()
5. is_cold_start=True только при первом вызове после cold start
6. Все тесты в test_modal_metrics.py проходят
</success_criteria>

<output>
After completion, create `.planning/phases/36-error-classification-observability/36-01-SUMMARY.md`
</output>
