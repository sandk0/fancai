---
phase: 36-error-classification-observability
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - backend/app/core/error_classifier.py
  - backend/app/models/chapter.py
  - backend/alembic/versions/2026_03_28_0001_add_error_type_to_chapters.py
  - backend/app/tasks/book_tasks.py
  - backend/app/services/modal_client.py
  - backend/tests/core/test_error_classifier.py
  - backend/tests/tasks/test_chapter_logging.py
autonomous: true
requirements: [STAB-04, OBS-01, OBS-02]

must_haves:
  truths:
    - "ErrorClassifier маппит FunctionTimeoutError -> 'timeout', JSONDecodeError -> 'json_error', RemoteError -> 'modal_error', InputCancellation -> 'cancelled'"
    - "error_type сохраняется в колонку chapter.error_type при ошибке обработки"
    - "При truncated response (result=None от Modal) выполняется 1 retry с max_tokens*1.5, при повторном truncated — error_type='truncated'"
    - "Per-chapter structured log содержит 9 полей: chapter_id, book_id, duration_ms, result_type, error_type, finish_reason, cold_start_ms, inference_ms, is_cold_start"
    - "modal_response_to_chapter_result() обрабатывает новый формат {'result': ..., 'metrics': ...} с backward compatibility"
  artifacts:
    - path: "backend/app/core/error_classifier.py"
      provides: "classify_error() функция и ERROR_TYPE_* константы"
      exports: ["classify_error", "ERROR_TYPE_TIMEOUT", "ERROR_TYPE_JSON_ERROR", "ERROR_TYPE_MODAL_ERROR", "ERROR_TYPE_CANCELLED", "ERROR_TYPE_TRUNCATED", "VALID_ERROR_TYPES"]
    - path: "backend/app/models/chapter.py"
      provides: "Колонка error_type в модели Chapter"
      contains: "error_type"
    - path: "backend/alembic/versions/2026_03_28_0001_add_error_type_to_chapters.py"
      provides: "Alembic migration для error_type"
      contains: "add_column"
    - path: "backend/app/tasks/book_tasks.py"
      provides: "ErrorClassifier интеграция + structured logging + truncated retry"
      contains: "classify_error"
    - path: "backend/app/services/modal_client.py"
      provides: "Обновлённый modal_response_to_chapter_result с metrics extraction"
      contains: "modal_json.get"
    - path: "backend/tests/core/test_error_classifier.py"
      provides: "Тесты classify_error для всех 5 типов ошибок"
      contains: "test_classify"
    - path: "backend/tests/tasks/test_chapter_logging.py"
      provides: "Тесты structured logging с 9 полями"
      contains: "chapter_processed"
  key_links:
    - from: "backend/app/core/error_classifier.py"
      to: "backend/app/tasks/book_tasks.py"
      via: "classify_error() вызывается в except блоках chapter loop"
      pattern: "from app.core.error_classifier import classify_error"
    - from: "backend/app/tasks/book_tasks.py"
      to: "backend/app/models/chapter.py"
      via: "local_chapter.error_type = error_type"
      pattern: "error_type"
    - from: "backend/app/services/modal_client.py"
      to: "backend/app/tasks/book_tasks.py"
      via: "modal_response_to_chapter_result принимает новый формат"
      pattern: 'modal_json.get\("result"'
---

<objective>
ErrorClassifier + Alembic migration + structured logging + truncated retry

Создать `classify_error()` для типизации pipeline-ошибок (OBS-01), добавить колонку `error_type` в Chapter (Alembic migration), интегрировать structured logging с 9 полями (OBS-02), обработать truncated response от Modal с retry (STAB-04), обновить `modal_response_to_chapter_result()` для нового формата.

Purpose: Каждая ошибка классифицирована, каждая глава имеет structured log — основа для retry-стратегий Phase 37 и диагностики.
Output: ErrorClassifier модуль, migration, обновлённый book_tasks.py с logging, обновлённый modal_client.py.
</objective>

<execution_context>
@/Users/sandk/Documents/GitHub/fancai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/Documents/GitHub/fancai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-error-classification-observability/36-CONTEXT.md
@.planning/phases/36-error-classification-observability/36-RESEARCH.md
@.planning/phases/36-error-classification-observability/36-01-SUMMARY.md

<interfaces>
<!-- Новый формат Modal response (после Plan 01) -->
```python
# extract_chapter() return при finish_reason="stop":
{"result": {"entities": [...], "descriptions": [...], "relationships": [...]}, "metrics": {"cold_start_ms": 12000, "inference_ms": 8500, "finish_reason": "stop", "is_cold_start": True}}

# extract_chapter() return при finish_reason="length":
{"result": None, "truncated_text": "первые 500 символов...", "metrics": {"cold_start_ms": 0, "inference_ms": 15000, "finish_reason": "length", "is_cold_start": False}}
```

From backend/app/models/chapter.py (текущий):
```python
class Chapter(Base):
    __tablename__ = "chapters"
    parsing_error: Mapped[str | None] = mapped_column(Text, nullable=True)
    parse_attempts: Mapped[int] = mapped_column(Integer, default=0, nullable=False)
    # ДОБАВИТЬ: error_type: Mapped[str | None] = mapped_column(String(20), nullable=True)
```

From backend/app/services/modal_client.py:
```python
def modal_response_to_chapter_result(modal_json: Dict[str, Any]) -> ChapterAnalysisResult:
    entities = [ExtractedEntity(...) for e in modal_json.get("entities", [])]
    # ОБНОВИТЬ: читать из modal_json.get("result", modal_json) для backward compat
```

From backend/app/tasks/book_tasks.py (строки 558-576):
```python
if use_modal:
    extractor = get_llm_extractor()
    modal_json = await asyncio.wait_for(
        asyncio.to_thread(extractor.extract_chapter.remote, ...),
        timeout=VPS_TIMEOUT,
    )
    result = modal_response_to_chapter_result(modal_json)
```

From backend/app/tasks/book_tasks.py (строки 767-798):
```python
except Exception as e:
    local_chapter.parsing_error = str(e)[:1000]
    local_chapter.parse_attempts += 1
except BaseException as fatal_err:
    # InputCancellation ловится здесь
```

From backend/app/core/logging.py:
```python
from loguru import logger
# Production: serialize=True -> JSON output с logger.bind() extra полями
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ErrorClassifier модуль + тесты + Alembic migration + Chapter model</name>
  <files>
    backend/app/core/error_classifier.py,
    backend/tests/core/test_error_classifier.py,
    backend/app/models/chapter.py,
    backend/alembic/versions/2026_03_28_0001_add_error_type_to_chapters.py
  </files>
  <read_first>
    - backend/app/core/retry.py
    - backend/app/models/chapter.py
    - backend/tests/core/test_circuit_breaker.py
    - backend/alembic/versions/2026_03_27_0001_add_image_prompt_en_usage_records_modal_flag.py
  </read_first>
  <behavior>
    - Test 1: classify_error(FunctionTimeoutError()) == "timeout"
    - Test 2: classify_error(asyncio.TimeoutError()) == "timeout"
    - Test 3: classify_error(TimeoutError()) == "timeout" (builtin)
    - Test 4: classify_error(json.JSONDecodeError("msg", "doc", 0)) == "json_error"
    - Test 5: classify_error(RemoteError()) == "modal_error"
    - Test 6: classify_error(InputCancellation()) == "cancelled"
    - Test 7: classify_error(RuntimeError("unknown")) == "modal_error" (fallback)
    - Test 8: classify_error(ValueError("bad")) == "modal_error" (fallback)
    - Test 9: VALID_ERROR_TYPES содержит ровно 5 элементов
    - Test 10: ERROR_TYPE_TRUNCATED == "truncated"
  </behavior>
  <action>
**1. Создать `backend/app/core/error_classifier.py` (D-02, OBS-01):**

```python
"""Error classification for pipeline observability (OBS-01)."""

from typing import Optional

# Error type constants — единый источник правды для Phase 36-38 (D-02)
ERROR_TYPE_TIMEOUT = "timeout"
ERROR_TYPE_JSON_ERROR = "json_error"
ERROR_TYPE_MODAL_ERROR = "modal_error"
ERROR_TYPE_CANCELLED = "cancelled"
ERROR_TYPE_TRUNCATED = "truncated"

VALID_ERROR_TYPES = {
    ERROR_TYPE_TIMEOUT,
    ERROR_TYPE_JSON_ERROR,
    ERROR_TYPE_MODAL_ERROR,
    ERROR_TYPE_CANCELLED,
    ERROR_TYPE_TRUNCATED,
}


def classify_error(exc: BaseException) -> str:
    """Classify pipeline exception into normalized error_type.

    Uses type(exc).__name__ for Modal SDK exceptions (not isinstance)
    because Modal SDK is optional dependency — import may not exist in tests.

    Args:
        exc: The caught exception

    Returns:
        Normalized error type string from VALID_ERROR_TYPES
    """
    import asyncio
    import json

    exc_type = type(exc).__name__

    # Modal SDK exceptions — string comparison (no import needed)
    if exc_type == "FunctionTimeoutError":
        return ERROR_TYPE_TIMEOUT
    if exc_type == "InputCancellation":
        return ERROR_TYPE_CANCELLED
    if exc_type == "RemoteError":
        return ERROR_TYPE_MODAL_ERROR

    # Standard library exceptions — isinstance safe
    if isinstance(exc, json.JSONDecodeError):
        return ERROR_TYPE_JSON_ERROR
    if isinstance(exc, (TimeoutError, asyncio.TimeoutError)):
        return ERROR_TYPE_TIMEOUT

    # Fallback: unknown -> modal_error
    return ERROR_TYPE_MODAL_ERROR
```

**2. Создать `backend/tests/core/test_error_classifier.py`:**

Тесты для каждого маппинга. Modal SDK exceptions мокаются через создание классов с нужным `__name__`:
```python
class FunctionTimeoutError(Exception): pass
class RemoteError(Exception): pass
class InputCancellation(BaseException): pass
```
10 тестов в `TestErrorClassifier` class.

**3. Добавить `error_type` в `backend/app/models/chapter.py`:**

После строки `parse_attempts` (строка 50):
```python
error_type: Mapped[str | None] = mapped_column(String(20), nullable=True)
```
Добавить `String` в import если отсутствует (уже есть в строке 12).

**4. Создать Alembic migration:**

Файл: `backend/alembic/versions/2026_03_28_0001_add_error_type_to_chapters.py`
`down_revision` — взять из последней миграции `2026_03_27_0001_add_image_prompt_en_usage_records_modal_flag.py`.

```python
"""Add error_type to chapters for error classification (OBS-01)"""
from alembic import op
import sqlalchemy as sa

# revision и down_revision вычислить из существующей последней миграции

def upgrade() -> None:
    op.add_column('chapters', sa.Column('error_type', sa.String(20), nullable=True))

def downgrade() -> None:
    op.drop_column('chapters', 'error_type')
```

`nullable=True` без default — PG17 metadata-only ADD COLUMN (Pitfall 4 из RESEARCH).
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/backend && uv run python -m pytest tests/core/test_error_classifier.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/core/error_classifier.py существует и содержит `def classify_error(`
    - backend/app/core/error_classifier.py содержит `ERROR_TYPE_TIMEOUT = "timeout"`
    - backend/app/core/error_classifier.py содержит `ERROR_TYPE_JSON_ERROR = "json_error"`
    - backend/app/core/error_classifier.py содержит `ERROR_TYPE_MODAL_ERROR = "modal_error"`
    - backend/app/core/error_classifier.py содержит `ERROR_TYPE_CANCELLED = "cancelled"`
    - backend/app/core/error_classifier.py содержит `ERROR_TYPE_TRUNCATED = "truncated"`
    - backend/app/core/error_classifier.py содержит `VALID_ERROR_TYPES` с 5 элементами
    - backend/app/core/error_classifier.py использует `type(exc).__name__` для Modal exceptions (не isinstance)
    - backend/app/models/chapter.py содержит `error_type: Mapped[str | None] = mapped_column(String(20), nullable=True)`
    - backend/alembic/versions/2026_03_28_0001_add_error_type_to_chapters.py существует
    - Migration содержит `op.add_column('chapters', sa.Column('error_type', sa.String(20), nullable=True))`
    - backend/tests/core/test_error_classifier.py содержит >= 10 тестов
    - Все тесты test_error_classifier.py проходят
  </acceptance_criteria>
  <done>ErrorClassifier модуль с 5 типами ошибок, Chapter model с error_type колонкой, Alembic migration, 10+ тестов — всё GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Интеграция в book_tasks.py — ErrorClassifier + structured logging + truncated retry + modal_client</name>
  <files>
    backend/app/tasks/book_tasks.py,
    backend/app/services/modal_client.py,
    backend/tests/tasks/test_chapter_logging.py
  </files>
  <read_first>
    - backend/app/tasks/book_tasks.py
    - backend/app/services/modal_client.py
    - backend/app/core/error_classifier.py
    - backend/app/core/logging.py
    - backend/tests/tasks/test_book_status_semantics.py
  </read_first>
  <behavior>
    - Test 1: log_chapter_result() вызывает logger.bind() с 9 полями при success
    - Test 2: log_chapter_result() вызывает logger.bind() с error_type при failure
    - Test 3: modal_response_to_chapter_result принимает новый формат {"result": {...}, "metrics": {...}}
    - Test 4: modal_response_to_chapter_result принимает старый формат {"entities": [...], ...} (backward compat)
    - Test 5: extract_metrics_from_response() извлекает metrics из нового формата
    - Test 6: extract_metrics_from_response() возвращает {} для старого формата (backward compat)
  </behavior>
  <action>
**1. Обновить `backend/app/services/modal_client.py`:**

Добавить backward-compatible extraction:
```python
def extract_modal_metrics(modal_json: Dict[str, Any]) -> Dict[str, Any]:
    """Извлечь metrics из Modal response (backward compatible с D-08)."""
    return modal_json.get("metrics", {})


def extract_modal_result(modal_json: Dict[str, Any]) -> Dict[str, Any]:
    """Извлечь result из Modal response (backward compatible).

    Новый формат: {"result": {...}, "metrics": {...}}
    Старый формат: {"entities": [...], "descriptions": [...], ...}
    """
    result = modal_json.get("result")
    if result is not None:
        return result
    # Backward compat: старый формат — весь dict является result
    if "entities" in modal_json or "descriptions" in modal_json:
        return modal_json
    return modal_json
```

`modal_response_to_chapter_result()` обновить:
```python
def modal_response_to_chapter_result(modal_json: Dict[str, Any]) -> ChapterAnalysisResult:
    result_data = extract_modal_result(modal_json)
    entities = [ExtractedEntity(...) for e in result_data.get("entities", [])]
    # ... остальное без изменений, но читает из result_data вместо modal_json
```

**2. Обновить `backend/app/tasks/book_tasks.py`:**

**2a. Добавить imports (в начало файла):**
```python
from app.core.error_classifier import classify_error, ERROR_TYPE_TRUNCATED
from app.services.modal_client import extract_modal_metrics, extract_modal_result
```

**2b. Добавить helper для structured logging (D-06, D-07):**
```python
def _log_chapter_result(
    *,
    chapter_id: str,
    book_id: str,
    duration_ms: int,
    result_type: str,  # "success" | "error"
    error_type: str | None = None,
    metrics: dict | None = None,
) -> None:
    """Per-chapter structured log с 9 полями (OBS-02, D-06, D-07)."""
    m = metrics or {}
    logger.bind(
        chapter_id=chapter_id,
        book_id=book_id,
        duration_ms=duration_ms,
        result_type=result_type,
        error_type=error_type,
        finish_reason=m.get("finish_reason"),
        cold_start_ms=m.get("cold_start_ms", 0),
        inference_ms=m.get("inference_ms", 0),
        is_cold_start=m.get("is_cold_start", False),
    ).info("chapter_processed")
```

**2c. В process_chapter_safe, Modal path (строки 558-589) — добавить chapter timing, metrics extraction, truncated retry, structured logging:**

ПЕРЕД Modal extraction (строка ~555, после `check_time_budget`):
```python
chapter_start = time.monotonic()
```

ПОСЛЕ Modal extraction (заменить строки 569-589):
```python
modal_json = await asyncio.wait_for(
    asyncio.to_thread(extractor.extract_chapter.remote, ...),
    timeout=VPS_TIMEOUT,
)

# Extract metrics (D-08 backward compat)
metrics = extract_modal_metrics(modal_json)

# STAB-04, D-03: Handle truncated response
if modal_json.get("result") is None and "truncated_text" in modal_json:
    # D-04: 1 retry с max_tokens * 1.5
    logger.warning(
        f"Truncated response for chapter {idx + 1}, retrying with higher max_tokens",
        chapter_id=str(chapter_id),
        truncated_preview=modal_json["truncated_text"][:100],
    )
    # Retry: создать новый extractor call (или тот же, max_tokens на Modal стороне фиксирован)
    # Так как max_tokens задан в Modal через SamplingParams(max_tokens=32768), retry с тем же параметром
    # При повторном truncated — помечаем error_type='truncated'
    retry_json = await asyncio.wait_for(
        asyncio.to_thread(extractor.extract_chapter.remote,
            chapter_text=local_chapter.content,
            system_prompt=EXTRACTION_SYSTEM_PROMPT,
            schema_json=EXTRACTION_SCHEMA_JSON,
        ),
        timeout=VPS_TIMEOUT,
    )
    retry_metrics = extract_modal_metrics(retry_json)
    metrics = retry_metrics  # Обновить metrics на retry
    if retry_json.get("result") is None and "truncated_text" in retry_json:
        # D-04: Повторно truncated — пометить и продолжить
        local_chapter.error_type = ERROR_TYPE_TRUNCATED
        local_chapter.parsing_error = f"Truncated after retry: {retry_json['truncated_text'][:500]}"  # D-05
        local_chapter.parse_attempts += 1
        await session.commit()
        duration_ms = int((time.monotonic() - chapter_start) * 1000)
        _log_chapter_result(
            chapter_id=str(chapter_id), book_id=str(book_id),
            duration_ms=duration_ms, result_type="error",
            error_type=ERROR_TYPE_TRUNCATED, metrics=metrics,
        )
        return  # Продолжить к следующей главе
    modal_json = retry_json  # Retry успешен — продолжить нормально

result = modal_response_to_chapter_result(modal_json)
# Сохраняем raw descriptions для image_prompt_en
result_data = extract_modal_result(modal_json)
modal_raw_descriptions = {
    d.get("content", ""): d.get("image_prompt_en", "")
    for d in result_data.get("descriptions", [])
    if d.get("image_prompt_en")
}
```

**2d. В except Exception (строки 767-786) — добавить ErrorClassifier и structured logging:**
```python
except Exception as e:
    error_type = classify_error(e)
    logger.opt(exception=True).error(f"Error parsing chapter {idx + 1}: {e}")
    try:
        await session.rollback()
        if local_chapter:
            local_chapter = await session.get(Chapter, chapter_id)
            if local_chapter:
                local_chapter.parsing_error = str(e)[:1000]
                local_chapter.error_type = error_type  # OBS-01
                local_chapter.parse_attempts += 1
                await session.commit()
    except Exception as commit_err:
        # ... existing error handling ...
    duration_ms = int((time.monotonic() - chapter_start) * 1000) if 'chapter_start' in dir() else 0
    _log_chapter_result(
        chapter_id=str(chapter_id), book_id=str(book_id),
        duration_ms=duration_ms, result_type="error",
        error_type=error_type, metrics=metrics if 'metrics' in dir() else None,
    )
```

**2e. В except BaseException (строки 791-798) — добавить ErrorClassifier для InputCancellation (Pitfall 1):**
```python
except BaseException as fatal_err:
    error_type = classify_error(fatal_err)
    logger.opt(exception=True).error(
        f"Fatal error in chapter {idx + 1} processing: "
        f"{type(fatal_err).__name__}: {fatal_err}"
    )
    # Попытка сохранить error_type (может не получиться если session broken)
    try:
        async with AsyncSessionLocal() as emergency_session:
            ch = await emergency_session.get(Chapter, chapter_id)
            if ch:
                ch.error_type = error_type
                ch.parsing_error = f"{type(fatal_err).__name__}: {str(fatal_err)[:500]}"
                ch.parse_attempts += 1
                await emergency_session.commit()
    except Exception:
        pass  # Best effort
```

**2f. После успешной обработки главы (строки 734-747) — добавить success structured log:**
После `await session.commit()` (строка 742) и перед progress update:
```python
duration_ms = int((time.monotonic() - chapter_start) * 1000)
_log_chapter_result(
    chapter_id=str(chapter_id), book_id=str(book_id),
    duration_ms=duration_ms, result_type="success",
    error_type=None, metrics=metrics if 'metrics' in dir() else None,
)
```

**Инициализация переменных:** В начале `process_chapter_safe` (после получения chapter):
```python
chapter_start = time.monotonic()
metrics: dict | None = None
```

**3. Создать `backend/tests/tasks/test_chapter_logging.py`:**

Тесты structured logging helper и modal_client обновлений:
- `test_log_chapter_result_success` — проверить что logger.bind вызван с 9 полями
- `test_log_chapter_result_error` — error_type передан
- `test_extract_modal_result_new_format` — {"result": {...}} извлекает result
- `test_extract_modal_result_old_format` — {"entities": [...]} возвращает весь dict
- `test_extract_modal_metrics_present` — {"metrics": {...}} извлекает metrics
- `test_extract_modal_metrics_absent` — {} при отсутствии metrics
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/backend && uv run python -m pytest tests/tasks/test_chapter_logging.py tests/core/test_error_classifier.py tests/services/test_modal_client.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/tasks/book_tasks.py содержит `from app.core.error_classifier import classify_error`
    - backend/app/tasks/book_tasks.py содержит `def _log_chapter_result(`
    - backend/app/tasks/book_tasks.py содержит `logger.bind(` с chapter_id, book_id, duration_ms, result_type, error_type, finish_reason, cold_start_ms, inference_ms, is_cold_start
    - backend/app/tasks/book_tasks.py содержит `local_chapter.error_type = error_type` в except Exception блоке
    - backend/app/tasks/book_tasks.py содержит `local_chapter.error_type = error_type` или `ch.error_type = error_type` в except BaseException блоке
    - backend/app/tasks/book_tasks.py содержит `ERROR_TYPE_TRUNCATED` для обработки truncated response
    - backend/app/services/modal_client.py содержит `def extract_modal_metrics(`
    - backend/app/services/modal_client.py содержит `def extract_modal_result(`
    - backend/app/services/modal_client.py modal_response_to_chapter_result использует `extract_modal_result`
    - backend/tests/tasks/test_chapter_logging.py существует и содержит >= 6 тестов
    - Все тесты test_chapter_logging.py, test_error_classifier.py, test_modal_client.py проходят
    - `cd backend && uv run python -m pytest -v --tb=short` — full suite green (кроме pre-existing failures)
  </acceptance_criteria>
  <done>ErrorClassifier интегрирован в chapter loop (оба except блока), structured logging с 9 полями, truncated retry с D-04 логикой, modal_client backward compatible. Все тесты GREEN.</done>
</task>

</tasks>

<verification>
- `cd backend && uv run python -m pytest tests/core/test_error_classifier.py tests/tasks/test_chapter_logging.py tests/services/test_modal_client.py -x -v` — все тесты проходят
- `grep -n "classify_error" backend/app/tasks/book_tasks.py` — вызывается в обоих except блоках
- `grep -n "error_type" backend/app/models/chapter.py` — колонка добавлена
- `grep -n "chapter_processed" backend/app/tasks/book_tasks.py` — structured log message
- `grep -c "logger.bind" backend/app/tasks/book_tasks.py` — минимум 2 вызова (success + error)
- `grep -n "extract_modal_result\|extract_modal_metrics" backend/app/services/modal_client.py` — обе функции
- `cd backend && uv run python -m pytest -v --tb=short 2>&1 | tail -5` — full suite
</verification>

<success_criteria>
1. classify_error() корректно маппит все 5 типов ошибок (OBS-01)
2. error_type колонка добавлена в Chapter и Alembic migration создана
3. error_type сохраняется при ошибках обработки (оба except блока)
4. Truncated response от Modal обрабатывается с 1 retry (STAB-04, D-04)
5. Per-chapter structured log с 9 полями через logger.bind() (OBS-02, D-06, D-07)
6. modal_response_to_chapter_result() backward compatible с новым и старым форматом (D-08)
7. Все новые и существующие тесты проходят
</success_criteria>

<output>
After completion, create `.planning/phases/36-error-classification-observability/36-02-SUMMARY.md`
</output>
