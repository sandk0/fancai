---
phase: 37-sub-batch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modal/config.py
  - modal/app.py
  - modal/llm_extractor.py
  - backend/app/services/batch_grouping.py
  - backend/tests/tasks/test_batch_grouping.py
  - backend/tests/tasks/test_batch_extraction.py
  - backend/tests/tasks/test_modal_compile_cache.py
autonomous: true
requirements: [BATCH-01, BATCH-02, BATCH-03]

must_haves:
  truths:
    - "Oversized главы (>32K estimated tokens) НЕ попадают в batch -- маршрутизируются отдельно"
    - "extract_chapters_batch() в LLMExtractor обрабатывает список глав за один GPU pass и возвращает per-item результаты с metrics"
    - "Compile cache volumes смонтированы в Modal app -- повторные cold start быстрее на 20-30s"
    - "Token estimation len(text)//2 корректно роутит главы по размеру"
    - "Группировка последовательная: 2-12 глав в batch, бюджет 128K tokens"
  artifacts:
    - path: "modal/config.py"
      provides: "Константы OVERSIZED_THRESHOLD_TOKENS, BATCH_TOKEN_BUDGET, BATCH_MAX_CHAPTERS, BATCH_MIN_CHAPTERS, обновлённый LLM_TIMEOUT"
      contains: "OVERSIZED_THRESHOLD_TOKENS = 32000"
    - path: "modal/app.py"
      provides: "compile_cache, triton_cache, nv_cache volumes в COMMON_CLS_KWARGS + TORCHINDUCTOR env vars"
      contains: "fancai-compile-cache"
    - path: "modal/llm_extractor.py"
      provides: "extract_chapters_batch() метод с per-item finish_reason проверкой и batch_size в metrics"
      contains: "def extract_chapters_batch"
    - path: "backend/app/services/batch_grouping.py"
      provides: "estimate_tokens(), group_chapters_into_batches() -- чистые функции для VPS-side группировки"
      contains: "def group_chapters_into_batches"
    - path: "backend/tests/tasks/test_batch_grouping.py"
      provides: "Тесты token estimation, oversized routing, batch grouping"
      contains: "test_estimate_tokens"
    - path: "backend/tests/tasks/test_batch_extraction.py"
      provides: "Тесты extract_chapters_batch mock, partial truncation"
      contains: "test_partial_truncation"
    - path: "backend/tests/tasks/test_modal_compile_cache.py"
      provides: "Тесты конфигурации compile cache volumes"
      contains: "test_compile_cache"
  key_links:
    - from: "backend/app/services/batch_grouping.py"
      to: "modal/config.py"
      via: "Импорт OVERSIZED_THRESHOLD_TOKENS для роутинга"
      pattern: "OVERSIZED_THRESHOLD_TOKENS"
    - from: "modal/llm_extractor.py"
      to: "modal/config.py"
      via: "Импорт LLM_TIMEOUT"
      pattern: "from config import"
---

<objective>
Modal batch-инфраструктура + VPS-side группировка глав

Создать `extract_chapters_batch()` в Modal LLMExtractor для обработки нескольких глав за один GPU pass через vLLM native batch chat API. Добавить compile cache volumes для ускорения cold start. Реализовать VPS-side функции группировки глав по token budget с pre-validation oversized глав.

Purpose: Фундамент для batch-обработки -- Modal-side метод + чистые функции группировки. Plan 02 (orchestration) зависит от этих артефактов.
Output: Новый Modal метод, обновлённые config/app, модуль batch_grouping, Wave 0 тесты.
</objective>

<execution_context>
@/Users/sandk/Documents/GitHub/fancai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/Documents/GitHub/fancai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-sub-batch/37-CONTEXT.md
@.planning/phases/37-sub-batch/37-RESEARCH.md
@.planning/phases/36-error-classification-observability/36-02-SUMMARY.md

<interfaces>
<!-- Существующие интерфейсы, которые executor должен знать -->

Из modal/config.py (текущее состояние):
```python
LLM_MODEL_ID = "Qwen/Qwen3.5-9B"
VOLUME_PATH = "/models"
VOLUME_NAME = "fancai-models"
LLM_GPU = "L40S"
MAX_MODEL_LEN = 65536
GPU_MEMORY_UTILIZATION = 0.90
KV_CACHE_DTYPE = "fp8"
NUM_GPU_BLOCKS_OVERRIDE = 512
LLM_TIMEOUT = 900
VPS_TIMEOUT_BUFFER = 60
SCALEDOWN_WINDOW = 120
```

Из modal/app.py (текущее состояние):
```python
model_volume = modal.Volume.from_name(VOLUME_NAME, create_if_missing=True)
COMMON_CLS_KWARGS = dict(
    volumes={VOLUME_PATH: model_volume},
    scaledown_window=SCALEDOWN_WINDOW,
    enable_memory_snapshot=True,
    experimental_options={"enable_gpu_snapshot": True},
)
```

Из modal/llm_extractor.py (контракт extract_chapter):
```python
@modal.method()
def extract_chapter(self, chapter_text: str, system_prompt: str, schema_json: str) -> dict:
    """Returns: {"result": parsed_json | None, "metrics": {...}, "truncated_text"?: str}"""
```

Из backend/app/core/error_classifier.py:
```python
ERROR_TYPE_TIMEOUT = "timeout"
ERROR_TYPE_JSON_ERROR = "json_error"
ERROR_TYPE_MODAL_ERROR = "modal_error"
ERROR_TYPE_CANCELLED = "cancelled"
ERROR_TYPE_TRUNCATED = "truncated"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 тесты + batch_grouping модуль + Modal config/app обновления</name>
  <files>
    backend/tests/tasks/test_batch_grouping.py,
    backend/tests/tasks/test_batch_extraction.py,
    backend/tests/tasks/test_modal_compile_cache.py,
    backend/app/services/batch_grouping.py,
    modal/config.py,
    modal/app.py
  </files>
  <read_first>
    modal/config.py,
    modal/app.py,
    modal/llm_extractor.py,
    backend/app/tasks/book_tasks.py (строки 1-50: импорты и константы),
    backend/tests/tasks/test_time_budget.py (паттерн существующих тестов)
  </read_first>
  <behavior>
    Тесты batch_grouping:
    - test_estimate_tokens: `estimate_tokens("а" * 100)` == 50 (len//2 для RU)
    - test_estimate_tokens_empty: `estimate_tokens("")` == 0
    - test_oversized_chapter_routed_to_sequential: глава с len >64000 chars (>32K tokens) попадает в oversized, НЕ в batches
    - test_normal_chapters_grouped: 6 глав по ~10K tokens -> 1 batch (сумма < 128K budget)
    - test_budget_splits_batches: главы суммарно >128K tokens -> 2+ batch
    - test_max_chapters_per_batch: 15 глав по 1K tokens -> split при 12 (BATCH_MAX_CHAPTERS)
    - test_small_remainder_goes_to_oversized: остаток из 1 главы (< BATCH_MIN_CHAPTERS=2) -> oversized
    - test_sequential_order_preserved: главы 1-10 распределяются последовательно, не перемешиваются
    - test_all_oversized: все главы oversized -> batches пустой, все в oversized

    Тесты batch_extraction (мок):
    - test_extract_chapters_batch_returns_per_item: мок vLLM chat возвращает List[RequestOutput] -> per-item dict с result, metrics, chapter_idx
    - test_partial_truncation: один item finish_reason="length" -> result=None + truncated_text, остальные result=parsed
    - test_empty_chapters_returns_empty: пустой chapters -> []
    - test_batch_size_in_metrics: metrics содержит batch_size == len(chapters)

    Тесты compile cache:
    - test_compile_cache_volume_names: проверка строковых имён volumes в app.py
    - test_torchinductor_env_vars: проверка TORCHINDUCTOR_CACHE_DIR и TORCHINDUCTOR_FX_GRAPH_CACHE в llm_image.env
  </behavior>
  <action>
    1. Создать `backend/app/services/batch_grouping.py` с функциями (per D-01, D-02, D-03, D-04, D-05, D-06, D-07):

    ```python
    OVERSIZED_THRESHOLD_TOKENS = 32000   # D-07
    BATCH_TOKEN_BUDGET = 128000          # D-03
    BATCH_MAX_CHAPTERS = 12             # D-04
    BATCH_MIN_CHAPTERS = 2              # D-04

    def estimate_tokens(text: str) -> int:
        """Token estimation: len(text) // 2 для русского текста (D-02)."""
        return len(text) // 2

    def group_chapters_into_batches(chapters: list) -> tuple[list[list], list]:
        """Группировка глав в sub-batches + выделение oversized (D-01, D-05).

        Последовательная группировка: главы набираются в batch пока сумма estimated tokens < BATCH_TOKEN_BUDGET
        и len(batch) < BATCH_MAX_CHAPTERS. Oversized (>OVERSIZED_THRESHOLD_TOKENS) -> отдельный список.
        Остаток < BATCH_MIN_CHAPTERS -> переносится в oversized.

        Args:
            chapters: список объектов с атрибутом `content` (строка текста главы).

        Returns:
            (batches: list[list[chapter]], oversized: list[chapter])
        """
    ```

    Алгоритм группировки (per D-05):
    - Итерировать по chapters последовательно
    - Для каждой главы: estimate_tokens(ch.content or "")
    - Если tokens > OVERSIZED_THRESHOLD_TOKENS -> oversized.append(ch), continue
    - Если current_tokens + tokens > BATCH_TOKEN_BUDGET или len(current_batch) >= BATCH_MAX_CHAPTERS -> финализировать текущий batch (если >= BATCH_MIN_CHAPTERS -> batches, иначе -> oversized), начать новый
    - Добавить ch в current_batch, current_tokens += tokens
    - Финальный batch: если >= BATCH_MIN_CHAPTERS -> batches, иначе -> oversized

    2. Создать тестовые файлы (Wave 0):

    `backend/tests/tasks/test_batch_grouping.py` -- unit тесты для estimate_tokens и group_chapters_into_batches. Использовать простой dataclass-стаб:
    ```python
    @dataclass
    class FakeChapter:
        content: str | None = None
    ```

    `backend/tests/tasks/test_batch_extraction.py` -- тесты с мок vLLM RequestOutput. Тестировать extract_chapters_batch логику БЕЗ реального GPU -- мокировать `self.llm.chat()` возвращая List[MockRequestOutput]. Каждый MockRequestOutput имеет `.outputs[0].text` (JSON string) и `.outputs[0].finish_reason` ("stop" или "length").

    `backend/tests/tasks/test_modal_compile_cache.py` -- проверка строковых имён volumes и env vars в modal/app.py через import и inspection:
    ```python
    def test_compile_cache_volume_defined():
        """Verify compile_cache volume variable exists in app module."""
        # Read modal/app.py as text and check for 'fancai-compile-cache'
        app_py = Path("modal/app.py").read_text()
        assert "fancai-compile-cache" in app_py

    def test_torchinductor_env_in_image():
        """Verify TORCHINDUCTOR_CACHE_DIR env var set in llm_image."""
        app_py = Path("modal/app.py").read_text()
        assert "TORCHINDUCTOR_CACHE_DIR" in app_py
        assert "TORCHINDUCTOR_FX_GRAPH_CACHE" in app_py
    ```

    3. Обновить `modal/config.py` -- добавить константы (per D-03, D-04, D-07, D-15, D-16):
    ```python
    # Sub-batch (Phase 37)
    OVERSIZED_THRESHOLD_TOKENS = 32000   # D-07: глава > 32K tokens -> sequential
    BATCH_TOKEN_BUDGET = 128000          # D-03: max_model_len/2
    BATCH_MAX_CHAPTERS = 12              # D-04: максимум глав в batch
    BATCH_MIN_CHAPTERS = 2               # D-04: минимум для batch
    ```
    Обновить LLM_TIMEOUT:
    ```python
    LLM_TIMEOUT = 1800  # D-16: 30 мин -- Modal safety net для batch
    ```

    4. Обновить `modal/app.py` -- добавить compile cache volumes (per D-13, D-14):
    ```python
    compile_cache = modal.Volume.from_name("fancai-compile-cache", create_if_missing=True)
    triton_cache = modal.Volume.from_name("fancai-triton-cache", create_if_missing=True)
    nv_cache = modal.Volume.from_name("fancai-nv-cache", create_if_missing=True)
    ```
    Обновить llm_image -- добавить env vars:
    ```python
    llm_image = (
        modal.Image.from_registry("nvidia/cuda:12.8.1-devel-ubuntu22.04", add_python="3.12")
        .pip_install("vllm>=0.18.0", "pydantic>=2.0")
        .add_local_dir(_modal_src, remote_path="/root")
        .env({
            "TORCHINDUCTOR_CACHE_DIR": "/root/.inductor-cache",
            "TORCHINDUCTOR_FX_GRAPH_CACHE": "1",
        })
    )
    ```
    Обновить COMMON_CLS_KWARGS -- добавить 3 volumes:
    ```python
    COMMON_CLS_KWARGS = dict(
        volumes={
            VOLUME_PATH: model_volume,
            "/root/.inductor-cache": compile_cache,
            "/root/.triton": triton_cache,
            "/root/.nv": nv_cache,
        },
        scaledown_window=SCALEDOWN_WINDOW,
        enable_memory_snapshot=True,
        experimental_options={"enable_gpu_snapshot": True},
    )
    ```
  </action>
  <verify>
    <automated>cd backend && uv run python -m pytest tests/tasks/test_batch_grouping.py tests/tasks/test_modal_compile_cache.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/services/batch_grouping.py содержит `def estimate_tokens(text: str) -> int`
    - backend/app/services/batch_grouping.py содержит `def group_chapters_into_batches(`
    - backend/app/services/batch_grouping.py содержит `OVERSIZED_THRESHOLD_TOKENS = 32000`
    - backend/app/services/batch_grouping.py содержит `BATCH_TOKEN_BUDGET = 128000`
    - backend/app/services/batch_grouping.py содержит `BATCH_MAX_CHAPTERS = 12`
    - backend/app/services/batch_grouping.py содержит `BATCH_MIN_CHAPTERS = 2`
    - modal/config.py содержит `LLM_TIMEOUT = 1800`
    - modal/config.py содержит `OVERSIZED_THRESHOLD_TOKENS = 32000`
    - modal/config.py содержит `BATCH_TOKEN_BUDGET = 128000`
    - modal/app.py содержит `fancai-compile-cache`
    - modal/app.py содержит `fancai-triton-cache`
    - modal/app.py содержит `fancai-nv-cache`
    - modal/app.py содержит `TORCHINDUCTOR_CACHE_DIR`
    - modal/app.py содержит `TORCHINDUCTOR_FX_GRAPH_CACHE`
    - modal/app.py содержит `/root/.inductor-cache`
    - tests/tasks/test_batch_grouping.py содержит `test_estimate_tokens`
    - tests/tasks/test_batch_grouping.py содержит `test_oversized_chapter`
    - tests/tasks/test_modal_compile_cache.py содержит `test_compile_cache`
    - `cd backend && uv run python -m pytest tests/tasks/test_batch_grouping.py tests/tasks/test_modal_compile_cache.py -x -v` exits 0
  </acceptance_criteria>
  <done>
    batch_grouping.py создан с estimate_tokens() и group_chapters_into_batches(). modal/config.py обновлён с batch-константами и LLM_TIMEOUT=1800. modal/app.py обновлён с compile cache volumes и TORCHINDUCTOR env vars. Все Wave 0 тесты группировки и compile cache проходят.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extract_chapters_batch() в Modal LLMExtractor + тесты</name>
  <files>
    modal/llm_extractor.py,
    backend/tests/tasks/test_batch_extraction.py
  </files>
  <read_first>
    modal/llm_extractor.py,
    modal/config.py,
    modal/app.py,
    backend/tests/tasks/test_batch_extraction.py (Wave 0 стабы из Task 1)
  </read_first>
  <behavior>
    - test_extract_chapters_batch_returns_per_item: 3 главы -> 3 результата, каждый с chapter_idx, result, metrics
    - test_partial_truncation: 3 главы, одна finish_reason="length" -> result=None + truncated_text[:500]
    - test_empty_chapters_returns_empty: [] -> []
    - test_batch_size_in_metrics: metrics.batch_size == len(chapters)
    - test_cold_start_tracking: первый вызов is_cold_start=True, второй False
  </behavior>
  <action>
    Добавить `extract_chapters_batch()` в класс `LLMExtractor` в `modal/llm_extractor.py` (per D-08, D-09, D-10, D-11):

    ```python
    @modal.method()
    def extract_chapters_batch(
        self,
        chapters: list[dict],  # [{text, system_prompt, schema_json}]
    ) -> list[dict]:
        """Batch extraction: несколько глав за один GPU pass (D-08, D-10).

        vLLM continuous batching обрабатывает все главы параллельно.
        Общие SamplingParams для всех глав в batch (D-09).
        Per-item обработка результатов с finish_reason проверкой (D-11).

        Args:
            chapters: список dict с полями:
                - text: str -- текст главы
                - system_prompt: str -- system prompt (одинаковый для всех)
                - schema_json: str -- JSON schema (одинаковый для всех)

        Returns:
            list[dict]: для каждой главы:
                - chapter_idx: int -- индекс в исходном списке
                - result: dict | None -- распарсенный JSON или None при truncation
                - metrics: dict -- cold_start_ms, inference_ms, finish_reason, is_cold_start, batch_size
                - truncated_text: str | None -- первые 500 символов при truncation
        """
        from vllm import SamplingParams
        from vllm.sampling_params import StructuredOutputsParams

        if not chapters:
            return []

        inference_start = time.monotonic()

        # D-09: Общие params для всех глав
        params = SamplingParams(
            max_tokens=32768,
            temperature=0.1,
            structured_outputs=StructuredOutputsParams(
                json=chapters[0]["schema_json"]
            ),
        )

        # D-08: Build messages list для vLLM batch chat
        messages_list = [
            [
                {"role": "system", "content": ch["system_prompt"]},
                {"role": "user", "content": f"<book_text>{ch['text']}</book_text>"},
            ]
            for ch in chapters
        ]

        # Один вызов -- continuous batching vLLM
        request_outputs = self.llm.chat(messages_list, params)

        inference_ms = int((time.monotonic() - inference_start) * 1000)
        batch_results = []

        # D-11: Per-item обработка
        for idx, req_output in enumerate(request_outputs):
            output = req_output.outputs[0]
            item_metrics = {
                "cold_start_ms": self._cold_start_ms if self._is_first_call else 0,
                "inference_ms": inference_ms,
                "finish_reason": output.finish_reason,
                "is_cold_start": self._is_first_call,
                "batch_size": len(chapters),
            }

            if output.finish_reason == "length":
                batch_results.append({
                    "result": None,
                    "truncated_text": output.text[:500],
                    "metrics": item_metrics,
                    "chapter_idx": idx,
                })
            else:
                parsed = json.loads(output.text)
                batch_results.append({
                    "result": parsed,
                    "metrics": item_metrics,
                    "chapter_idx": idx,
                })

        self._is_first_call = False
        return batch_results
    ```

    Обновить тесты в `backend/tests/tasks/test_batch_extraction.py`:
    - Использовать мок LLMExtractor -- создать класс с `self.llm = Mock()`, `self._cold_start_ms = 500`, `self._is_first_call = True`
    - Мокировать `self.llm.chat()` -> возвращает `[MockRequestOutput, ...]`
    - MockRequestOutput: `.outputs[0].text` = json string, `.outputs[0].finish_reason` = "stop" | "length"
    - Вызывать метод напрямую БЕЗ Modal decorator (импортировать как обычную функцию)
    - Для import: скопировать логику extract_chapters_batch в тестовый хелпер (Modal код недоступен на VPS). Тестировать чистую логику обработки request_outputs, не Modal transport.

    ВАЖНО: Поскольку `modal/llm_extractor.py` работает на Modal container и не может быть импортирован на VPS (нет vLLM), тесты должны тестировать ЛОГИКУ через вынесенный хелпер или через мокирование. Рекомендуемый подход: вынести `_process_batch_outputs()` как чистую функцию для тестирования, а `extract_chapters_batch` вызывает её.
  </action>
  <verify>
    <automated>cd backend && uv run python -m pytest tests/tasks/test_batch_extraction.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - modal/llm_extractor.py содержит `def extract_chapters_batch(`
    - modal/llm_extractor.py содержит `@modal.method()` перед extract_chapters_batch
    - modal/llm_extractor.py содержит `messages_list = [` (batch message construction)
    - modal/llm_extractor.py содержит `self.llm.chat(messages_list, params)` (vLLM batch call)
    - modal/llm_extractor.py содержит `"batch_size": len(chapters)` в metrics
    - modal/llm_extractor.py содержит `"chapter_idx": idx` в результатах
    - modal/llm_extractor.py содержит `output.finish_reason == "length"` (per-item truncation check)
    - tests/tasks/test_batch_extraction.py содержит `test_extract_chapters_batch_returns_per_item`
    - tests/tasks/test_batch_extraction.py содержит `test_partial_truncation`
    - `cd backend && uv run python -m pytest tests/tasks/test_batch_extraction.py -x -v` exits 0
  </acceptance_criteria>
  <done>
    extract_chapters_batch() добавлен в LLMExtractor с per-item finish_reason проверкой, batch_size в metrics, chapter_idx маппингом. Тесты мокированного batch extraction проходят.
  </done>
</task>

</tasks>

<verification>
```bash
# Все Wave 0 тесты + grouping + extraction + compile cache
cd backend && uv run python -m pytest tests/tasks/test_batch_grouping.py tests/tasks/test_batch_extraction.py tests/tasks/test_modal_compile_cache.py -x -v

# Полный тест-suite не сломан
cd backend && uv run python -m pytest -v --tb=short
```
</verification>

<success_criteria>
- batch_grouping.py создан с корректной группировкой (9+ тестов проходят)
- extract_chapters_batch() добавлен в LLMExtractor (4+ тестов проходят)
- modal/config.py обновлён: LLM_TIMEOUT=1800, batch константы
- modal/app.py обновлён: 3 compile cache volumes, TORCHINDUCTOR env vars
- Полный backend test suite проходит без регрессий
</success_criteria>

<output>
После завершения создать `.planning/phases/37-sub-batch/37-01-SUMMARY.md`
</output>
