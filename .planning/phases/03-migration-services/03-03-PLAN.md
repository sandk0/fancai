---
phase: 03-migration-services
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - backend/app/services/imagen_generator.py
  - backend/app/middleware/rate_limit.py
  - backend/app/core/config.py
  - backend/requirements.txt
  - backend/tests/services/test_imagen_generator.py
  - backend/tests/middleware/__init__.py
  - backend/tests/middleware/test_rate_limit.py
  - backend/app/routers/descriptions.py
  - backend/app/routers/images.py
  - backend/app/routers/books/processing.py
autonomous: true
requirements:
  - MIGR-04.1
  - MIGR-08

must_haves:
  truths:
    - "imagen_generator генерирует изображения через OpenRouter (FLUX.2 или доступную image-модель) вместо Google Imagen 4"
    - "Перевод промптов RU->EN выполняется через OpenRouter LLM (generate_text) вместо google-genai"
    - "google-genai SDK полностью удалён из requirements.txt и нигде не импортируется"
    - "Rate limiting применяется по user ID для авторизованных и по IP для анонимных пользователей"
    - "AI-эндпоинты (descriptions, images, processing) декорированы @rate_limit с AI presets"
    - "Промпты для генерации изображений содержат NSFW-защиту через суффикс 'SFW, safe for work' в переводе"
  artifacts:
    - path: "backend/app/services/imagen_generator.py"
      provides: "ImagenService мигрирован на OpenRouter — generate_image() вместо Google Imagen 4, translate через generate_text, NSFW-защита через промпт"
      contains: "get_openrouter_client"
    - path: "backend/app/middleware/rate_limit.py"
      provides: "Расширенный rate_limit декоратор с per-user ID идентификацией и AI presets"
      contains: "user_id"
    - path: "backend/tests/middleware/test_rate_limit.py"
      provides: "Тесты per-user rate limiting"
      min_lines: 40
  key_links:
    - from: "backend/app/services/imagen_generator.py"
      to: "backend/app/core/openrouter_client.py"
      via: "get_openrouter_client().generate_image() и generate_text() для перевода"
      pattern: "get_openrouter_client.*generate_image|generate_text"
    - from: "backend/app/middleware/rate_limit.py"
      to: "request.state или current_user"
      via: "Извлечение user_id для идентификации"
      pattern: "user_id|current_user"
    - from: "backend/app/routers/descriptions.py"
      to: "backend/app/middleware/rate_limit.py"
      via: "@rate_limit декоратор с AI preset"
      pattern: "rate_limit.*ai_operation"
---

<objective>
Мигрировать генерацию изображений с Google Imagen 4 на OpenRouter image-модели, расширить rate limiting на per-user ID, применить rate limiting к AI-эндпоинтам, полностью удалить google-genai SDK.

Purpose: Завершить миграцию всех 5 AI-сервисов на OpenRouter. Удаление google-genai SDK устраняет vendor lock на Google. Per-user rate limiting защищает AI-эндпоинты от злоупотреблений.
Output: Мигрированный imagen_generator + расширенный rate limiter + rate limiting на роутерах + полное удаление google-genai
</objective>

<execution_context>
@/Users/sandk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-migration-services/03-CONTEXT.md
@.planning/phases/03-migration-services/03-RESEARCH.md
@.planning/phases/03-migration-services/03-01-SUMMARY.md
@.planning/phases/03-migration-services/03-02-SUMMARY.md

<interfaces>
<!-- Из openrouter_client.py (создан в Plan 01) -->
```python
from app.core.openrouter_client import OpenRouterClient, get_openrouter_client

class OpenRouterClient:
    async def generate_text(
        self, prompt: str, system_prompt: Optional[str] = None,
        temperature: float = 0.3, model: Optional[str] = None,
    ) -> str: ...

    async def generate_image(
        self, prompt: str, model: str = "...", size: str = "1024x768",
    ) -> bytes: ...
```

<!-- Из backend/app/middleware/rate_limit.py (расширить) -->
```python
class RateLimiter:
    async def is_rate_limited(self, identifier, endpoint, max_requests, window_seconds) -> tuple[bool, dict]: ...

def rate_limit(max_requests: int = 10, window_seconds: int = 60):
    """Декоратор. Сейчас определяет identifier через current_user.id или IP."""
```

<!-- Из backend/app/services/imagen_generator.py (мигрировать) -->
```python
class PromptTranslator:
    """Переводит промпты RU->EN через Gemini. Мигрировать на generate_text()"""
    _client = genai.Client(api_key=...)

class ImagenService:
    """Генерирует изображения через Google Imagen 4. Мигрировать на generate_image()"""
    _client = genai.Client(api_key=...)

    async def _generate_with_imagen(self, prompt, config):
        """Вызов Imagen 4 API — заменить на openrouter generate_image()"""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Мигрировать imagen_generator на OpenRouter и удалить google-genai</name>
  <files>backend/app/services/imagen_generator.py, backend/app/core/config.py, backend/tests/services/test_imagen_generator.py, backend/requirements.txt</files>
  <behavior>
    - Test: imagen_generator генерирует изображение через openrouter_client.generate_image() (mock)
    - Test: PromptTranslator переводит RU->EN через openrouter_client.generate_text() (mock)
    - Test: Переведённый промпт содержит суффикс "SFW, safe for work" для NSFW-защиты
    - Test: Файл НЕ содержит import google.genai
    - Test: google-genai отсутствует в requirements.txt
  </behavior>
  <action>
1. ПЕРВЫМ ШАГОМ: Определить доступную image-модель на OpenRouter:
   ```bash
   python -c "
   import httpx, json
   resp = httpx.get('https://openrouter.ai/api/v1/models')
   models = resp.json()['data']
   image_models = [m['id'] for m in models if 'image' in str(m.get('architecture', {}))]
   print('Image models:', image_models[:10])
   "
   ```
   Обновить OPENROUTER_IMAGE_MODEL в config.py (сейчас пустая строка из Plan 01):
   - Если FLUX.2 Pro/Klein доступен — использовать его
   - Если нет — использовать любую доступную text-to-image модель
   - Если image-моделей нет совсем — реализовать fallback: использовать stability.ai напрямую или логировать warning и возвращать placeholder
   Решение зафиксировать в SUMMARY.

2. Мигрировать `PromptTranslator` в imagen_generator.py:
   - Убрать `from google import genai`, `from google.genai import types`
   - Заменить `self._client = genai.Client(api_key=...)` на `self._client = get_openrouter_client()`
   - Заменить вызов перевода: вместо `client.aio.models.generate_content(...)` использовать `await self._client.generate_text(prompt=..., system_prompt="Translate the following Russian text to English. Return ONLY the translation, no explanations. The result must be SFW (safe for work).", temperature=0.1)`
   - NSFW-защита: Добавить суффикс "SFW, safe for work" к переведённому промпту перед передачей в generate_image(). Это компенсирует отсутствие встроенного safety filter в FLUX (в отличие от Imagen 4). Конкретная реализация:
     ```python
     translated = await self._client.generate_text(prompt=..., system_prompt="...SFW...")
     safe_prompt = f"{translated}. SFW, safe for work, appropriate content"
     ```

3. Мигрировать `ImagenService`:
   - Убрать `from google import genai`, `from google.genai import types`
   - Заменить `self._client = genai.Client(api_key=...)` на `self._client = get_openrouter_client()`
   - Заменить `_generate_with_imagen()`:
     - Было: Google Imagen 4 API через genai
     - Стало: `await self._client.generate_image(prompt=safe_prompt, model=settings.OPENROUTER_IMAGE_MODEL, size=size)`
   - Обработка ответа: generate_image() возвращает bytes — сохранять напрямую (без декодирования base64 на стороне сервиса)
   - Сохранить логику: prompt translation -> SFW suffix -> generate -> cache -> save

4. Удалить google-genai из requirements.txt:
   - Убрать строку `google-genai==1.61.0` и комментарий
   - Финальная проверка: `grep -r "import google" backend/app/` должен вернуть пустой результат

5. Обновить `test_imagen_generator.py`:
   - Заменить моки google-genai на моки openrouter_client
   - Mock generate_image() -> bytes (PNG данные)
   - Mock generate_text() -> переведённый промпт
   - Проверить что переведённый промпт содержит "SFW" суффикс
   - Проверить что google.genai не импортируется

ВНИМАНИЕ: Если image-модели на OpenRouter недоступны (см. ловушку 7 из RESEARCH.md), реализовать fallback: использовать generate_text() для генерации описания изображения и placeholder, логировать warning. Или использовать другой API провайдер (stability.ai). Решение зафиксировать в SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/backend && python -m pytest tests/services/test_imagen_generator.py -x -v && ! grep -r "import google" app/services/ app/core/openrouter_client.py && ! grep "google-genai" requirements.txt</automated>
  </verify>
  <done>
    - imagen_generator.py использует openrouter_client (generate_image + generate_text для перевода)
    - NSFW-защита реализована через суффикс "SFW, safe for work" в промпте перевода и в финальном промпте
    - OPENROUTER_IMAGE_MODEL в config.py обновлён на реально доступную модель (или задокументирован fallback)
    - google-genai полностью удалён из requirements.txt
    - Нигде в backend/app/ нет import google.genai
    - Все 5 AI-сервисов мигрированы на OpenRouter
    - Тесты проходят
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Расширить rate limiting на per-user ID</name>
  <files>backend/app/middleware/rate_limit.py, backend/tests/middleware/__init__.py, backend/tests/middleware/test_rate_limit.py</files>
  <behavior>
    - Test: rate_limit декоратор использует user_id из current_user для авторизованных запросов
    - Test: rate_limit декоратор использует IP для неавторизованных запросов
    - Test: При превышении лимита возвращается 429 с Retry-After header
    - Test: Rate limit ключ содержит user:{id} для авторизованных
    - Test: AI presets ai_operation (10/min) и ai_image (5/min) существуют
  </behavior>
  <action>
1. Расширить `rate_limit()` декоратор в `backend/app/middleware/rate_limit.py`:
   - Текущая логика уже извлекает user_id из current_user — проверить что это работает корректно
   - Улучшить формат идентификатора: `f"user:{user_id}"` для авторизованных, `f"ip:{ip}"` для анонимных
   - Добавить JSON тело в 429 ответ (по решению пользователя — Claude's Discretion):
     ```python
     raise HTTPException(
         status_code=429,
         detail={
             "error": "rate_limit_exceeded",
             "message": "Слишком много запросов. Повторите позже.",
             "retry_after": rate_info["reset_in_seconds"],
         },
         headers={"Retry-After": str(rate_info["reset_in_seconds"])},
     )
     ```
   - Добавить preset для AI-эндпоинтов в RATE_LIMIT_PRESETS:
     ```python
     "ai_operation": {"max_requests": 10, "window_seconds": 60},  # 10/min для AI
     "ai_image": {"max_requests": 5, "window_seconds": 60},       # 5/min для генерации изображений
     ```

2. Создать `backend/tests/middleware/__init__.py` (пустой).

3. Написать тесты в `backend/tests/middleware/test_rate_limit.py`:
   - Mock RateLimiter с Redis
   - Тест: авторизованный пользователь использует user:{id} как ключ
   - Тест: анонимный использует ip:{host}
   - Тест: превышение лимита возвращает 429
   - Тест: preset "ai_operation" существует с корректными значениями
   - Тест: preset "ai_image" существует с корректными значениями
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/backend && python -m pytest tests/middleware/test_rate_limit.py -x -v</automated>
  </verify>
  <done>
    - rate_limit декоратор корректно определяет identifier: user_id для авторизованных, IP для анонимных
    - 429 ответ содержит JSON с error, message, retry_after и Retry-After header
    - AI presets добавлены: ai_operation (10/min), ai_image (5/min)
    - Все тесты проходят
  </done>
</task>

<task type="auto">
  <name>Task 3: Применить rate limiting к AI-эндпоинтам</name>
  <files>backend/app/routers/descriptions.py, backend/app/routers/images.py, backend/app/routers/books/processing.py</files>
  <action>
1. Добавить `@rate_limit` декоратор к AI-эндпоинтам в трёх роутерах:
   - `backend/app/routers/descriptions.py` — найти эндпоинты извлечения описаний, добавить `@rate_limit(**RATE_LIMIT_PRESETS["ai_operation"])`
   - `backend/app/routers/images.py` — найти эндпоинты генерации изображений, добавить `@rate_limit(**RATE_LIMIT_PRESETS["ai_image"])`
   - `backend/app/routers/books/processing.py` — найти эндпоинты обработки книг, добавить `@rate_limit(**RATE_LIMIT_PRESETS["ai_operation"])`

2. В каждом файле добавить импорт:
   ```python
   from app.middleware.rate_limit import rate_limit, RATE_LIMIT_PRESETS
   ```

3. Декоратор @rate_limit ставить ПЕРЕД декоратором @router (ближе к функции), чтобы rate limit проверялся до выполнения основной логики.

ВАЖНО: Не менять логику самих эндпоинтов — только добавить декоратор и импорт.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/backend && grep -l "rate_limit" app/routers/descriptions.py app/routers/images.py app/routers/books/processing.py && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - @rate_limit применён к AI-эндпоинтам: descriptions (ai_operation), images (ai_image), processing (ai_operation)
    - Импорт rate_limit и RATE_LIMIT_PRESETS добавлен во все три файла
    - Существующая логика эндпоинтов не изменена
  </done>
</task>

</tasks>

<verification>
```bash
# Полная проверка Plan 03
cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/backend

# 1. Все тесты плана проходят
python -m pytest tests/services/test_imagen_generator.py tests/middleware/test_rate_limit.py -x -v

# 2. google-genai полностью удалён
! grep -r "import google" app/services/ app/core/
! grep "google-genai" requirements.txt

# 3. Все 5 AI-сервисов используют openrouter_client
grep -l "get_openrouter_client\|openrouter_client" app/services/entity_synthesis_service.py app/services/consistency_manager.py app/services/gemini_extractor.py app/services/entity_deduplication_service.py app/services/imagen_generator.py

# 4. Rate limit presets содержат AI-операции
grep "ai_operation\|ai_image" app/middleware/rate_limit.py

# 5. Rate limit применён к роутерам
grep "rate_limit" app/routers/descriptions.py app/routers/images.py app/routers/books/processing.py

# 6. NSFW-защита в imagen_generator
grep -i "sfw\|safe.for.work" app/services/imagen_generator.py

# 7. Полный набор тестов фазы
python -m pytest tests/core/test_openrouter_client.py tests/services/test_entity_synthesis.py tests/services/test_consistency_manager.py tests/services/test_gemini_extractor.py tests/services/test_entity_deduplication.py tests/services/test_imagen_generator.py tests/middleware/test_rate_limit.py -x -v
```
</verification>

<success_criteria>
- imagen_generator полностью мигрирован с Google Imagen 4 на OpenRouter image-модели
- NSFW-защита реализована через "SFW, safe for work" суффикс в промптах
- google-genai SDK полностью удалён из проекта (requirements.txt + все импорты)
- Rate limiting расширен на per-user ID идентификацию
- AI-эндпоинты декорированы rate limiting (10/min для LLM, 5/min для images)
- Все 5 AI-сервисов работают через единый openrouter_client.py
- Все тесты проходят
</success_criteria>

<output>
После завершения создать `.planning/phases/03-migration-services/03-03-SUMMARY.md`
</output>
