---
phase: 03-migration-services
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/openrouter_client.py
  - backend/app/core/config.py
  - backend/app/services/entity_synthesis_service.py
  - backend/app/services/consistency_manager.py
  - backend/tests/core/test_openrouter_client.py
  - backend/tests/services/test_entity_synthesis.py
  - backend/tests/services/test_consistency_manager.py
autonomous: true
requirements:
  - MIGR-01
  - MIGR-02
  - MIGR-05

must_haves:
  truths:
    - "Пользователь может вызвать AI-функции (извлечение сущностей, проверка консистентности), и они работают через OpenRouter вместо google-genai"
    - "При недоступности основной модели (Gemini 3 Flash) запрос автоматически выполняется через Claude Haiku 4.5 или Gemini 2.5 Flash Lite — пользователь получает результат без ошибки"
    - "Невалидный JSON от LLM не вызывает бессмысленные повторы через fallback — ошибка парсинга пробрасывается вверх сразу"
  artifacts:
    - path: "backend/app/core/openrouter_client.py"
      provides: "Единый клиент OpenRouter API с fallback chain, _inline_defs, generate_text/generate_structured/generate_image"
      exports: ["OpenRouterClient", "get_openrouter_client", "_inline_defs", "FALLBACK_MODELS"]
    - path: "backend/tests/core/test_openrouter_client.py"
      provides: "Тесты fallback chain, _inline_defs, generate_text, generate_structured"
      min_lines: 80
    - path: "backend/tests/services/test_entity_synthesis.py"
      provides: "Тесты миграции entity_synthesis на OpenRouter"
      min_lines: 40
    - path: "backend/tests/services/test_consistency_manager.py"
      provides: "Тесты миграции consistency_manager на OpenRouter"
      min_lines: 40
  key_links:
    - from: "backend/app/services/entity_synthesis_service.py"
      to: "backend/app/core/openrouter_client.py"
      via: "get_openrouter_client().generate_text()"
      pattern: "get_openrouter_client.*generate_text"
    - from: "backend/app/services/consistency_manager.py"
      to: "backend/app/core/openrouter_client.py"
      via: "get_openrouter_client().generate_text()"
      pattern: "get_openrouter_client.*generate_text"
    - from: "backend/app/core/openrouter_client.py"
      to: "OpenRouter API"
      via: "httpx.AsyncClient POST /chat/completions с fallback chain"
      pattern: "FALLBACK_MODELS.*for.*current_model"
---

<objective>
Создать единый OpenRouter клиент с fallback chain и мигрировать два простых LLM-сервиса (entity_synthesis, consistency_manager) с google-genai SDK на OpenRouter API.

Purpose: Это фундамент всей миграции. Клиент openrouter_client.py будет использоваться всеми 5 AI-сервисами. Два простых сервиса (response_mime_type only) мигрируются первыми как валидация подхода.
Output: openrouter_client.py с тремя методами + два мигрированных сервиса + тесты
</objective>

<execution_context>
@/Users/sandk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-migration-services/03-CONTEXT.md
@.planning/phases/03-migration-services/03-RESEARCH.md

<interfaces>
<!-- Существующие типы и контракты, используемые в миграции -->

Из backend/app/core/config.py (нужно добавить OPENROUTER_API_KEY):
```python
class Settings(BaseSettings):
    # ... существующие поля
    GOOGLE_API_KEY: str = ""
    LANGEXTRACT_API_KEY: str = ""
    # ДОБАВИТЬ: OPENROUTER_API_KEY: str = ""
```

Из backend/app/core/retry.py (переиспользовать):
```python
def retry_llm_extraction(func):
    """Tenacity retry с exponential backoff для LLM вызовов"""

def retry_api_call(func):
    """Retry для общих API вызовов"""
```

Из backend/app/monitoring/metrics.py (переиспользовать):
```python
def record_llm_request(model: str, operation: str, duration: float): ...
def record_llm_error(model: str, operation: str, error_type: str): ...
def record_llm_rate_limit(model: str, operation: str): ...
```

Из backend/app/services/entity_synthesis_service.py (мигрировать):
```python
class EntitySynthesisService:
    async def _call_gemini(self, prompt: str) -> dict:
        # Использует google.genai.types, extractor._client
        # response_mime_type="application/json"
```

Из backend/app/services/consistency_manager.py (мигрировать):
```python
class ConsistencyManager:
    # Использует google.genai.types, extractor._client
    # response_mime_type="application/json"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать OpenRouter клиент и OPENROUTER_API_KEY конфиг</name>
  <files>backend/app/core/openrouter_client.py, backend/app/core/config.py, backend/tests/core/__init__.py, backend/tests/core/test_openrouter_client.py</files>
  <behavior>
    - Test: _inline_defs корректно разворачивает $defs/$ref в JSON Schema (Pydantic модель с вложенными типами)
    - Test: _inline_defs не ломает плоскую схему без $defs
    - Test: generate_text возвращает текст при успешном ответе API (mock httpx)
    - Test: generate_text при сбое первой модели переключается на fallback (mock 500 -> 200)
    - Test: generate_text при сбое всех моделей пробрасывает исключение
    - Test: generate_structured возвращает dict, соответствующий Pydantic модели (mock httpx)
    - Test: fallback НЕ срабатывает на json.JSONDecodeError (ошибка парсинга — не HTTP ошибка)
    - Test: fallback срабатывает на httpx.HTTPStatusError (5xx) и httpx.TimeoutException
  </behavior>
  <action>
1. Добавить `OPENROUTER_API_KEY: str = ""` в Settings в config.py (рядом с GOOGLE_API_KEY). Добавить `OPENROUTER_IMAGE_MODEL: str = ""` — пустая строка как placeholder; конкретная модель будет определена в Plan 03 Task 1 после проверки доступности на OpenRouter (RESEARCH.md подтвердил что black-forest-labs/flux-1.1-pro возвращает 404 на 2026-03-01).

2. Создать `backend/app/core/openrouter_client.py` по паттерну из RESEARCH.md:
   - Константы: OPENROUTER_BASE_URL, FALLBACK_MODELS (3 модели по решению пользователя)
   - Функция `_inline_defs(schema)` для рекурсивного разворачивания $defs/$ref
   - Класс `OpenRouterClient` с httpx.AsyncClient:
     - `generate_text()` — JSON mode через response_format={"type": "json_object"}, fallback chain
     - `generate_structured()` — JSON Schema через response_format с inlined schema, fallback chain
     - `generate_image()` — POST /images/generations, без fallback (одна модель)
   - ВАЖНО: В fallback loop ловить ТОЛЬКО httpx.HTTPStatusError и httpx.TimeoutException. json.JSONDecodeError и ValidationError — пробрасывать вверх без fallback (см. ловушку 5 из RESEARCH.md). НЕ использовать broad `except Exception` — это из RESEARCH.md code sample, план требует точных типов исключений.
   - Singleton через get_openrouter_client() с lazy init из settings
   - Интеграция с существующими метриками: вызывать record_llm_request/record_llm_error из monitoring/metrics.py
   - Логирование каждого переключения fallback с указанием причины (по решению пользователя)

3. Создать `backend/tests/core/__init__.py` (пустой).

4. Написать тесты в `backend/tests/core/test_openrouter_client.py`:
   - Mock httpx.AsyncClient через pytest monkeypatch или unittest.mock
   - Тесты _inline_defs: плоская схема, схема с $defs/$ref, вложенные $ref
   - Тесты generate_text: успех, fallback при 500, все модели недоступны
   - Тесты generate_structured: успех с Pydantic моделью, inlined schema корректна
   - Тесты fallback: НЕ срабатывает на JSONDecodeError, срабатывает на HTTPStatusError и TimeoutException
   - Тест: broad Exception (не HTTPStatusError/TimeoutException) пробрасывается вверх без fallback
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/backend && python -m pytest tests/core/test_openrouter_client.py -x -v</automated>
  </verify>
  <done>
    - openrouter_client.py создан с тремя методами и _inline_defs
    - OPENROUTER_API_KEY добавлен в config.py
    - OPENROUTER_IMAGE_MODEL добавлен как пустая строка (placeholder до Plan 03)
    - Все тесты проходят: fallback chain, _inline_defs, generate_text, generate_structured
    - Fallback ловит ТОЛЬКО httpx.HTTPStatusError и httpx.TimeoutException (не broad Exception)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Мигрировать entity_synthesis и consistency_manager на OpenRouter</name>
  <files>backend/app/services/entity_synthesis_service.py, backend/app/services/consistency_manager.py, backend/tests/services/test_entity_synthesis.py, backend/tests/services/test_consistency_manager.py</files>
  <behavior>
    - Test: entity_synthesis._call_gemini заменена на вызов openrouter_client.generate_text (mock)
    - Test: entity_synthesis обрабатывает ответ OpenRouter корректно (JSON parsing)
    - Test: consistency_manager LLM-вызов заменён на openrouter_client.generate_text (mock)
    - Test: consistency_manager обрабатывает ответ OpenRouter корректно
    - Test: Оба сервиса НЕ импортируют google.genai
  </behavior>
  <action>
1. Мигрировать `entity_synthesis_service.py`:
   - Заменить `import google.genai.types as types` и `extractor._client` на `from app.core.openrouter_client import get_openrouter_client`
   - Заменить метод `_call_gemini()`: вместо `client.aio.models.generate_content(config=types.GenerateContentConfig(response_mime_type="application/json"))` использовать `client.generate_text(prompt=prompt, system_prompt="Respond ONLY with valid JSON, no markdown.", temperature=0.3)`
   - Убрать зависимость от `extractor._client` (gemini_extractor) — сервис теперь получает свой клиент напрямую через get_openrouter_client()
   - Сохранить существующую логику `parse_json_safe()` после получения ответа
   - Убрать все import google.genai

2. Мигрировать `consistency_manager.py`:
   - Аналогично: заменить `import google.genai.types as types` и `extractor._client` на openrouter_client
   - Заменить вызов `client.aio.models.generate_content(config=types.GenerateContentConfig(response_mime_type="application/json"))` на `client.generate_text()`
   - Убрать зависимость от extractor._client
   - Сохранить существующую логику парсинга ответа
   - Убрать все import google.genai

3. Обновить/создать тесты `test_entity_synthesis.py`:
   - Mock get_openrouter_client() чтобы вернуть мок с generate_text
   - Проверить что _call_gemini (или её замена) вызывает generate_text с правильными аргументами
   - Проверить что google.genai не импортируется (через grep по файлу в assert)

4. Обновить/создать тесты `test_consistency_manager.py`:
   - Аналогичная стратегия мока
   - Проверить что вызывается generate_text, не google.genai

ВАЖНО: Не трогать gemini_extractor.py, entity_deduplication_service.py, imagen_generator.py — они мигрируются в Plan 02.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/backend && python -m pytest tests/services/test_entity_synthesis.py tests/services/test_consistency_manager.py tests/core/test_openrouter_client.py -x -v</automated>
  </verify>
  <done>
    - entity_synthesis_service.py вызывает openrouter_client.generate_text() вместо google.genai
    - consistency_manager.py вызывает openrouter_client.generate_text() вместо google.genai
    - Оба файла НЕ содержат import google.genai
    - Все тесты проходят (клиент + оба сервиса)
  </done>
</task>

</tasks>

<verification>
```bash
# Полная проверка Plan 01
cd /Users/sandk/Documents/GitHub/fancai-vibe-hackathon/backend

# 1. Все тесты плана проходят
python -m pytest tests/core/test_openrouter_client.py tests/services/test_entity_synthesis.py tests/services/test_consistency_manager.py -x -v

# 2. Нет import google.genai в мигрированных файлах
! grep -r "import google" app/services/entity_synthesis_service.py app/services/consistency_manager.py app/core/openrouter_client.py

# 3. OPENROUTER_API_KEY присутствует в config.py
grep "OPENROUTER_API_KEY" app/core/config.py

# 4. openrouter_client.py экспортирует нужные символы
python -c "from app.core.openrouter_client import OpenRouterClient, get_openrouter_client, _inline_defs, FALLBACK_MODELS; print('OK')"
```
</verification>

<success_criteria>
- OpenRouter клиент создан и полностью протестирован (fallback chain, _inline_defs, все три метода)
- entity_synthesis и consistency_manager мигрированы с google-genai на OpenRouter
- Fallback chain работает: HTTP ошибки -> следующая модель; ошибки парсинга -> пробрасываются вверх
- OPENROUTER_API_KEY добавлен в конфигурацию
- Все тесты проходят
</success_criteria>

<output>
После завершения создать `.planning/phases/03-migration-services/03-01-SUMMARY.md`
</output>
