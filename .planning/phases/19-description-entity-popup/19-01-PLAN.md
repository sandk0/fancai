---
phase: 19-description-entity-popup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Reader/DescriptionDrawer.tsx
  - frontend/src/components/Reader/EntityBottomSheet.tsx
  - frontend/src/components/Reader/EntityPopup.tsx
  - frontend/src/components/Reader/EpubReader.tsx
  - frontend/src/locales/ru/translation.json
  - frontend/src/locales/en/translation.json
  - frontend/src/components/Reader/__tests__/DescriptionDrawer.test.tsx
  - frontend/src/components/Reader/__tests__/EntityBottomSheet.test.tsx
autonomous: true
requirements:
  - ENT-01

must_haves:
  truths:
    - "Тап на описание открывает DescriptionDrawer с типом, текстом и кнопкой генерации/просмотра"
    - "Кнопка генерации видна ВСЕГДА: 'Сгенерировать' без изображения, 'Посмотреть' с изображением"
    - "Генерация запускается по кнопке в drawer, НЕ автоматически при открытии"
    - "Тап на сущность открывает EntityBottomSheet (Vaul) вместо floating popup"
    - "Кнопка 'Подробнее' в EntityBottomSheet закрывает sheet и открывает EntityDrawer"
    - "DescriptionDrawer и EntityBottomSheet закрываются при навигации (свайп/тап)"
  artifacts:
    - path: "frontend/src/components/Reader/DescriptionDrawer.tsx"
      provides: "Расширенный drawer с snap points [0.4, 0.8], useGenerateImage, превью"
      min_lines: 80
    - path: "frontend/src/components/Reader/EntityBottomSheet.tsx"
      provides: "Vaul bottom sheet для сущностей, snap points [0.3, 0.6]"
      min_lines: 50
    - path: "frontend/src/components/Reader/__tests__/DescriptionDrawer.test.tsx"
      provides: "Тесты DescriptionDrawer: кнопка генерации, превью, snap points"
    - path: "frontend/src/components/Reader/__tests__/EntityBottomSheet.test.tsx"
      provides: "Тесты EntityBottomSheet: рендеринг, кнопка Подробнее, truncation"
  key_links:
    - from: "frontend/src/components/Reader/EpubReader.tsx"
      to: "frontend/src/components/Reader/DescriptionDrawer.tsx"
      via: "bookId prop для useGenerateImage"
      pattern: "bookId.*book\\.id"
    - from: "frontend/src/components/Reader/EpubReader.tsx"
      to: "frontend/src/components/Reader/EntityBottomSheet.tsx"
      via: "замена EntityPopup на EntityBottomSheet (без position prop)"
      pattern: "EntityBottomSheet"
    - from: "frontend/src/components/Reader/EpubReader.tsx"
      to: "handlePanelDismiss"
      via: "isPanelOpen и handlePanelDismiss включают isDrawerOpen и popupEntity"
      pattern: "isDrawerOpen.*popupEntity"
---

<objective>
Расширить DescriptionDrawer (snap points, генерация изображений, превью) и заменить EntityPopup (floating card) на EntityBottomSheet (Vaul bottom sheet). Подключить оба компонента к panel dismiss flow.

Purpose: ENT-01 -- popup описания показывает кнопку генерации и полное содержимое; entity popup унифицирован как bottom sheet.
Output: Обновленный DescriptionDrawer, новый EntityBottomSheet, тесты, i18n ключи.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-description-entity-popup/19-CONTEXT.md
@.planning/phases/19-description-entity-popup/19-RESEARCH.md
</context>

<interfaces>
<!-- Существующие типы и контракты -->

From frontend/src/types/api.ts:
```typescript
export interface Description {
  id: string;
  content: string;
  text?: string;
  type: string; // location, character, atmosphere, object, action
  chapter_number: number;
  // ...
}

export interface GeneratedImage {
  id: string;
  description_id: string;
  image_url: string;
  status: string; // 'completed', 'pending', etc.
  // ...
}
```

From frontend/src/types/entity.ts:
```typescript
export interface EntityDetail {
  id: string;
  name: string;
  type: string; // character, location, object
  biography?: string;
  visual_summary_clean?: string;
  visual_summary?: string;
  avatar_url?: string;
  // ...
}
```

From frontend/src/hooks/api/useImages/useImageMutations.ts:
```typescript
export function useGenerateImage(options?): UseMutationResult<{
  image_id: string;
  description_id: string;
  image_url: string;
  generation_time: number;
  status: string;
  created_at: string;
  message: string;
}, Error, { descriptionId: string; bookId?: string; params?: Record<string, string> }>;
```

From frontend/src/components/Reader/DescriptionDrawer.tsx (текущий):
```typescript
interface DescriptionDrawerProps {
  description: Description | null;
  image?: GeneratedImage;
  isOpen: boolean;
  onClose: () => void;
  onOpenImage: (description: Description, image?: GeneratedImage) => void;
}
```

From frontend/src/components/Reader/EntityPopup.tsx (заменяемый):
```typescript
interface EntityPopupProps {
  entity: EntityDetail | null;
  position: { x: number; y: number } | null;
  onClose: () => void;
  onOpenDrawer: (entityId: string) => void;
}
```

From frontend/src/components/UI/MobilePanel.tsx (паттерн snap points):
```typescript
// Vaul Drawer.Root с snapPoints, activeSnapPoint, setActiveSnapPoint
// h-[95dvh] для Content
// useState + useEffect для sync activeSnap при open
```

From frontend/src/components/Reader/EpubReader.tsx (integration points):
```typescript
// Строка 77: const [isDrawerOpen, setIsDrawerOpen] = useState(false);
// Строка 312: const isPanelOpen = isTocOpen || isSettingsOpen || isEntityDrawerOpen || isSearchOpen;
// Строка 315-320: handlePanelDismiss -- НЕ включает isDrawerOpen и popupEntity
// Строка 505-508: popupEntity, popupPosition state
// Строка 521-527: handleEntityClick принимает (entity, position)
// Строка 726-731: <EntityPopup entity={popupEntity} position={popupPosition} .../>
// Строка 762-771: <DescriptionDrawer description={drawerDescription} image={drawerImage} .../>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DescriptionDrawer с генерацией изображений и EntityBottomSheet</name>
  <files>
    frontend/src/components/Reader/DescriptionDrawer.tsx,
    frontend/src/components/Reader/EntityBottomSheet.tsx,
    frontend/src/locales/ru/translation.json,
    frontend/src/locales/en/translation.json,
    frontend/src/components/Reader/__tests__/DescriptionDrawer.test.tsx,
    frontend/src/components/Reader/__tests__/EntityBottomSheet.test.tsx
  </files>
  <behavior>
    DescriptionDrawer:
    - Рендерит тип-badge на русском (i18n: reader.description_drawer.type.location и т.д.)
    - Рендерит полный текст описания
    - Показывает кнопку "Сгенерировать" когда нет изображения (t('reader.description_drawer.generate'))
    - Показывает кнопку "Посмотреть" когда есть изображение со status=completed (t('reader.description_drawer.view_image'))
    - Показывает спиннер при isPending мутации (t('reader.description_drawer.generating'))
    - После генерации показывает превью изображения (img с image_url)
    - Клик на превью вызывает onOpenImage
    - Snap points [0.4, 0.8], БЕЗ max-h-[60vh]

    EntityBottomSheet:
    - Рендерит имя сущности
    - Рендерит тип на русском (i18n: entities.types.character/location/object)
    - Показывает avatar_url или placeholder иконку по типу (User/MapPin/Package)
    - Truncated описание до 100 символов
    - Кнопка "Подробнее" вызывает onOpenDrawer(entityId) и onClose()
    - Snap points [0.3, 0.6]
    - Не рендерится когда entity === null
  </behavior>
  <action>
    1. Создать тесты:
       - `DescriptionDrawer.test.tsx`: мокнуть useGenerateImage, react-i18next, vaul. Тесты на: (a) рендеринг типа и текста, (b) кнопка "Сгенерировать" при отсутствии изображения, (c) кнопка "Посмотреть" при наличии completed image, (d) спиннер при isPending, (e) превью после генерации, (f) клик на превью вызывает onOpenImage, (g) не рендерится при null description.
       - `EntityBottomSheet.test.tsx`: мокнуть react-i18next, vaul. Тесты на: (a) рендеринг имени и типа, (b) truncation 100 символов, (c) кнопка "Подробнее" вызывает onOpenDrawer, (d) placeholder иконки по типу, (e) не рендерится при null entity.

    2. Переписать `DescriptionDrawer.tsx`:
       - Добавить props: bookId (string) для useGenerateImage
       - Заменить max-h-[60vh] на Vaul snap points [0.4, 0.8] с useState для activeSnapPoint (паттерн из MobilePanel)
       - Использовать h-[95dvh] на Drawer.Content
       - Тип-badge через i18n: `t('reader.description_drawer.type.${description.type}')`
       - Кнопка изображения ВСЕГДА видна:
         * image?.status === 'completed': "Посмотреть" -> onOpenImage
         * generateMutation.isPending: disabled спиннер (Loader2 animate-spin) + "Генерация..."
         * default: "Сгенерировать" -> generateMutation.mutate({ descriptionId: description.id, bookId })
       - После успешной генерации: превью img с generateMutation.data?.image_url, клик -> onOpenImage
       - Закрытие: onOpenChange -> onClose (как сейчас)
       - Использовать useGenerateImage из @/hooks/api/useImages/useImageMutations

    3. Создать `EntityBottomSheet.tsx` (НОВЫЙ файл):
       - Props: entity (EntityDetail | null), isOpen (boolean), onClose, onOpenDrawer (entityId: string) -> void
       - НЕ принимает position -- bottom sheet всегда снизу
       - Vaul Drawer.Root с snapPoints={[0.3, 0.6]}, activeSnapPoint через useState
       - Drawer.Content с h-[95dvh], handle bar, bg-[var(--color-bg-elevated)]
       - Layout: avatar/placeholder (User/MapPin/Package иконки) + name + type (i18n) + truncated description (100 chars)
       - Кнопка "Подробнее" (border-t) -> вызывает onOpenDrawer(entity.id) и onClose()
       - Drawer.Title sr-only для accessibility

    4. i18n: добавить ключи в ru/translation.json и en/translation.json:
       - reader.description_drawer.generate: "Сгенерировать" / "Generate"
       - reader.description_drawer.generating: "Генерация..." / "Generating..."
       - reader.description_drawer.view_image: "Посмотреть" / "View image"
       - reader.description_drawer.type.location: "Локация" / "Location"
       - reader.description_drawer.type.character: "Персонаж" / "Character"
       - reader.description_drawer.type.atmosphere: "Атмосфера" / "Atmosphere"
       - reader.description_drawer.type.object: "Объект" / "Object"
       - reader.description_drawer.type.action: "Действие" / "Action"
       - Проверить/добавить entities.types.character, entities.types.location, entities.types.object в ru.json
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npx vitest run src/components/Reader/__tests__/DescriptionDrawer.test.tsx src/components/Reader/__tests__/EntityBottomSheet.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - DescriptionDrawer с snap points [0.4, 0.8], кнопкой генерации, спиннером, превью, i18n типами
    - EntityBottomSheet с snap points [0.3, 0.6], именем, типом, описанием, кнопкой "Подробнее"
    - Все тесты проходят
  </done>
</task>

<task type="auto">
  <name>Task 2: Wiring в EpubReader и panel dismiss flow</name>
  <files>
    frontend/src/components/Reader/EpubReader.tsx
  </files>
  <action>
    1. Заменить импорт EntityPopup на EntityBottomSheet:
       - `import { EntityBottomSheet } from './EntityBottomSheet';`
       - Удалить `import { EntityPopup } from './EntityPopup';`

    2. Убрать popupPosition state (строка 508):
       - Удалить `const [popupPosition, setPopupPosition] = useState<{ x: number; y: number } | null>(null);`
       - handleEntityClick (строка 521-527): изменить сигнатуру -- больше НЕ принимает position, только entity:
         ```typescript
         const handleEntityClick = useCallback(
           (entity: EntityDetail) => {
             setPopupEntity(entity);
           },
           []
         );
         ```
       - handleEntityPopupClose (строка 550-553): убрать setPopupPosition(null)
       - handleEntityPopupOpenDrawer (строка 543-548): убрать setPopupPosition(null)

    3. Обновить useEntityNameHighlighting call (строка 534-541):
       - onEntityClick теперь передает только entity без position -- проверить что useEntityNameHighlighting передаёт оба аргумента, и если да, то обновить hook call site чтобы игнорировать второй аргумент:
         ```typescript
         onEntityClick: (entity, _position) => handleEntityClick(entity),
         ```

    4. Заменить EntityPopup на EntityBottomSheet в JSX (строка 726-731):
       ```tsx
       <EntityBottomSheet
         entity={popupEntity}
         isOpen={!!popupEntity}
         onClose={handleEntityPopupClose}
         onOpenDrawer={handleEntityPopupOpenDrawer}
       />
       ```

    5. Обновить DescriptionDrawer props (строка 762-771):
       - Добавить bookId={book.id}

    6. Расширить isPanelOpen (строка 312):
       ```typescript
       const isPanelOpen = isTocOpen || isSettingsOpen || isEntityDrawerOpen || isSearchOpen || isDrawerOpen || !!popupEntity;
       ```

    7. Расширить handlePanelDismiss (строки 315-320):
       ```typescript
       const handlePanelDismiss = useCallback(() => {
         setIsTocOpen(false);
         setIsSettingsOpen(false);
         setIsEntityDrawerOpen(false);
         setIsSearchOpen(false);
         setIsDrawerOpen(false);
         setPopupEntity(null);
       }, []);
       ```
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npx vitest run --reporter=verbose && npm run build</automated>
  </verify>
  <done>
    - EntityPopup заменен на EntityBottomSheet в EpubReader
    - popupPosition state удален
    - isPanelOpen включает isDrawerOpen и popupEntity
    - handlePanelDismiss закрывает DescriptionDrawer и EntityBottomSheet
    - DescriptionDrawer получает bookId для генерации
    - Build проходит без ошибок, все тесты зелёные
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run --reporter=verbose` -- все тесты зелёные
- `cd frontend && npm run build` -- сборка без TS-ошибок
- DescriptionDrawer показывает тип, текст, кнопку генерации
- EntityBottomSheet показывает entity данные с кнопкой "Подробнее"
- Навигация (свайп/тап) закрывает оба bottom sheet
</verification>

<success_criteria>
- DescriptionDrawer с snap points, кнопкой генерации/просмотра, спиннером, превью
- EntityBottomSheet заменил EntityPopup (floating card -> Vaul bottom sheet)
- Panel dismiss flow включает оба компонента
- Все тесты проходят, build зелёный
</success_criteria>

<output>
After completion, create `.planning/phases/19-description-entity-popup/19-01-SUMMARY.md`
</output>
