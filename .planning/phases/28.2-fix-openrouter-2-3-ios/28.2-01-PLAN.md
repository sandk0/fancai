---
phase: 28.2-fix-openrouter-2-3-ios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/openrouter_client.py
  - backend/app/services/imagen_generator.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Image generation использует отдельный circuit breaker от LLM вызовов"
    - "Каждый этап pipeline (translation, generation, save) логируется с timing"
    - "При ошибке backend лог содержит конкретный этап провала и duration"
  artifacts:
    - path: "backend/app/core/openrouter_client.py"
      provides: "Отдельный circuit breaker для image generation"
      contains: "openrouter_image_breaker"
    - path: "backend/app/services/imagen_generator.py"
      provides: "Structured logging с timing на каждом этапе pipeline"
      contains: "pipeline_stage"
  key_links:
    - from: "backend/app/services/imagen_generator.py"
      to: "backend/app/core/openrouter_client.py"
      via: "openrouter_image_breaker вместо единого openrouter_breaker для image calls"
      pattern: "openrouter_image_breaker"
---

<objective>
Разделить circuit breaker на LLM и Image + добавить structured logging с timing на каждом этапе pipeline генерации изображений.

Purpose: Единый circuit breaker блокирует генерацию изображений когда LLM вызовы (entity extraction, translation) проваливаются 5 раз подряд. Отсутствие logging не позволяет определить на каком этапе pipeline генерация проваливается (translation? circuit breaker? timeout? FLUX.2?).

Output: Раздельные circuit breakers + structured logging с timing для диагностики.
</objective>

<execution_context>
@/Users/sandk/Documents/GitHub/fancai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/Documents/GitHub/fancai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28.2-fix-openrouter-2-3-ios/28.2-RESEARCH.md

@backend/app/core/openrouter_client.py
@backend/app/services/imagen_generator.py
@backend/app/core/retry.py
</context>

<interfaces>
<!-- Текущие интерфейсы из openrouter_client.py -->
```python
# Текущий единый circuit breaker (строка 85-90)
openrouter_breaker = CircuitBreaker(
    failure_threshold=5,
    recovery_timeout=60,
    expected_exception=CIRCUIT_BREAKER_EXCEPTIONS,
    name="openrouter_api",
)

# Метод generate_image (строка 524) — вызывает _post_with_breaker
async def generate_image(self, prompt: str, model: str, aspect_ratio: str, image_size: str) -> bytes:

# _post_with_breaker (строка 222) — использует openrouter_breaker
async def _post_with_breaker(self, endpoint: str, body: dict) -> httpx.Response:
```

<!-- Текущие интерфейсы из imagen_generator.py -->
```python
# _generate_with_retry (строка 364) — retry wrapper для generate_image
@retry_image_generation
async def _generate_with_retry(self, prompt: str, aspect_ratio: str) -> bytes:

# generate_image (строка 381) — основной метод ImagenService
async def generate_image(self, description: str, ...) -> ImageGenerationResult:
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Отдельный circuit breaker для image generation + structured logging pipeline</name>
  <files>backend/app/core/openrouter_client.py, backend/app/services/imagen_generator.py</files>
  <read_first>
    - backend/app/core/openrouter_client.py (текущий CB и _post_with_breaker)
    - backend/app/services/imagen_generator.py (generate_image pipeline)
    - backend/app/core/retry.py (IMAGE_GENERATION_EXCEPTIONS)
    - backend/app/monitoring/metrics.py (circuit_breaker_state, circuit_breaker_failure_count)
  </read_first>
  <action>
**В `backend/app/core/openrouter_client.py`:**

1. Создать второй circuit breaker рядом с существующим `openrouter_breaker` (после строки 90):
```python
# Отдельный circuit breaker для image generation.
# LLM failures (entity extraction, translation) НЕ должны блокировать image generation.
openrouter_image_breaker = CircuitBreaker(
    failure_threshold=5,
    recovery_timeout=60,
    expected_exception=CIRCUIT_BREAKER_EXCEPTIONS,
    name="openrouter_image",
)
```

2. В `_update_cb_metrics()` (строки 93-100) добавить метрики для нового CB:
```python
def _update_cb_metrics() -> None:
    """Update Prometheus gauges for circuit breaker state."""
    for name, breaker in [("openrouter_api", openrouter_breaker), ("openrouter_image", openrouter_image_breaker)]:
        state = breaker.state
        state_value = {"closed": 0, "half_open": 1, "open": 2}.get(state, -1)
        circuit_breaker_state.labels(name=name).set(state_value)
        circuit_breaker_failure_count.labels(name=name).set(breaker.failure_count)
```

3. Добавить метод `_post_with_image_breaker` рядом с `_post_with_breaker` (после строки 268):
```python
async def _post_with_image_breaker(self, endpoint: str, body: dict) -> httpx.Response:
    """HTTP POST через image-specific circuit breaker."""
    if openrouter_image_breaker.opened:
        logger.error(
            "[OpenRouter] Image circuit breaker OPEN -- "
            "image запросы заблокированы на 60 сек"
        )
        _update_cb_metrics()
        raise CircuitBreakerError(openrouter_image_breaker)

    async def _do_post() -> httpx.Response:
        resp = await self._get_client().post(endpoint, json=body)
        resp.raise_for_status()
        return resp

    try:
        result = await openrouter_image_breaker.call_async(_do_post)
        _update_cb_metrics()
        return result
    except CIRCUIT_BREAKER_EXCEPTIONS:
        _update_cb_metrics()
        raise
```

4. В методе `generate_image` (строка 566) заменить:
   - `resp = await self._post_with_breaker("/chat/completions", body)` на
   - `resp = await self._post_with_image_breaker("/chat/completions", body)`

**В `backend/app/services/imagen_generator.py`:**

5. В методе `generate_image` класса `ImagenService` (строка 381) добавить structured logging с timing на каждом этапе. Заменить тело метода (от `start_time = time.time()` до конца) на:

```python
start_time = time.time()

try:
    # Конвертируем тип в enum
    try:
        desc_type = DescriptionType(description_type.lower())
    except ValueError:
        desc_type = DescriptionType.LOCATION

    # Stage 1: Redis cache check
    stage_start = time.time()
    effective_aspect = aspect_ratio or "4:3"
    cache_key = f"imagen:cache:{hashlib.md5((description + effective_aspect).encode()).hexdigest()}"

    redis_client = None
    try:
        import redis.asyncio as aioredis
        redis_client = await aioredis.from_url(settings.REDIS_URL)
        cached_url = await redis_client.get(cache_key)

        if cached_url:
            cached_url_str = cached_url.decode("utf-8")
            logger.info(
                "Image pipeline: cache HIT",
                extra={
                    "pipeline_stage": "cache_check",
                    "duration": f"{time.time() - stage_start:.2f}s",
                    "result": "hit",
                },
            )
            return ImageGenerationResult(
                success=True,
                image_url=cached_url_str,
                generation_time_seconds=0.0,
                model_used="cache",
                prompt_used=description,
            )
    except Exception as cache_e:
        logger.warning(
            f"Image pipeline: cache check failed: {cache_e}",
            extra={"pipeline_stage": "cache_check", "duration": f"{time.time() - stage_start:.2f}s"},
        )
    finally:
        if redis_client is not None:
            try:
                await redis_client.close()
            except Exception:
                pass

    logger.info(
        "Image pipeline: cache MISS",
        extra={"pipeline_stage": "cache_check", "duration": f"{time.time() - stage_start:.2f}s", "result": "miss"},
    )

    # Stage 2: Prompt engineering (includes translation RU->EN)
    stage_start = time.time()
    prompt = await self._prompt_engineer.create_prompt(
        description=description,
        description_type=desc_type,
        genre=genre,
        custom_style=custom_style,
    )
    translation_duration = time.time() - stage_start
    logger.info(
        "Image pipeline: prompt ready",
        extra={
            "pipeline_stage": "translation_and_prompt",
            "duration": f"{translation_duration:.2f}s",
            "prompt_preview": prompt[:100],
        },
    )

    # Stage 3: Image generation via OpenRouter FLUX.2 (with retry)
    stage_start = time.time()
    image_bytes = await self._generate_with_retry(prompt, effective_aspect)
    generation_duration = time.time() - stage_start
    logger.info(
        "Image pipeline: FLUX.2 generation complete",
        extra={
            "pipeline_stage": "flux2_generation",
            "duration": f"{generation_duration:.2f}s",
            "image_size_bytes": len(image_bytes),
        },
    )

    # Stage 4: Post-processing (base64 + save + cache)
    stage_start = time.time()
    image_base64 = base64.b64encode(image_bytes).decode("utf-8")
    image_url = f"data:image/png;base64,{image_base64}"
    local_path = await self._save_image(image_bytes, prompt)
    await self._cache_result(cache_key, image_url)
    post_duration = time.time() - stage_start

    total_duration = time.time() - start_time
    logger.info(
        f"Image pipeline: SUCCESS in {total_duration:.2f}s",
        extra={
            "pipeline_stage": "complete",
            "total_duration": f"{total_duration:.2f}s",
            "translation_duration": f"{translation_duration:.2f}s",
            "generation_duration": f"{generation_duration:.2f}s",
            "post_duration": f"{post_duration:.2f}s",
        },
    )

    return ImageGenerationResult(
        success=True,
        image_url=image_url,
        image_data=image_bytes,
        local_path=local_path,
        generation_time_seconds=total_duration,
        model_used=settings.OPENROUTER_IMAGE_MODEL,
        prompt_used=prompt,
    )

except ValueError as e:
    error_msg = str(e)
    total_duration = time.time() - start_time
    logger.error(
        f"Image pipeline: FAILED (non-retryable) in {total_duration:.2f}s",
        extra={
            "pipeline_stage": "error",
            "error_type": "ValueError",
            "error_message": error_msg[:200],
            "total_duration": f"{total_duration:.2f}s",
        },
    )
    return ImageGenerationResult(
        success=False,
        error_message=f"Image generation failed: {error_msg}",
    )
except Exception as e:
    total_duration = time.time() - start_time
    logger.error(
        f"Image pipeline: FAILED in {total_duration:.2f}s: {e}",
        extra={
            "pipeline_stage": "error",
            "error_type": type(e).__name__,
            "error_message": str(e)[:200],
            "total_duration": f"{total_duration:.2f}s",
        },
    )
    return ImageGenerationResult(
        success=False,
        error_message=f"Image generation error: {str(e)}",
        generation_time_seconds=total_duration,
    )
```

6. В `PromptTranslator.translate()` (строка 99) добавить logging с timing:
   - Перед вызовом `generate_text`: `stage_start = time.time()`
   - После успешного перевода: `logger.info("Translation complete", extra={"pipeline_stage": "translation", "duration": f"{time.time() - stage_start:.2f}s"})`
   - В `except` блоке (строка 145-147): `logger.error(f"Translation failed, using original text", extra={"pipeline_stage": "translation", "duration": f"{time.time() - stage_start:.2f}s", "error": str(e)[:200]})`
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/backend && uv run python -c "from app.core.openrouter_client import openrouter_image_breaker; print('Image CB exists:', openrouter_image_breaker.name)"</automated>
  </verify>
  <acceptance_criteria>
    - openrouter_client.py содержит `openrouter_image_breaker = CircuitBreaker(`
    - openrouter_client.py содержит `async def _post_with_image_breaker(`
    - openrouter_client.py: метод `generate_image` вызывает `_post_with_image_breaker` (не `_post_with_breaker`)
    - imagen_generator.py содержит `"pipeline_stage": "translation_and_prompt"` (logging stage 2)
    - imagen_generator.py содержит `"pipeline_stage": "flux2_generation"` (logging stage 3)
    - imagen_generator.py содержит `"pipeline_stage": "complete"` (logging success)
    - imagen_generator.py содержит `"pipeline_stage": "error"` (logging failure)
    - `cd backend && uv run python -c "from app.core.openrouter_client import openrouter_image_breaker"` exits 0
    - `cd backend && uv run python -m pytest tests/ -x --timeout=60` passes (no regressions)
  </acceptance_criteria>
  <done>Image generation использует собственный circuit breaker. Каждый этап pipeline (cache, translation, generation, post-processing) логируется с timing и stage name.</done>
</task>

</tasks>

<verification>
- `cd backend && uv run python -c "from app.core.openrouter_client import openrouter_breaker, openrouter_image_breaker; print(openrouter_breaker.name, openrouter_image_breaker.name)"` выводит `openrouter_api openrouter_image`
- `cd backend && uv run python -m pytest tests/ -x --timeout=60` проходит без ошибок
- `grep -c pipeline_stage backend/app/services/imagen_generator.py` >= 6 (минимум 6 точек логирования)
</verification>

<success_criteria>
- Image generation использует отдельный circuit breaker `openrouter_image_breaker`
- LLM fallback chain failures НЕ блокируют image generation
- Backend логирует pipeline_stage + duration на каждом этапе (cache, translation, generation, post-processing, error)
- Существующие тесты проходят
</success_criteria>

<output>
После завершения создать `.planning/phases/28.2-fix-openrouter-2-3-ios/28.2-01-SUMMARY.md`
</output>
