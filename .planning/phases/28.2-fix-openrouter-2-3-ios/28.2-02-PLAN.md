---
phase: 28.2-fix-openrouter-2-3-ios
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Reader/DescriptionDrawer.tsx
  - frontend/src/services/imageCache.ts
  - frontend/src/components/Reader/__tests__/DescriptionDrawer.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "DescriptionDrawer использует async endpoint с polling вместо синхронного POST"
    - "Пользователь видит текст ошибки при провале генерации (не просто красную кнопку)"
    - "iOS IndexedDB ошибка при клике на description не блокирует работу"
    - "Пользователь видит прогресс генерации (Generating... статус) без timeout на 120 секунд"
  artifacts:
    - path: "frontend/src/components/Reader/DescriptionDrawer.tsx"
      provides: "Async generation с polling через TQ + показ ошибок"
      contains: "generateAsync"
    - path: "frontend/src/services/imageCache.ts"
      provides: "Graceful error handling для getWithMetadata при iOS IndexedDB failure"
      contains: "switchToFallback"
    - path: "frontend/src/components/Reader/__tests__/DescriptionDrawer.test.tsx"
      provides: "Тесты для error state display и async generation flow"
      contains: "shows error message"
  key_links:
    - from: "frontend/src/components/Reader/DescriptionDrawer.tsx"
      to: "frontend/src/api/images.ts"
      via: "imagesAPI.generateAsync() вместо generateImageForDescription()"
      pattern: "generateAsync"
    - from: "frontend/src/components/Reader/DescriptionDrawer.tsx"
      to: "frontend/src/api/images.ts"
      via: "imagesAPI.getTaskStatus() для polling"
      pattern: "getTaskStatus"
---

<objective>
Переключить DescriptionDrawer с синхронного endpoint на async endpoint с polling + показ ошибок + iOS IndexedDB graceful handling.

Purpose: Синхронный endpoint имеет axios timeout 120s, а полный pipeline (translation + generation) может занять до 210s. Async endpoint возвращает 202 мгновенно и использует polling для отслеживания статуса. Это устраняет root cause Бага 1 ("2/3 случаев генерация не доходит до OpenRouter"). Баг 2 (iOS storage error) требует graceful handling в imageCache.

Output: DescriptionDrawer с async generation + polling + error display + iOS storage fix.
</objective>

<execution_context>
@/Users/sandk/Documents/GitHub/fancai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandk/Documents/GitHub/fancai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28.2-fix-openrouter-2-3-ios/28.2-RESEARCH.md

@frontend/src/components/Reader/DescriptionDrawer.tsx
@frontend/src/hooks/epub/useImageModal.ts
@frontend/src/hooks/api/useImages/useImageMutations.ts
@frontend/src/hooks/api/useImages/useImageQueries.ts
@frontend/src/api/images.ts
@frontend/src/services/imageCache.ts
@frontend/src/services/memoryFallbackCache.ts
@frontend/src/components/Reader/__tests__/DescriptionDrawer.test.tsx
</context>

<interfaces>
<!-- Из images.ts: async generation API (уже используется в useImageModal) -->
```typescript
// frontend/src/api/images.ts
async generateAsync(descriptionId: string, params: ImageGenerationParams, signal?: AbortSignal): Promise<AsyncGenerationResponse>
async getTaskStatus(taskId: string, signal?: AbortSignal): Promise<TaskStatusResponse>

// frontend/src/types/api.ts
interface AsyncGenerationResponse {
  task_id: string;
  status: string;
  message: string;
  status_url: string;
}

interface TaskStatusResponse {
  task_id: string;
  status: 'PENDING' | 'STARTED' | 'SUCCESS' | 'FAILURE' | 'RETRY';
  message?: string;
  result?: {
    success?: boolean;
    image_id?: string;
    image_url?: string;
    generation_time_seconds?: number;
    error_message?: string;
  };
}
```

<!-- Из useImageMutations.ts: текущий useGenerateImage (будет заменён) -->
```typescript
export function useGenerateImage(options?) {
  // mutationFn: imagesAPI.generateImageForDescription(descriptionId, params)
  // Синхронный POST /images/generate/description/{id}
  // Timeout: 120s (axios)
}
```

<!-- Из imageCache.ts: getWithMetadata -->
```typescript
async getWithMetadata(userId: string, descriptionId: string): Promise<{ url: string; metadata?: Record<string, unknown> } | null>
// try-catch уже вызывает switchToFallback при ошибке IndexedDB
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Переключить DescriptionDrawer на async generation + показ ошибок + iOS storage fix</name>
  <files>frontend/src/components/Reader/DescriptionDrawer.tsx, frontend/src/services/imageCache.ts</files>
  <read_first>
    - frontend/src/components/Reader/DescriptionDrawer.tsx (текущий синхронный flow)
    - frontend/src/hooks/epub/useImageModal.ts (reference: async pattern с polling)
    - frontend/src/api/images.ts (generateAsync, getTaskStatus API)
    - frontend/src/hooks/api/useImages/useImageMutations.ts (текущий useGenerateImage, будет не использован)
    - frontend/src/hooks/api/useImages/useImageQueries.ts (useImageForDescription)
    - frontend/src/services/imageCache.ts (switchToFallback, getWithMetadata)
    - frontend/src/services/memoryFallbackCache.ts (MemoryTable.get())
  </read_first>
  <action>
**В `frontend/src/components/Reader/DescriptionDrawer.tsx`:**

Переписать DescriptionDrawer для использования async endpoint с TQ polling (паттерн из useImageModal):

1. Заменить импорт `useGenerateImage` на прямые импорты API и TQ:
```typescript
import { useState, useEffect, useRef, useCallback } from 'react';
import { useQuery, useQueryClient } from '@tanstack/react-query';
import { imagesAPI } from '@/api/images';
import { imageCache } from '@/services/imageCache';
import { imageKeys, getCurrentUserId } from '@/hooks/api/queryKeys';
import { useRegenerateImage } from '@/hooks/api/useImages/useImageMutations';
```

Убрать импорт `useGenerateImage` (больше не нужен — DescriptionDrawer будет напрямую вызывать `imagesAPI.generateAsync`).

2. Внутри компонента DescriptionDrawer заменить `useGenerateImage()` mutation на async state management:

```typescript
// --- Async generation state ---
const [taskId, setTaskId] = useState<string | null>(null);
const [genStatus, setGenStatus] = useState<'idle' | 'generating' | 'error'>('idle');
const [genError, setGenError] = useState<string | null>(null);
const queryClient = useQueryClient();

// Reset generation state on description change
useEffect(() => {
  setTaskId(null);
  setGenStatus('idle');
  setGenError(null);
  regenerateMutation.reset();
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [description?.id]);

// TQ polling query (паттерн из useImageModal)
const POLLING_INTERVAL = 3000;
const { data: taskStatus } = useQuery({
  queryKey: imageKeys.taskStatus(taskId!),
  queryFn: () => imagesAPI.getTaskStatus(taskId!),
  enabled: !!taskId && isOpen,
  refetchInterval: (query) => {
    const status = query.state.data?.status;
    if (status === 'SUCCESS' || status === 'FAILURE') return false;
    return POLLING_INTERVAL;
  },
  refetchOnWindowFocus: false,
  retry: 2,
});

// React to polling completion
useEffect(() => {
  if (!taskStatus || !taskId) return;

  if (taskStatus.status === 'SUCCESS' && taskStatus.result?.success) {
    setGenStatus('idle');
    setTaskId(null);
    // Invalidate TQ cache so useImageForDescription refetches
    const userId = getCurrentUserId();
    queryClient.invalidateQueries({
      queryKey: imageKeys.byDescription(userId, description?.id ?? ''),
    });
    queryClient.invalidateQueries({
      queryKey: imageKeys.byBook(userId, bookId),
    });
    queryClient.invalidateQueries({ queryKey: imageKeys.userStats(userId) });
    // Cache the new image
    if (taskStatus.result.image_url && description) {
      imageCache.set(userId, description.id, taskStatus.result.image_url, bookId).catch(() => {});
    }
  } else if (taskStatus.status === 'FAILURE') {
    const errorMessage = taskStatus.result?.error_message || taskStatus.message || t('reader.description_drawer.generation_failed');
    setGenError(errorMessage);
    setGenStatus('error');
    setTaskId(null);
    notify.error(t('reader.description_drawer.generation_error_title', 'Ошибка генерации'), errorMessage);
  }
}, [taskStatus, taskId, description?.id, bookId, queryClient, t]);
```

3. Заменить `handleGenerate` на async version:
```typescript
const handleGenerate = useCallback(async () => {
  if (!description) return;
  setGenStatus('generating');
  setGenError(null);
  try {
    const result = await imagesAPI.generateAsync(description.id, {});
    setTaskId(result.task_id);
  } catch (error: unknown) {
    const err = error as { response?: { status?: number }; message?: string; details?: { detail?: string } };
    const isConflict = err.response?.status === 409 || err.message?.includes('already exists') || err.details?.detail?.includes?.('already exists');
    if (isConflict) {
      // Image already exists — just invalidate TQ cache to refetch
      const userId = getCurrentUserId();
      queryClient.invalidateQueries({ queryKey: imageKeys.byDescription(userId, description.id) });
      setGenStatus('idle');
    } else {
      const errorMessage = error instanceof Error ? error.message : String(error);
      setGenError(errorMessage);
      setGenStatus('error');
      notify.error(t('reader.description_drawer.generation_error_title', 'Ошибка генерации'), errorMessage);
    }
  }
}, [description, queryClient, t]);
```

4. Обновить `renderImageButton()`:

- Заменить `generateMutation.isPending` на `genStatus === 'generating'`
- Заменить `generateMutation.isError` на `genStatus === 'error'`
- В error state ДОБАВИТЬ показ текста ошибки:
```tsx
if (genStatus === 'error') {
  return (
    <div className="mt-4 space-y-2">
      <button
        onClick={handleGenerate}
        className="w-full py-2.5 min-h-[44px] rounded-lg bg-destructive/10 text-destructive text-sm font-medium hover:bg-destructive/20 transition-colors"
      >
        {t('reader.description_drawer.retry', 'Повторить')}
      </button>
      {genError && (
        <p className="text-xs text-destructive/70 px-1">{genError}</p>
      )}
    </div>
  );
}
```

- Generating state — тоже обновить:
```tsx
if (genStatus === 'generating') {
  return (
    <button
      disabled
      className="mt-4 w-full py-2.5 min-h-[44px] rounded-lg bg-primary/10 text-primary/50 text-sm font-medium flex items-center justify-center gap-2"
    >
      <Loader2 className="w-4 h-4 animate-spin" />
      {t('reader.description_drawer.generating')}
    </button>
  );
}
```

5. Обновить image preview секцию: убрать `generateMutation.data` check, оставить только `image?.status === 'completed'` (TQ query теперь автоматически обновится после polling completion через invalidation).

6. Убрать старый `useEffect` с `generateMutation.reset()` (заменён новым useEffect с reset generation state).

**В `frontend/src/services/imageCache.ts`:**

7. В `getWithMetadata()` (строка 230) — метод уже корректно обрабатывает ошибки через `switchToFallback` и возвращает `null`. Но нужно убедиться что `notifyFallbackOnce()` не показывает пугающую ошибку при каждом клике. Проверить что `switchToFallback` вызывается только один раз (flag `useMemoryFallback`). Это уже реализовано корректно (строка 34: `if (!useMemoryFallback)`).

8. Однако, добавить более информативное логирование в `switchToFallback`:
```typescript
function switchToFallback(err: unknown): void {
  if (!useMemoryFallback) {
    useMemoryFallback = true;
    const errorName = err instanceof Error ? err.constructor.name : 'Unknown';
    const errorMsg = err instanceof Error ? err.message : String(err);
    logger.warn(`[ImageCache] Switching to in-memory fallback (${errorName}): ${errorMsg}`);
    notifyFallbackOnce();
  }
}
```
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npx vitest run src/components/Reader/__tests__/DescriptionDrawer.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - DescriptionDrawer.tsx содержит `imagesAPI.generateAsync` (async endpoint)
    - DescriptionDrawer.tsx содержит `imagesAPI.getTaskStatus` (polling)
    - DescriptionDrawer.tsx содержит `refetchInterval` (TQ polling pattern)
    - DescriptionDrawer.tsx НЕ содержит `useGenerateImage` (старый sync mutation удалён)
    - DescriptionDrawer.tsx содержит `genError &&` (показ текста ошибки)
    - DescriptionDrawer.tsx содержит `notify.error` (toast при ошибке)
    - imageCache.ts: switchToFallback содержит `err.constructor.name` (информативное логирование)
    - `cd frontend && npm run build` exits 0 (TypeScript compilation)
  </acceptance_criteria>
  <done>DescriptionDrawer использует async endpoint с polling. Ошибки генерации показываются пользователю текстом. iOS IndexedDB fallback корректно логируется.</done>
</task>

<task type="auto">
  <name>Task 2: Обновить тесты DescriptionDrawer для async generation flow</name>
  <files>frontend/src/components/Reader/__tests__/DescriptionDrawer.test.tsx</files>
  <read_first>
    - frontend/src/components/Reader/__tests__/DescriptionDrawer.test.tsx (текущие тесты)
    - frontend/src/components/Reader/DescriptionDrawer.tsx (обновлённый компонент после Task 1)
  </read_first>
  <action>
Обновить тесты DescriptionDrawer для нового async flow:

1. Обновить моки — убрать mock `useGenerateImage`, добавить моки для `imagesAPI`, `useQuery`, `useQueryClient`:

```typescript
// Mock TanStack Query
const mockInvalidateQueries = vi.fn();
vi.mock('@tanstack/react-query', () => ({
  useQuery: vi.fn(() => ({ data: undefined })),
  useQueryClient: () => ({ invalidateQueries: mockInvalidateQueries }),
}));

// Mock images API
const mockGenerateAsync = vi.fn();
const mockGetTaskStatus = vi.fn();
vi.mock('@/api/images', () => ({
  imagesAPI: {
    generateAsync: (...args: unknown[]) => mockGenerateAsync(...args),
    getTaskStatus: (...args: unknown[]) => mockGetTaskStatus(...args),
  },
}));

// Mock imageCache
vi.mock('@/services/imageCache', () => ({
  imageCache: {
    set: vi.fn().mockResolvedValue(true),
  },
}));

// Mock queryKeys
vi.mock('@/hooks/api/queryKeys', () => ({
  imageKeys: {
    taskStatus: (id: string) => ['images', 'task', id],
    byDescription: (uid: string, did: string) => ['images', 'desc', uid, did],
    byBook: (uid: string, bid: string) => ['images', 'book', uid, bid],
    userStats: (uid: string) => ['images', 'stats', uid],
  },
  getCurrentUserId: () => 'test-user',
}));
```

2. Обновить существующие тесты:
   - `shows "Generate" button when no image` — без изменений (всё ещё рендерит "Generate")
   - `shows spinner and "Generating..."` — теперь нужно simulating async state. Нет прямого мока mutation state, нужно вызвать handleGenerate через клик и mock generateAsync:
     - Mock `mockGenerateAsync.mockResolvedValue({ task_id: 'task-1' })`
     - Click "Generate" button
     - Assert "Generating..." appears (после setState)
   - `calls mutate with correct params` — заменить на: calls `generateAsync` with description.id
   - Убрать тест `shows image preview after successful generation` с `generateMutation.data` (больше нет mutation data — image приходит через TQ invalidation + useImageForDescription)

3. Добавить новый тест для error state с текстом ошибки:
```typescript
it('shows error message when generation fails', async () => {
  mockGenerateAsync.mockRejectedValue(new Error('OpenRouter timeout'));

  render(<DescriptionDrawer {...defaultProps} />);

  const btn = screen.getByText('Generate');
  fireEvent.click(btn);

  // Wait for error state
  await waitFor(() => {
    expect(screen.getByText('Повторить')).toBeInTheDocument();
  });
  expect(screen.getByText('OpenRouter timeout')).toBeInTheDocument();
});
```

4. Добавить тест для 409 conflict handling:
```typescript
it('handles 409 conflict by invalidating query cache', async () => {
  mockGenerateAsync.mockRejectedValue({ response: { status: 409 }, message: 'already exists' });

  render(<DescriptionDrawer {...defaultProps} />);

  const btn = screen.getByText('Generate');
  fireEvent.click(btn);

  await waitFor(() => {
    expect(mockInvalidateQueries).toHaveBeenCalled();
  });
});
```

5. Сохранить тесты: type badge, full text, "View image" с TQ query image, null description — они не зависят от generation flow.

6. Добавить `import { waitFor } from '@testing-library/react'` если ещё не импортирован.
  </action>
  <verify>
    <automated>cd /Users/sandk/Documents/GitHub/fancai/frontend && npx vitest run src/components/Reader/__tests__/DescriptionDrawer.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - DescriptionDrawer.test.tsx НЕ содержит `useGenerateImage` (старые моки удалены)
    - DescriptionDrawer.test.tsx содержит `generateAsync` (мок нового API)
    - DescriptionDrawer.test.tsx содержит `shows error message` (тест error display)
    - DescriptionDrawer.test.tsx содержит `409` (тест conflict handling)
    - `cd frontend && npx vitest run src/components/Reader/__tests__/DescriptionDrawer.test.tsx` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Все тесты DescriptionDrawer обновлены для async generation flow. Новые тесты покрывают error state display и 409 conflict handling.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run src/components/Reader/__tests__/DescriptionDrawer.test.tsx --reporter=verbose` — все тесты pass
- `cd frontend && npm run build` — production build успешен
- `grep -c generateAsync frontend/src/components/Reader/DescriptionDrawer.tsx` >= 1
- `grep -c useGenerateImage frontend/src/components/Reader/DescriptionDrawer.tsx` == 0 (старый sync mutation удалён)
- `grep genError frontend/src/components/Reader/DescriptionDrawer.tsx` — ошибки показываются
</verification>

<success_criteria>
- DescriptionDrawer использует async endpoint (POST /images/generate/async/{id}) вместо синхронного
- Polling через TQ useQuery с refetchInterval отслеживает статус задачи
- Нет axios timeout проблемы — async endpoint возвращает 202 мгновенно
- Ошибки генерации отображаются текстом под кнопкой retry
- 409 conflict обрабатывается через TQ invalidation (не показывает ошибку)
- iOS IndexedDB fallback логирует тип ошибки для диагностики
- Все тесты проходят, production build успешен
</success_criteria>

<output>
После завершения создать `.planning/phases/28.2-fix-openrouter-2-3-ios/28.2-02-SUMMARY.md`
</output>
